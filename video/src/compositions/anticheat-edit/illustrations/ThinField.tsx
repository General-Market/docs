import React from "react";
import { SceneFrame, StatSolo } from "../props";

export const ThinField: React.FC = () => (
  <SceneFrame kicker="THE PROBLEM IS STRUCTURAL" title="Almost nowhere is safe">
    <StatSolo value="2 / 200" caption="venues that don't run these plays" />
  </SceneFrame>
);
