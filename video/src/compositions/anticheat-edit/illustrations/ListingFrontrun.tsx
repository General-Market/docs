import React from "react";
import { SceneFrame, StatSolo } from "../props";

export const ListingFrontrun: React.FC = () => (
  <SceneFrame kicker="MECHANISM 04 / 13" title="Knowing before the news">
    <StatSolo value="+800 bps" caption="the listing pop — taken before you see it" tone="bad" />
  </SceneFrame>
);
