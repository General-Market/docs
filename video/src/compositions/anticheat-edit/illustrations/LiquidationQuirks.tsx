import React from "react";
import { SceneFrame, StatSolo } from "../props";

export const LiquidationQuirks: React.FC = () => (
  <SceneFrame kicker="MECHANISM 13 / 13" title="The wick they catch">
    <StatSolo value="-40%" caption="the wick they liquidate you into" tone="bad" />
  </SceneFrame>
);
