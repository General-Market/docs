import React from "react";
import { SceneFrame, StatSolo } from "../props";

export const FundingEdge: React.FC = () => (
  <SceneFrame title="The window you can't reach">
    <StatSolo value="2 min" caption="the funding window — open to them, locked to you" />
  </SceneFrame>
);
