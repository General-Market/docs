strategy = "random"
deposit = 10
stake = 1
max_batches = 5
max_exposure = 100
poll_interval = 30
auto_claim = true
auto_withdraw = true
claim_above = 5
withdraw_below = 2
rpc_url = "http://localhost:8546"
vision_api = "http://localhost:10001"
data_node = "http://localhost:8200"
oracle_discovery = "static"
oracle_urls = ["http://localhost:10001", "http://localhost:10002", "http://localhost:10003"]
pnl_file = "pnl.json"
batch_ids = []              # empty = join any, e.g. [1, 3, 7]

[vault]
enabled = false
factory_address = ""
vault_address = ""          # set to use existing vault, empty = create new
name = "Vision Bot Fund"
symbol = "vbFUND"
perf_fee = 2000             # basis points (2000 = 20%)
seed_deposit = 100.0        # initial USDC to deposit
auto_reconcile = true
