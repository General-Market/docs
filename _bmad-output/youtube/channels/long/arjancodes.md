# ArjanCodes Long-Form Transcripts

50 video transcripts.

---

## 1. When One Model Isn’t Enough: CQRS Explained
**Channel:** ArjanCodes | **Views:** 21K | **Date:** 2 days ago | **Duration:** 22:25 | **ID:** Spp-uzXzzdE
**Link:** https://youtube.com/watch?v=Spp-uzXzzdE

### Transcript:
Let's say you're doing customer support for OpenAI and you want to compute some data to get an idea of how broken your product is, but your API is a bit limited and can only return all ticket data at once. That is probably a lot of data. On top of that, say you're running some analytics script that gathers that data while those support tickets are flowing in, your reads and your rights may interfere and that leads to all sorts of performance issues. Now, you can avoid all those problems using the CQRS architecture. And I'll explain exactly how it works in this video using an example with fast API and MongoDB. I talk about when to use it and when not to use it and why MongoDB actually fits this architecture really well. That's why they're also the sponsor of this video. Now, let's take a look at the code. Like I said, the starting point is a fast API app that uses MongoDB. So first create this file that uses pyongo to set up a MongoDB database. So create some uh URL. This by the way I'm running MongoDB locally on my machine with Docker Compose and then I define a database type. I create a client with that URL. And then I have a couple of functions that are useful in my app like getting the database which will be injected into the endpoints uh shutting down the database and some helpful things like being able to convert strings to and from object ids. Now the API app itself is then also pretty straightforward. So I have a collection of tickets, customer support tickets. These tickets can have a status that's either open, triaged or closed. Uh I have a couple of other functions that are useful like getting the current time. Uh making a preview of a message that's part of a customer support ticket. Then I create the app. Uh there's a startup phase which gets the DB and set some indices which is helpful for uh you know common queries that we do. Uh then there's of course shutting down the database when the app shuts down. And then there's a couple of pyantic models. uh one for creating tickets uh one for updating tickets. As you can see, we can update the status or add a note to a customer support ticket. Then we have a ticket details model that contains basically everything related to the ticket. And then there's another uh model here called ticket list item that I'll talk about in a minute. And then we have some endpoints. So I can post to the tickets endpoint and that gets a payload which is this ticket in then creates a ticket. uh we have updating a ticket uh that this is actually kind of a complicated operation because well uh of course first have to check that the ticket actually exists then uh we can only update certain things in certain ways. For example, if a status is closed then uh we cannot reopen that ticket in this particular setup. And for updating the agent there is actually no rule though we can simply update that. And then I have list tickets endpoint that retrieves tickets. talk more about that in a minute. Uh we have retrieving a particular ticket uh given an ID and I have a sort of dashboard endpoint that does some aggregates like collecting how many tickets are open, triaged and closed. So that's the API and this interacts with this database that I created right here. Let's say that there is a UI requirement that the list view needs some extra information like a short preview of the agent note or a has note boolean flag. So what you would do if you just implement stuff right inside this fast API app is that you would have this ticket list item model that contains the um preview message and a has node boolean value, right? And then in the endpoint where we list the tickets, we would basically return that as a response model. And that's already where you see kind of a problem occurring here because in this uh list view, we need to of course compute this preview and compute whether there there is a note. That means that every time you list the tickets using this endpoint that these things are going to be computed. Now, of course, computing a preview message and whether there's a note or not is not really that computationally intensive unless you have a lot of tickets, but uh you can imagine that there are other things that may also add uh extra uh computation and extra problems if you need to compute them every time you want to list the tickets. And as you can see, that happens right here. So I call a make preview helper function that does this job and I turn a note into a boolean. Again here it's not a big deal but if you have many tickers this kind of stuff can add up at the same time in order to make this preview and determine whether there is a note uh then actually the this endpoint does need to retrieve all that data. So there is that problem as well and if you have many tickets that can be problematic. So the problem is here that we have certain read requirements that directly affect the code here and the models that we use. And if you're not careful then the right model might get reshaped just to make reads easier. You know maybe we decide to add the message preview and whether there's a note or not to the document and then it's also easy later on to get all sorts of inconsistencies. And when you start seeing these kind of things in your code, that's a point where CQRS perhaps starts to make sense. Now what is CQRS? That stands for command query responsibility segregation. And the idea is actually quite simple. You have commands and you have queries. The commands they change state. So they update things in a database. And the queries read state. And the interesting thing about CQS is that these two things, they don't have to look the same. They can be completely different and they can also be optimized very differently. In essence, you can even split up the databases. So your commands work with a different database than your reads, for example. Now, CQS does not automatically mean that you also need like a bunch of microservices. It's really about splitting commands and queries, writing state, reading state. It also does not require event sourcing even though it's often mentioned together with event sourcing because event sourcing actually works pretty well with CQS. So you uh post your events. So that's basically the write operations but then you also have read model where you store snapshots or other kind of aggregates based on those events. Now in this example everything is going to be simple. So there's just going to be one fast API app, one MongoDB database, but within that database, I'm going to create separate collections for reading tickets and for modifying tickets. So the first thing that I'm going to do is currently there's a single tickets collection. But let's split this up into two collections. So we have the uh commands collection, which is let's say uh ticket commands. And we're going to have the reads collection and this is tickets reads. So we'll use two different collections here. One to modify tickets and one to read tickets. And that also means we need to project. So if you create a ticket, it means you need to store that ticket into these two different places. So uh I'll also remove this collection so I can start updating the code everywhere and see where the problems are. So the first thing that we need to do is here create some indices. So we have uh status and updated ads. So let's say that uh these are indices we want to put on the command side of things like so and we can also add them on the read side. So on this collection we can create these indices as well status and update to that and I can add has note because that's an extra thing that we're going to need on the read side. So currently I have this model for patching the ticket. But if you want to split this up into commands, you could uh create an update status command which checks that the status is not closed because you can't reopen closed tickets. And we can add an agent note command which is less restrictive. So what you can do then is split up this class basically. So I'm going to copy this and I'm going to call this update status like so. if I can write. So of course this doesn't need the agent note and this is called add agent note and I remove the status. So now we have these two patch options. And now what we can do is add some commands. For example, let's say we have a command update status. This gets a database. So that needs to be injected obviously. and we're going to provide it with an instance of this particular model like so. This is going to return none. And then we can basically take the part of the logic that's in this uh patching thing here. So I'll just uh copy this like so. And then we can edit it here. So let's see. We don't need the database because we already have this. Uh, next to the command, I also will need the ticket ID like so. And then of course this is going to be the command collection. And let's remove the agent note part here. And then I can update the rest of the logic. And what we can also do is clean things up a bit. So for example, I think the commands which really work on the data level, they don't need any knowledge of the API. So a command shouldn't raise an HTTP exception. I think it should raise a value error. So let's change this so that it's a value error like so. And then of course there's no status code. But then we can do it like this. And then what we need to do is replace the business logic. So we have this part basically that we need to put in there. And just going to put that here. And then of course this needs to be commands dot status is not closed. And then it's also going to raise a value error like so. There we go. And then finally we can update the command like so. So this is nice. Now our business logic is right here. And then what we can do is create an endpoint for updating the status. And I've already prepared that because otherwise this video becomes too long. So the endpoint looks like this. [snorts] So if updating the status we get the database and then we simply call the command and that also means that now our routing is nicely separated from the command which is cool. Similarly we can also create a command for adding an agent note. So that looks like this. So it's almost exactly the same. So we get the ID, we check that the ticket actually exists and then we simply update the commands collection with this agent note. So again very simple and then we can also add a separate endpoint for this just like I did with the ticket status and I removed this update endpoint since that is now being replaced by two separate endpoints. Now the third command that we have here that's also writing stuff is creating a ticket. So we can also add a command for that. This also gets a database and it gets a ticket in model and we want that to return a string ID. Then I'm going to take basically this code and move it over to my create command like so. And then of course this also needs to be the commands collection. And then I simply want to return the inserted ID right here. So that's creating the ticket. And then in my endpoint instead of doing all of this I can basically create my ticket with the payload like so. And then we could now retrieve again the ticket from the database or uh we can keep this simple in this particular example and simply return a dictionary with the id like so. So now we've a separated routing the API bit from the actual command. But next what we need to do is actually separating write and read storage. So that means that we have ticket commands which is currently where we're writing things to that we will also have ticket reads where we're reading things from and then we need to project it. And then what we can do next in our app is that commands only write to the right model and that queries only read from the read model. And that's also why I like using MongoDB in this particular example because this is really built around working with application data and it's not forcing data into a fixed shape. it has a lot of flexibilities. So for CQS specifically uh the document model of MongoDB maps pretty natural to read projections and it has a lot of flexibility in in the schema. So the read model can basically evolve independently from the right model and it also means that the read side is eventually consistent by design. In practice that means that a write might take a short moment before it shows up in the read model because we have to do that projection. So for a brief window of time a list or dashboard might be slightly out of date but for most dashboards that's fine. Uh it is something to be aware of though. Another thing that uh we can do is that we can use the same ID across these different collections to keep the projections simple and traceable. Now personally I've worked with MongoDB for over 10 years actually. I've used it in all the products I built and I'm still using it. It's behind my course portal for businesses. We have an analytics dashboard within Iron Codes that uses MongoDB and I have several other tools that we use internally as well. Now I don't host this myself but I use MongoDB Atlas. This removes most of the operational overhead for me. There's backups. It's secure. It's easy to scale. And you even get a free sandbox database that is actually already pretty good. If you want to try MongoDB for yourself, check out the link in the video description. Now, like I said, we have to if we have these two different collections, we need to make a projection from the write database to the read database. And of course, like a Swedish chef, I have already prepared this. So, here's a project ticket function that takes a database and the ticket ID. And what this does is that it retrieves the document from the right database and then it prepares a new document for reading. So this contains this preview. So we compute this only when we do this projection and whether or not this ticket has a note. And then finally it inserts this projection into the reads collection. So what you need to do is that whenever you create or update a ticket we need to run this projection logic. Right? So in the tickets endpoint where we create a ticket, we not only need to create it but we also need to update it. We also need to project it. So this is also going to need the database and this will need our ticket ID like so. And same for updating the status that happens here and of course adding an agent note. So there we also need to do this projection. So now it means that every time we make a change, we create a ticket or we update it. Then we also update our projection. And now I can go down into the read endpoints like getting a tickets. And then of course here I can do the reads collection like so. And in the list tickets endpoint I can also switch to the reads collection like so. But now of course I won't have to do these jobs anymore. I can simply directly refer to the preview because that's already part of the projection. and the has node as well. I don't need to do this. So now I don't need to get that data anymore. And that means I can also update this projection because now we need the uh subject status updated at we need to preview and the has note. So I only need to retrieve the preview and has note. And when you have a lot of tickets, this is potentially a lot less data that you're retrieving from the database like so. So I'm running the fast API app here locally on my machine and connects also to a local MongoDB. So now let's see if we can uh do a few tests here. So here's a go request to create a ticket with a subject and a message. So we get this ID. So now I'm going to post this go request to update the status to triaged. And now what we can do is also check that the read model actually works. So I'm going to get the ticket with this particular ID and we're going to see if the status actually was correct. So let's read this. And when we look at the particular ticket, you can see that the status is actually triaged. So similarly, we can now also check that listing all the tickets. As you can see, there are a couple of tickets here that I already added in some earlier tests. But here you can see that now we have the projection that is right in there and that's taken from the read model. So whether it has a note or not uh is right in here. Now let's do another test and let's add a note to one of these tickets. So now that has been changed and now let's see what happens with our tickets endpoint. So again we have a listing of all the tickets and as we can see that here we have the preview message but now has node has been set to true because that is happening in the projection. So now we have cleanly separated writing to a database which then does a projection and reading from it. And in this demo of course I do all of this synchronously right if you take a look for example at creating a ticket. So then we create it and then we're going to project it. So that's of course to keep it simple here. But conceptually the read side is still allowed to lag behind the right side. In real systems this often happens asynchronously. So the projection will happen after you already sent the response. And in real systems you probably also need to handle failures during projection. Uh for example by retrying it later or replaying it. Now what has happened here after all these changes? Well, first the list endpoint no longer fetches these full messages. It no longer computes the right fields. It can filter on things like has no cheaply. And that's a really nice advantage of the CQRS architecture. Another thing that's nice that you can now do all sorts of analysis and aggregates [clears throat] without affecting the right part of the system. For example, here I have a dashboard endpoint that does some uh grouping based on uh whether tickets are open, triaged or closed. And I do this of course on the read collection. So if I retrieve this data, so now you see we get this aggregate result. So this only works with the reads collection. The right collection is not affected by this. And that is exactly what we want, right? And you can imagine that in order to support the dashboard more easily, we can also create these projections automatically without even requesting it. So that the dashboard always gets a more or less recent projection of the sales data which is then for example run once an hour in order to get the latest information but then every time you load your dashboard you simply get that summary information the aggregate information without actually having to uh access any of the tickets at all. So there's a lot of things you can do on the read side now to evolve it without affecting how tickets are actually being stored. Now of course in this case I'm using a single database in MongoDB with two different collections. So CPU, memory and the disk are still shared but architecturally the right side stays stable and the read side can evolve with needs of the user interface. And of course later on you can split this up more. You can create a completely separate database for the read side of things or you could even use different types of databases depending on what kind of operations you need to perform. Now when do you use secrets? When should you avoid it? Well, first it starts to make sense when reading and writing starts to diverge. Your needs are going to be different. Uh for example, if you have dashboards, aggregates or you need uh derived fields and that require some computations. It's also a great way to protect business logic in the sense that for example, if you do updates to uh the ticket structure, that doesn't necessarily have to affect the read side of things. So that's another nice thing that you get as well. Now when do you not use this? Well, if you just have like a small CRUD app uh and basically reading and writing is not that different. Uh you don't want the added complexity. Basically the opposite of the reasons why you would use it. So in practice though I would say start simple. Don't immediately start using CQRS but only refactor towards this type of architecture when the pain is real when you actually need this type of thing. If you are in that situation when reading and writing becomes very different and the operations are complex, CQS is a really great architecture. Now like I said in this example, preview and has node they're pretty small but you know that's exactly how real systems start drifting by adding these small things and that might grow into more and more complexity later on. But I'd love to hear from you. Have you implemented CQS yourself? Have you used it? What have you learned from that? Let me know in the comments. Now, if you enjoyed this video, check out my video on the specification pattern. It's another great and well kind of scary way to keep your business rules under control. The video is right here. Thanks again to MongoDB for sponsoring this video and thank you for watching and see you next

---

## 2. Stop Building Ugly APIs: Use the Fluent Interface Pattern
**Channel:** ArjanCodes | **Views:** 30K | **Date:** 9 days ago | **Duration:** 17:36 | **ID:** f1Nb_JV_s8Y
**Link:** https://youtube.com/watch?v=f1Nb_JV_s8Y

### Transcript:
Let me show you two ways to describe the same animation. So here's the first version. So this animation consists of a number of steps. We rotate, we move, we scale, we fade, we move again, and we fade again. And then there's durations for each of these steps. Second version looks like this. So it's the same behavior, but it has a completely different feel. And the second one is using a pattern called fluid interface. In this video, we're going to start with a small animation engine and make it cleaner step by step and refactor it into a fluent API that feels like you're telling a story in Python. Now, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get this at iron./design guide. This contains seven steps I take when I design new software. Hopefully, it helps you avoid some of the mistakes I made in the past. Iron/design guide. The link is also in the video description. Now, what is a fluid interface? A fluid interface is a style where methods return self. So, you can chain them. For example, in Python, that means you could type something like this, etc., etc., etc. Now, of course, it's not a valid code. I'm simply typing it here, but that's the idea. And the main goal for this type of interface is that it's more readable and that it's more like a flow. So it's not really about performance or having a clever design or something like that. You typically use this interface to write code that describes a sequence of things to do in an order that they should happen. And even if you've never heard of the name fluid interface, this is probably a pattern that you've seen before. If you use pandas for example, uh it uses the fluent interface on data frames or in OMS like SQL alchemy, you use it to construct SQL queries. And finally, pipeline tools are also a really good candidate for fluid interfaces. And these APIs, they feel comfortable because they match how we think about transformations. And that's why I picked animation as a domain to show you how this pattern actually works. So the way I set this up is that this is a simple example application that animates shapes on a TK inter canvas. So this is my main file. So the first step is some basic TK setup like creating a canvas. Uh then I create a graphics render. That's something that I built myself. I include in the code example. Then I define an animation for a square. I create a shape of that square where I pass the animation. So animations are stored alongside shapes. I do the same thing for a triangle. So it has some steps and durations. I then create the shape of the triangle. Uh then I have these two shapes. And then whenever I click a button or touch a key, it will start the animation by playing a particular scene. So that gets the renderer and these shapes. When I run the script, it shows the canvas. And I can click on this and then we have these two animated shapes that I just created. So like I said, I already created a simple graphics engine and animation system for this. The graphic engine is very basic. It has a convos and it has a render method. And this render method allows us to render a shape. So we get the shape ID, we get some points and we get a color. So graphics render doesn't know anything about specific shapes. it works on a pretty low level and then it's uh if the shape is not there yet it's going to store it and otherwise it's simply going to render it. So it's a really simple setup. The animation system is slightly more complex. So first I have an animation step protocol. So this is basically a definition of what an animation actually is and it's very simple. It has an apply method that gets shape data. So that's basically if you look at the graphics uh library that's basically list of points and points are tuple of two floats and it gets a color which is analyze for a string and it gets some time value between zero and one that determines where we are in the animation and it returns computed shape data and color. So this is typically a pure function. uh there's a helper uh function to do linear interpolation and then we have some example animations like moving. So we get uh dx and dy and we apply that to the shape data. We have rotating, we have scaling and we have fading which is the only animation type that actually modifies the color. Then I have a data class called animation that contains a list of these steps and default that's an empty list. I also have duration. So the animation specifies how long each step is supposed to take and they happen in sequence. And there's a start time. So you can let the animation start later. And then there's some helpful properties like uh computing the total duration of the animation and computing the end time which is the start time plus the total duration. Finally, we have a shape data class. So a shape is nothing more than something that has an ID which is used by the graphics engine. We have a bunch of points and we have a color and then it can have an associated animation. It's optional. And then finally, there's a play scene function that gets a renderer that gets a bunch of shapes and then it looks for animations in those shapes and it goes through a while loop to actually animate all of those shapes and render them. Now maybe you want to do this slightly differently because now play scene is sort of both the animation engine and the game loop. So you might want to split this up in a more complete example, but I've kept it relatively simple in this particular case. And then the way I used in the main function I showed you before is that we create an animation object like so. I create shapes and then in the end I call play scene passing the render and the shapes. Now, the issue is that this is at the moment kind of clunky. It doesn't really feel that great to work with animation objects in that way. One thing we could try to do is maybe rewrite this slightly differently. For example, I could also do an instep so we don't define this whole animation object all at once. So, I could do something like this. Uh let's say the start time is zero. And then basically for each of these steps, we can do something like this. square animation dot steps dot add or append actually. And then we can append a step like rotate 60. And then we can do the same thing for the durations. So now we did this one and then we can do the same thing for all of the other steps as well, which uh is probably even more verbose than what we had before. So either of these options is not really great. It feels clunky. On top of that, you have to mentally keep steps and durations in sync. These always need to be the same length. So it's really easy to make off by one mistakes and reading this is kind of hard. Now in the end, the way that we define the animation is independent from uh how play scene actually uses it to actually render the animation. Play scene only needs access to the data in shapes and animations. doesn't care how it was defined, whether that's nonfluent or fluent. It just sees a list of steps and durations. And this for loop here iterates over these things in order to compute where we are in the animations. So rendering this animated scene doesn't rely on how you define animations. And in general, that's something to consider when you write software. You can typically make a distinction between how things are defined and how they're being used. And if you can design it in such a way that these things are independent then this gives you a lot of flexibility. Now the first thing that I want to do is that I at least want to make it a bit easier to do stuff like this. So uh what we can already do is extend the animation class so that we can append step and the duration uh without having to touch these uh steps and durations list directly. And in order to do that, let's create an add method in the animation class. So this is going to get a step and it's going to get a duration. And then what I'll do simply is self.steps.append the step and self do.append the duration. So now that we have this at least this already becomes simpler because now I can simply write square animation add and then we add a rotate 60 with a duration of 1 second like so. And I can remove these lines and now I can basically rewrite these steps and duration so that each of these is just a single line. So that's already a lot simpler than what we had before. And at least this way we don't manipulate steps and durations separately and there's one place namely the animation class that is responsible for keeping this logic in sync. Now if we want to turn this into a fluent API this is actually really simple now because instead of returning none we need this to return the animation object and then we can chain these method calls. So what I'm going to do is let this return self like so. And then in the animation class I will add a return method. And that's all there is to it. And what I could do now is in my main file I can actually call add several times. So I can do add rotate but then I can call add and then I can add a move like so with a duration of one. And I can add the rest of the steps as well. And then you get something that looks like this. It's just to be sure. Let's run this and make sure it still does exactly the same thing. And fortunately it does. So what makes this work is the fluent interface method chaining. That's the key ingredient. However, that's still a bit lowle. We're adding generic steps like uh rotate and move and we need to know about all of these different objects. So that also means in my imports I need to import all of these things which is kind of annoying. Or I can import animation but then here I' have to write animation.rotate animation.ove etc. So this is all still a bit lowle and uh it's still not very easy to read this. If you really want to turn this into a fluent API, then you need to give it domain specific verbs. So what you can do then is add methods to the animation class that add certain types of steps directly. For example, I can create a move method here. A move gets a dx, it gets a dy, and it gets a duration. And let's default put that to let's say half a second. And just like the add method, move is also a fluent interface method. So this also returns self. So then what this does is return self.add add move dx dy and we pass the duration as well. So move is simply a wrapper for adding a specific move operation and I can add similar methods for all of the other operations that we have. So rotating, scaling, fade to etc. And now that we have this I can go back into the main file. I can actually write this in a much simpler way. So I can simply write square animation dot rotate 60° and duration one dot move 200 1 second dot scale 1.3 etc etc. So then ultimately you end up with something like this for all of these animation moves. But then what you can do instead is actually not do it here but call it directly on the animation object. And then if you put this between parentheses like so. And let's format this nicely. Then you get something like this. And you can make this even shorter by removing the places where the duration is 1 second because that's the default. like so. And actually start time zero is also already a default value. So now we went from this very complicated animation setup to just a few simple lines of code by relying on uh the fluent interface pattern most importantly but also being a bit smarter about the default value. So it's almost like reading English, right? We have an animation, we rotate, then we move, then we scale, then we fade, etc., etc. So that's the fluid interface in short. And just as a final check, let's run this one more time. And indeed, this still works. And just to make sure that I'm not just u making this up, let's take this square and move it, let's say, to 400 and scale it to three and see what happens then. And here, this is what happens. So actually, this is pretty cool. We now have a little graphics and animation engine that you can use for games and whatnot. Now, like I said, it's kind of limited. I didn't really uh work on a proper game loop. Maybe I should do another video about that. I've been thinking about doing that for a while, actually. It could be fun, but I'm not sure how interested you are in stuff like that because Python is not really the default language to do game development in. Let me know in the comments if you'd like me to do that video. Now, fluid interfaces, what I showing today, they're really a great fit when you have sequences of operations where the order matters, like in this animation engine that I showed you. If you want to uh have an API that reads more like a story, like a domain specific language, things like animations, data pipelines, configuration flows, validation chains, query builders, these are all good examples of where the fluid interface is a really good pattern. But there's also situations where you shouldn't use this. Uh if you need like simple one-off operations, then well just do a single function call and that's totally fine. Or maybe you have domain where things are not naturally sequential. Uh or maybe you want to have very strong guarantees about immutusability at each step. The fluent interface that I showed you here basically modifies the animation at each step. So uh that's something to be aware of. Or another case where a fluent interface doesn't really work that well is if you need to do things that are out of order like first you want to apply step three but you want to skip the step after that etc. Also if you have really long chains then maybe a fluid interface is not a really good approach either or you could create another layer on top of that that generates uh subchains. So there's still some possibilities but it's maybe not the best fit in the end. fluid interfaces. They're a tool to increase readability. And once they start reducing readability of your code, then well, they're probably the wrong tool for you. And by the way, if you enjoy refactoring APIs like this and want more videos about design in Python, give this video a like and subscribe to the channel for more. It helps me a lot. Now, like I said, fluent interfaces, they're a great way to make your Python code more expressive and storylike, and they're already everywhere in the Python ecosystem. Once you understand the pattern, you see it all over the place. But I'd love to hear from you. Where could you apply a fluent interface in your own code? Or maybe you've already built this before. Let me know in the comments. And you might think, hey, doesn't this pattern actually look like the builder pattern you covered recently in another video? Well, yes it does, but actually the intent is quite different. Builder is about constructing some complex object in a controlled manner. Whereas the fluent interface is about making an API read more like a sequence of actions. So in our case, the animation isn't just built. It represents a timeline of operations. So in essence, builder and fluent interface, they're quite similar, but they're not exactly the same thing. If you want to learn more about the builder pattern, check out this video next.

---

## 3. 10 Python Features You’re Not Using (But Really Should)
**Channel:** ArjanCodes | **Views:** 52K | **Date:** 2 weeks ago | **Duration:** 20:45 | **ID:** cXl-AUXHHZY
**Link:** https://youtube.com/watch?v=cXl-AUXHHZY

### Transcript:
Here's a CSV file with millions of sales records. And here I have a function that reads the file and computes the total sales amount. Now, when I run this, you see that every time I read this file, if I need it in my code somewhere, it's going to take time. I mean, it's not that bad. This file actually reads like 10 million records. So, I'm kind of surprised it works this fast. But every time you do it, you actually have to wait for the dis IO. This is what happens when I add caching. As you can see, the second read happens instantly. And what's really cool is that this is really easy to add in Python. In today's video, I'll walk through 10 features exactly like this. Really powerful and really easy to add in Python. Now, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get this at iron.codes/design guide. contains seven steps I take when I design new software and hopefully it helps you avoid some of the mistakes that I made in the past. Guide the link is in the video description. There's lots of features in Python that help you reduce code size, improve clarity, eliminate entire classes of bugs, and speed up expensive operations. And unfortunately, most developers don't know they exist or don't know when to apply them. And because they're part of the standard library, you actually don't need extra dependencies of frameworks. Python is really a batteries included language. Now the first thing I already kind of showed you is caching. So in the before version of the code, we simply had this total from file function. There's nothing special here. Just uses pandas to read a CSV file. But if you want to cache this function, that's actually really easy to do. So in this case you simply import from funk tools the cache decorator and then you just write cache on top of the total from file function. If I now run the code then you see that this is what happens. So the second read happens instantly. You also see that it doesn't print reading the file anymore because this is actually cached. So it doesn't execute the function twice. So this is really perfect for expensive file parsing, large configuration loading, uh calculations that are expensive rendering templates or anything deterministic. You eliminate duplicated work and it's going to make your system much more responsive. It's great fit for uh CLI tools, ETL processes, any repeated batch operations. The second feature that I use a lot is protocols. So if you take a look at this piece of the script. So what this does is that it takes a sale object that's actually a very simple class that just has an amount and a currency and a converted value and then it uses a rate fetcher to convert that sale amount into a different currency. Now the code contains these types of uh type annotations which is fine. Actually the Python interpreter ignores these at runtime. So they're mainly there for you as a developer to better understand the code. The issue here is that the value that's injected here is of type static rate fetcher and that directly ties this function to this particular class. So if you wanted to replace this with some other rate fetching method maybe you want to take it from a database or you want to use an external API. This is uh annoying because then uh the type doesn't match. You can't simply create another class that has exactly this structure and then put it in here. Well, you can actually your Python code will run, but you run into all sorts of uh type issues while you're working on the code. So, that's not so nice. What's nice about protocols is that it allows you to specify a sort of contract of what an object is supposed to be like when you use it. So what you can do instead of directly putting in this static rate fetch is actually create a protocol and that I'm going to import from typing and then what we'll do is I'll create a class that's called let's say rate fetcher and this is a protocol class and this is going to have a single method called get rate and we don't provide an implementation because this is a protocol. And now I can change the type here to a rate fetcher instead of a static rate fetcher. As you can see, there's no type errors here. It's all very clean. This works just as well. And now I could create another class and simply inject it here if I wanted to. For example, let's say I have a unit rate thatcher which simply always returns one. This can be something you might want to do uh in a test or something. And now what I can do is in the code where I actually call this. So that's happens right here. I can now input a unit rate fetcher. And that also works as expected. So let's run this and see what happens. So of course here the converted value is the same as the original value because we're applying a conversion rate of one. So in short this protocol defines a structural interface and any object that implements the methods in this case get rate is accepted regardless of inheritance. And this is really nice for plug-in systems uh test fakes dependency inversion and strategy patterns. You get a lot of flexibility without tightly coupling classes through inheritance chains and type checkers can now warn you if an object doesn't satisfy the interface. The third feature I want to show you is replace from the data classes module. If you look at what happens in this convert sale function, you see that actually it gets the rate and then it stores the converted amount in the sale object. So it mutates the sale object that you pass as an argument and then it returns that as a value. Now, it would be cleaner if you would get a new sale object instead of mutating the existing one. Now, you can manually extract the data from the sale object and create a new one. But if you use a data class, there's actually a much cleaner way of setting that up. So, from data classes, I'm going to import data class and replace, which is something that we'll use later on. So first I'm going to convert this sale class into a data class. So this is going to have an amount which is a float. It's going to have a currency which is a string. And I of course need to type that correctly. And it's also going to have a converted value which is a float or a non value. And default this is going to be none. There we go. And now what we can do is make this a frozen data class so that we can't modify objects once we have created them. And of course now we have an issue here because in the convert sale we now try to assign a value to the converted uh instance variable of the sale object which is not allowed. But instead what we can now do is use replace. And this is actually really easy. I can just create a new sale object and that's going to be a call to replace. We pass the original sale object as an argument and then we assign the converted value to sale amount times rate like so. So this gives me a new sale object and then I can remove this line that's no longer needed and I can return that as a value. Obviously, you can also directly return uh this call. So that would make it even shorter. Would look like so. Replace produces a new object with some of the fields changed. In this case, there's just a single one. And this is much safer, easier to reason about, and makes undo and redo patterns also very easy to implement. Also fits naturally with event sourcing, business workflows, and pure function. So this is a really nice feature of data classes in my opinion. The next feature I want to mention is pair-wise from iter tools. So here I have a function that computes sales deltas. So it takes a sequence of numbers and then for each of the pair of numbers in the sequence it computes the delta and then returns a list containing all of these deltas. So in this case I just implemented a manual uh for loop that goes through the entire list of numbers and computes that. But actually there's a much simpler way to do this. Namely by using the pairwise function from its. So I'm going to import this and then instead of doing all of this going to delete all of this code and I'm going to return a list comprehension of b minus a for a and b in pairwise of the numbers. And that's it. It's really easy. So this handles these type of adjacency algorithms very elegantly. It reduces mental load, removes uh off by one mistakes and works great if you're dealing with time series data logs signals other types of deltas that you might need. The next feature I want to talk about is assignment expressions or the wall rust operator. It's kind of infamous. Uh some people like it, some people don't like it at all. I don't use it all that much, but when I use it, it can be really useful. So, here's an example of where you could typically use this. So, I'm reading some kind of large file in this function. And because I use a y loop in this case, I have to uh write this code twice. Unfortunately, Python doesn't have a do loop where you can kind of invert the uh the condition. So, in essence, I have to duplicate the read operation code in this case. What's interesting about the walrus operator is that it turns the assignment into an expression. So instead of doing it like this, I can delete this line. And then I can write it like so. And now I don't need to do this. Still need to add a column. And then that's all it takes. The warus operator simplifies code that reads, parses or streams. It can sometimes be really helpful in reducing repetitive code in loops. Now, like I said, I'm not using this all that often, but when I'm in need of this type of thing, it's very useful to have it as a feature in Python. Next up, and that's more of a library than a feature, is path flip. Pathflip is actually really helpful. So, if you take a look at this function that loads all the JSON files in a particular directory. So, I'm using os.list there and then using that to uh read the file, constructing the path, etc., etc. So there's a lot of manual work that happens here and with path lip this is actually much easier to set up. So the first thing that I'm going to do is from path lip I'm going to import path and then what I'm doing here is replace this entire for loop by something that's much simpler. So I turn the directory that's passed as an argument into a path. You can also directly accept a path argument here if you wanted to, but I'll leave it like this for now. So turn it into a directory, but then you can call glob on this and search for all the files ending with JSON. And now I can simply load the data by calling JSON.loads path.ext. And path itself is also a very helpful object. So for example I could do results and then I can do path stem equals the data and now I don't need all of this like so and then I can simply return the result. It's much simpler. So as opposed to OS path lib gives you highle path objects with lots of these type of intuitive methods. It avoids string based path entirely which reduces bugs. files, directories, globs, writes, read, all work consistently and overall it makes your file handling code feel more Pythonic and modern. The next thing that I want to show you is an easier way to suppress exceptions. So let's say we have a function that removes temp files. And here's what the body of that function looks like. So it just takes a temporary file and it unlinks it. And in this case, I'm also using path li and then the unlink method. And here I don't really care if this fails. If the temp file doesn't exist, I don't need to do anything. So I simply want to ignore that error. So to do that, I just put it in a try accept block, which is totally fine. But actually what you can do is use suppress, which makes this a bit shorter. And that is part of context lip. And what this gives you is a context manager that suppresses a particular type of exception. It's really easy. You just type with suppress and then the error that you want to suppress. And now I don't need this except part with the pass. So this is a much shorter way to write the code. And on top of that, I also find the intent clearer in that well we are explicit about that. We don't really care about this error. This is a great feature for tear down steps. optional files, retries and graceful fallbacks. The next feature that I want to talk about is context vars. So if you take a look at this example, we have a process user function. This is all asynchronous. So this gets a user ID and a request ID. And here I have another function that's called handle request that also gets a user ID and a request ID. And then I'm calling these using asyno.gather. So these are launched basically at the same time and handled concurrently. Now this is a relatively simple example but if you have like a whole chain of things where you have to pass through the request ID through all sorts of different layers and functions then you add lots of arguments to the functions even though the function itself may not necessarily need it. So instead what you can do is import context vars which gives you capability of doing this slightly differently. So what you can do then is create a request ID. And I'm doing this here as a global variable. You can also define it elsewhere, but we're going to define a context var. And actually I'm going to do it like this. It's a bit easier. And then I can simply specify a name request ID like so. And let's say default this is going to be unknown like so. And now what you can do in your handle request function. So that's let's say the top layer where you get the request ID for the first time is create a token. And let me also rename this so that we don't get any conflicts. And then once we're done with processing the user, I can reset the context like so. and process user. I'm simply not going to pass the request ID anymore like so. And then here inside I can access the context var using the get method. And this then gives me the request ID which I can then use in my process user function like so. Now you might wonder why do I need to create a context var here and can't I just use a global string if I wanted this? Well no that doesn't work because we have multiple concurrent function calls. So that's going to lead to all sorts of conflicts and that's exactly what context file solves. So each of these async tasks gets its own isolated logical context and this is really helpful for logging tracing uh metrics or user session info that you want to keep track of and this is of course especially powerful in asynchronous or concurrent programs. The next feature that I want to show you is match with guards. Now if you take a look at this function, this has like a bunch of uh if else statements. So if the amount is less than zero, we're going to return invalid. If it's zero, we'll return zero. If it's larger than 10,000, we return large. Otherwise, we return normal. So this is fine. You can imagine maybe if there are more cases like this, the if else statement becomes a bit more complicated. But what you can actually also do is use structural pattern matching to write this slightly differently. And what I like about this is that currently in the if else statement, we have to repeat the variable all the time. And you can actually shorten this a bit. So what you can do with pattern matching is that you write match the amount. Let me indent this. And then we're going to handle the cases. So in this case you can instead of writing this you can write something like this. So if it's less than zero we're going to return invalid. And then we can also do the other cases. So if it's zero we simply return zero. If it's larger than 10,000 we return that default we return normal. So in my opinion this is a really nice way to express logic like this. In essence, pattern matching gives you a declarative way to describe these cases and this makes it easier to write validation rules, classification, parsing logic, things like that. And as your logic grows in complexity, in my opinion, this is a bit easier to manage than all of these if else chains. The final feature I want to show you is exit stack from context lip. So if take a look at this function that reads files, it gets a list of paths and then it's going to read them and it will return a list of strings. And as you can see, what I'm doing here is sort of manual uh opening and closing of the files trying to figure out if it exists or not. This is not really a great way to write this. But from context lib, we can import exit stack. And this allows us to do this more cleanly. So what I'll do here is create a context manager like so. And then files is going to be stack dot enter context. And then I can call open for n paths if pexists. And then what I can do is simply return f read for f in files. And then I can remove all of this. like so. So here I'm using opening files as context but actually exit stack lets you manage an arbitrary number of context managers dynamically and it can be different things. It can be a context manager for accessing a database or for doing other things as well. And this is really nice for uh when you don't know in advance which resources you're going to need. This simply collects all the entered context and closes them automatically. So, if you have some optional resources, maybe a plug-in architecture, a conditional setup, some multi-resource operation, this can be really helpful. I hope you enjoyed this video covering some lesser known Python features. Give this video a like if you did and subscribe to the channel to watch more of my videos covering Python and software design. And I'd like to know, were you aware of all these features? Uh, which one are you going to use in your next project? And by the way, if you want to take this a lot further, make sure you check out my video on writing Pythonic code. This brings together many of the principles behind these features. Link is right here. Thanks for watching and see you next

---

## 4. This Design Pattern Scares Me To Death
**Channel:** ArjanCodes | **Views:** 64K | **Date:** 3 weeks ago | **Duration:** 29:36 | **ID:** KqfMiuL3cx4
**Link:** https://youtube.com/watch?v=KqfMiuL3cx4

### Transcript:
This is a video about the specification design pattern with a big warning. It's extreme overengineering. I wasn't even sure whether I should actually post this video. The reason I'm doing it is because, well, there are a couple of really interesting design ideas that I just don't want to keep from you. But frankly, when I look at the final version of the code, it scares the out of me. It's love craftsion. Is that a word? That should be a word. When you first look at it, you go like, but then it's also kind of interesting and then it sucks you in and then bam, you're done. We're going deep today. Higher order functions, decorators, thunder method, overrides, generics, the whole shebang. It's going to be insane, but really good. This video is sponsored by SER API. More about them later. Now, what is the specification? But let me first show you a code example. I have a user class with a bunch of different settings. And there's a function that checks whether this user can access some API. As you can see, the logic is kind of convoluted. There is an age, whether the user is active, is it banned, what country the user is in. Now, you can maybe refactor this a bit to turn a lot of these things into guard clauses, right? But it's still kind of complicated business logic. And you can imagine that this is maybe something that regularly changes, maybe because the company changes uh the policy or something like that. And perhaps you have very similar kind of business logic in other places as well like can you access some kind of report which you know looks kind of like it but is slightly different or maybe you have another function that checks whether you can access the CLI which again is slightly different from the first version. Now maybe this is intended maybe not who knows. The problem with this type of business logic is that well here this example is of course very basic but this would normally be spread out over your entire codebase and mixed with other types of business logic and whenever you want those rules to change it means you have to update this in many different places and you don't have a clear overview of what those rules actually are so today I'm going to show you the specification pattern which fixes this by turning these complicated if conditions into reusable building blocks and ultimately this will let you turn all of this code into data and I'll show you an example of how that works at the end of this video. Now this specification pattern doesn't come from the gang of four book it's from domain driven design and at the core the idea is to encapsulate business rules into small specifications and then you can combine these specifications using boolean logic and then you can reuse that everywhere. Now if you have a classical object-oriented language, you would then basically implement this with lots of small classes and each class represents one of these business rules. You can find a bit more information about this pattern on Wikipedia. Uh so you can see there is like a UML diagram where we have various types of specifications. Uh there are some code example which is extremely abstract. So, and when I read this the first time, I had a hard time understanding why you would actually need this because what's the advantage, right? You can just write the same logic in an if condition. So, why would you have all of these classes to wrap around that? So, that's what I'm trying to address in today's video. There's even a Python example in Wikipedia that adds all of these classes without really showing why you would want to do that. The only actual example that's given on the Wikipedia page is in my opinion something pretty basic that you can also just capture in a regular if condition. So in this video I hope I can take this a few steps further. Now the goal that we have is that we want to have rules that are reusable and composable. And obviously like I already said the example here is pretty basic. uh but I do think in Python we can do much better than this objectoriented example from Wikipedia. In Python we can use functions in much better way. So we can build a small functional framework for these rules and we can rely on other features like uh decorators and generics and cool stuff like that that we have access to in Python. So the first step that I want to take is to move these conditions out of this if statement into a sort of uh let's call that a predicate. Basically a predicate is a function that returns true or false and then we can later on combine these functions so that we get more complicated logic. So for example here you see a bit of code that could be turned into a predicate. So that would be then a simple function called let's say is admin. That's a user. It returns a boolean value and this returns whether the user is an admin. So that's a function returning true or false. That could be a predicate. That could be a rule. And we can do this for other types of rules as well. For example, is active like so. or this check whether the account is over 30 days. So these are some basic example of rules that we can pull out of these functions. Now I can directly call these functions as part of an if statement, right? I can replace uh this with a call to this function. So that would basically look like this. But the problem with this approach is that we now create the specification of the logic and we call it at the same time. That's the whole issue with these if statements. So what we actually need to do is find a way to specify the logic here without already evaluating it. So that means we need to somehow combine these functions and then only afterwards we should then execute them after they've been combined and then we can separate the specification which is the definition of how all these functions look like and how they're combined with actually using them. But of course if we don't evaluate the functions we don't have a boolean result. So we need a way to combine these functions logically without already evaluating them. So what we can do is create a small class. Let's call it the predicate that contains a function and that overloads a few basic logical operators and, or, and not. So, I'm going to create a new file here. Let's call that rules where we're going to encode this logic into. So, what I would like here is a class. Let's call that a predicate. And for now, I'll change this back later. I'm going to remove the user class and put it in here. Later on, I'll put it back in the main file. So my predicate is basically a wrapper around a function. So that needs an initializer that gets a function which is a collable and this function gets a user a user and returns a boolean value like so. and I store this function inside the instance and from typing we're going to import gable like so. So this gives me a predicted class that's basically a wrapper around function. And then what we can do so that we can still call it is override the call upon the method. This user lower case obviously returns a loop and that calls this function with the particular user. So it's really now a completely empty wrapper around this function. If you call the predicate then it's simply going to call the function. But what you can do now is since we are in a class we can override the logical operators. So and it's an example. So that gets an other predicate like so. And that will also return a new predicate. And this will return predicate object that is wrapped around a function lambda function that gets a user and that returns oh oh actually I don't need return self and other. So basically we generate a new function here that when we call it it's going to call this and logically combine the results. Now similarly I can define or and not invert. So now this predicate class allows me to specify the conditions without already executing them which is what we want in the specification pattern. So we can specify it and then when we need it we can actually run the function. So what you can do now is take uh these functions and actually uh from rules I'm going to need to import user and predicates because we won't need these. We can now specify that is admin is a predicate and that gets a lambda function of a user where we check whether the user is an admin like so that we delete that and same thing for is active and finally account older than 30 is also a predicate where we check that user account age is over 30 like so and I delete that. And now we can start composing the rules. For example, I could say that well a rule is that the user should be an admin or it should be active and account older than 30. And here I'm not executing the logic. I'm purely combining these specifications. So this is exactly the power of the specification pattern. So we have a predicate which specifies what a rule looks like. We can then define these rules and we can combine them without actually executing them. So let me just clean up this example a bit and remove this. So uh we have a user and what I can then do is print rule and rule actually this is now a function that we can call with a user. So I'm simply going to pass the user that I created here before. This is a sample user. So when I run this code, I get outcome true because well this particular user is not an admin but the user is active and the account age is 35. And if I do 25 for example, what the result is surely going to be false. So we've now created a very simple implementation of the specification pattern using functions where uh we have a predicate class that allows us to combine functions compose them logically without already calling them. So that gives me a rule system and then I can have a user and then I can print the rule. Before I clean this up and make these predicates much simpler, let me quickly talk about something related. getting consistent data from web scraping APIs because just like business rules, API responses can be surprisingly inconsistent. If you ever try to scrape data from Google or other search engines, you know how fragile that can be. One request times out, another gets blocked or the HTML changes and your scraper breaks. That is exactly the problem SER API solves. SER API gives you access to Google, YouTube, and many other search engines through an easy to use, reliable API. In a few lines of code, you can extract live search result data and use it in your application, train your AI models, or send it to a spreadsheet. They handle proxies, captures, and retries automatically and return well ststructured JSON, so you can focus on using the data, not fighting the network. It's really easy to use. Here's an example in Python of how to access the Google search API using the SER API package that's available for free. Now I just get the API key from an environment variable that I set in M files. That's why I'm doing load.N. Then I create a client, pass the API key, and then I run client.arch. I provide the query. I provide the target engine. In this case, that's Google search. And I also provide my location. And then I simply get back JSON data in a dictionary like structure that I can work with directly in Python like so. So this prints the first organic result. Let's run this and see what happens. As you can see, we get a dictionary here with all the information that we need. Or how about searching YouTube for content about the solid design principles in Python? It works almost exactly the same way except of course we change the engine to YouTube and then we can print the video results. And here we go. We have as a first result my video on Uncle Bob's solid principles. If you want to make your data pipelines more reliable, check out SER API. The link is in description or you can scan the QR code on screen. Now back to the video. The next step is making all of this a bit easier. If you have to write predicate and lambda functions all the time is kind of annoying. So let's define a small decorator. So from fun tools I'm going to import wraps because that's the best practice for defining decorators in Python. And then what I'm going to do I'm going to scroll down here and I'm going to define a decorator predicate and this is going to get a callable that gets a user and returns a bool. Actually maybe we should introduce a type alias for that. Let's say we have a predicate function which is a callable that gets a user and that returns a bull like so. And then we can also use that here which is nice like so. And then we can also use that in our decorator like so. And what this decorator is going to do, this will return a predicate because this is going to do the wrapping for us. So we use wraps around the function and then we have the wrapper. So this is the function that uh gets a user returns a bool and this calls the function with the user. And then finally, we're going to return a predicate wrapping around the wrapper like so. And now instead of having to write like this, I can go back to my function. So for example, this admin user returns a bool like so. And then I can remove this. And I'm going to import this predicate decorator. And I can do it like this. So now I have my function again defined in the way that I like it. It's much easier to read than uh this stuff. And now it turns into a predicate. And we should get exactly the same result as before. And we do. So this is much nicer, but there's still a couple of problems. We're kind of hard coding the user type here in our uh rules and we're also sort of mixing the idea of what is a rule and what is a predicate because for example I would like to do something like this. But of course that's not possible because all of these predicates they only accept a single user object and not any other arguments. So I need to refactor this code a bit so that it can also do stuff like this because then we can truly encode any types of rules with all sorts of different arguments. So the first step is that we go into our rules system and then we generalize these predicates and wrappers so that they can work basically with any type. Not just users but with any type. Because if you look at the predicate and the decorator, it doesn't really care that this is a user. This can basically be any type T, right? As long as it's something uh that the function accepts as an argument, we're good to go. So, what I'm going to do now is let's move user back to the main file where I think it also belongs like so. So, this one no longer going to import here because that's defined here. And of course we will need to import the data class again otherwise that doesn't work. And now what we can do in the rule system is that a predicate function is something generic. So it gets a type t and it's basically a function that gets a t and returns a boolean. The predicate class should also be generic and this should then of course get a predicate function of type t. And now of course we can also change this. So let's call this some object. This is going to be of type t. And we're going to do the same thing here. So this is a predicate of type t. This as well. This is already getting fancy, right? Like so. And we do that here as well like so. And our decorator is the same. So this also gets a type a predicate function of type t. This needs to be generic. And this also returns something generic. And then this also gets an object of type t. And now the type issues are gone. And we turn this into nice generic code. Let's take a look at our main file. Did this break anything? So, of course, here you see some issues because it's not clear now what type of predicate this is. Uh, this works as expected. So, uh, Python's type system can basically infer that this is a predicate with a user used as a generic type. But let's take these other ones and also turn them into more modern predicates using the decorator. So, is active. Let's say user returns a boolean view like so. And then this one we're going to delete because we don't need that anymore. And same thing for the accounts age. And for now I'm simply keeping it like this. Uh 30. There we go. And we delete this. There we go. And then let's for now keep it like this. And just to make sure that this still works as expected. And [clears throat] it seems like it does. So now we have our generic version. The next step is that I want to have a bit more flexibility in how I define these rules. So basically what I want is that this rule is able to accept other arguments than just a user. So let's just assume for example I would like to write something like this right and then here we use that as an argument. Now currently this doesn't work because the predicate accepts only a function that gets only a user. So we need to make this a bit more generic. So what we're going to do is that instead of the decorator returning directly a predicate function we can make it a rule factory. So that's basically a let's also rename this to rule in that case. So it gets a uh rule definition. A rule definition can something be a bit more generic so that it also accepts this kind of thing. So let's say we have a type rule definition which is a callable that accepts any type of argument. Now, ideally, I would like to be able to specify here that this should uh define a list of arguments and then the last one should be of type T. Unfortunately, that's not possible in Python's typing system. So, I'm going to leave it at that and accept the limitation of this. And then the final type that we need is that we will have a predicate factory of type T. And this is a callable that gets some arguments and that will return a predicate of type T. And then the rule decorator which was before the predicate decorator will return a predicate factory. Are you getting confused yet? I can understand if you are but give me a minute and then it'll all come together. So we have our rule decorator. This should now get a rule definition so that it accepts any function as long and that's something that we'll have to assume that the last argument of the function is going to be of type t. So our wrapper is going to have arguments type any. We don't really uh specify these at the moment. So I'm going to need to import this like so. And there is keyword arguments as well. And my wrapper will now return a predicate. And then at the end we are going to return the wrapper so that the type matches what we expect here. And inside the wrapper so this is now where we create the predicate and we're going to give it a function a lambda function that gets an object. So this is the thing of type T and it's going to call that other function where we have the arguments. The last argument is the object and then we have the keyword arguments. So this is not perfect because if you now define the object as a keyword argument is not exactly going to work but in most common cases this is going to be fine. It's a limitation. It's not perfect. Uh we have to work around it in some way. So this is our new rule decorator and now we need to change a few things in the main file as well. So of course we need that new decorator. So I'm going to add it here rule here as well. The only thing we need to fix now is that rule returns a function that creates a predicate. So instead of calling it like this, we now need to call everything like functions. But what we can do now is because these are now function calls, I can also change this into a function like so. And if we run this, you see that this still works. Even though at the moment, unfortunately, my uh type annotation system no longer recognizes this. I think we're just going way too far here. But I warned you, right? This was going to be blatant overengineering. Now the nice thing about this approach is that we have a rules management system with predicates and a handy decorator and we have the main file where we actually specify the domain. So we have the user and we have the rules that apply to uh this particular domain and we combine them in a specification without actually executing the logic. So this line here doesn't execute any logic. It doesn't call these uh functions directly. It simply calls the wrapper that generates the predicate which will only be uh called when we do this and that is basically the idea of the specification pattern. So we can specify the logic here without executing it which only happens here. And now that we have this we can basically build out this application. So I still have my data class. So this is a slightly more complete example where I've coded all of these original rules including uh the account holder them uh from which country uh a user is the the credit score etc. And then I can create these kinds of structures now uh which are the specification of a particular condition right and then this is again a function that I can call in other functions if I want to. So for example for reporting I can check for a list of users whether they adhere to these rules or same for a CLI tool or wherever you want to use it. Now the interesting thing and I think this is where we move from blatant overengineering to something that is maybe actually useful is that because we make the specification here it shouldn't be that hard to actually turn this into data. I mean we already have all of the rules and the logic. So this is something you can actually specify somewhere else and in order to not make this video too long. I'm not going to code this from scratch, but I'm going to quickly show you how you could do this. So the first thing that you need to do if you want to load it from let's say a config file is that you need to keep track of which rules are actually available. So in order to support that I've created a rules registry. So this is a dictionary that maps name to a predicate factory. And in my rule decorator, I do exactly the same as what I had before. I just add a single line where I also store this function in my rules dictionary. And then what I can do is I can write a function that loads a rule from a configuration file. Let's say a JSON file for example, where you can specify uh conditions like this with uh arguments and you can specify the logical relation between them. I have a more complete sample file here. So this is just a JSON file with a particular set of rules. So we have uh is active or we have account older with the argument 30 or from country Netherlands or Belgium or the credit score above 650 and so on. So you can build out this JSON structure without having to uh touch the code at all. And then I have a function that basically loads this JSON files. That's what happens here. And then it translates these conditions and logical combinations into the actual predicates. So here I'm getting the factory. I call the function with the argument. So this is going to return the predicate, the function that takes a user and returns true or false. And then I'm appending it to my list of predicates. And then depending on what the logic is, whether that's and or or I'm uh combining them in a different way. And then I return that as a result. And then in the main file, what I can do is I can create a rule by loading it from a config file. And then I can actually execute that rule. And then when I run this particular example, well, it runs a bunch of rules including the ones that it loaded from the config file. So that means that with this change, you can now move parts of this business logic into uh JSON or a YAML file or a database. You could let your product owners tweak uh thresholds and conditions in the user interface. You can reuse the same rules in code and in config and all thanks to this specification pattern where everything still composes nicely and stays testable. Now, in my opinion, like I already said in the beginning of the video, this is like nextderee overengineering and I happily accept your criticism in the comment section because it's all worth it to me. I mean, this ended up being such a cool design, and I didn't want to keep it from you. I mean, seriously, this predicate class that allows for logical composition, the decorator that generates these predicates, the generic version that works for any domain type, and finally, how this allows you to turn your business logic into data that you can specify in a JSON file or in a user interface. It's insane. But I'd like to know what you think. Did you learn something interesting today? Did this give you some ideas for your own projects? Do you think I'm completely out of my mind? Let me know in the comments. Like I said, it's very unlikely that you will actually use this, but this whole combination of predicates, thunder, method, overrides, generics, decorators, it's just a really cool design. Now, if you want to explore more software design patterns, maybe not as overengineered as this one, check out my design patterns playlist right here. Thanks for watching and see you next

---

## 5. 7 Things You Didn’t Know Dataclasses Could Do
**Channel:** ArjanCodes | **Views:** 35K | **Date:** 4 weeks ago | **Duration:** 20:24 | **ID:** YTsJQ4ixq88
**Link:** https://youtube.com/watch?v=YTsJQ4ixq88

### Transcript:
Most developers know the basics of data classes in Python. You define a simple class with some typed fields and then Python generates the boiler plate. Here you see an example of that. I have a user class. It's a data class. A user has a name and an email address. And because it's a data class, I can now initialize it as following. And I can simply print it. And it's going to print out the information in a readable way. Here you can see the result of running this script. But data classes can do way more than that. So today I'll show you seven things you probably don't know about data classes. Each of which makes your code safer, clearer, and easier to maintain. And the last one tends to surprise people. If you want to learn how to design software from scratch, grab my free guide at iron.code/design guide. It walks you through my sevenstep process for designing new software. Link is in the video description. The first thing that's really easy to do with data classes is default values. For example, if I have an active value that's boolean, I can by default make this true by simply assigning it a value. Then of course, if I run this now, each of these users are going to be active by default. However, since it's a default value, I can still override this and make one of these users false like so. So then we get this as a result. Now defaults are sometimes a bit complicated in particular if you're dealing with more complicated types. Let's say you have a tag which is a list of strings. Last a list of strings. Now you might think you can do this. However, this is problematic because actually this is an empty list and that's generated only once when Python runs the script. And if I run that now, you see actually data class protects us against this by raising a value error. So instead of doing this, what you need to do is define a field which you also need to import from the data classes module. And then we're going to define a field. And we're going to give it a default factory of a list like so. And now when I run this again, you see that this works as expected. Now there's a type error here. And actually the issue is that this is a list of strings, but we actually specify the default factory as just a list. So if you want to be precise, you also need to specify the default factory as a list of strings. Doesn't really change anything the way that Python works, but it's more precise in terms of typing. The second thing you can do with data class is that it allows for derived fields. These are fields that shouldn't be passed into the constructor. You can mark a field as not being part of the generated initializer and this lets you compute it later typically in the post initialization step and this particularly useful when the value should be stable and not recomputed every time uh unlike for example a property that recalculates on access. For example, let's say when we create a user, we want that use to have a slug which will point to a URL. Now, typically this is something you would create only once per user and you don't change it if the user information changes because then the URL doesn't work anymore. So the slug that's going to be a field, but we're going to set that as init false like so. So now this slug value is not something that you can set in the user initializer. But what you can do is define a post init method. And this is a method that is called right after the object has been constructed. So what you can do here for example is let's say we have a variable slugified and that's going to be the name dot let's convert this to lowerase and let's replace all the spaces by dashes like so. and then self dot slug equals slugified. Now you could set this directly. There's a reason why I'm doing this in two steps. That become clear in a second. A post in it by the way is also really helpful to clean up some stuff. So for example, I could do u something like this as well. And let's also change the name to title case like so. And now because here my name is lowercase. If I now run this then as you can see now the name is properly formatted. And also we have our little slug here. And if I change the name to let's say Alice Smith like so. And run this again. Now my slug is Alec. Did I actually write that? Oh okay. This is what I meant. So now you can see that the slug also is properly constructed. The third thing is to not forget that data classes are actually still classes. It's not just data. You can add methods, properties, helper functions, derived attributes, everything that a normal class can also have. And that means that if at least you're being careful, your domain logic can live right next to data that it belongs to. For example, you could define a method, let's say contact card that will return a string, which is let's say a formatted string containing the name and email address like so. And now I can print the contact card for a particular user. And then this is what we get. Or you could add properties as well. Let's say we want to know the domain belonging to the email address. So in this case, we're going to take the email address. We're going to split it by the at sign and then we're going to take the last part. And since it's the property, I don't need to write parenthesis. Let's run this and see what happens. And there we go. We have the domain of that particular user. In short, a data class is just a regular class. So you can still add these sort of things. Of course, be careful. Don't turn this into a god class, but this is still something that you can do. Fourth thing is that you can have frozen data classes. And that doesn't mean they're cold, but it means that the instance is immutable. and that improves safety by preventing accidental changes. Now, under the hood, this just blocks all assignments to attributes after initialization. So, in the case of the user, what I can do is turn this into a frozen data class like so. However, now you see that there's a problem in that we're trying to assign to attributes in the postinit method and that is now broken because if I now run this, you see that we get an error. So if you have a frozen data class, you can actually still do things like this, but you need to be a bit more explicit about it. So first let's just store this into a normal local variable like so. And now instead of directly assigning which is not allowed, we can do object dot set at. And we're going to set the name attribute to normalize name. And we will set the slug attribute to the slogified value like so. So now when I run this again, this works as expected. Now, as you can see, this is kind of annoying that we have to call it like this. I'd prefer actually if a frozen data class would for example still allow you to set the value in the postinit method but that would actually change the syntax of the Python language and data class is simply a module that is built on top of Python. So that simply doesn't work. However, as you can see this is still possible to do. Now one thing to remember is that immutability applies only to attributes themselves not to the contents of mutable objects that are stored inside them. And that means that a frozen data class with for example a list of tags is still uh vulnerable if you mutate the list later on. For example, if I take my uh user, let's say user two, I can take my tags list and I can simply append a value to it. And that is perfectly fine because I'm not mutating the object. I'm muting the list of tags inside of it and list is a mutable object. So here you see I've now added this tag. So that's something to be mindful of. Frozen doesn't mean that everything in a data class is read only. It only applies to the attributes. But there are more options that you can add to data classes. For example, ordering which makes them comparable. So you can sort them or use them in priority cues without writing that comparison logic yourself. Now the thing is by default let me just remove this for now. Let's say if we want to print uh u1 less than u2. So we're trying to compare these two and I try to run this. It doesn't work because this is not supported between these two instances. But what you can do is add an order equals true option to the data class. And now you see also the error disappears. I'll try to run this again. And now we actually see that user one is less than user two. So this actually works. So it's really easy to add this kind of behavior with data classes. Another thing you can do is set slots to true. And this actually improves memory usage and speeds up attribute access by removing the instance dictionary. That also means that you're not supposed to add attributes on the fly. Then it's not going to work correctly anymore. But in most cases, you're not doing that anyway. So setting slots to true is a really great way of making data classes containing lots of data a lot faster in terms of uh attribute access. And as you can see this still runs in exactly the same way. Another thing you can do is make fields keyword only and that means that you are limiting the way that the initializer is being used. For example, now I can't define a user simply by passing the arguments in order to the constructor. If I try to run this code, this is actually uh not going to work because it's not what the initializer expects. So, uh what this does is this forces you to use these keywords argument names which makes defining users in this case a lot more clear. Now, we know this is the name. We know this is the email address, but it can still help with readability in certain cases. And of course with keyword only arguments doesn't matter what the order is of the arguments and makes object construction overall clearer more explicit. So the result of making these type of changes is that you have data class that's faster. It's safer to use and it's easier to maintain. Now it's possible that next to the initializer that the data class generates, you also want to have other constructors. And because it's simply a class, you can add custom class methods that act as alternative constructors. actually do this all the time and these allow you to create objects from domain specific inputs. For example, let's say you want to generate a user from just the email address and that the name is basically extracted from the email address. So what you could do in that case is have a class method and let's call that from email. So that gets a class and an email address. That's the only argument that it gets. And this is going to return self. And self is something that we import from typing. And now we first create a name from the email address. So we do email.split. And I'm not doing a lot of validation here. I'm just assuming that this is a valid email address. If you want to make this more robust, you should add a validation. And we're going to take the first part. And let's say we replace the dot by a space. So we get first name dot last name and we capture that like so. And then what we do is return class where we pass the name and we pass the email and this should actually be local like so. And now what you can do is create a user from email. Let's say we have John Smith at the very famous website example.com. And we're going to print user three. Let's run this code. And now you see we have our user with name, email address, and it has all the other things set by default as well. And the only thing I did is add a class method that allowed me to construct a user from an email address. Now, like I said, you can also do this with regular classes, but I typically like to do this with data classes where I want to have various ways of constructing the object because you can imagine that you add more class methods here that construct users in lots of different ways. So this is a really clean way to allow for more flexibility with object construction while still relying on everything else that data classes offers. The seventh thing that is a really nice feature is that there are serialization helpers. Data classes provide builtin ways to convert objects into dictionaries or tupils and these are part of the data classes module. So we have as dict and we have as tupil and then what you can do for example is call as dict on let's say user one and we can print that or we can call as tupil on let's say user two and print that as well and when we run this then this is what we get. So the first thing we have here is a dictionary that's a representation of that particular user and here we have a tupole containing simply all the values of that second user. These helpers are really nice for logging, testing, structured data processing, or preparing data for JSON serialization. They give you a predictable, stable snapshot of an object field. Now, before I show you the final thing, which is something really cool that you can do with data classes, if you're enjoying this video, please give it a like and subscribe to the channel if you want to watch more of this type of content. It helps me more than you think and it keeps the channel growing. Now the final thing I want to show you is that data classes can be combined with abstract base classes. Yes. And then this lets you define shared structure and required behavior while still generating constructors automatically. And this is actually really neat. So from ABC I'm going to import ABC and abstract methods. And then let's say we create an account data class. And account is an abstract base class. Account is going to have an owner. And let's say it's going to have some sort of base fee which is a floating point. And then we can have an abstract method which is the monthly fee. And that's going to return a floating point value like so. And actually we can even turn this into a property. So now we have an account. It's an abstract base class. What I can do now is create another data class that's called free account which is a subclass of account. And this implements the property monthly fee. And for a free account that's simply going to be zero. But we can now also have a premium account. Like so which is also an account. But premium accounts have let's say extra storage gigabytes integer. Let's put that to 100 by default. And there we also have the monthly fee property. But here of course it needs to be slightly different. So let's say this is going to be self.base fee plus self let's use parenthesis self.extra storage times 0.10 10 like so. So now I have my two account types. And you might think that this completely breaks because we have a data class here that has two of these uh attributes. Free account doesn't have any attributes. It's still a data class. Premium account has an extra attribute. But actually all of this works as you would expect. So if I create let's say let me remove this a free account. This is a free account. And now what I can do is set the owner and the base fee which it inherits from the account abstract base class. So I can set owner to and I can set the base fee to let's say 10. Now base fee doesn't matter because in case of free account the monthly fee is always zero but you can actually still set it. And then if I print the free account then you see that it actually does what we expect it to do. So I'm the owner and the base V is 10. If we create a premium account, I can use the premium account class. And now you see we have three arguments. The owner, the base fee, and extra storage, which was added as an extra argument here. So let's create an owner. We set the base fee to let's say 20 and extra storage is 50 like so. And then let's also print the premium account. There we go. And as you can see this works as expected. And then if I print the monthly fee we can also do that like so. monthly fee is a property. So let's print that. That gives us 25 and that's also what we expect since the base fee was 20 and the extra storage is times 0.1. So that comes to 25. So if you're using abstract base class, this actually works really well together with data classes. This pattern is surprisingly expressive and maybe you didn't know that data class and abstract base classes work so well together. Now, of course, you have to be careful with inheritance in design. Don't create deep inheritance hierarchies with lots of dependencies because uh inheritance is one of the strongest types of coupling that there is. But if you need it, it works really well with data classes. Now, I hope this video showed you that even though data classes seem simple at first, they hide a surprising amount of expressive power. Let me know in the comments which of these features did you not know about. To be totally honest, I don't use data classes all that often in my Python code except for one specific case. If you want to learn exactly when that is, when data classes truly make sense, watch this video next. Thanks for watching and see you next

---

## 6. The Right Way to Use AI for Writing Maintainable Code
**Channel:** ArjanCodes | **Views:** 40K | **Date:** 1 month ago | **Duration:** 27:34 | **ID:** 8Pi6vGRlp1c
**Link:** https://youtube.com/watch?v=8Pi6vGRlp1c

### Transcript:
Today I'm going to show you how to properly direct an AI to write code for you. And in my opinion, not enough people are doing this. What I show you will massively improve the quality of the code that you produce with AI. Make sure you stick around to the end because I have something big planned for this year. It ties directly into what I talk about today. When I hear people talking about writing code with AI, I always hear stuff like, "I made this working app in 10 minutes," or, "It just took me a few hours to develop this amazing dashboard and now I have 10K monthly recurring revenue." Well, first, let's be real. There's much more to running a business than just having a working app. That's almost like a side note next to having to do all the other stuff like marketing, sales, finances, taxes, accounting, etc. Trust me, it's not that easy. Second, if your mindset is, hey, I just want to ship stuff that works, well, you're going to have a big problem in the long term. Designing your software properly has always been important. And with AI writing more and more of our code, it's even more important today. Think of design as your main tool for managing complexity. When your system is less complex, AI understands context better, your prompts become simpler and clearer, and the code it generates become something that you can actually maintain and keep improving almost indefinitely. Let me show you exactly how to do that using a real interaction I had with Chat GPT while working on a code example for another video. Now, obviously, you can use any AI coding assistant. It doesn't really matter. I use chat GPT a lot because it's pretty good and I also use it to help me write script outlines. And by the way, I never literally say what chat GPT produces. I mainly used to generate some talking points and then I rewrite it a ton because well, it's never exactly what I want. For this particular interaction with Chad GPT, I was working on a video covering the fluent interface design pattern. So now you know what video is going to come out in a few weeks. It's not really about the actual code here. I just want to show you the interaction I had with Jad GPT about this and what you can do as well to improve the code that you write with AI. So it starts with an example that I let Jad GPT generate that is some sort of animation system. And as you can see there is an animation class. It has steps like moving, rotating, scaling these kind of things. There's an animation runner that uh uses uh TK canvas to show an animation. It has a couple of methods. Uh there's uh the methods here like moving, rotating, etc. And then we have a method for running the animation where it goes through each of these steps and then updates the shape. And finally a demo where we have some animation object and we call some method on it. And and by the way, this is the whole idea of the fluid interface pattern where you can call these things in sequence, similar to the builder pattern, but slightly different. So that's basically the starting point that I let it generate. Now, if you just focus on code that works, you're probably pretty happy with this and you're like, okay, well fine, let's use that. But as you'll see in a minute, I actually spent a lot of time tweaking the details and letting it rewrite stuff and change things. And that's really important step to take. So a bunch of text that it generates. Let's see what I write. I write modify the design so that actions are not all separate method in the class. I also don't like the huge if statement in the run methods. So already I'm uh looking at this code and thinking oh actually I don't really like this. You know this is a bit messy because if we want to add more actions well this becomes even larger. You may remember the registry design pattern I talked about a while back that can help solve this. Another thing that I asked to do, I didn't really like having all these methods being part of the animator class. So, let's see what it comes up with. So, now it turned each animation step into a command class, which well kind of makes sense. And then it can loop through the commands and run an apply method on each step. So, what does this look like? So, we have now an animation step protocol. Well, nice. That introduced some abstraction. we have our steps like move, rotate, scale, fade, etc. So that's already an improvement in a sense from the version before where basically all the actual transformations were part of methods in the animator class. So these are now in separate classes. So we improved the design which is nice. And then animation has steps. We have a play method that runs these things. And then it did something weird which is that it now defined separate functions and then it uh monkey patched these into the animation class which is a bit uh awkward. That's not really what I meant. And for the rest there's a couple of minor changes as well. Doesn't really matter. So I didn't really like that. So I wrote okay make the extension methods part of the animation class after all. And then I asked it an abstraction question. Is there a way to make the animation step classes independent of animator? Uh why is that important? Well, if you look at animator, you see that it gets a list of animation steps. And if we scroll up, we also see that each of these animations, they actually get an animator as an argument. So, this is a circular dependency. And I don't like circular dependencies in my designs. Now, interestingly, I didn't provide it with a solution. I asked it, is there a way to do this? And that's also one thing you can do when you're working with AI assistants is really ask it about these things and see what it comes up with. So that's exactly what it did here. So let's see what it came up with. So it says it offers a cleaner, more idiomatic design and then it now made animation step independent of animators. Let's see what it came up with. So we have animation step. It now has two methods transform shape and transform color. And I like this idea because this means that uh animation step deals directly with the things that it modifies. So it modifies shapes and it modifies colors. Then we have our various animation steps. We have move, rotate, scale, fade, etc. And then we have our animation class that represents a sequence of these steps. Animator is currently responsible for setting up the TK canvas. It has a shape. Uh it draws the shape. It updates the shape. It has a helper for the fade effect. Uh run method. Uh already when I'm looking at this, I don't really like this design because this means that the animator class has lots of different responsibilities, which is not great. So let's see what I say next. In terms of design, I think an animation shouldn't create an animator object. An animation should be played on a particular shape. So here I'm instructing it to revise how the concepts are related to each other. Right? So in this case it's it's a bit weird that an animator object is uh created by an animation. I feel like an animation should be part of a shape. Makes more sense to me conceptually. That's exactly where AI models often break down that they don't have a clear understanding of the concepts. They're just text generators. So you need to often step in and specify exactly how the domain fits together. That's what I'm doing here. So it agrees with me obviously because AI is always agrees with you. Animation should not create its own animator. Great. Let's see what it comes up with. Now we have an animation step still that same protocol. We have our various uh steps like moving, rotating. So nothing has changed there. Then we have the animation which no longer creates the animator apparently. So this is just a representation of the animation in steps. That's all it is. The animator now has a play method that takes an animation and a shape and applies that animation to the shape. So it sort of did what I asked it to do. So let's see what I write next. Change the code so that moving, rotating, and scaling also have a duration. I think the fade class can then also have its own method. duration should be part of each animation step. So basically what I'm doing here is I've identified that hey it's actually weird that we have a fade helper. So I'm doing low-level fading stuff still in the animator class. And uh this probably happens because there is a duration that's part of the fade. But actually I realized while I was looking at this code that well duration is something that could be part of any type of animation. So if you want to rotate a shape that should also happen [clears throat] for a duration or maybe you want to move or uh do other kind of things. So an animation should always have a duration step and that also means that this behavior we should be able to move out of the animator class where it doesn't belong anyway. And then what I'm instructing it to do is that well the animator is responsible for keeping track of time and calling the functions correctly. So let's see what it does now. final code. Uh spoiler alert, it's not final yet. So now it's also started to import um uppercase list and tupil from typing which I don't want it to do but it doesn't matter. We have our animation step which is a uh duration floating point and uh then we have our data class again that our subclass of animation step. And by the way here you also see something strange and that you typically if you inherit uh which in this case we need to do because duration is shared. Uh we might want to turn this into an ABC. It makes more sense in that way. Protocols in my opinion are more suited for dock typing style of uh type relationships. Anyway, we have the animation steps and now what it did is that the fade class, it now has an apply method that applies to shape and color. Also, you may notice that in the past we had these two methods in animation step. It reverted back to an apply method. I'm not 100% sure why. Anyway, now it means that our animator class no longer has fade helper methods, which I think is uh much better. play now uh deals with time. It takes a look at each step duration and then plays the animation in sequence, which is what I want. And then we have a main function with some example code. So let's see next. I'm wondering, hey, why does animator have both a draw and update method? Aren't these mostly the same? So if you look at animator, you see that these are these two methods here. Uh we flatten the uh sequence of points because that's what the convos requires. Why don't we do that in both cases? And these look kind of similar to me. So why are these two separate methods? So that's what I'm wondering. Great question. And yes, you're absolutely right. I love when I work with AI coding assistant that I feel very right all the time. That's great. So it explains why these were there in the uh previous design. Basically, there's apparently a difference between creating a new polygon and updating an existing polygon in TK canvas. I'm not a TK expert, so I didn't know this, but that's a good thing to learn. Uh, and instead, what it did is that it now created a render method where it checks whether this is a first time draw or whether we are updating an existing polygon. And the nice thing about doing this is that now we only have to do one time this uh point flattening. Let's see what I write next. Did I like this? Yes, please update that. Also, make sure you use type annotations where possible. Don't use uppercase list, dict, etc. But the more modern lowerase variants. I normally have to do this like three or four times whenever I'm creating a code example with AI. But all right, so let's see what it did. Fine. Yeah, we have our lowerase 2 list, which is great. And now we have the uh updated animation code right here. So now let's see what I write. In the current design, there can only be a single shape since there's a single item in animator. Let's take a look what it did. So we have the animator class and here you can see we render a particular shape. And what happens is that the way it changed it is that animator now has an item integer [clears throat] which is an ID in uh TK canvas and it stores it there. And basically there's one of these things which means that I can now only keep track of a single shape. So that's the point that I'm writing about here. That's not clean. Actually, it's not even uh not clean. It's just really bad design. Also, it's a bit weird that animator creates a canvas. Shouldn't that be part of some overall graphics engine? It's there. Again, I'm discussing with AI about the domain and how things are supposedly related. And of course, if you look at the design, you see that animator still has a lot of responsibilities. It's responsible for playing an animation. It's also responsible for rendering shapes and creating a canvas. To me, that's something that should be separated. So, let's see what happens. You're absolutely right again. I feel so happy now. Okay, let's see what it does. So, we have the updated code. Now, we have still have our animations. We have our shape. We have a various animation steps. We have the animation class which represents a sequence of these steps. And then we now have a graphics renderer. Yay. And this has gvas. This has items. So it now supports multiple items which is great. And then it has a render method that renders shapes. Awesome. And a shape now also has some shape ID that is basically an identity that maps to the identity in the canvas. Okay. And then finally we have an animator class which now has a play method to play one animation on a list of shapes. That's also not what we want. Okay, let's see what I write. The renderer should know about shape objects. It should get an ID, points, and color. Uh, that's the first part. Is there a way to rewrite uh this piece of code so it's a bit simpler or is another built-in function that can help do this? So, the issue that I'm pointing out here is not so much that we're playing one animation on multiple shapes. I think I'm going to address that later, but that the graphics renderer needs to know about shapes. So this is a question of where do you put boundaries in your design, right? So when we're talking about shapes and animations, that to me seems like something that is a bit higher level, whereas uh graphics rendering needs to deal with lower level things like polygons and colors and stuff like that. That's why I pointed it out. Also, I was wondering if this list comprehension couldn't be done in an easier way. So let's see what it comes up with. So first render should not know about shape objects. Absolutely correct. Of course, of course, absolutely correct. And then can we replace this by something else? So this thing uh we can actually replaced by something else. As you can see there's a chain from iterable from it tools that we can use which is maybe a bit nicer. Uh we can uh define a utility function. Sure. Or uh we can use uh this which just make things a lot worse. So actually uh in the end uh what I did I think is uh to use uh this version instead of the list comprehension. For the second part the renderer interface we now have the graphics renderer. So now instead of it accepting shapes we get a shape ID we get points and we get a color which I prefer because then the graphics render now is nicely lowle and we can write a layer on top of it. And I like that because now this class is also pretty simple. So uh it's incorporated all of that. So now we have our new animation system. We have the graphics renderer which gets these lower level things. So the shape data and the color which I want. And then we still have our animator class and a sample main function. Okay. Let's see what I write next. Animator doesn't need to be a class. It can be a single function. Is that true? Let's take a look. Yeah, I actually agree. As you can see, animator now simply gets a graphics renderer. And the only thing it is is basically this uh play method and that's all it is. So we don't need it to be a data class. We can simply turn it into a function instead. What else do I write? Okay. Uh another thing is that I write don't make animation a frozen data class. Doesn't make a lot of sense. Split the code into separate files. Uh actually I kind of disagree with myself here because the reason that animation is a frozen class is that it's uh nonmutable. I basically create a new animation every time I add uh some uh kind of a step to it. So in that sense I actually still prefer it to be frozen now that I'm looking at it. But here I was thinking about it differently. It doesn't really matter. And then finally split the code into separate files. So let's see what it did. So we have our graphics file which now has the graphics renderer now also uses dependency injection to inject the canvas which is nice. Then we have the animation file which uh has animation steps and it has a shape class and it has an animation class and it has a play animation function now which gets a renderer without type annotations for some reason. It gets an animation it gets shapes and that's it. So, as you can see, it still runs one animation on all the shapes. And finally, we have a main file where we import all of this stuff and then uh use it. So, let's see what I write next. Change the code so that a shape object can have an animation associated with it. Okay. So, here I realize that what it currently does is play one animation on all the shapes which I didn't want. An animation should also have a start time and it should return a computed end time. So I'm specifying more clearly what I expect in terms of the domain and the play function then gets the renderer and the list of shapes with associated animations and plays them until all animations are finished. So as you can see in this current version it's different because it gets both an animation and a list of shapes which I didn't want. So let's see what it does. It's still not getting annoyed with me. I like that. So we have an updated animation.pi API file where again we have all the steps of the various options. There we have a shape. A shape now has an optional animation object which is nice. And then an animation has a start time and well that's basically it. And then we have the uh graphics.pi file which I don't think changed all that much. And then we have a main pi file where it now added a play scene function. So it moved this from the animation file to the main file which yeah to me doesn't make a lot of sense but at least now it gets a render and gets a list of shape which is what I wanted. So then we have the loop where it goes through all these things and it updates the things and it renders the shape. So let's see what I write next. I made some modifications to the code. See below. So here you see an example of where I thought it would be easier if I just worked myself on the code a bit. So, I probably ran this a couple of times in VS Code, uh, changed a few things, which was easier than doing a continuous back and forth in the chat, and then I asked it to, uh, add the missing type annotations because it didn't add all of those, and change it so that the duration isn't stored in each step, but in the animation object itself. I thought that would make it easier to add different types of animation, [snorts] and then the steps don't really care about the duration. So, let's see what it did. So we have our animation step. We have our various types here like move, rotate, etc. And we have an animation which has steps and it has durations and it has a start time. And then when I add a step, I also specify a duration. Finally, I ask it to change the main file. So the animation is only played when I click on screen because I noticed when I started the application, it started immediately and then I can't easily show it. So then it added this a little bit to start the animation a bit later. But then I noticed something strange which is that these shapes were really flying off the screen and that didn't really match what I saw in the way that I defined the size of the canvas and the actual transformations. So I asked it I thought okay maybe uh it was somehow out of bounds. So please update the code so that the shapes always stay within the visible conf. So it did that uh by doing something really stupid which is by clamping it to the boundary of the confus. I didn't want it to do that. I wanted to change the values. So simply modify the animation. So then it start to get sidetracked. It's going to update the pipeline so that it computes how far it would move and then adjust the No, no, no, no, no. I pressed stop. I said no. just update the actual hardcoded animations so that the shapes stay on the compass and make the compass a bit bigger as well because it was like this tiny screen. So let's see if it actually got that. So it increased the conf size which is great and I think it also now uh updated these uh points and transformations as well so that they're a bit more moderate. So I thought okay maybe that solves the problem then. Unfortunately, the shapes still leave the screen and at the end there is like a weird color change look really weird. So I asked it what's going on and then it came up with this which is that apparently it applies these transformations cumulatively and that means that instead of doing the transformation in steps it just adds the step every time which is really stupid and also realized that when I looked a bit more [clears throat] closely at the algorithm that it generated. So, as you can see, if you create systems like this, you're going to have to constantly deal with stuff like that. Okay. So, let's see what it now does. So, then I tried to fix it, but it went off into some tangent again. So, in the end, what I did is I modified the play scene function myself so it doesn't cumulatively add the animations. uh there is uh just some issue with uh where and when do we update these shapes because the issue is when you apply animations to shapes is that what is the ground truth right is it the current position of the shape or the current uh value of the shape or is it what the animation is playing and then uh you get a problem if the animation finishes the shape reverts to the previous position and stuff like that. So I need to fix that. So then I went into more of an algorithmic discussion of okay what do we mutate? Do we store temporarily the animated points and color and then apply it later or do we go the other way around? So that's what the next part of the interaction is about. And then finally it came up with this version of the play scene function which well if you uh look at it it's does the animation then applies the final state. I didn't like that at all because well that's a lot of duplication. So then I thought okay how about we modify the shape position and color directly. So the end state is correct but we store temporarily the starting point and the color and use that for the animation baseline. And then it says oh that's the most clean solution ever. Yeah of course it is. Uh so let's see what it did then. So now again uh play scene is a lot shorter. It stores the original state for the animation math which is nice. And then we have our while loop that actually plays the animation. But I still didn't really like this because if you look at the the function, this is now pretty long. And I thought there was a way to do it in a cleaner fashion by uh letting every step simply compute a uh local t. So instead, what I did here is go through each of these steps and duration using a for loop. And then I use some uh time cursor to compute the local time in the animation. uh determine what the point and the color is and then basically apply it at the end and then we have basically this uh much cleaner logic for each step in the automation. So the final for loop actually looks like this which is much shorter than we had before. So then I worked a bit more on this play scene function. I pasted it in here. Here we have this for loop that we talked about before and then finally it comes up with this version of the code where we have this for loop like I instructed it before. So as you can see there's like a ton of back and forth before I'm actually happy with the code. And very few of these interactions are about adding features or making the code work. They're really about structuring and organizing and rewriting everything. Because as you saw, an LM can definitely write code, but it writes much better code when you guide it with the right design principles. And in my opinion, this also where things are heading in the future. I mean, more and more as a developer, you're going to be valued by how well you can direct AI coding assistants. how clearly they can define responsibilities. How well they can structure systems and decouple things and how well are you able to steer them towards solutions that are robust, maintainable and scalable. And in my opinion, the truth is that if you understand good design, you're going to move 10 times faster than somebody who just prompts blindly. Because if you just end up using the very first version that AI generates, well then uh you're going to build on top of that, you're going to create like a huge mess in the end. The challenge is going to be how to direct AI to introduce these design things so that the code stays nice and clean. And that means that even though AI may write a bunch of code for you, you still need to understand the fundamental principles of design. And that's why I'm working on something new. It's called the Software Design Mastery Program. Now, I'm not going to reveal too much just yet, but I can tell you this. It's way more than just an online course. It brings together everything I've learned from over 20 years of teaching at university, teaching hundreds of thousands of developers here on YouTube. The practical experience I've gained from running online courses that have helped thousands of people grow their career. It's by far the most comprehensive thing that I've ever created and it's designed to help you future proof your skills, learn to think and design like a senior engineer. It's going to be something special. You really don't want to miss this. If you want to be the first to know when it opens, get on the waiting list now at iron.co/mastery. Finally, I wish you a great new year where you will build things that you're proud of. I've got some great videos coming up for you in the next few months, so make sure you stick around.

---

## 7. I Made a Classic Refactoring Mistake
**Channel:** ArjanCodes | **Views:** 23K | **Date:** 1 month ago | **Duration:** 16:23 | **ID:** b6RnBKvQ40o
**Link:** https://youtube.com/watch?v=b6RnBKvQ40o

### Transcript:
In my video covering refactoring complicated business logic, I made some mistakes which you noticed in the comments section. Well spotted, by the way. I want to go over them in this video and show you exactly what I did wrong, why these types of mistakes happen, and some of the traps that you can easily fall into. And I've seen some of you do that in a comment section actually. Now, if you haven't watched that video yet, I've put a link in the description. In short, the idea of that video was to refactor a piece of code and clean it up. This is what the original version of the code looks like. There is this huge approve order function that's very messy, lots of complicated if else statements. And I ultimately refactored it into this version which is much shorter. It has rejection rules which is a list of lambda functions and then I've split out the various checks and conditions into separate functions. Now before I did any major refactoring, I wrote tests and I showed you that those tests actually worked. The problem is that those tests were not complete and actually I introduced a few errors in particular three that I want to address today and I can prove that the new version that I created is indeed broken because if I run piest on the original version of the code then this is what you get and I've added a few tests for these three errors that you noticed in the comment section. So when I run those same tests on the new version, you see that I now get three failed tests. So it's clear that the refactored version of the code actually changes the behavior with respect to the original version. Now which three errors were these exactly? The first one is that a test for an admin is run only if the user is not premium. So here you can see that so if the user is premium then we have all of this logic else and then there is the admin check. Now in the new version of the code actually there is a uh privilege override check. So if the user is admin it's always approved but clearly that's not always the case. And I wrote this test for that. So I created a user a premium user that is also an admin. And then I create some order with an amount. There's no discount. And the EU uh and the region and the currency are mismatched. So when you try to do that then actually this order is rejected in the original version of the code but in the refactored version of the code this is actually automatically approved which is a mismatch in behavior. The second problem is that if the user is in the region EU in the original version of the code there is no check for negative prices in the items. As you can see here we have this piece of logic. So if the region is not in the EU then we check for negative prices. Otherwise, we check if the currency matches. Whereas in the updated code, if you take a look here, you see that there is a a lambda function that checks whether any of the item prices is less than zero. And it does this for basically all cases unless you fail earlier. So also there there is a mismatch in behavior. I also wrote a test for that. So that's this third one actually. So create a user, I create an order. Uh the region currency matches and then I have an item with a negative price and I'm going to check that indeed this order is approved which is the case in the original version. There you see that all the test pass. The third issue is that if the user is a premium user and not a trial user and then places a bulk order that's under or equal to thousands, they would be approved in the original version. So that's what you see here. So if there's a premium user, if the order amount is less than a thousand uh and the order type is bulk and not a trial, then it returns approved. And that happens even if the order had a discount. But in the final version actually you can see that uh there is a rejection rule that checks whether the order has a discount. So if there is no discount then it's going to be rejected. So also wrote a test for that. So that's this one. So I create a user. I create an order with a certain amount type bulk. Uh the user is not on a trial. I added a discount and then basically approve order is going to return approved. Whereas in my updated version, it's going to reject it because the has discount is true in the order object and that's this rejection rule right here. So those are the three mismatches. A few other things I noticed in your comments. So one is you noticed that while I was refactoring this whole thing I had some issues with and or combinations of conditions in particular uh this type of thing where we have a combination of and or and has precedence over or but overall it just shows how hard it is to have these kind of complicated and or statement. It's really easy to mess that up. Uh of course if you want to avoid that maybe you can try to split things up. Perhaps we could somehow split this function into two separate function. One checking uh the order amount and another checking the order type. Or you can use parenthesis like I did here to clarify what the supposed order is. There was also a question about the data structure. So what I did in the final version in the in the first version actually there is no data structure whatsoever. That's just to check for uh region EU. In the new version, I created a dictionary where I had these tupils, EU to euro, US to US dollar, etc. Uh that map to true and then I can check whether or not uh the uh region has a valid currency. And this is of course way easier to extend than the first version. The question is why did you use dictionary? Why not something else like a set or something or a list? Now there's no particular reason I did this. I think a set would actually also be just fine. And then you can actually simply do a check whether a certain tupole is in the set. So I think if I were to refactor this again, I would probably turn this into a slightly simpler data structure. Uh the most important point though is that it helps to come up with these type of data structures because they help you organize the code better. That was the main thing that I wanted to communicate with that. Another thing you mentioned is that well this returns a string, right? it returns approved or rejected like right here. And I didn't change that. Now, the reason I didn't change that is that I didn't want to change the outside behavior of the function. I still wanted to be able to run the same tests on it. If I change this to a boolean, well, then that wouldn't work. If I change it to an enum, uh maybe if the enum values happen to be these string values, but uh that was the reason why I didn't change that in my refactor. Now obviously I think it's better to use something else than a string. I think you should use a boolean or an enum if you have multiple different approval outcomes. So if I were to further improve this function after the refactor and it's used in only a couple of places, I would probably change that as well. Another thing some of you are wondering about is why is this a list of lambda functions and why not just the boolean expressions? You could simply compute all of these boolean expressions and then there's no need to call the function here. We can just check with any whether the order should be rejected or proved. Now there's actually two reasons why you might want to set it up this way. The first one is that the lambda function is executed only when we call it here. So for performance reasons that can be an interesting solution because for example we don't have to run this code at all if the amount is not eligible or if the order has a discount then we can reject early. So there's a performance gain there. Now probably in this example it's not a huge thing. So may not be a good reason to do it. But a second reason why I did this is that I like the idea of separating the specification of the rules from the execution of the rules. That's essentially what's happening here. Right? What you see here is the specification. We don't uh run any of this code. We simply specify what these rules are. And here we actually execute the rules and we check whether the order adheres to that specification. And in terms of design, that is actually a nice thing to do because then this rejection rules list is an object that you can also pass around and you can uh run it somewhere else applying it to some other type of object for example. And that's for me the main reason that I did this and it's actually a bit of foreshadowing because in a couple of weeks I'm going to post a video covering the specification pattern which is exactly this. Now in that video I'm going to do some blatant overengineering. This is child's play related to that. So, [clears throat] make sure you're subscribed to the channel. I noticed that like 2/3 of you who are watching my videos are not subscribed. Now, if you subscribe, this actually helps me out a ton because it helps uh the YouTube algorithm promote my content more and more people can see the videos and learn from them. So, I really appreciate if you can give me some love and subscribe to the channel and give this video a like while you're at it as well. Now, let's talk about some traps. So from a purely technical perspective, the before version that we have here and the after version that we have here, they they behave differently, but there's no way around it and I've proven it with the test. Now that is always a danger obviously when you're refactoring code. You may always change behavior. Um it also raises the question but what is actually correct? Which version is correct? Which version should we consider as sort of the ground truth? Is that the original one? I think some of you in the comments were making that assumption. Well, you could say that well the original code is what you're refactoring, so that is the ground truth. But of course, there is a reason why you're refactoring it. Uh some things in my opinion make more logical sense in the improved version like admin orders always being accepted or does an EU order also shouldn't have items with a negative price. The point is that when you're doing refactoring, you're always doing interpretation as well and you're making assumptions. So does that then mean that the new version is the correct one? [snorts] Uh I mean I did make quite a few assumptions in the refactoring process and not even explicitly. Uh I did not check that this was actually correct. So uh I don't think we can consider the updated version that I created as the new correct version of the code. I mean maybe there are other mistakes that we haven't uncovered yet. I mean who knows right? So in my opinion neither the original version nor the updated version of the code we can really consider that as sort of the truth. Well then maybe the test represent the truth right because in the test we basically capture the behavior. And now that I've added these extra tests to check these three uh logic changes. Well now maybe these represent the truth. But that's also not really the case because uh the tests are incomplete. If I run the original set of tests I ended up with in the video and I also print a coverage report and you see that these tests together, these are 13 tests. They cover 86% of the code. However, we know that there are logic errors in the updated version of the main. Right? So added these three tests which identifies these logical errors. But if I run the test again with these three added test which should be a more complete set of test, you see that actually the coverage is still at 86%. So if you say well in order to avoid these types of errors we need to aim for 100% coverage. That's only part of the story. Obviously, it can be a good thing that you have a high coverage percentage, but coverage only says something about how much of the code is run as part of your test. It doesn't say anything about test correctness. So, no matter what you do, there's no absolute guarantee that your tests are going to completely and accurately reflect the truth. And we may need to look even wider than just the code or the test. Perhaps uh the code and the test are not the truth, but the design document and the domain model that we made is the truth, right? But that's also not really the case because well, that's going to contain vague language, our own interpretation of things. Maybe it's based on interviews with the customer or user that are incomplete. So that's not something that we can use as ground truth either. But then maybe the truth is then what the user wants. Well, not really because typically users don't know what they want. Uh it's the whole reason agile exists, right? We build something quickly with incomplete information, show it to the user, and then we're going to iterate multiple times to get something that is uh what the user actually wants. So, what does it all mean? Do we then just do random things? Well, of course not. I mean, we can follow and we should follow best practices in designing and testing our software. uh we we can follow an agile process but we just have to accept that we don't live in a 100% deterministic world that's guided by perfect logic. In fact, your job as a software engineer is to be the bridge between that imperfect vague real world and the simple deterministic logic world of software and programming. And to me that shows actually another trap. I there there is actually no ground truth when you think about it. And even if there is one, it will be there only for a really short time until the requirements change. So we have to be careful as developers to not get stuck in too technical thinking. In the end, we're part of a process of trying to understand and specify a domain, interpreting it and codifying it, and then going back to the user with the result. And because of that result, the user changes requirements or we change our interpretation which leads to a new version and on it goes. In that sense, software design is not about code at all. Principles like abstraction, coupling, cohesion, keeping things simple. They're just tools to help us understand and specify a problem in a continuously changing domain. And the simpler we make things, the better we and our users can understand them and the better we will be in coming up with a good solution in the next iteration. The videos that I put here on YouTube, they're educational videos and as you can see, I make mistakes. In fact, I make many of them. I always do my best to make less mistakes, but if there's one thing that I learn is that no matter what I do, I'm going to make mistakes. I do hope that this shows you that you don't have to be some sort of special genius to be a software developer. If I can do it with all the mistakes I make, you can definitely as well. And you know, the best way to learn is to be critical and ask questions. That's what the comment section is for. Don't worry about me. I can take it. Don't hold back. But be nice. Some of you can be a bit nicer, but don't ever hold back. All right. Now, your comments help everybody learn, including me. I don't post these videos just to hear that they're great or something. They're my perspective, and I can be opinionated, and sometimes I'm wrong about things. They are a starting point for discussion. A video like this doesn't stand on its own. It needs your opinion, your criticism, your thoughts, and all of those different views put together is what turns this into a valuable learning experience for everyone. Anyway, since this is the new year, I am wishing you a great 2026 with lots of learning. And next week, I'm going to do that again because I didn't plan this video. I wanted to skip a week due to the holidays, but after reading your comments, I still wanted to post this video. So, that's what I did. Now, I hope you had a great holiday. Stay critical but nice.

---

## 8. How to Tell If Your Code Is Actually Production-Ready
**Channel:** ArjanCodes | **Views:** 39K | **Date:** 1 month ago | **Duration:** 24:40 | **ID:** GMBiCMsEsq8
**Link:** https://youtube.com/watch?v=GMBiCMsEsq8

### Transcript:
Here's an example of a simple fast API app that has a single endpoint convert and that converts from one currency to another. This is just some hard-coded rate here. Uh it gets raised from that dictionary and then computes the conversion. And this actually works. I can send a call request to this API and I get the results that I want. But is this production ready? Well, not really. So, I'm going to take this service that just works and upgrade it step by step into something you can actually deploy and rely on in production. And along the way, I'm going to talk about things like validation, error handling, monitoring, deployment, and other things that are important when you want production ready code. So, let's dive in. Before I start working on the code, what does production ready actually mean? I mean, there's no fixed definition of what that is. It can mean different things for different types of applications. It depends on what is more important. Perhaps security is a key issue for you because it's a public API or something. Or perhaps you are thinking more about scalability and you expect this to be used by millions of people. Or perhaps you really want this to be robust and make sure that it absolutely never crashes. In any case, production ready doesn't mean it works on my machine. And to me typically production ready code means that it works reliably in realworld conditions whatever those conditions are. So it means that at the core it needs to be observable. It needs to be reasonably secure. It needs to be maintainable right you need to be able to work on it and reasonably resilient. Now I start with this very naive uh fast API route but uh it only supports hard-coded uh conversion. There's no validation whatsoever. So, let's improve this step by step. Now, before I start, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get this at iron.code/design guide. This contains the seven steps I take when I design new software and hopefully it helps you avoid some of the mistakes that I made in the past. Iron/design guide. The link is also in the description of this video. Now, the first step that we're going to take is looking at the data that we actually expect. So even before you think about validation, you have to make sure that the data that your app works with is actually appropriate. And in this case, we're passing a from and two currencies as a string, which well makes sense. But as you can see, the amount is in float. And floats are actually kind of dangerous for uh currency handling because they're not very precise. So it's better to use something like the decimal type for amounts and rates. So what we're going to do as a first step is to fix that. So, I'm going to import the decimal type. And that means we're going to need to make some changes here and here as well. So, first I'm going to turn these conversion rates into decimal values like so. Simply going to copy paste this. Now, normally I would use AI to edit this stuff for me, but I'm not doing that on purpose so it's a bit easier for you to follow in the video. So now my rates are in decimal which is great. I can also turn my amount in decimal as well. And now let's see what happens when we do this conversion. So this still works in exactly the same way except now our types are much safer. So like I said that's something you need to do before you even think about uh validation. Make sure that your types are appropriate. So next step is validating the input. So we don't want to be able to send like weird stuff to our API. So you need to add checks for that. And actually fast API which is the library that I'm using here has support for doing this via paidantic which makes this really easy. In order to do that I'm going to import the query type from fast API and then we can use that to put some guard lines on these types. For example, from currency is a string. But we can make sure that this string has let's say a minimum length of three. And we can also give it a maximum length of three. So we are always sure that currencies have an exact length of three. And we can do the same thing for the two currency like so. And for the decimal amounts we can also supply a query where let's say we want to have positive amounts only and then this is what we end up with. So we still have the same types of data but with query we're actually doing some validation. So now when I try to pass a negative amount we're actually going to get this error from the API. So that already gives us a pretty good layer of protection. The next step to turn this into more production ready code is to actually extract business logic into a surface. So it's not really good practice to have this very specific logic inside your endpoints. You typically want to split the API layer from the actual business logic. So what I'm going to do to solve that is I'm going to create a file service.pi PI and within that I'm going to create a class called exchange rate service and this is going to be the service that will take care of our currency conversion. So what I'll do is have a method convert that gets a from currency of course that needs to be a comma and a two currency and it's going to get an amount of course we need to import decimal like so and this is where our logic is going to reside. So I'm going to take this part going to copy that over. And for now I will also move over the rates like so. So you could turn this into a member variable of the exchange rate service if you wanted to, but I'm going to make more changes later on. So I won't do that for the time being. And then ideally what we want this to return is not some JSON thing but simply the value so that we can later on uh handle that properly in the API. This is also going to return a desk like so. So that's our exchange rate service very basic. But then in the endpoint we can inject this service. So I'm going to use the depends type from fast API to handle that. And we're going to have a service here. And we have depends on the exchange rate surface. And of course, we will need to import that like so. And of course, I need to specify the type here and use an equal sign like so. And now what I can do is result equals surface dot comfort. And then I supply the information. I won't need this anymore. There we go. And we're going to return the result in a JSON structure like so. Let's restart the server. And then let's do our currency conversion again. And we see that this still works. But now we moved all the complexity. Well, it's not that complex, but we at least move all that logic outside of our API endpoint. So, that's a much better setup. And while we're at it, we can also organize our endpoints a bit better. So, instead of having everything in the main file, we can actually create a separate file called API where we're going to have the API endpoint. So, let's use the API router type from fast API for this. So, we're going to create the router, and then I'm going to take my endpoint right here, including some of these imports, and I'm going to move that over right here. I'll move this down like so. And then this, of course, needs to be router dot get the convert endpoint. So now I have my endpoint here as part of this router and I could even create a sub route for this called conversions or something like that if I wanted to. But now that we have this, our main file can become way simpler. So instead of doing all these imports here, I can import from API the router and then simply include it in the app like so. And for some reason it forgot that it imported the router. And then I don't need this endpoint anymore. And that also removes most of the other imports that I had here. So basically my main file is simply setting up my fast API app including the router and then running the service. And let's just check that this still works as expected. We still get our currency conversion. But now this much more cleanly separated. We have the logic in the service. We have the routers. we can add more in our API layer and in the main file we patch everything up. Now our next step is to add some persistence. Of course you don't want to typically hardcode your exchange rates right here because these are things that are likely to change over time. Now you could imagine that this is some external service that you use to get the current rates. You could also imagine that this is a database that contains those values and that there is another automation that updates the rates in a database from time to time. So instead of doing it like this, you can use something like SQL alchemy to store conversion rates into a database. In order to do that, I've created another Python file called models.pi where we're going to model the structure of the database using SQL alchemy. So, I've already added the dependency to my Pi project file, but from SQL Alchemy, we're going to import a couple of things that we will need like integers and strings like so. And from the OM, we're going to import the declarative base mapped and mapped colon like so. And now we're going to create a base that we can use as a basis for our models. So the first model that we're going to need is the conversion rate model. This is a subclass of base. Let's give this a table name conversion rates. Pretty basic. And then we can add our data here. So, we probably want some sort of ID. So, that's an integer. You could use strings or anything else. I'm using integers to keep it simple. And this is a column in our table of type integer. And we also want to give this a status that it's a primary key. Like so. Next to ID, we're going to have the from currency. which is a string and we want that to be a string of length three and then two currency is almost the same. We have a rate that's going to be decimal. And I also want there to be a time stamp. That's not an equal sign. There we go. And this is a date time object which of course we also need to import. and default we want that to be datetime dot now. There we go. So this is our conversion rate that we're going to store in the database. Similarly, you might also want to store each conversion that you actually do as sort of log. You could create another class called conversion that contains that information. So from to currency, the amount, the computed result, when it was created. It's not necessary, but optionally, this is what you could do. So once you've got this set up, you can create the database. I've already set this up to save some time, but basically what I'm doing is I'm importing this base that we created. I then create the engine local session. I then call base metadata.create all. This makes sure that all the tables are created and then I get a link to a local session. As you can see, I'm using SQL light here for the time being, but of course, you could connect this to a cloud database or any database that you want. Now, the nice thing is because we separate the business logic, the conversion logic from the API endpoint, we only need to add complication in the service. And again, I already prepared this otherwise this video would be way too long. But the exchange rate service now has a database session. I'll show you later on how to include this in the API. And we have our conversion method that takes the from and the two currencies that gets the conversion rate from the database and then computes the result, stores the conversion in the database and then returns the rate and the result that we got. The only thing that we now need to do is modify this dependency injection mechanism because we can't directly depend on the exchange rate service. It needs the database session. So what I'm going to do here is define a function get exchange rate service. And from our database layer, we're going to import get the database. Our session is constructed like so. And then we're going to create an instance of the exchange rate service where we pass the session. And now our dependency is injected by using this function instead. And the final thing we need to do is simplify this because the new conversion logic already returns an object. So when I now send the call request, we get the rate and we get the result as expected. The next step is that you might want to add a health route to make sure that API is actually available. This is now really easy to do. So we have our router already. So I can add a simple endpoint called health. And this is actually often checked by backend infrastructure like uh Kubernetes to make sure that an API is actually available so that it knows whether it needs to restart the service if there is a crash. So in this case we're going to have a health endpoint that we can use for that and that's going to return let's say uh status okay like so. So now we have our health endpoint and when I call this we get the okay status as expected. The next step to making this more production ready is that instead of just assuming that a rate exists we actually need to check it and then raise an error if it's missing. So part of that is done in the surface. So as you can see we try to find a rate entry here and then we simply use it assuming that the rate is there. But as you can see, if I take like from a currency, let's say Singapore dollars or something, which isn't there, then now we're going to get an internal server error. That's not really helpful. So instead, what you can do is raise an exception here. So if not rate entry, so we didn't find it. Or we can even add extra checks like making sure that the rate entry is positive. Then we can raise an HTTP exception status code of let's say 404 and the detail that uh exchange rate not found. Yeah. or uh from currency to to currency like so the only thing I need to do is from fast API import http exception. By the way, this is kind of a shortcut. Ideally, you would want to create a custom type of exception here so that the service is not dependent on fast API. Then you raise that, you catch it in the API layer and then you transform it into an HTTP exception. I'm just taking a little shortcut here. So now let's run this again. And now we see we get an actual useful error here, which is that our exchange was not found for this particular version. The next step to make this production ready is to manage configuration. And that means you don't hardcode stuff like the database URL, but you actually store these in configuration files or environment variables. What you can do, for example, is you use pantic settings and an end file. So I've created another file here called config.pi that has a settings class and it has the API URL and has some other config settings as well like log level or the database URL. And then instead of hard-coding your database URL, you can actually from config import a get settings function. And then you can do something like this, right? And now you don't have this hard-coded value here anymore, but you can actually load this from settings or define it in a N file so that when you start your service, you can actually supply the URL in a variable, [clears throat] which is way better. And that also means that you can easily differentiate between let's say local staging and production environments by simply changing these values. The next step in order to make this API more production ready is to add some limitations on usage. And you can do that by adding some rate limiting. And this is actually really easy to add with fast API if you use the slow API library. So I've already added this dependency slow API and I'm going to import the limiter class and then what I'll do in my router is I create the limiter and then within the limiter I can specify how I want the limiter to work. So from slow API util I'm going to import get remote address and this is going to be the strategy that the limiter function will use like so. And now what you can do in your endpoints is actually add a limiter decorator where we're going to limit this to for example five queries per minute. It's really easy. The only thing we need to make sure is that we add the request object here because that is actually what the limiter relies on. So if you leave that out, it's not going to work correctly. And now let's run this one more time. And as you can see, this still works. But if I do this a lot, then at some point, hey, we're getting our rate limit that we are hitting right now because we only can do five requests per minute. The next step to make this more production ready is that you also want to write some tests. You're going to need some test for this service and API routes. You can use a test database that has a clean setup and then ensure that you cover success invalid inputs and all various sorts of edge cases and how far you go obviously depends on how important robustness is for you. But having some test in place is actually really helpful. Now I created a simple sample setup here that you can use as a starting point. So I have a test folder that has a configuration file where I'm using an in-memory database for these tests so it doesn't interact with the actual database and I have some helper functions so that I can override the dependency to get the database. There's a fixture for returning a client etc etc. And then I've written a couple of tests as an example. So for example here we have a successful currency conversion test where we add a sample conversion rate to the database. We then send a request to the client and then we check that we get the right information. And same thing for a missing conversion rate. And you can also do the same thing for a service. So you can use the service to check that actually a conversion that runs through the service is actually handled correctly. So these are just some examples uh you can use as a starting point. Like I said, I'm not going to build this from scratch because that would make the video way too long. But do check out the example codes in the git repository. The link is in the video description. Now the next step to make it more production ready is to actually think about monitoring and logging. As you can see, currently in my exchange rate service, I'm simply printing some stuff. But actually, that's not really helpful when you're dealing with a production app. you typically want to use logging. Now, Python already has a built-in logging module that you can use for that. So, you can simply instead of print do logging info for example and then this is going to log that value. Then later on you can also more easily add more structure to these logs you can save them to file. You can send them to a service like a sentry or paper trail or things like that and that makes it way easier to see what is actually happening in your app. So don't print stuff to the console, but log things properly. The final step is deployment, which of course is also something that you need to think about. If you write the code and you have your API, that doesn't mean that it's ready to have it on a server somewhere. You need to specify what the configuration is of that server. You can use a Docker file for that. And then you will also need a workflow that actually uh builds that uh Docker image based on the file and then actually deploys it. There's several videos I posted already on the channel showing you how to create a Docker file and working with GitHub action so you have a workflow that deploys your app when, for example, you commit to the main branch. And by the way, if you find this content useful, make sure to hit the like button, subscribe to my channel. It helps a lot and keeps my content going. Now, like I mentioned at the start, I've all these steps that I've shown you today, it's not exhaustive, right? But it's a good start. But I'd love to hear from you. What else do you check before shipping something to production? Do you have some horror stories you'd like to share? Do you have any specific tips that you'd like to share? Things that I didn't cover in this video? Let me know in the comments. I have a full video on how to structure your Python project professionally. The folder structure, module layout, even Docker file, GitHub actions, everything. You can check out that video right here.

---

## 9. Retry Pattern: The Secret to Resilient Python Code
**Channel:** ArjanCodes | **Views:** 27K | **Date:** 1 month ago | **Duration:** 24:02 | **ID:** 43vH_lnM7x0
**Link:** https://youtube.com/watch?v=43vH_lnM7x0

### Transcript:
Here's an example of piece of code that calls the Chuck Norris API to retrieve a random joke. Let's run this and see what happens. Oops, that's a crash. Let's try this again. Ah, and now it works. I didn't change anything. So clearly, if there is some temporary issue with the API that you're connecting to, the whole script crashes. Now, of course, this is a bad example. Actually, this API never fails because, well, it's the Chuck Norris API. Merely suggesting the possibility probably means that this is my last video ever. Anyway, today I'll show you how to stop your code from crashing randomly whenever you're connecting with APIs or other services by using the retry pattern. I'll go step by step and show you simple retries, exponential backoff, decorators, fallbacks, and more. This video is sponsored by SER API. More about that later. The problem that I encountered at the start of the video is so-called transient failure. Most modern systems rely on a bunch of external services, APIs, databases, large language models, you name it. And these services sometimes fail briefly, even when your code is correct. Now, the retry pattern wraps an operation that might fail and automatically retries it before giving up. It's actually a really simple way to make your code more robust and fault tolerant. And by the way, maybe you noticed I had this little wound here. Actually, what happened is that I was in a fight to the death with Chuck Norris. I'm really attempting fate here. Anyway, while I'm still alive, let's build a simple retry function ourselves. And as you can see, what I secretly added to the example is a random failure because, like I said, the Chug Norris API never fails. So let's create a function called retry and this function is going to get an operation which is going to be a callable. So it will be a function and it will call this function up to three times and we want it to wait let's say 1 second between the attempts. So from typing I'm going to import the callable type and let's assume this operation doesn't take any arguments because otherwise we don't know how to call it. And well, we could make the return type any but instead what we can also do is make retry a generic function. So it's going to return something of type T. So that's the operation. Then we have the number of retries that we want to have and let's say that's by default it's three and we'll also have a delay in seconds. So let's say that's going to be 1 second by default. And then this is going to return a type t because that's the result of the operation. So for attempts in range let's say from one to retries + one. We're going to try to return the call of the operation like so. And then if there is some exception and this is actually one of the rare cases where you want to basically catch everything because we're actually going to reerase this if we can't retry anymore. So if uh attempts equals retries, that means we have now had the maximum number of retries. We're going to erase the exception. Otherwise, we're going to do times sleep the delay. And obviously, we're going to need to import time like so. And perhaps let's print some logging. There we go. So this is our simple pretty naive uh retry function. And then what we can do here is we have a joke and that equals retry. We will retry fetch joke and we'll leave everything else as the default value. So three times and one second delay. And then we're going to print the joke like so. Let's run this and see what happens. So immediately we get a good result. Let's try that again. Ah, there we go. We got two failed attempts and then finally the after three attempts it doesn't work anymore. So, as you see, it retries it until it reaches that end point. And as you can see, this doesn't always work. In some cases, it still fails because we set the maximum number of retries to three. But also, sometimes it actually helps. Like in this case, for example, the first time it failed, but then the second time it succeeded. So, it's a small change. I mean, this is pretty easy to add to your code, right? especially if you turn this into uh a function that's lives in another module that you can just import wherever you need it and already makes your code a lot more robust. Before I continue, let's talk about something that's closely related and that's reliable and accurate APIs. If you ever try to scrape data from Google or other search engines, you know how fragile that can be. One request times out, another gets blocked or the HTML changes and then your scraper basically breaks. That's exactly the problem SER API solves. SER API gives you access to Google, YouTube, and many other search engines through a really easy to use consistent API. They handle proxies, captures, and retries automatically and return well ststructured JSON so you can focus on using the data, not fighting the network. It's really easy to use. Here's an example in Python of how to access the Google search API using the SER API package that's available for free. Now, I just get the API key from an environment variable that I set in M files. That's why I'm doing load. M. Then I create a client, pass the API key, and then I run client.arch. I provide the query. I provide the target engine. In this case, that's Google search. And I also provide my location. And then I simply get back JSON data in a dictionary like structure that I can work with directly in Python like so. So this prints the first organic result. Let's run this and see what happens. As you can see, we get a dictionary here with all the information that we need. Or how about searching YouTube for content about the solid design principles in Python? It works almost exactly the same way except of course we change the engine to YouTube. And then we can print the video results. And here we go. We have as a first result my video on Uncle Bob's solid principles. If you want to make your data pipelines more reliable, check out SER API. The link is in description or you can scan the QR code on screen. Now back to the video. So we have this basic retry function, but retrying too fast can make things worse. You might overload a service or get rate limited. So just passing a delay like I did here may not be enough. Now a solution that you commonly see used in order to address this issue is exponential backup. And that means that each retry waits a little bit longer. So how do you add this? Well, first of course we need a backup value. So let's say that's going to be 2 seconds like so. And then instead of just sleeping for the delay, we're going to compute the sleep time. And that's going to be the delay times and then back off to the power of attempt minus one. and we do minus one because then in case of the first attempt this is going to be back off to the power of zero which is one. So we just have delay as the sleep time and then it exponentially adds the backup every time we need to retry. So that's the sleep time and then of course we also need to pass that to time do sleep like so. And finally, for debugging purposes, let's also print a message here. We're trying in sleep time. Let's format this to one decimal seconds. Like so. Let's run this here. Clearly, this works. This also works. These jokes are really bad anyway. Doesn't matter. Okay. Let's increase this number a bit so we can see what is actually happening. And let's also increase the number of retries to let's say 10. So we can see what actually exponential backoff is doing. I'm not going to run this completely until the end, but you can clearly see that the exponential backoff is working appropriately. And of course, it's up to you as a developer to decide, hey, how much back off do we actually allow for? Because at some point, well, you're just going to have to give up. Instead of a function, you can clean things up even more by using a decorator. And this lets you add retry behavior to any function with just a single line. Before I implement the decorator, later on in the video, I'll show you a case where it's actually not the best design. So stay tuned for that. So what I'm going to do is to define a retry decorator. This is also going to be a generic function. And this is going to get these arguments. So we still need the retries and the delay and the back off just like before. But then inside this function we will have our decorator function. Let's call that decorator. And this doesn't need to be explicitly generic because the top level is already generic. And then within the retry decorate we will have this decorator function. And this is going to get as an input a function. So that's the function that the decorator wraps around and that's a callable that we don't know the input arguments of. It doesn't matter. And it returns something of type T. And the decorator is also going to return a callable that also returns something of type T. So this basically means that the decorator doesn't change the signature of the function. But we're not there yet. If we simply define the decorator like this, actually we're going to break introspection. So if you would type the decorator on top of fetch joke and you would try to print the name of the fetch joke function, you would actually get the decorator function name. So in Python, that's why it's actually best practice to use a wrapper and that's from fun tools. So we're going to wrap around the function and then we will define a wrapper function within the decorator function. And I'm simply using the any type here because I don't really use these arguments. So let's keep it simple. And the wrapper is actually going to return this t type. Going to indent this. This is going to call the function. Here we're going to return the wrapper. And there we will return the decorator like so. So this is our decorator. It looks a bit convoluted but it's basically a function that wraps another function. And there's an extra level so we don't break against respection. And now the way we use this is simply write retry decorator like so above the function that we want to decorate. And that also means that in our main function we can now call fetch joke just like a normal function except that it's actually decorated but we don't notice that when we actually try to use it. So let's see what happens when we run this. It looks like we had an error here and I think the problem is that this actually needs to have parentheses because there are arguments involved. Let's try that again. Yeah, there we have the retry decorator at work. And we end up with a pretty good Chuck Norris joke actually. Now, a retry decorator like this is not just useful when you're dealing with REST APIs. So, in particular, if you're using LMS and you want to get JSON data from them, well, that's something that breaks quite a lot actually in my own experience. So you basically tell the LM to return valid JSON and then it just replies sure here's your JSON followed by something that's well it's not JSON. When your code interacts with LMS the retry pattern is actually incredibly useful. So here you see an example of how you would use this exact same decorator that I have here to wrap around a function that calls an LLM. So in this case I'm using uh OpenAI. I have an API key and then uh what this simply does is uh it's an assistant that extracts information from text and returns it as JSON data and the input is to extract the username and age from a text. So in the main function I have this text and then I call this function with retry because on top of it we have the retry decorator. So let's run this. As you can see it successfully parses the JSON. Actually most times this goes well. So I can't easily show you an example where this breaks but actually in some case the code did retry a couple of times while I was testing this. So LMS that's a really good use case for the retry pattern. Now in production you could even improve this further by adding a repair step between retries. For example, you could wrap the response in braces or validate it against a schema before you're parsing it. But already this simple version of the retry pattern turns unreliable API behavior into more stable software. Now Chuck Norris obviously doesn't need retries to get valid JSON. His Chuck Norris LLM simply never fails. Now sometimes retrying the same operation isn't what you want to do. For example, if a service is completely down or your credentials are invalid or whatever, retries just waste time. In that case, you might want to use a fallback strategy. Basically trying a different route. Let's go back to our toign ignores API. So we have fetch joke which fetches the joke from the API. But what we can also do is introduce let's say a fetch backup API that returns a backup joke. Now what you could do in the main function is try to do this and if there is an exception actually do that. But you can also integrate the fallback strategy into the decorator. For example, here what you can do is create a backup function. And this is also going to be a callable that will have the same signature as the original operation. And then what we can do in the decorator is if we have some exception or if we reach the number of retries then we're simply going to return the call of the backup function like so. And here there is another one. So we will do exactly the same thing. Like so. And what you can do now in the fetch joke is that we can state that our backup function is actually the fetch backup API. And now let's increase the failures like so. So that we can see this in action. So here you see after a couple of tries we get three failed attempts and then it automatically calls the backup API. Like I mentioned earlier, the decorator may not always be the best design. And here you see an issue in that the backup function is treated differently from the main function. And that means that this design doesn't work if you have let's say two backup APIs or you want to try twice the normal API and then you want to try the backup API and then maybe a second backup API or whatever. So it doesn't give you a lot of control and that's partly because the backup function here is treated differently from the function that you're calling here while in essence there's no need to do that. So what I want to do to show you a slightly different design is go back to that simple retry function. So this is not going to be a decorator anymore and I will remove all of this. This is simply going to return something of type T like so. And then here we're going to de-indent this. This is no longer a decorator. And we're going to delete this as well. So now it's again a bit shorter. Of course, now we need to pass the operation again that we're going to call. But we're going to do something different here. So instead of passing the operation and then a backup function, maybe other functions, we're going to pass a list of operations. And each of these operations is going to have exactly the same shape. And now what we can do is we can simply provide a list of functions that the retry decorator should call in order. And we don't need to distinguish anymore between what is like a main API and a backup API or a second backup API. So this I'm going to delete. And we don't even need to know the number of retries because that's simply the length of the list. Now you might think, hey, but then I have to write the function name three times. Actually, no, that's not necessary. I'll show you in a second how that works. So we have our operations and we can now also drastically simplify the code. So in this case, what we can do with a for loop is that we can iterate over these operations. I still want to have the attempt number. So I'm going to use the enumerate syntax here. So we're going to enumerate over the operations. So we're going to get the attempt and the operation. And then I'm simply calling the operation like so. And then if there is some exception, so we simply write the attempt number. This is not needed because that's going to be caught by our loop in any case. Of course, we still need the sleep time. And then what happens after the sleep period is over? then we actually go back to the next iteration in the loop. So here what we can do at the end is basically raise a runtime error that the retries have failed like so. And now what I can do is do a retry and then I'm going to simply supply the operations. So, for example, if I do it like this, we're simply going to try fetch joke only once. Let's run this. There we go. One single retry. But if we want to do this three times, I'll simply multiply this list by three. Let's try that again. And now we get the results that we expect. And actually, this looks like it's an actual Chuck Norris API failure. What is happening? Ah, interesting. Okay. Anyway, this is a really easy syntax to basically specify that we want to call fetch joke three times. And then if after three times we want to try the backup API, we can simply add that to the list. Like so. Let's run this again and see what happens. Finally, it calls the backup API. So even though the decorator is an interesting design, it also makes a distinction between in this case the main API call and the backup API call and you may not want that. For example, you could even take this a step further and after the backup API if that also fails, we could retry fetch joke again. We have a lot of flexibility here. So it depends on where you want flexibility in this pattern. Do you want to use it in a very simple way with existing functions and a decorator works really well or do you need a lot of control over how it's going to retry things and then maybe the decorator is not an ideal solution. Now I created all these retry decorators and functions from scratch. You don't need to do that. There are libraries for that in Python. A common one is tenacity which handles all of these things including exponential back off selective retries logging. So when you're dealing with production code, you don't need to reinvent the wheel. Here's an example of how tonasti works. So it looks very similar to what we had. Uh you can specify uh waiting times uh when it should stop etc etc. And there's actually quite a lot of flexibility in tenacity. So don't build this yourself. I purely did this to explain how it works. Use a library like tenacity instead. Now when is the retry pattern a good solution? Especially if you know that the error is temporary. Let's say a network hiccup or rate limits or maybe some LM failure. It's a really great solution for that. Also, you should make sure that the operation is actually safe to repeat. So, it shouldn't matter that you call something two, three, four times. And finally, it's a good solution if you want a better user experience or very resilient backend code that works with APIs that may not always be predictable. When do you avoid it? Well, in particular, if you know that the failure is permanent. For example, if your API key is wrong. Well, in that case, it doesn't matter that you retry because the key is always going to be wrong. Or maybe the user has supplied invalid input. Well, then retrying also doesn't make sense because the input is invalid. It doesn't fix that magically. Um, another issue is with a retry pattern is you want to be careful that your action, your retry action doesn't have some sort of side effect like uh storing something in a database or whatever because then maybe the operation fails but you may get all sorts of duplicate data and that's of course something you want to avoid. Also, you want to avoid retrying too often. So, you end up with some sort of retry storm that just makes the problem worse and worse. So, it's a great pattern, but use it with care. Like I showed you, you can even combine this with fallbacks and then your systems become much harder to break or even use circuit breaker, which is another pattern that I also want to explore on the channel later on. But to me personally, it's especially helpful when I'm dealing with LMS. Now, if your like button fails, don't worry. You can just retry it, maybe even with exponential backoff. Anyway, I'd love to hear your thoughts. Do you already use retry logic in your projects? Have you built fallbacks like I've shown you today? Let me know in the comments below. Now, if you want to explore more software design patterns like this one, check out my design patterns playlist right here. Thanks for watching and see you next

---

## 10. How to Refactor Insanely Complicated Business Logic (Step by Step)
**Channel:** ArjanCodes | **Views:** 114K | **Date:** 2 months ago | **Duration:** 23:23 | **ID:** mH7e7fs9gaE
**Link:** https://youtube.com/watch?v=mH7e7fs9gaE

### Transcript:
Take a look at this mess. This is a function that approves an order for a particular user. As you can see, there's like a bunch of if else statements here. Uh orders, currencies, different types. In this video, I'm going to fix this code step by step and show you how to take impossibly complex logic like this and turn this monster into Python code that is actually clean and readable. Before we start, if you want to learn how to design software from scratch, take a look at my free design guide at r.code/design guide. This takes you through the seven steps that I always take whenever I design new piece of code. /design guide link is also in the video description. Before I start working on this code, one thing that's important to note is that this type of code doesn't start out this way. It grows this way. You know, you start very simple where you just have nonpremium users. So, approving an order is pretty basic, but then you know, you add premium users, you add discounts, you add amount limits, and then there's more and more rules and you don't want to like refactor things. So you just add an extra if statement. Just do it quickly, right? And before you know it, you have like 12 levels of indentation and no idea what any of this means anymore. And the result of that is that you're going to have these confusing checks and duplication and all sorts of stuff that's really going to complicate working on this code in the future. Now the good news is that you can actually refactor and clean up code like this. Uh but you have to follow a couple of specific steps that I'm going to show you in this video. So first let's take a look at the code in total. So uh apart from this function, there's a couple of data classes. We have an item which has a name and a price. I have an order that has an amount, discount as a region. There's a currency. There's a type like bulk, normal, and there's items in the order which are these things basically. And we have a user class which is uh premium, admin, trial, and the user also has a region. Then we have this horrible approve order function. And then there's a main function that creates a sample user. And then it creates an order with an amount and a couple of other things as well. And then it checks whether the order is approved or not. So when I run this, we can see that the order has been approved. So how do you start? You could just randomly start moving around code and seeing if that improve things, but that's not really a good way to do this. Now, boringly, unfortunately, we're going to have to spend some time understanding what is actually happening before you can start working on the code. And this is not good news because we're going to have to spend some time trying to drill down what this logic actually does. But one thing you can do to make this a bit easier to also externalize kind of what you learn is to write tests while you're understanding the logic of the function. Like for example, if we take a look here, we see that there's this uh large if statement about premium users and then uh as you can see there is here an else where if the user is an admin. What you can basically grasp from this is that if a user is not premium but it's an admin then basically the order is always going to be approved and all other cases that's going to be rejected. Also you can see that there's a difference which makes a big difference between order amount. So if it's less than a th00and then if it's bulk and the user is not trial then we do approve and otherwise it's rejected. So you can get a bit more clarity already by trying to drill through this logic to understand what is actually working and know it's probably not what you want to hear but if you want to work on code you have to at least spend some time trying to understand it and writing tests allows you to specify what this code is supposed to do. So these are not correctness tests. These just lock in what the current behavior is. So what I did is already create a couple of these tests. This is the test file where I have a helper function to make an order, make a user and then we have the various scenarios, right? So there is uh we have a premium user then we have an order and then for that particular region US dollar without discount we assert that the order is actually approved and they have a bunch of other tests here as well that capture like various combinations of orders and users and different types. Also there are tests for admin users. So if we have an admin user and we have an order then basically it's always going to be improved. But also if let's say an item has a negative value that will still result in an approve order because admin basically bypasses everything. And then there's regular users where we also have a few test cases. We also check that there is exception handling. So if we have some missing amount so that's basically going to result in a rejected order. So if I run these tests by running by test you see that they all pass. So that's the first step understanding what the code does and capture the current behavior in tests. Now you may not always be able to directly write these tests because you have to assume that the code is written in a way that you can actually test it. Well then your first priority is going to be to do the minimal work necessary in order for you to write these characterization tests. And unfortunately this is something that you're going to have to do. It might involve moving a couple of functions around and creating a setup of the code so that you can actually write tests for it. At that point, you're not going to have a safety net. So that's dangerous, but there's also no way around it. But know that once you have these tests in place, they actually start functioning as a safety net. So you can refactor and change things without fear of breaking the existing behavior. So let's say you did the minimal refactoring in order to be able to write these tests and you wrote the test, you captured the behavior. Now we can actually start working on this code. Now one thing that's really important that you need to fix as one of the very first steps is to try and remove indentation layers. So instead of all these nested conditions, we ideally want to reject early and you can do that by using something called a guard clause. So in this case, well, we've noticed that if you're an admin, then basically your order should always be approved. So what you can do is add a guard clause for that. So if we have an admin user, we're going to return approved. And that means we can remove this check right here because we already handled it. So that already removes some complexity. Now if the user is premium then we have all of this logic. If the user is not premium we're going to do this. So that means we have another guard clause here. So we can rewrite this as follows. If not user is premium we're going to return rejected. And now we can take this and this and we can remove that and we can de-indent the code like so. And while you're doing these edits, you can run the test to make sure that the logic is still adhering to the behavior that you captured in the start. So already this simplifies things. Let's continue with this idea. So we have another if statement here that if the order amount is more than a,000 then there is all of this logic else and then if we have this case it's approved and otherwise it's going to be rejected. So that means that if the order amount is less than a th00and and if it's not bulk or the user is the trial then it's going to be rejected. So you basically flip the logic in order to uh come up with these guard clauses, right? So what I can write here instead is if the order amount is less or equal than a,000 and uh order dot type is not bulk or user is trial then we're going to reject the order. So that basically handles the case where this is not true and it's not bulk or the user is a trial. So that means I can again remove this entire if statement. Now if all of those things are true, we have the case where the order is approved. So at the end I'm going to write that as the final return value. But now I can delete this and I can also delete this if statement. We can de-indent this like so. So again, we've captured a specific case in a guard clause. Slightly more complicated, but still doable. And we can continue. We have another if statement. If the order doesn't have a discount, then we have all of this logic. Else we're going to reject it. In other words, if order has discount, we're going to reject it. Remove the else statement. Remove this if statement. and deindent this. Let's run the test one more time. As you can see, this still works correctly because we modify this logic step by step and every time we run the test to make sure we didn't make a mistake. So now it's already simplified by a lot, right? We have these guard clauses, these special cases that we're going through and uh we have way less indentations. So it's already a lot easier to understand what is going on here. And again we can continue. So here if the user region is not EU then we have this code. But if it's EU and the order currency is not Euro well then it's going to be rejected. In other words we have yet another guard clause. So if user region equals view and order currency is not euro we're going to reject the order and again this means well we have this approved here but that's basically this at the end that will happen. So I can delete this entire part. I can remove the if statement and then this is what we have and actually this is uh double right now. So now we've simplified this approve order a lot by removing most of the indentation and the only thing we did is analyzed what is happening in these if statements identifying the special cases and putting them as guard clauses at the top. Let's run the test one more time and this still passes. So that's a really big step that already improves a lot of the code. If you do just this, that's already a major win. Now let's take the next step in improving this function. So one thing that you've probably noticed that that there is this huge try accept block. So basically if there is any sort of problem, we're just going to return rejected. And especially in Python code, this is a huge red flag because this try accept block now hides all sorts of real issues. It's better to handle expected problems directly instead. For example, what might happen is that if the amount is not defined, then this is going to lead to an exception and that means that the order is going to be rejected. Instead of relying on a very broad exception handling mechanism, you can also be explicit about what you expect. So for example, here if order amount is non, it's not defined, then we're going to return rejected. And actually when you think about it, there's many cases where elements of objects may not be defined and may lead to issues. So the question is whether you should really put these kind of explicit things here at all and whether you should actually at all try to capture all of those exceptions. In fact, it's better to just let your program fail. But that is changing the behavior. So you need to be aware of that. For now, I'll just leave this in here as a guard clause. But let's remove that try except block because it doesn't really help with tackling complexity. Overall though, if you don't have a really clear reason of why you should do something with an exception, simply don't catch it and let the program crash. I call it the let it burn approach and it actually works pretty well. So now we did actually change the behavior of this function, but it does mean that if something truly fails, there will be an error. So we can deal with it. Now the next step is to work on these conditions and simplify them and give them names. So our logic here becomes sort of selfdocumenting and you can write helper functions for that. For example, let's say we make a helper function is eligible amount that takes an order and it takes a user returns a boolean value and basically this is going to capture this condition. So, we're going to return order amount. It's over a,000 or order dot type is bulk and the user is not in trial. I'm inverting the logic again to make it clearer. And then what you can do here instead of this complicated conditions we can simply write if not is eligible amount we pass the order and the user and then we're going to reject it. This simplifies the logic makes it easier to understand. And we can do the same thing here. I can create a has valid currency checker that gets an order and the user and returns boolean value. So if the user region equals EU and order currency doesn't equal euro and again I've inverted the logic here because we're rejecting order. So here it makes more sense to check the not case. So if there is not a valid currency, then we're going to reject the order. In this case, we're not changing the behavior. We're just moving things around and giving our conditions a name so that they're easier to understand. Let's run the test one more time. And this still works as expected. The next step in order to simplify this logic is by looking at loops and comprehension. So now we have all of these guard clauses, which is great. We've named the conditions so that they're easier to understand. But there is also still this loop. And we can also slightly simplify this. And in fact, in this case, we can rely on Python syntax to create another case. So if any item price is less than zero, we're also going to reject the order. and I don't need this for loop anymore. So these kind of syntactical elements are actually really powerful and useful in Python. So rely on them if you can. So let's run the test again. This still works, but at the same time don't overdo this. If the logic inside the loop is really complex, then you can also keep that explicit for clarity. The next thing we're going to do is to merge duplication. If you look at this approve order function at the moment, you see that almost every branch in in each if statement basically results in either approved or rejected. And that duplication hints at that you can actually merge these conditions that will lead to that same outcome. Now you could create like a huge uh if condition here uh which will again be harder to read. So I wouldn't really recommend that. But there is actually a clean way in which you can express this. So we have this first case where if a user is admin is simply going to be approved. So I'm going to leave that as is. But then we have all these different situations where actually the order is going to be rejected. So what you can do instead is group these conditions in a data structure. But I can create a structure. Let's call that rejection rules. And this is going to be list of lambda functions. So we have not uh user is premium. We have order amount is none. We have amount not eligible. order has discount currency is not valid and finally we have this case. So now we've captured all of these conditions into a simple list and then we can use any we call a rule. So if any of these conditions hold then we're simply going to return rejected. And now all of these different if statements we can simply remove them. And we've put all the rejection rules in this list. So if there are more cases and that's actually really nice, we can actually simply add a rule to this list. We can add another lambda function here. And then this is going to make sure that if that condition holds, the order is going to be rejected. So I feel this is a pretty clean way to express more complex logic like that. You don't add more branching chaos. It's actually pretty simple now to work on. And again, let's run the test to make sure that all of this still works. Now we're now at a place where the logic is seems actually pretty stable. I don't think there's much we can improve uh in the current version anymore. But what you can still do is uh try to map your understanding of what is actually happening and move parts of that to uh data structures and uh making that part more flexible as well. For example, here we have a function that checks whether a currency is valid. Well, the main idea is is that the region of the user and the currency of the order should map, right? But currently, of course, this is only done for uh the euros. Uh if you want to do it for other currencies as well, well then now this becomes complex again. And what you can do instead is refactor this so that it uses a data structure so that's easier to work on. So what you can do is create let's say uh valid region currency and let's make that a dictionary that maps a tupole to boolean value. So we have EU and euro and we can also have uh US and US dollar and that's also a valid combination. And I did something weird here. Of course this needs to be an equals sign. So now I have my valid regions. And then what I can do here is return a valid region currency get. And then I'm supplying the user region and the order currency. And if that doesn't exist, it's going to be false. Like so. Let's run this test one more time. And this still works. And just to show you that I'm not faking it, let's turn this into false just to make sure. And then we run the test. We see that there's actually a problem now. So the test actually do something. Not making this up. Okay. Setting this back to true. The nice thing about now moving this into a data structure is that we can add other combinations as well like I already did here like US and US dollar. And this is the point where you start to really benefit from working on refactoring this code so that you can actually improve things later on. I mean this would have been incredibly hard to add in the very first version before we started refactoring. This is the original version of the code. So I had to go in here and you see that there's a a not region here. There's order euro currency here. So it's almost impossible in this version of the code to actually add support for mapping US to US dollar. It's really hard. So our refactoring where we now modeled this as a separate function actually makes things a lot easier. In summary, I started with pretty complex logic and then refactored it in steps. So first you need to write tests to capture the current behavior. Do the minimal refactoring necessary in order to be able to do that. The second step is to flatten the structure by using guard clauses. Simplifies the if statements in the logic. Then simplify and name the conditions so that it's easy to understand what is happening. Then merge duplicates like having this order rejection code everywhere but then you can merge that and makes the code even simpler. Removing unnecessary try except blocks simplifying loops and finally moving stable rules into data so that they're easier to extend later on. So this final version is pretty short readable. It's easy to test. is basically the complete opposite of where we started. Now, if you found this video useful, give it a like, subscribe to my channel so you don't miss any of my upcoming videos. And I'd love to hear your stories. What's the worst nested if code that you ever had to deal with? And how do you start when you need to refactor code like this? Do you have any tips that you want to share? Let me know in the comments. Now, if you enjoyed this video, you might also like other videos I did covering software design and patterns in depth. Check out the playlist right here.

---

## 11. The Lazy Loading Pattern: How to Make Python Programs Feel Instant
**Channel:** ArjanCodes | **Views:** 41K | **Date:** 2 months ago | **Duration:** 19:39 | **ID:** ENnDxEOAKKc
**Link:** https://youtube.com/watch?v=ENnDxEOAKKc

### Transcript:
I have a script here that loads data from a pretty large CSV file. As you can see, there's like millions of records in here. Now, I'm doing this in a pretty naive way. I'm simply loading it here. And then I have some functions to analyze sales, count the number of sales, and there's a simple textbased user interface. This is what happens when I run this code. So, as you can see, it loads all that CSV data, and that actually takes a pretty long time. So, the program is not frozen. It's working, but it's loading these millions of rows before doing actually anything useful. This is a classic example of eager loading. But today, I'm going to fix this by using the lazy loading pattern and show you a couple of things that you can do. Actually, the concept of lazy loading dates back to already 1960s when operating systems began loading memory pages only when accessed. Martin Fowler later popularized it in patterns of enterprise application architecture, describing how OMS like Django delay database queries until the data is actually used. And you probably also use this daily yourselves by using uh Django, SQL Alchemy, TensorFlow and even Pandas rely on it to handle large data more efficiently. And there's many other real life examples of lazy loading as well in uh websites that already show the UI before the data is actually there or operating systems that show you the login screen while it's still in the background loading all sorts of stuff. So let's see how you can use that same principle in Python code. Now before we start, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you at iron.codes/design guide. This walks you through the seven steps I take whenever I'm designing a new system. So at the start of the video, I already showed you the eager, naive approach to this. And as you can see, we now finally have the user interface. So now I can do things with the data like I can analyze the sales data which as you can see is a huge number of dollars or I can count the total number of sales records as well using that same data and of course now the interaction is pretty fast because we already loaded the data. Quitting is also really fast which is great. Now of course you don't want your user to wait 10 seconds just to get to the menu. It's not great UX. So let's make this more responsive by deferring that loading of all the data until it's actually needed. So if I go back to the main file, as you can see, what happens here is that at the start of the main function, I actually load the sales data. So a very simple way that you could do lazy loading is by basically taking this and moving it to the point where you actually made a choice like so. And now when I run the main file again, I get the user interface immediately. Now, of course, as soon as I then pick one of these options like analyzing the sales data, well, then we're back to loading all of that data again, which simply takes a lot of time. And in fact, we made it kind of worse because now if I pick the second option, counting the total sales record, well, it's going to load the CSV data again. So it seems like we traded that initial loading time that we had when starting the application for something that's arguably even worse because now the user always has to reload all that data every time and that's also not really what you want, right? At least quitting is still fast. That's something one thing you can do to avoid having to load the data all the time is to combine the lazy loading pattern with caching. And that's actually pretty easy to set up in Python because font tools built-in standard library already has caching possibilities. So from font tools I'm going to import the cache decorator. And if you want to cache loading sales data then it's actually really easy. We can simply write cache on top of this load sales function. That's all that it takes. We don't have to change anything else here. So let me show you how that actually works. So I'm going to start my main file again. Now of course I have to uh pick an option again. So I'm going to analyze the sales data. So at this point it's going to load all that data from the file which is what we want, right? We need the data in order to do this. And then it computes the total sales. When I do this again, it has cached the value. So it's way faster. And then if I do count total sales record, it's also fast because it doesn't need to load the data that has been cached. So as you can see, lazy loading combined with caching is actually a really powerful feature and you can implement it in Python in a way that doesn't really break the design too much by relying on the cache decorator for example. So let me quit again. Quitting is still fast, although it's not that fast actually. I'm not sure what it's actually doing. Ah, there we are. So quitting is now actually slower and that's probably because Python needs to do some clean up, clean up the memory before actually quitting the application. So as you can see, lazy loading caching together works really well. But there are some cases where you do want to lazy load things, but actually caching doesn't really work all that well. For example, let's say we have a function that gets conversion rates between different currencies and it uses some external API for that. So this is going to give us a dictionary of strings to float values and we're going to return just a few random conversions. Let's say the base currency is US dollar. So there the conversion is one. We have euro which is 1.1. Not sure what the current conversion rate is. And let's say we also have the Japanese yen and that's I don't know something like this but going to print some info here. So normally this would be an API call, right? And let's simulate that. by sleeping for a couple of seconds. This is a very slow currency conversion API that we're using. And then what we're going to do in analyze sales is that we're going to convert the total to the currency that we want. We can actually also rewrite that using some smarter Python syntax. So, we're going to take the sum of a float conversion of the amount field in the data for S in the list of sales like so. And then we don't need all of this. [snorts] But what we can do now is we get the rate and that's going to be get conversion rates for some currency. And let's also pass that as an argument like so. And if it's not there, we're simply going to return 1.0. So no conversion at all. And then we're going to return total times the rate. So now our sales analysis allows us to modify the currency and it uses this external service to get actually the conversion rate. Of course, we also have to change the UI for that. So if we want to analyze the sales data, then we're going to get the currency as an input like so. And let's make this slightly more robust by automatically uppercasing it. And if there's no response, then we're simply going to assume that the currency is going to be US dollar. And then analyze sales. gets the currency as an argument and then let's also write here total sales in currency like so there we go let's run this see what it does so first the option is to analyze sales data we enter the currency and now it starts loading the CSV data just like before which unfortunately still takes a while but in a minute I'm going to show you something you do about that? So it fetches the conversion rate which also took a little while and then it computes the total sales in that currency. So if we do that again then of course the data is cached but the conversion rate is not cached. So you could do that. I could decide to also cache the conversion rates and basically do this right. The problem is that conversion rates change over time. So if we simply cache them, it means that now basically until we restart the application, it's always going to use those same conversion rates. And well here, of course, in the particular example, I'm kind of faking it. But if it's a remote service, then caching the result may actually lead to all sorts of problems. But there we can perhaps be smarter about how we are going to cach this. And what you can do is actually assign timing to this. So here's how you can actually do this. I already created a function for this and it's called TTL cache. This basically puts a time limit on your cache. So it's a decorator just like the regular fun tools cache decorator. But this decorator gets a number of seconds and after that it's no longer going to cache the result. So basically how it works is it has cache data and has the cache time. Then it checks whether the cache result is still valid. If so it returns this and otherwise it recomputes this and it stores this. And by the way, I didn't pay any attention here to make this thread safe or anything. It's really just an example of how you could in a simple way set this up. And now what we can do in case of the conversion rate is make this a TTL cache. And we can give it like I don't know, let's say we want to refresh our conversions every 60 seconds. Then this is how you can do it. So by setting it up this way, you have lazy loading. You only load things when you actually need them. you cache results and when you work with a remote service you can also cache things and then only reload them if you actually need them. So in this case we're okay with being at most 1 minute behind in terms of conversion rates. So when you run this again let's analyze the sales data one more time fetches conversion rates and then it gives you this total number of sales if we do this again. So now you see it was way faster. So it basically didn't need to load the data that was already in the cache and also it cached the conversion rate. But then if we continue this after a minute the conversion rate cache clears and it's going to reload this. Now there is one more way you can approach this and for that I'm going to remove the cache operator here and that is that sometimes you may not actually use all the data. So for example, in case of loading the sales data, it's possible that you only want to know sales numbers of the first 10,000 records or something. And then of course, you don't need to load in all those 10 million records to actually do the computation. So what you can do in that case is not let load sales return a list, but actually use the generator mechanism in Python. And that gives you way more control over when you actually need to load that data. So, we're going to return a generator and this is what we'll import from typing like so. And of course, uh the generator is a bit of a complicated type to specify, but this is basically what we're going to return. And there's some other options here as well. I did a video covering generators more in detail. I also talk about how this type annotation actually works. Won't spend too much time on it here. Uh let's remove this because that's no longer true. But uh what you can do now is actually instead of returning all of this data is to actually put the for loop here like so. And then we're going to yield the row. So now we're not loading everything here, but we supply a generator which gives us way more control. For example, what you can do now in analyze sales, let's take this loading option here and move it to analyze sales. We're going to refactor this in a minute. And we won't use this as an argument. We'll also remove it here. And actually, if we want to do this cleanly, we should probably supply a path here and not put it hardcoded here. Like so. So now I can use the generator version of load sales to actually only load the data that I actually need. So let's say we start with the total is zero and then we're going to use the enumerate function and what is sle sale enumerate over load sales let me supply the path and we start at one and then total plus equals the floatingoint conversion value of the sales amount. [clears throat] So we delete this. We also delete this. Then if I is let's say larger than 10,000, let's assume we just want the first 10,000 records. Then we're going to break. And then we still get the conversion rate and we return the total times the rate. So now we're not only lazy loading, we also only load the data that we actually want. Let's run this one more time. We analyze the sales data, US dollar. And as you can see, loading is now much faster because actually we only load the data that we need. So there's various strategies that you've seen today. We can use caching to make sure we load things only once. we can make sure to not load everything at the start of our application so that we can already interact with the application without having to uh wait for 10 seconds before we can actually start. Um we've seen an example of loading only the data that you need by using generators. So there's a lot of techniques you can uh use to make your program your code feel more responsive um while still actually loading the data that you need when you need it. And that is the essence of the lazy loading patterns. It's not really a design pattern per se like strategy or abstract factory. It's more of a way of thinking of how you're going to set up your code so that it runs in the most efficient way possible. Now there's even more that you can do and that's al something that you see used a lot in operating systems and websites which is that you kind of become smart about the data that you actually load but not lazy loading but preloading actually loading things already in advance before you actually know that you're going to need them. And what you could do here is basically as soon as you show the user interface and while the user is still thinking about which option they want and they're typing in whatever they need, you can actually already start loading data in the background. And here's an example of how you could do that. So I already wrote a version for this because I don't want to code all of this from scratch. It will make the video way too long. But Python of course has support for threading and um threading combined with uh the UI works actually really well in Python. So what I did here, I still have all these things that are cached. It's almost the same as uh what you've seen before, but I have a function that's called preload the sales data. So I'm not using a generator here. I'm simply preloading it. And within that I define a function that actually uh calls computing the total sales and the sales count the thing that we did before. And then it starts a thread to actually do this. And in my main function I call that function so that it starts the thread. So when you run this then here's what happens. Now it's coded a bit dirty because it prints this stuff as part of the user interface. It didn't really do a good job there. But as you can see, as it already shows the UI, it does this in the background. So while I'm thinking about what I want, it already has done it. So I can now analyze my sales data. Let's say I want to convert to euro. It still fetches those conversion rates, but it has already loaded the data. So that means this version has lazy loading. So it shows the UI immediately, but it's also smart in that it preloads things and then it caches those results. So there's a lot of different concepts you use together here. So when to use lazy loading? Well, especially when you're loading a large amount of data, obviously if startup speed is important to you if you have some operations that are uh rarely used. So preloading everything would be wasteful. But there's also some disadvantages to lazy loading, which is that it may add some extra complexity and perhaps you don't really care. It makes in some case the behavior of the system a bit harder to predict, especially if you preload your data in a separate thread like I shown you just now. And by adding stuff like multi-threading, you may actually introduce other problems because now you also have to make sure that your code is thread safe. So it's definitely not for everything, but know that this is a possibility uh that you can do. Other problems you may encounter with this pattern is if you cache uh lists or uh dictionaries that change later on, you may get inconsistent results. And in terms of caching, you have to be careful to not cache functions that depend on external states like APIs. Now, I've shown you one way you can address that by using a time limit on your on your cache, but this is then something that you need to think about. Final thing to be mindful of with the lazy loading is that it can actually hide performance issues if you're not careful because a property access might suddenly trigger a big query or uh loading some big file that you didn't expect. So use this sparingly where the performance gain is really important and not just because you can. Like I said, lazy loading is not really a design pattern, but it's more of a principle to delay uh cost until it's absolutely necessary. And when you combine this with caching, it's a really powerful way to optimize performance without over complicating your architecture. But I'd love to hear from you. Do you use lazy loading or caching in your projects? Maybe in Django, Pandas, or your own APIs? Let me know in the comments. And if you enjoyed this video, don't be lazy and actually click that like and subscribe button because that really helps my channel. Now, if you want to explore more software design videos like this one, check out my design patterns playlist right here. Thanks for watching and see you next

---

## 12. Stop Hardcoding Everything: Use Dependency Injection
**Channel:** ArjanCodes | **Views:** 119K | **Date:** 2 months ago | **Duration:** 32:37 | **ID:** Xhzn1eAxoXk
**Link:** https://youtube.com/watch?v=Xhzn1eAxoXk

### Transcript:
Here we have code example of a simple data pipeline that loads data and then does some transformation and then exports it again to JSON. Now, currently all of this is hardcoded in the class and then called from the main function. That's problematic. It just means that whenever you want to change something, you're going to have to dive into this class and make all sorts of modifications. Also, this class is pretty hard to test because once you created an instance [clears throat] of the data pipeline, well, you basically have all this behavior baked in. It's not easy to replace. Today, I'm going to refactor this code and introduce dependency injection to make all of that a lot easier. First, I'm going to do that in a very manual, old-fashioned way, which actually is my preferred way of doing dependency injection, but then I'll also show you an example of how you can use a container to do that in a more structured manner. This video is sponsored by thesis. I'll talk more about them later. Now, like I said, the problem in this example is that all this logic is embedded directly inside the class. It can't really easily be reused or tested. Let's say if you want to swap in this [clears throat] uh data loader by loading it from a different type of file, uh that's not really easy to do. You're going to have to modify the class or maybe you want to write to a database instead of exporting it to a JSON file. In that case, you also have to edit the class itself. And that makes this code pretty fragile, hard to test, and really hard to extend. Now dependency injection is a design pattern that says well instead of creating all those dependencies inside a class you pass them in from the outside and it gives you a lot of flexibility testability better separation of concerns and in our data pipeline this means that we should inject the data loading mechanism. We should inject a sort of transformer and we inject an exporter. And as you can also see from this example, these dependencies that you inject, they may be objects, but in the original code, they may not surface as object. They may also serve as methods in this case. So for example, this method is part of the class. But it's better if that was removed and that was injected from elsewhere. I'm going to start by splitting out the responsibilities basically these things from the pipeline itself. And yes, I am wearing a different hoodie because I'm re-recording this section. And I thought of a better way to explain this also merge. Anyway, let's split up this class. So the first thing I'm going to do is I'm going to take these two methods and I'm going to turn them into functions like so. Doesn't need self anymore obviously and same thing for exporting like so. Let's create some space. And now instead of calling self.load load data from the as I'm simply calling the function and instead of calling self.export I am calling the function. So that already improves things quite a bit. Now I need to do the same thing for the transformation. So I'm going to create a another function here called let's say clean data and that's going to get data and it will also return data like so. So now we've basically have the behavior loading the data, cleaning the data, and exporting to JSON. And we've extracted that from the actual data pipeline class. Now, you could also turn this into just a function. I I think actually typically if I were building something like this, I would actually do that. But I'm going to keep it like this for now because I want to show you a few varieties of how you can approach this. Right? So now the issue is that there's still the dependency. So even if we now moved out all of these responsibilities to different functions, I'm still loading the data from CSV. And if if I want to do something else, then I need to go here and then actually edit the code right here instead of just doing something here to modify the behavior of the pipeline. Also, I can't add steps to the pipeline at the moment. I can't export it to a different format without editing editing the code here. So that's basically where the dependency injection pattern comes into play. Now a simple thing you can actually do is injecting these functions instead of calling them and that in essence turns the run method here into a higher order method turn form functional programming. The way that you would do that is then pass a function as an argument to the run method. So I could create a loader function and this is going to be a callable which I'll need to import from typing. And by the way I'm doing all of this manually because I'm I disabled all the autocomplete and everything because I I believe it's better in terms of uh video for you to explain the things. Now loader that's a callable and that's not going to take any argument and it's going to return data. And then here I can call the loader or maybe let's be a bit more explicit and say that it's a loader function like so. And now what I can do here in my pipeline.run I can pass the load data from CSV function. And when I run this we get exactly the same result. And we also get the same output JSON file. So essentially what we're doing here is dependency injection. We're injecting this function into the method. Now obviously you could do the same thing for cleaning the data or for exporting the data, right? And then in essence you would add extra argument to this run method. And then well while we're at it, I would actually turn this into a function because we're really clearly following functional stuff. So that's like a functional version of dependency injection. the way you would often see dependency injection used actually with objects. Now in Python functions are objects but we can also follow a more class-based approach. In that case what you would do is not have these functions but actually would have some classes. For example, let's create a class in memory loader. And this is going to then again have a method. We can simply call that load. That is going to get self. And we will also have a class clean missing fields. And let's call this method transform. And obviously that will also need a self. And then we will have a class JSON exporter like so. And let's make this class a bit more generic by adding an initializer where we're going to pass the file name like so. Let's call this export. So now we have these three classes. And then what you can do in the data pipeline is also add an initializer. And there we're going to pass the loader. In that case, that's the in memory loader. We have the transformer and that's the clean missing fields class. And we have the exporter and that's the JSON exporter. And let's store these objects like so. And now the pipeline becomes really simple. I can actually remove these comments because these are no longer hardcoded. Call loader dot load. And then let's call this transformed. And that is self.t transformer dot transform the data. And finally, we're going to export this like so. Still need all of this empty space. So now we've injected these objects in the data pipeline. And then of course later on the run method could be more complicated. We could do more things than just calling these three things. Maybe you want to add some logging or other types of behavior. This is just a basic example. And of course, uh, totally forgot, we don't need the loader anymore. So now what we can do is in our main function, we actually create those objects. So we're going to have the loader, we have the transformer, and we have the exporter. And here we now pass the file name. And in our pipeline we then pass these as arguments when we create it. And of course when we run the pipeline we don't pass this function like so. So now we've done is our data pipeline no longer has the responsibility of creating these things. It simply calls the functions that it needs. And in the main function we patch everything up. We create our loader, our transformer, our exporter. We put the pipeline together and we run the pipeline. So let's run this one more time and as you can see we get exactly the same result. Now there's one issue here which is that data pipeline currently is directly dependent on these very specific loaders transformers and exporters. Typically what you would do with dependency injection is that you would also introduce some abstraction. So you can replace the in-memory loader with an actual CSV loader for example or with a database loader or you could add a different type of data transformer without having to change anything in the data pipeline. Now of course Python ignores these type annotations. They're mainly there for you as a developer. But that also makes it a good thing that you need to think clearly about what this pipeline actually expects, what it actually needs. So let's create some types to specify that more explicitly. And I'm going to do that with protocols which is a nice built-in Python feature. So what I'm going to do is I will now define protocols for these three different types of things. So the first thing that we have is a loader. That's a protocol class. And the only thing that this has is a load method like so. So this is the shape of our loader class. And then we're going to have a transformer. And the only thing this will have is a transform method that takes data and returns more data. Like so. Finally, we're going to have an export. And surprise surprise, the only thing that this is going to have is this method. And important thing here, I'm defining the exporter here with this method. I don't say anything about the initializer, right? Because well with the JSON exporter, we might want to supply a file name or with a CSV exporter as well or an Excel exporter. But if your exporter is, I don't know, a network stream or something, then you will want a very different initializer. So I'm not fixing the behavior of the initializer in the protocol. I'm simply fixing the behavior of when you call its methods. So now we have these three things, the data loader, the transformer and the exporter. I can go in my data pipeline and I can change the types. So this is now a data loader and this is a transformer and this is an exporter. And the nice thing now is if you look at the data pipeline itself, it doesn't know anything about how it loads the data, how it transforms it, how it exports it. That's all reflected in the specific implementations of these classes. And the only thing that happens is it uses the functionality that it can because it knows that loader has a load method that comes from the protocol. It knows that transformer has transform methods [clears throat] and that you can export it by calling this method. It doesn't know anything specific about how those things are being implemented. And that's the really nice thing about dependency injection. So we now inject these things. We create the concrete things here in the main function. That's where we define what we want our program to actually do. Then we construct the pipeline and we run it. Now defining these types doesn't actually change the behavior specifically in Python because it does still exactly the same thing as you can see also right here. But it means we now have a lot more flexibility. I can now create uh let's say an actual CSV loader class. As long as it has this method, it doesn't matter. I can pass it to my data pipeline and it's going to work. Same thing for the transformer. If I want to apply some different type of transformation instead of clean the missing fields, then I can simply do that. For example, let's say I have a class that's called remove aron. And what this is going to do is it will transform the data by removing anywhere where there is somebody that's named ion in the data because obviously we don't want that right. So what I'll do is if row uh name does not equal aron. So now the only thing I need to do I don't need to change anything here because this is an injected dependency but the only thing I do is my transformer is remove and now let's run this again and now as you can see I'm no longer in the JSON file and that's exactly the power of dependency injection. On top of that, that also makes something like the data pipeline really easy to test because you can replace let's say loader by uh a test loader that returns some test data or the exporter by a fake exporter. So your test doesn't actually write JSON files all the time. And that is exactly why the dependency injection pattern is so powerful. And as I've shown you, you can do this with classes like I've done here and some abstraction with protocols. This is especially useful if your class are a bit more complex. So here they're just a single method. Uh if you just have a single method, actually you can also do dependency injection but with functions like I've shown you before. So depending on the complexity, use functions, use classes. That's basically up to you as a developer. Now so far I've built a pipeline that loads data, transforms it, then exports it, and it's cleanly separated and easy to test. But let's say this pipeline is used by an AI assistant or co-pilot, something that's actually talking to users in real time. Now, if all of this does is dump raw JSON or a long text description in a chat window, it's not exactly a great user experience. And I really believe that generating interfaces on the fly based on model responses is the future of AI native apps. And that's exactly what today's sponsor, Thesis, delivers with C1, which is a generative UI API that turns LLM output into live interactive interfaces like charts, forms, cards, and dashboards. Here you see a simple example of a finance app. And as you can see, instead of replying with plain text, C1 lets your app respond with the UI that you can actually interact with in real time. And this works with your existing model and back end. You just pass the model's response through the C1 API and on the front end you render it using their React SDK. Now to get started building your own app with C1, just run this line. It is the easiest way to get started and it uses Nex.js. Now if you prefer Python back ends, simply follow the C1 plus Python fast API quick start to get up and running in minutes. The integration is lightweight. It supports all major LLMs, front-end frameworks, and tool integrations. And it's not just components. It handles streaming, error states, tool calls, and even that let you plug in your own React components with logic validation and state. And this is really great if you're building a dashboard, a copilot, a search UI, or something more experimental. Also, thesis has a playground that you can use to quickly build your app as I'm doing right here. So here I made very quickly a weather forecast UI and I'm really excited about how easy all of this is. You should definitely check out Thesis and if you use the link in description, you get up to 5 million tokens for free. Now back to our dependency injection example. I've showed you how to build this basically manually, but in larger systems, maybe you want a bit more control over where you do the wiring up of all those instances, how many instances you have of each of these things. So, you may want something more than what I've shown you today. Now, one solution to that is that you use a dependency injection container framework. And there are packages for Python like inject that allow you to do that. They offer many other features to related to creating and connecting instances. But in this video, just for fun, I want to build a tiny custom container. So you get an idea of how all of that works under the hood. And my container is going to allow you to register providers under a certain name, resolve them later, and even optionally cache singletons. So let's add that to our code. So I'm going to create a class container that will handle all of this work and this will have obviously an initializer and I want this class to keep track of the different providers. So these are basically the things that create instances for us. So that's going to be self.providers. And the type is going to be a bit complicated. It's going to be a dictionary that maps a string. So that's basically the provider name to a tupole that contains a collable that returns some object and there's a boolean value. I'll explain why in a second. Just have to add this collable to my import. Now the reason I I have this complicated type is well it's a dictionary because I want to map the provider name to some sort of structure that can generate the instance for me. So one aspect of that is that there's going to be a callable basically a function that doesn't take any arguments and then returns some object. Maybe you can improve this by using generics in some way or having like um a sort of protocol superclass kind of thingy to u indicate that something can be part of container. I'll leave that up to you to to think about that. The second part of the tuple is boolean and that's going to indicate whether or not we want this thing to be a singleton or not. And of course if it's a singleton we don't want it to create every time new instances. It should do that only once because it's a singleton. So next to these providers, we will also need to store these singletons and that's going to be also a dictionary from the provider name to an actual instance which for now will just make any. And initially this dictionary is empty and the provider dictionary is empty as well. So that's the structure that we're going to need. Then let's add a way to register providers. So we need a method for that. It needs a name under which we can store that provider. We need the actual provider, the function that creates the thing. So that's the callable that returns an any object. And we need a boolean. What is this? and we need a boolean indicating whether we're dealing with a singleton. So there we go. Return type is none of this methods. And what we're going to do here is in our provider dictionary under the name map, we're going to store a tuple containing the provider and the singleton. So that basically makes sure that all of this information that we pass here is stored correctly in our container setup. Now the most important job of course that this container system has that it should be able to provide us with instances of things. It should be able to resolve those. So I'm going to add a resolve method and we will need a name. So if I register something under I don't know transformer I can then resolve transformer and it's going to give me a specific thing this going to return any again maybe you want this to be a bit stricter and have a class hierarchy for now I'm doing it like this and now we have to figure out the logic for actually creating the instances so the first thing we can do is if it's a singleton and we already have one then it's simple right so let's handle the simple cases first so If if name occurs in self do singletons it means we already have an instance and we can simply return that instance like so that's already one case that we handle. Now next if we don't have a singleton it means we need the provider in order to create the instance for it. So let's do a check. So if the name is not in self providers, it means we don't have a provider for the specific thing and I'm not sure why I wrote is. It's not English, it's Python. Then we're going to raise a value error. Let's say no provider registers for name like so. Okay. So now we know we don't have a singleton object that we could return. We also know that we have a provider for this name. So let's retrieve that. So we're going to get the provider and the singleton value from the providers dictionary like so. And now we can create the instance by calling the provider function. That's a callable remember. Now we're almost there. We have an instance. So we could in principle return this. But there's one more check that I want to do. So if this is a singleton, it means we only want to create this once. So if we're dealing with a singleton, then I'm going to add this instance to my singleton's dictionary like so. And finally, I'm going to return the instance. And this is basically what the container does. So now we can use this class to create all the different objects and patch things up in that way. So how does that actually work then? Well, instead of creating these things here directly in the main function, what we're actually going to do is first create a container like so. And then let's register our providers. So I'm going to call the register function. We have the loader. So that's going to be the provider will be the in-memory loader and as you can see I'm passing the class here. So what that basically does is that you call this uh initializer of the class and that is basically that callable that we have right here. So we register the loader and is this a singleton? Well let's say that this thing is a singleton it only wants one of these loaders. Next, we can also register a transformer and that's going to be clean missing fields. And let's leave off the singleton setting here. And finally, we're going to register the exporter. Now, in this case, it's slightly more complicated because our exporter actually needs a file name. So, we can't just pass the class name here. So what we'll do is create a lambda function that returns a JSON exporter with the file name that we need like so. You could also use partial function application if you want to. There we go. And finally you could even register the pipeline and this also gets a lambda function which creates the data pipeline but inside now we can resolve the things that the data pipeline actually needs. So the loader is going to be the container.resolve and then we're going to pass it the loader like so. And there's a column missing here. the transformer will load in as well. And finally, the exporter and I'm missing a parenthesis like so. So now we've registered all these things in the container. Now, instead of creating them explicitly here, what I'm going to do is I'm going to use the container dot resolve. And now I'm telling it to create a pipeline. Then I have my pipeline object. I can call run on it and then print this message. So let's see what happens when we run this. So now again pipeline is completed and we have our output JSON file and just like before >> [snorts] >> uh we're still using dependency injection so I can still replace these things with uh different types of objects. So my transformer again I can change to remove aron like so and then let's try that again. And now you see that we get the same output JSON but without my information in it. Now you might wonder why would you do all of this if you can just create your objects here? Isn't that much simpler? And the short answer is yes. That is actually much simpler. The reason why this is an interesting approach to dealing with instantiation and connecting things is that this allows for a bit more flexibility in terms of where you do that. For example, now that these are basically methods calls, you could perhaps patch all of this up by reading that information from a separate uh config file or a setup file, right? For example, you can imagine that for your pipeline, maybe you have a JSON file where you specify what kind of loaders, transformers, and exporters you want to use in your pipeline. So then you register those things and you resolve them based on the data in the JSON file. And having a container setup like this makes that a bit easier. Final thing that I want to quickly show you before I end this video is that dependency injection is a really popular pattern. It works really well. One example of where it's incredibly useful is in APIs where you often need to inject things like uh database sessions and things like that or other services. And that's also why fast API has builtin dependency injection. And here's what that looks like. So here I just built an extremely simple fast API app just to show you what it does. So we still have the same type of example. So there is a transformer that cleans missing fields exactly the same as what we had before. But then we can have a dependency. So in this case that's a function that gives me a transformer and it does that by returning an instance of clean missing fields. And what you can do in fast API is if you have your endpoint in this case I have an endpoint for processing data. So you get the data but there's also a transformer and that's injected via the depends mechanism that is supplied from fast API. So basically what happens is if this endpoint is being called then it will call this function to create this thing for us and you can use it in your endpoint. Now again, you could also decide to just create this on the fly by explicitly calling this function, but the dependence mechanism is a bit cleaner because it also allows you to replace the dependency by a mock much easier. And then finally, the only thing I'm doing in this example is running the API using UV core. And I'm simply going to post this request to my API. And there we see we have our cleaned data using the same transformer as we had before. Now like I said this type of dependency injection is handled entirely by the framework. You don't need a container or anything like that. So it's really great for clean architecture in your back end. Now dependency injection when should you use it? Actually I use it really a lot. I like to uh use dependency injection if I want to have testable business logic. So I inject certain things and then I can replace them with mocks if needed. I also like it a lot when I need to swap implementations. For example, swapping out local storage versus cloud storage or something like that. Um, in machine learning, you might want pipelines where you try out different components. So, you want to be able to quickly inject those things. In web frameworks like fast API, that's really helpful because then you can inject your database connection and then your endpoints are going to be independent of that specific thing. in particular if you use some abstraction but it's not always a solution. Uh if you have just a really small script you don't really need it. Don't start building your own containers and going all out. It's not really necessary. Sometimes just calling a function is enough. But overall though I think it's really important that you know about dependency injection and where you can use it. It's lightweight. It's elegant. It's really powerful, especially if you're able to limit it to what you actually need. And regarding that, actually, when I think about what I need in terms of dependency injection, it's very rare that I'm going to need some custom dependency injection container solution. In like 99% of the cases, I can just patch things up in a main function. I just make sure that I've introduced some abstraction here and there nicely decoupled things and then just patch them up. I don't need a complicated framework for that. If I'm using fast API, I am using their dependency injection framework cuz it's built in and it's really easy to use. But other than that, I typically don't use things like inject, but keep things pretty simple. And by the way, if you enjoyed this video, if it helped you in some way, make sure you inject a little like or perhaps you can subscribe to my channel. This is totally free and it actually helps me a lot. So, thank you so much. But I'd love to hear what you think. Have you used dependency injection in your own projects? Maybe even without knowing it. Did you ever build your own container? Do you use frameworks like inject? Let me know in the comments. Also, if you'd like me to do more of these design pattern videos, what kind of patterns would you like me to cover? If you do you know some exotic patterns that nobody knows about that I could cover on the channel, let me know in the comments. I've already done several design pattern videos in the past, but there's a lot more that's coming. If you want to keep track of that, make sure you check out this playlist. Thanks for watching and see you next

---

## 13. Stop Overwriting State And Use Event Sourcing Instead
**Channel:** ArjanCodes | **Views:** 31K | **Date:** 2 months ago | **Duration:** 25:12 | **ID:** t-LC1dWLpNs
**Link:** https://youtube.com/watch?v=t-LC1dWLpNs

### Transcript:
Imagine in a game your player's inventory shows three swords, two bows, and a banana, but you have no idea how they got there. In particular, where did the banana come from? The problem is if you just maintain state, you lose the history. And this is where the event sourcing pattern comes in. So, today I'll show you the basic pattern plus a few more advanced techniques such as caching and projections. This video is sponsored by Flux. I'll talk more about them later. Now, what is event sourcing? Most applications store the result of a series of changes like a balance, a list of items, a status flag. In event sourcing, you actually never store the final state. You store each change that leads to it. And these changes are called events. For example, item added or item removed. Then if you want to know what the current state is, you replay those events and you construct the state on the fly. Now this can be useful in for example banking where you want to keep track of transactions and not the balance. Blockchain is also an example of event sourcing and this has many applications like memecoins and rockps. Another example is the tool that 99% of developers use daily which is git. I mean, it's not exactly event sourcing, but the core idea is the same. You store every change, never throw anything away, and you derive the current state from the history of changes. Let's quickly build a basic example of event sourcing. And we're going to start with an inventory in a game, the example I mentioned at the start of the video. So, I start with nothing more than this empty main file that I have here. So, let's add some files. So the first thing that I'm going to do is I will add an event file and this is where we're going to define the event types that our system is going to work with. So what we'll need is two classes. One is going to be an enum with different types of events and one is going to be the actual event class. So from enum I'm going to import the string enum type and then I'm going to define an event type class. So let's say we have item added and we have item removed. Obviously later on we can add more types of events here. Then I want to create an event class. So let's use data class for that. And since events are typically right once, we don't want to modify events after the fact. I'm going to make this frozen. This is going to have a type which is the enum that I defined before. And let's say it just has some string data for the moment. And also it's going to be helpful to have a time stamp so we know when the event was raised. And in order to store that I'm going to import the date time object. Time stamp is a datetime object and we're going to use the field from data classes to give that a default value. So we're going to use uh datetime dot now as a factory in order to generate this timestamp when we create the event like so. So this gives us a basic event and the event type setup. Next we're going to need some sort of store that keeps track of the events that have been raised in the system. And this event store is obviously going to need the event. And let's make that a class. So we have event store. There's an initializer. And the only thing that this is going to have is a list of events that initially is going to be empty. And then we need some helpful methods like being able to append an event. Note that here I'm only talking about events. I'm not talking about inventory yet. That comes later. This is not going to return anything. And the only thing that this does is call the event method on the events list like so. And then let's also add a method that gets all the events. So this is going to return a list of events [snorts] and I'm doing it like so so that you actually get a copy and you're uh not accidentally changing the list of the events in the event store. So this is really simple event store. In fact, you could even decide to not have this at all because it's literally just a list of events. So, the basic ingredients for event sourcing are now here. But let's adapt this a bit to our use case, which was inventory items in a game, right? So, let's add another file, inventory. And this is where we're going to model the inventory system on top of our event store. So, what we need from events, we're going to need the events. And we will also need the event type. And from the event store, we're going to need the event store class. And now I'm going to create a class inventory. This has an initializer. And what I'm actually going to do is I'm going to supply this with a store object. So that's the event store. like so. So now we can use dependency injection to provide an event store to our inventory class which then uses this store to add and remove inventory items. So let's add a method here. Add item and for now let's say we represent the item simply by a string. This doesn't return anything. So the only thing that we're doing now, we're not storing somewhere a dictionary of items or something. We're actually adding an event and then if we want to know what the inventory is later on, we're going to replay those events and construct the inventory from that. So in this case, adding an item basically means that we add an event to our event store. So we call append and we're going to supply it with an event and the event type is going to be item added and the data that we supply is the item that we pass as a string. So that adds an item. The next thing that we need to add is a method that provides us the items and this needs to collect that information from the event store. So what I want this method to return is a list and a list of tupils where for each item we have the count of that particular item in our inventory. So you get like two swords, one bow, three bananas, etc. And in order to make that a bit easier, I'm going to use the counter class from the collections library. So this will help me quickly uh keep count of the various items that I'm retrieving from the event store. So count that's going to be a counter of strings because I want to do this for every string item. And for now, initially that's of course empty. And then I'm going to iterate all over the events. And I'm using the get all events method to do that. And what I'll do is if the event dot type equals item added then counts for that particular item which is in the data attribute of the event. We're going to add one And because I'm using counter here, I don't have to initialize this entry in the dictionary. That's what the counter already solves for me. So that is what actually makes it easier. If an item is removed, we subtract one from that particular count. And now what we're going to do is we're going to return a comprehension. And I'm going to get the item and the count but only if the count is larger than zero. We don't want negative things in our inventory obviously. So that is get items. So now we have inventory which is a very basic layer on top of our event store. So let's build a simple example just to see how this works. So from the event store I'm going to import the event store class and from inventory I'm going to import the inventory class. And then in the main function I'm going to create the store. I'm going to create the inventory. Oh, not with a capital letter. We're not in Swift or C# land. And I'm going to pass the store like so as an argument. So now I have my inventory. And what I can do then is add a bunch of items. And then let's print inventory.get items like so. Let's run this and see what we got. So now we have a sword, a potion, a bow, a shield, and a torch. No bananas. We can slightly extend the inventory with a few more helpful methods. For example, uh let's add a get count method that for a particular item counts how many there are in the inventory. So this is going to return an int. And what we can do is basically rely on get items. So I'm going to turn the result of get items into a dictionary and that will allow me to get the result of the particular item that I'm passing and by default I'm going to return the value zero if that item is not there. So there we have a helper. And then what we can do in the main function is for example print um inventory uh get count for a shield like so. Let's run this again. Now you see we get one shield. And if we try to find all the bananas then we will see that there is no banana. Still no banana. I'm kind of disappointed. Anyway, that's one thing we can do to make this a bit easier. And now we can also do is for example have a remove item. And here what we'll basically do is append an event of event type item removed like so. And of course that doesn't work that way. It needs to be parenthesis. Okay, there we go. We can make this a bit more robust, for example, by checking that there is actually such an item like so. And now we can do the main function is also remove an item. So let's say I remove the shield like so. Then let's run this again. And now we [clears throat] see that shield is no longer in the inventory when I retrieve that. So this is the basic idea of event sourcing. You don't keep track of the state. You keep track of the changes and then you compute the state from the changes. Now at this point I didn't use a database or something. It's just a list of events. But of course, you can extend this so that these events are actually stored in a database. Now, one issue with this approach is that every time we get the items in the inventory, it needs to compute all of that on the fly. And if you have many, many different events, that's going to take a long time. So, typically what you also often see added to event sourcing systems is caching. And this is actually really easy to add in Python. So what I'm going to do is from from fun tools I'm going to import cache. And what we can do then is take our get items method and we're going to add the cache decorator. So this will cache the computation of get items whenever we call that. Now the only thing we need to do now is make sure that if you add or remove an item that we invalidate this cache otherwise uh get items is not going to give the correct result. Fortunately this is also really easy to add. So let's say we have a method invalidate cache. I'm putting an underscore here because this is typically something that is only used internally. And what this going to do from get item is to call cache clear which is a method that's added by the cache decorator. And of course the result here is going to be none like so. And now we can very easily go into add item and call invalidate cache if I'm able to type correctly like so. And this same line we can add it to removing an item. So let's go back into our main file. Let's run this one more time to see that we indeed get the same result. And then let's say we are going to remove another item here. And we will remove the bow. And let's also finally add that banana. Why not? And now we're going to print inventory dot get items. And there we have it. No more bow but with a banana. And by the way, if you paid attention, uh all these other methods like get count for example, it relies on get item. So that's automatically cached as well, which is really neat. And finally, a note doing caching like this actually a lightweight form of projection. we have a derived view that is stored somewhere in memory. Now before I'm going to continue improving this inventory model, let's take a moment to talk about something that affects every project, which is managing development environments. Now, so far we've been working in a pretty clean Python setup. But real projects are rarely that simple. You often need more than just Python. Maybe some node scripts, a reddish server, a C compiler for that one weird package that you're using. And of course, it all works perfectly on your machine. That's where Flux comes in. Flux lets you create reproducible, sharable development environments, not just for Python, but for your entire stack. It's like poetry or UV, but for everything. And it's built on top of Nyx. Let me show you how easy it is to get started. Let's say we have a little Python script that simulates a million dice rolls using NumPy. This is what the code looks like. there is some missing type annotations and so on. It's not really important what the example is exactly. Then let's initialize the environment with flows. And now let's activate the environment. And I get an all my Zshell update for free. And let's run the file. There we go. That's a clean Python environment with NumPy installed. Now, if you have an existing project already, you don't have to do this way. Flux can automatically set up the Python environment for you from a requirements.txt or a PI project tunnel file. If you want Reddus 2, yep, that runs right inside the environment. No brew install on Mac OS. No pseudo or other types of system pollution. It's really neat. And you can also manage services with Flux services, starting, stopping them, and printing the status. You can commit your environment which is the flux folder in your directory to a repository and then share it with your teammates or you can push it to flux hub. Your teammates clone the repo, activate the environment and they're ready to go. Python, Node, Reddus, CUDA, whatever you need. It works on Mac OS, Linux, Windows Subsystem for Linux 2 and gives you the same results across dev CI and production. I highly recommend you try them out. It's free at flock.dev. The link is also in the video description. Now, as next step, let's make the inventory a bit more sophisticated. And we'll start tracking not just the item name, but also where it came from, how rare it is. And to do that, we're going to switch to using a proper item data class. And here's what that roughly looks like. So, I have a data class item, which has a name, a rarity, and an origin. So, now instead of just passing a sort, we can also track where it came from and how rare it is. In order to support items, we need to make the event class more generic. Now, we could change the string data that we have here to have an item object. But we can also choose to make event generic. So in this case, we're going to have an event that will have a type T. And T is the type of the data. And default, we will make that a string. This is using Python's generic mechanism. And this needs to be supported throughout the chain. So we have the event here, but the event store now also needs to be generic like so. And then the events is going to be a list of events. And each of these expects an event of type T like so. So now in the future, if we want to use this event store for other types of things, we can actually do that. Now there's still the issue of the event type being a string enum, you could opt to turn this actually into a string. So then you can have any different type of event that you want and you're not dependent on this string enum. But I'll leave it like this for the time being. And then of course we also need to change things in the inventory because now the event store is generic. So what I'll do here is from item I'm going to import the item class and I want an event store of type item like so. And then we'll need to change a few more things here as well. So for example, add item expects an item and remove item also expects an item like so. And then the only thing we need to fix is the count because this is a string value. So that simply doesn't work. So what we need to do then is collect the name of each item like so. And then we're going to add that as an index to my counter. And then finally, of course, we also need to update the main file. So, I already prepared this. So, what I'm doing here is I'm creating a bunch of different items, some swords, potions, bows, whatever. I set up a store and inventory just like before. I add a couple of items, remove a couple of items, and then showing the inventory and also counting the number of swords in the inventory. So, let's run this. And now I get some problem. And the issue is that in inventory I forgot to change something. So get count actually also needs to pass an item type. And then here we need to search by item name like so. Let's try this again. Uh that doesn't work either. The reason it doesn't work is because in inventory I forgot to add dot name to this check. Like so. Let's run this again. And now it works. Final thing that I want to quickly show you is that because now we have a full history of uh what happens, we can actually build projections on top of this system. So here I have a couple of examples. So for example, here's a projection called get most collected items. And this returns a list of the most frequently added item names. So basically that's going to look at the events and then count uh the number of occurrences and then simply return the most common ones. Or here's another projection which is get item origins that returns a mapping of item names to all the origins that they've appeared in. So there for each added item I'm adding the origin to it for that particular inventory item and then return that as a dictionary. And then I can simply extend the main file by calling those projection functions on the event store that we have until now. So let's run this one more time. And then you see we still have the current inventory, but now we also have the most collected items and the item origins. And note that these functions don't change the state in any way. They just analyze it. And of course, you can build more projections on top of this items by rarity, timebased graphs, even per player breakdowns. And by the way, if you want to learn how to design software like this from scratch, I have a free guide for you. You can get this at r.co/design guides. This contains the seven steps that I take whenever I design new software and help you avoid some common design pitfalls. The link is in the video description. Now, event sourcing, there's lots of pros to this. You have the full history. You can audit, debug or time travel. The state is rebuildable, so you can replay this from scratch or build new projections at any time you want. Also, the logic is decoupled. So the write and read models, they are basically separate. And there's powerful analytics. If you have projections, you can build those and it gives you a lot of insight without actually cluttering the core logic. You have immutable data. Each event is basically a fact that you don't overwrite in the future. But at the same time, there is also cons to event sourcing. So obviously it's going to be more complex than just keeping track of the state. Uh the queries might be harder. You can't just select everything from the inventory. You need to replay that first or projected. Uh event schemas might evolve. So if you change the event format later, then old events will need migration. And of course it's not always necessary. You know if you don't really care about history in a particular setup then this is overkill. So use it when you need uh traceability roll back or auditing. If your domain is complex or highly dynamic, if you want to derive multiple views from that same data [clears throat] with CQRS for example, a pattern I will discuss in a separate video or maybe you need time based analytics or debugging, then this is very helpful. But don't use it if you're just building something really small or temporary and you just need a simple CRUD operation. Um, also if you have very strict performance needs, this might not be the optimal pattern for you. In summary, as you've seen today, event sourcing is a pattern where you store the changes and then build the state from that. So, I started with a simple example with just strings and then adding caching evolving to a complete item data structure and built custom projections all from that single stream of events. Now, I'd love to hear what you think. Have you used event sourcing before? Are there current projects that you're working on where you think, "Oh, that could actually work pretty well here." Let me know in the comments. Also, if you have other specific patterns in mind that you'd like me to cover in a video, let me know in the comments. Now, if you enjoyed this video, check out this playlist for more in-depth design pattern discussions in Python. Thanks for watching and see you next

---

## 14. Learning Python Feels Easy. Until It Isn’t.
**Channel:** ArjanCodes | **Views:** 40K | **Date:** 3 months ago | **Duration:** 25:51 | **ID:** 8_Pf4y5yqCE
**Link:** https://youtube.com/watch?v=8_Pf4y5yqCE

### Transcript:
Python is by far the most popular programming language in the world right now. If you look at the tobi index, the difference especially since past few years is staggering. It's become the main language of AI, machine learning, data analysis, automation. It's getting very popular for backends as well with fast API due to its large ecosystem of tools. And on top of that, it's really simple to get started with Python. But there's problem though. In my experience, the learning curve for Python looks something like this. Learning the basics seems really easy and you can build up your skill really fast, but then you enter a sort of no man's land where you're basically on your own. Python has very few safety nets. Use type annotations or not. Use functions or classes or whatever you want. You can override dunder methods to your liking. And then there's advanced stuff like generators and context managers and protocols. Going from beginner to expert in Python is actually really hard, much harder than for example Rust where you know once you survive the borrow checker ownership and lifetimes well writing a full-blown CLI isn't that much harder than a hello world program. Now, in this video, I'm going to focus on how to become better at Python after that initial honeymoon and the exact steps I would take if I wanted to become really good at Python faster. Basically, the things I wish I knew 10 years ago. This video is sponsored by Data Camp. Talk more about them later. The first step, the most important one, is to master the core. Most people try to rush through the basics, but these are actually really important. you need to make sure that you don't have them wrong because if that happens then basically everything that you do afterwards is going to get even worse. What is the basics? It's things like conditionals, dictionaries, loops, functions, things like that. Things that Python is actually really good at. So let's say as an exercise you want to build a dictionary that maps strings to their length but only includes words that are longer than four characters. So here I have a simple main function that has a list of words and then prints out those words. Very basic, right? But already if you want to learn the fundamentals, you need to know, hey, I need a main function. And the reason you want that, you don't want to just put this at the top level in your Python script is that you don't want to pollute the global namespace because this words variable is then going to be accessible everywhere and you don't want that. So that's why you need a main function. This type annotation here doesn't return results. it returns none. And then we have this little bit here that makes sure that if you run this script that it's actually going to call this function because that's not going to happen automatically. So that's a basic setup, fundamental setup of a Python script. And then if we want to do this exercise where we map these words to their lengths, well then we're going to need a dictionary. Let's call that a length map. And this is a dictionary of well strings to integers. And initially we have it as an empty dictionary. Also here you don't strictly need to add this type annotation but it actually clarifies the code. We now know that the length map map maps strings to integers. That's the dictionary. Now next step you might want to do in this case to solve this exercise to make this mapping is to use a for loop which of course Python also has. So for each word in the list of words, if the length of that word is more than four, then we're going to add it to the map. And then finally, let's print this length map. So this combines a bunch of different Python fundamentals, right? We have functions, we have a list, we have a dictionary, we have a for loop, we have a conditional statement here. And when I run this then you see we get Python and refactor which are both words that have more than four characters and the other words it ignores. A different way to do this is to rely on a built-in Python feature called a comprehension. Now most people when they talk about comprehensions they only think about list comprehensions but you can actually also do it for dictionaries and for sets. Many people don't know that, but it's a core fundamental thing to be aware of. So instead of this for loop, what you can do is basically refactor this into a dictionary comprehension, which is basically just going to be a single line of code. So that's going to be a word to the length of the word. So that's going to be the map for word in words. if the length of the word is more than four like so. And now I can remove this entire for loop here. And then this is what we have. Let's run this. We'll get exactly the same result. So that's in Python a really neat way to solve this. Now you don't want to use these types of dictionary comprehensions or comprehensions at all if your conditionals are going to be very complicated because then this becomes quickly very hard to read. In many cases, I actually like using a loop. But knowing both of these tools and having them available to you as a fundamental skill is really important. Now, the next step is that once you know that core, then you want to make sure that your code looks and feels like Python. I already gave sort of an example of that where we want to make a switch from these for loops to comprehensions. Again, not always, but in many cases, they can be a great solution. But another example of where you can make things more Pythonic is let's say you want to print out these words with their index. So you might start with something like this. So now we have the indices and then we're going to print that index and we're going to print the particular word in the list like so. And when I run this then this is what we get. Now, this works, but it's not really the Pythonic way of doing this because Python has better support for this kind of thing, namely using the enumerate function. So, instead of using this, I'm going to use enumerate. Remove this parenthesis. And that's going to give me the index and the word. And then I don't need to access it like this, but I can simply print it like so. And this is going to give me exactly the same result, but it's more Pythonic. It's more the way you should do things in Python. And it's not just cleaner, it also avoids uh index errors. It's in my opinion improves readability and using these types of features also shows you understand the language really well. Once you know those fundamentals, once you know how to roughly write Pythonic code, code that works in a Python setting, then the next step is start building some tools. Don't immediately jump into giant web frameworks like Django. That's going to be way too much. It's also going to put you like in a box because it decides a lot of things for you. Just start building some tools. For example, you could write some tools that are helpful to you as a developers. Maybe a couple of scripts that automate some things like renaming files or scraping some data. These force you to use things like file IO, some standard library modules, error handling, that kind of stuff. For example, here's a very simple script you could write that you could run in a folder and then it's going to rename JPEG files from this to this. Building these type of things is nice because it allows to use path lilip which is a really good library. It teaches you some basic file operations. So building little tools like this is actually pretty nice and I use Python myself a lot in this way where you know if I have some folder I need to organize some stuff I just click quickly write a script to deal with that for me. For example, a couple of months ago, I wrote a simple Python script that takes my Final Cut Pro libraries. I'm using Final Cut Pro as a video editing tool for all the videos on this channel. But whenever I share the library with my editor, uh there's going to be some settings that are changed like the where is the cache folder, where are the assets located. So, I have a little script that basically opens the Final Cut Pro project files, an XML file, I believe, and then replaces those folder names with the correct ones on my machine. So, I don't have to manually click through the thing in Final Cut Pro. I can simply run my script and it does it for me. So, these type of small things are really nice ways of getting better at Python quickly without diving in too deeply at the same time. Now the fourth step is to learn more about how Python actually thinks. Python is based on a certain object model that's actually very elegant and consistent. But it's really important for you as developer that you understand how this works. Most importantly know that everything in Python is an object including functions. It's also important you know how scopes work, where are certain variables going to be available and how Python passes references typically and not copies of things. That can lead to problems if you're not aware of those things. For example, if you take a look at this main file that I was working on, let's say I add a function here. Let's call that list words. And that's going to get a list of strings. And what that will do is basically this part. So let's copy that right here. Now obviously I can simply call this function like so. And then when I run this, this is what it does. But if you know that functions are basically objects, well, you could also do things like this. We can in essence create a reference to this function and name it something else. But then we can simply call that as a function just like before and it's going to give us exactly the same result because functions are basically objects that you can refer to. And in essence what a function is actually an object with a call dunder method. So you could even create a class called list words and we could define a call dunder method where you can pass some arguments. Let's say in this case we want to pass the list of words like so. And then we're simply going to do this. And now we have an object. Oh, I actually of course need to copy the entire thing like so. There we go. And then once you have this class, you can create an object like this list words function. And that's going to be list words like so. And then I can call this as a function and pass the list of words. And then this is what we get. So knowing that this is how Python thinks, how Python works really helps you understand the language better and also come up with better solutions to your problems. Now you might think, hold on, that's all easy for you to say, but learning all those fundamentals takes a lot of time. In fact, to learn them efficiently, you need to actually practice them. Just watching tutorials like this one is good, but it's not enough. It's like you're trying to learn playing the bass guitar by watching videos and not by actually playing the bass guitar. In fact, many studies in education and programming show that passively watching video tutorials doesn't help you remember the information as well as using an interactive learning method where you program and build hands-on projects as you learn. Honestly, I recommend Data Camp for beginners learning Python because the platform is super interactive. It lets you code right inside their environment, getting instant feedback as you go. I'm excited to tell you about two of their Python learning tracks because they're genuinely something I wish I had when I was getting started many years ago. I've been recommending Data Camp for years. If you're just getting started, I suggest the Python programming fundamentals track, which is designed to teach you the basics from variables, functions, and all the way up to working with data. If you're looking to push things to the next level, get job ready as a Python developer. They also have the associate Python developer track, which dives deeper into realworld skills like working with APIs, data structures, and debugging. Both tracks are hands-on, projectdriven, and designed to actually help you practice not just passively, but interactively learning. If you're serious about learning Python the right way, don't miss out. Check out the link in the video description to get started for free with Data Camp. The next step in becoming a Python expert is to use abstractions and types to better understand your data. Python is dynamically typed, but that doesn't mean you should just write unstructured, loosely defined code. So, I've already shown you in previous examples in this video today that I'm using type annotations all over the place. For example, here I have my list of strings or here I specify the return type of a particular function. So doing those things really helps you understand better what the structure of your data is and what the contracts are, what methods and functions expect in terms of arguments, right? I can just look at the signature of this list words function to know, oh, I need to supply a list of strings because that's what this function needs. I don't have to look at the body to understand that this is what words is. On top of that, using type annotations like this forces you as a developer to think about the structure of your data. In my opinion, that's the most important role of type annotations. Now, you don't have to go full-on Java style object-oriented programming in Python. Dock typing is actually how the system works. And that means that it will look more at the structure of the type versus having to explicitly define it everywhere. For example, let's say I want to turn this into a bit more objectoriented code with typing. What I could do is I could create a class called let's say transformer and that's going to have a method uh transform and that's going to get a list of strings like so. And it's also going to return a list of strings. Let's say for now what this does is uh return word. So now this just returns the identity. But let's say I want to reverse this. Just going to remove all of this. I'm going to create my transformer like so. And let's run this code. And now we have all the reversed words. And then let's say I create a separate function with the great name do something. Don't really call your functions like this by the way. And let's say that gets a transformer object and it gets a list like so. And then this is what it's going to do. Right? So this does exactly the same thing before. And as you can see I'm using type annotations everywhere. And actually it's not complete because do something doesn't return a value. [clears throat] So I also need to add a return type annotation of none here to be strict. All right. So these are type annotations. But you can take this a step further. For example, in this case I'm just iterating over the my list argument here. So it doesn't need to be list. I just need something that I can iterate over. So in this case I can also be more specific and I can import from typing the iterable type and then it's not a list of strings but this is actually an iterable and when I run this well of course the result is going to be exactly the same just changing the types but now what I can do is I can also turn words in let's say a set like so and then let's [clears throat] also turn this into an iterable of completeness and this also works just like before I get exactly the same result and I don't get any error here because I'm being more precise with my typing. Now another thing you could do is that the transformer in fact we can also make that more generic and that's by using a protocol. So let's first change the name here. So this is actually going to be a reverser. reverser. Weird word. But then what you can do is create a new class transformer which is a protocol. And this is going to have that method like so. And then in my do something function, I can actually say that I get a transformer here, but I'm passing a reverser, which is a specific way of transforming these words. And because I made the type more generic here, I can also create let's say a class uh doubler which has this same method. But what I'm going to do is return an fstring with the words twice. And now I can create a doubler like so. I don't need to change anything in this function. And let me run that again. And now we see we get this as a result. And so knowing your way around types and abstractions is a really helpful skill to have. So once you know the fundamentals, once you understand how Python works, once you know your way around type annotations and abstractions, then it's important to start thinking more like a software designer. That means you need to learn about things like separation of concerns, dependency injection, uh the keep it stupidly simple or the you ain't going to need it principles, things like this. And having all of this in the back of your mind really helps you write simpler code and code that's much easier to work with. And I've already shown you examples of this in the video. For example, the do something function has a transformer object that it gets as an argument. So in essence, this is dependency injection. We're [clears throat] injecting the dependency on this transformer object to the function. We're not creating it here. And by being mindful about that in your code, it means you set up things much nicer. You'll end up with probably a main function where you create all sorts of different objects, patch them up, and then you run your code. And that also makes things easy to test. For example, if I want to test do something, well, then I can just pass in a mock transformer and I can check that it actually does what it's supposed to be doing. Obviously, these are very simple functions, but in a more real life setting, this is going to help you out a lot. So, know all of those design principles and patterns. And actually, I have a whole playlist talking just about that with lots and lots of videos covering basically every aspect of software design that you need to know about. I've put a link to that playlist in the description. Now our next step in becoming an expert Python engineer is that you don't just write your scripts but you actually also write tests for them. If you want to go all the way you can actually write those tests before you write the code and you're basically doing testdriven development but just starting to write test. That's where you move from writing code to just writing code that's actually uh maintainable and easy to manage in a production environment. Now, I recommend you start with Piest that works really well and even though it's not in a standard library, it's much better than the built-in unit test library. And it's important that you learn how to use its basic features like fixtures, parameterization, mocking, those kind of things. Now, I don't have the time [snorts] to dive fully into piest today. There's um several videos on my channel that talk about piest. But basically, if you have a simple function like this is prime, you can start writing tests for this. A very basic thing you could do is write a test is test uh zero is prime for example. And there we're just going to assert that is prime zero equals false because 0 is not a prime number. Now you can also instead of uh writing it like this. I like this way of writing it for test by the way but you can also write it like so. And now let's run piest and we get that our single test passed. But piest has a lot of other features. For example, you could also write a more generic test is prime function that gets let's say a value of n and the expected value like so. And then we're going to assert that is prime n equals that expected value like so. and going to import my test like so. And then what I will do here is I will use parameterization. And that's actually really easy. I can just write park.parameterize and I want to parameterize the n value and the expected value. And then I'm going to supply the list of options. So for example two is a prime number. Three is also a prime number. So I can run this test. So if I have these two options already like so. And now when I run pi test I'm getting that two of these tests have passed. And obviously I can now add more options like uh 17 for example is also a prime number but uh 10 is not a prime number. And I can also test my special cases here like zero is not a prime number like so. And now when I run these tests, this is really easy to write more extensive tests. And if you really want to take this to the next level, there are also libraries out there like hypothesis that automatically generate these lists of tests to run that also focuses on adding the various edge cases. But already knowing these types of features is going to really help you write tests much faster. And having these tests actually really helpful because once you have them in place, you can refactor things and change things. And if you break things accidentally, then the tests are going to fail if you wrote them well. So you're going to feel more comfortable working on your code. Now finally when you're ready you can decide to go really deep and dive into the internals of Python and learning how those different dunder methods like uh it next call all work. Uh you can start writing your own context managers using enter and exit or with context lib. It's going to ultimately help you write better code and especially if you're building complicated libraries. Let's say you're building something like fast API. Well, then you're going to need type introspection and things like that. So, you really need to dive into the internals of Python in order to do that. Or if you're building something like Pyantic, you know, that's those are the type of things that you will need to know. You need to be comfortable with dunder methods and Python's internals. Now, there's one more thing you can do to become better at Python faster. But if this video helped clarify your path to mastery, hit the like button and subscribe to the channel. That way you won't miss any of my upcoming videos. Now what I've talked about in this video was to focus on the core, write clean code, understand the internals and build from that. But the final really important thing you need to do is to use the tools that Python already gives you. If you want real leverage in Python, you need to master the standard library. This is full of utilities that solve common problems without needing any external dependencies. modules like it tools, collections, phone tools, path flip. These are really essential. Just as a simple example, here's a script that can wrap long text and it uses text wrap which is a built-in library. It's from the standard library and it can already do stuff like this. When I run this, this is what we get. It just works. There's no need to add dependencies, third party packages here. You just need to know what Python already gives you for free. Now, if you want to learn more about that, I actually made a video covering 10 standard library gems that most people actually don't know about. You can watch it right here. Thanks for watching and see you next

---

## 15. Why Your Code Isn’t Pythonic (And How to Fix It)
**Channel:** ArjanCodes | **Views:** 139K | **Date:** 3 months ago | **Duration:** 26:39 | **ID:** bsU7AFjh4m8
**Link:** https://youtube.com/watch?v=bsU7AFjh4m8

### Transcript:
The code that you're seeing here works, but it's ugly, repetitive, for both, totally unpythonic. Today, I'm going to take this pretty clunky fitness tracker script and refactor it step by step into clean, beautiful, idiomatic Python. Before I start refactoring that code, let's talk about this word Pythonic. It's one of those terms that lots of Python developers throw around, but what does it actually mean? Is it some kind of secret club that only a few gurus are a part of? Or do you need to buy a bunch of iron gold merch like the hoodie that I'm wearing right now or this nice hat or this mug that's now available? Of course not. There's no clear definition. But to me, Pythonic code is code that uses Python's strengths, like readability, simplicity, for example, in many case using functions over classes and expressiveness, using features of Python that are really powerful like comprehensions or generators. And it should feel natural in Python. Well, that's not very specific, obviously. Now the closest thing you we might have to a definition is the Zen of Python. If you write import this in a Python script or in the command line interface like here, you get the Zen of Python. And this outputs a number of guiding principles like uh beautiful is better than ugly. Don't know exactly what that means. That's kind of up to you what you find beautiful or what you don't find beautiful, right? To me, the favorite one is simple is better than complex. And that doesn't just apply to Python, but to any programming work. And in fact, most anything that you do in life. But in this video, when I talk about Pythonic, I mean that I'm going to be using idiomatic syntax, things that fit well with Python. I'll try to avoid overengineering and overall make the code just easier to reason about. Now, with that in mind, let's take a look at the original version of the code. The starting point is this fitness tracker class that has a log food method to log some food items. So this writes to a foodc file. We have a log activity method as well that logs to activities CSV. And we have some method that prepares some sort of summary for the day. And then it retrieves information from this CSV file and from this one as well. And then it's going to print out some sort of summary. Some issues in this code are the manual file opening and closing, no type annotations, there's no data classes or something, no error handling, pretty verbose uh control flow, especially in run day summary. And the question is also what the added value of the class here is. So there's lots of things wrong with this code. I mean it works. When you run this, it creates these CSV files and it prints out a summary. And this is what these files look like. Pretty basic obviously, but clearly there's a lot to improve in this code. So the first thing that I'm going to do to make this more Pythonic is to replace that fitness tracker class or actually just remove it and turn these methods into functions. Because as you can see, the class has no member variables. There's no reason for this to be a class. We don't need multiple instances of a fitness tracker. So we can simply turn this into functions. So that's actually very easy to do. I just remove this. And then I de-indent this part like so. And we will remove the self from all of these argument list because obviously we don't need those anymore. Like so. Now the nice thing is at the bottom of the script, we don't need to create an object anymore. So we don't need that object and we can simply call the functions directly. So we're logging the food, we're logging activity and we're running the day summary like so. And now I'm going to run the code and as you can see it does exactly the same thing and it creates the files again. But now we went from having this class to having just a couple of simple functions. And typically I much prefer this kind of setup. Actually, overall I typically start with functions and I only add classes when I feel like they add some value. On top of that, if you want to write tests for this, that's also a bit easier because you don't need to create an object. Next step is that I want to take a look at how we're dealing with files. Now, you can do it this way in Python. You can open files and close files, but uh this is actually pretty dangerous if something goes wrong. For example, let's say that for some reason you're not able to open a file or here in the runday summary if you call that before you actually log anything then well these files don't exist. Now there is a check here. I'll come to that later but there's all sorts of things that can go wrong here and then if it goes wrong we don't properly close the file and in terms of resource management that's not great. Now the Pythonic way to do this is to have instead of these uh manual opens and closes to use a context manager instead. And this also makes the code look a lot cleaner. So how do you do that? Well, this line we're going to replace with a context manager. So we type with open food as f. And now we can within that with statement add a block where we can do something with the file. And we don't need to close the file anymore because the context manager already handles this. And same thing, we can also do that here. And then I can delete that. And finally, we have the same issue in of course run day summary. So here we're going to do the same thing, but now I'm going to put the entire for statement inside the body of this width. And then I will remove the close operation here because that's not needed. Same here. There we go. That actually makes the code a bit shorter because now we don't need to manually close the files anymore and it's going to be much more safe because if there is some error here while we try to open this file, the context manager is going to make sure that that resource is actually released. Let's run this again and see what happens. Oh, now we're having minus 400 calories because I'm actually adding this data to the existing file. So, as you can see, there's now multiple entries. I ate two bananas and I ran twice and burned 300 calories each time. The next step, you see that my ID is already giving all sorts of errors here is that we want to add type annotations. Now, strictly speaking, this of course not necessary in Python. And especially in older Python code, you're not going to see a lot of use of type annotations. And there's still plenty of packages today that don't have type annotations, even though they're very useful. I find by adding type annotations, it not only makes your code a lot easier to read and understand, it also forces you to think about what kind of thing a certain data item is. Like for example here calories, we have no idea what that is. Is that a number? Is that a string? Is that a list of things? It's unclear, right? You now you have to inspect the body to see what this is actually is. And it seems if you read and interpret this that okay, it's probably a number, but it's not clear at all from the function signature right here. And by adding type annotations, it just makes it much easier to understand what a function expects in terms of data and what it's going to return. So here, let's add those annotations. And it's very simple to do. So item, for example, that's a name of an item. So we're going to make that a string. And calories is a number. Let's say that's a whole number. We don't deal with half calories. So I'm making that an integer. And then we have date. So if you look at what date actually is, you might think that is a datetime object, but actually, as you can see, it's represented here as a string. So date is actually a string. On top of that, it needs to accept optionally a nonv valueue. So it's an optional string and we can denote that by writing string or null like so. With type annotate, it's also important that you don't forget about the return value. What's the result of a function here? That's also pretty straightforward. It doesn't return any result. So we simply write none. log food doesn't return a value and with log activity we can do the same thing. So date again that's a string or it's num calories burnt that's again a number and the activity that we're logging that's a string also I'm not sure why there is an extra daytime import here I'll just remove that finally let's take a look at run day summary so here also we have date So that's also a string. There's also no return value here. It simply prints a summary like so. As you can see, my IDE still gives me some of these red squiggly lines. I've set type checking here to strict. So I actually do see these errors, and I I like that. Now, the reason this happens is because I define food here as an empty list, but it's unclear what type of list this is. This can in principle contain anything, right? But we're adding integers here. So that's why we get the error. In order to solve this, you can specify the type of the list when you define it. In this case, that's a list of integers. As you can see, now this error disappeared. So we can do the same thing for activity. And that is basically all we need to do. Now type annotations are in principle ignored by the Python interpreter. So, if I run this, I simply get exactly the same result. Well, it's now - 600 because I added yet another banana and I ran yet another uh time and burned 300 calories. But the program does the same thing. Python ignores these type annotations. They're really there to be helpful to you as a developer. And that's also how you should view them. And by the way, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get this at r.designguide. /designguides. This contains the seven steps that I take when I design new software. Hopefully, it helps you avoid some of the mistakes that I made in the past. Guidu link is also in the description of the video. Now, the next thing that I'm going to do, that's in this run day summary function is this part. So, what this does, this checks that food.csv exists before I open it. And it's all fine. It's a programming technique called look before you leap, right? We check if it exists, then we open it. Actually, a more Pythonic way of handling this is that it's easier to ask forgiveness than permission. And that basically means that instead of checking whether the file exists, we're just going to try and open it. And if that doesn't work for some reason, we handle the error. So if we do that here, then instead of this if statement, what we're going to do is a try accept block. So that's going to be a try and this is going to be an accept file not found error. That's the kind of error that we might get. And then we can print a message to the user. And of course I can do the same thing right here. The reason that this is a arguably nicer approach is because you are explicit about the kind of errors that you're handling and you don't have to do all these various checks. So that's actually pretty nice. Also in this case, it actually means we don't have to import OS anymore. Now the next step to make this even more Pythonic is by taking a look at the structure of the data that we're using. Now if you look at these logs, they all have the same three fields. There is some sort of item or description. Uh there is a date and there is a number of calories. So instead of passing around these three arguments everywhere, we can also model this in a class. Hey, and you thought I didn't like classes. So actually sometimes I still do like classes. And I'm not just going to create a class for this. I'm going to use a data class like so. And then I'm going to define that data class. I'm going to call it entry. Entry is going to have a date. It's going to have a description and it's going to have calories like so. And that means we can now also remove a lot of duplication because actually logging food and logging activities is almost the same thing apart from the file name. So the first thing I'm going to do is rename log food to let's say append entry and this function is going to get file name. So can use it for food or for activities or for anything else that we want. And I'm going to pass an entry and that is of type entry. That's the class we just defined right here. And if you want to do it in a really neat way, you can see that there's here a part that handles when the date is none. Now, what we can do instead is actually add that feature to the data class so that when you create the entry, it will actually fill in a default date. Now, in order to do that, we're going to need a helper function. Let's call that function today. This going to return a string. And what that will do is it's going to return this exact value. And now what we can do and I'll just move date down the list so that we can set a default value for it. Then what we can do is we can create a field which we need to import from data classes. And the default factory for this field is the today function like so. Now you might wonder why can't I just call today here? Well the problem is that it will then calculate that default value once and then every time you use entry throughout your code is always going to be exactly the same date. This way by using default factory it means that this function is called whenever we create the entry object. Now what we can do is refactor this append entry function. So this we don't need anymore because that's already handled in the entry data class. And then we open the file. So that needs to be the file name like so. And then what we're going to write is entry. Entry dot description and entry dot calories. And by the way, you can probably improve this even more by setting also default values for the description and or the calories, but I'm not doing that for this particular example. And then the print statement, we also need to update of course. So that's going to be appended the entry. So slightly changing the behavior of the code here. Uh and then we have entry dot description. We have the calories and we have the date like so. So that's appending the entry. Now what we can do next that I can actually remove this log activity function because we don't need that anymore. And then what we're going to do here is append entry. And then I'm simply going to provide the file name and the entry object. So the file name here for this is food csv and the entry is a banana with a 100 calories like so. And here we're going to do exactly the same thing. Append entry. Here we have activities csv. And then the entry is running with 300 calories like so. And then we run the day summary. And here we can actually also call the today function that we made before like so. Let's run this again and see what happens. So now you can see we added yet another banana and we started running even more. And then we have our summary just like we had before. So this is nice because now it provides a bit more structure to our code and that's also a really nice addition when you're writing Python scripts that you think about structure a bit and it's going to simply make your code a lot easier to read. The next step is that we think a bit more about constants and where we define those things centralized file paths and how we're using them. Now if you look at the code we for example here I've open food.csv. that works, but it's also uh pretty brittle. Uh instead of defining it here as a string, it's probably better to define it as a constant at the top of the file. Now, you can also load that from a config file. I I like to do it at the top of Python file if it's not uh too many constants. So, what you can do is something like this like so. And then instead of referring to the string here, we can refer to the constant. And then we also do that over here. So now if you want to use a different file, we only need to change that in one place, namely here. And of course, we can do the same thing for the activities. Like so. Now, if you want to be really neat, actually, of course, these are file names currently, but it would be even better if we use actual paths because then we have a lot more flexibility. And one neat way to do that is to use path lip. So, I'm going to import path from there. And instead of using strings here, I turn these into path objects. And nice thing about that is that you can then combine different paths. You can create like a tople directory and then have relative paths below that. There's a lot of features in path flip which are really nice. And what's really cool is that opening a file actually works with path out of the box. And then of course we also need to update this because this is going to be of type path and not a string. So there we go. Let's run this and see what happens. We're already at minus,000 calories. I'm starting to like this example. So, we're using path lip, which is a really nice addition. And now that we have these paths separately, we can also take a look at run day summary and simplify that as well. Because actually, if you look at this code, well, that's roughly the same as this code. That's just a few name changes here and there. So, what we can actually do is uh duplicate this like so. And then we can actually just create a read entries function that reads entries from a file. And of course for that we're also going to use the path type. And then what we'll do here is I will just name this entries and we're going to open the file. And then if you want to be really neat we can start using more Pythonic style coding. So instead of creating a list of entries here that we're returning uh as a value at the end, what we can do instead is actually using an iterator. So what I'm going to do is read entries is going to give me an iterator of entry objects and from typing I'm going to import the iterator. And then what I'll do here, instead of dealing with the list of entries, I'm simply going to yield an entry object with description, calories, and finally the date. And I'm missing a parenthesis there. And I have one extra there. And this we don't need. So now we have a read entries function and if you look at the try except block here actually it does nothing else than just print a message. So the question is whether we really need this right because if there is some sort of exception it's already going to print some sort of message and that's also a nice thing about using try except block that actually often later on you can remove them again. So here reading entries I can simply remove this and I'll just assume that my interpreter is going to print the error whenever I encounter one. So now I have a function for reading entries from a file. So then what I can do here is actually read food in a much easier way. So I'm just going to create a list from read entries of my food CSV file. And then of course it's not going to be a list of integers anymore, but a list of entries. And there's just one part that we don't need to forget to do a bit later on, but I'll do that in a minute. But now I can basically remove all of this. And same for the activity. I can simply create a list from reading the entries from the activities CSV file like so. And then I can just remove all of this because that's already covered in this helper function. So then what we're going to do is now we can compute sums. For example, the food total we can make that the entry dot calories for entry in food if entry dot date equals the date. That also automatically adds our filter. And here we can do the same thing for the activity. Well, that's going to be almost the same. Entry dot calories for entering in activity if entry dot date equals date. So now you see that step by step we're relying more and more on the strings of Python by using these types of expressions which is a really powerful feature of Python. Let's run this again. And we're already at -200 calories. Yay. Now feel that we're almost there. One other thing that I want to do is instead of having these print statements, I want to have proper logging. That's another example of relying more on Python's standard library. And for that, we're going to import the logging module. And then we're going to set up logging. So that's a basic configuration where we log a particular level and we also supply it with a format. So we have time and the level name and the particular message like so. And then for example instead of this print statement here we can write a logging message like so. And then let me remove this print statement. Now here I feel like this is actually not logging. This is really just the result of the program. So I'll leave these as print statements. But when I run this now, you see that we're going to get these logging messages which contain a lot more detail in terms of the time when that happened and the logging level. So that's a nice thing to have as well. The final thing that I want to do is take this code and put it in a main function. Of course, the main function also doesn't return any result. So indent this. That's part of the main function. And then if name equals main, we're going to call the main function. Now, in essence, it doesn't really do anything. I mean, the code just runs exactly like it did before, but now at least we have this explicit entry point, which is nice. And also, if you define some variables that you're going to need here, then it's not going to pollute the global namespace, which is also uh really neat. So now when we look at this final version of the code, this is actually to me a pretty good setup like this. We have some logging. We've defined constants at the top of the file. I have some helper functions like the today function. I use a data class to structure my data that I'm working with. I then have functions that do a particular job that I need without having too much duplication. So appending entries and reading entries. And then I have my function for creating a summary. They all have nice type annotations. I'm using the standard library features like path lip and daytime data classes uh logging which is all very helpful. It makes my Python code a lot shorter. And then finally the actual behavior of what I want the script to do is in a main function. Now if you like these kind of refactorings and practical Python tips, make sure to subscribe clicking a subscribe button. actually also probably burns like 0.01 calorie or something. So, you know, it's a start. Anyway, I hope this video gave you a better idea of what I think Pythonic means, but I'd love to hear from you. What are some unpythonic patterns that you often see? How would you refactor this code that I showed you today? And when you started learning Python, did you have habits that you needed to unlearn? Let me know in the comments. Now, during refactoring, I mentioned a few times that you should rely on the standard libraries, things like data classes, path lip, etc. Actually, the standard library is one of the best things of Python. If you want to learn more about some of the real gems that are in there, check out this video next. Thanks for watching and see you next

---

## 16. You’re Passing Way Too Many Arguments (and How to Fix It)
**Channel:** ArjanCodes | **Views:** 115K | **Date:** 3 months ago | **Duration:** 19:45 | **ID:** UG5jbLReDiM
**Link:** https://youtube.com/watch?v=UG5jbLReDiM

### Transcript:
Do you ever feel like you're just passing the same five arguments through every function in your code? You could use a singleton to try to fix that, but that's not really recommended. But there's another way to solve that, and that's with a pattern called the context object. I'm going to start today with a simple example and improve it step by step and introduce this pattern. Along the way, I'll also talk about when not to use this pattern and what kind of features we have available in Python to avoid too much coupling in our code. The context object appears all over packages and libraries. A very good example of a context object is typically in the back end, you're going to have a request object that's going to contain the HTML uh data or JSON data that's been sent, header, stuff like that. So this is a good example of a context object that's used quite a lot in the back end. Another example where you might see it is if you have some template rendering engine like Django or something. It's going to have a context that contains all the variables and the data that you need in order to render that particular template. Here's the example we're going to work with today. As you can see, there is a render article function right here that gets a user ID, article ID, a database session, a logger, an API key, and maybe in the future even more things. Then there's a bunch of code here. There's some uh check if uh the user is allowed to uh do this. We're getting an article. If it's not there, there's an article not found result. And then finally if everything works out as we want then we can actually render the HTML and return that as a result. Then I have a main function that's uh sets up some logging. It initialize the database creates a session has an API key and it then calls render article to render some of those articles. Now this doesn't use an actual database in the cloud or something. This uses um SQL alchemy together with a SQLite in memory database for just for demo purposes. And this is the code for that. So there's an article table. There's a function to initialize the database. And this also fills our in-memory database with a few articles. So we can just write some code around that. And then here's a function that gives us a database session using a generator and context manager. So nothing too fancy, but at least allows us to write some code that kind of resembles what you might encounter in real life. So let me run this file and then this is what we get as a result. So it renders the first article and the second one basically there's unauthorized access by user 41 and that's because there is this hard-coded check right here. But if I change this to user 42, then actually this is an article that is not found because the there is no article with this ID in our inmemory database. So then we get this as a result. By the way, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get it at iron.co/designguide. This contains the seven steps I take whenever I design new software. Hopefully it helps you avoid some of the mistakes I made in the past. Iron.co/designg codes/design guides link is in the video description. Now the problem we need to solve is that the render article has a lot of different things that you need to pass as an argument. Now you might be able to split up this function a bit more. You know perhaps uh we could do the authorization check somewhere else or maybe we could pass the article as an argument uh to the render function. You know uh but still you still need a logger. So there's only so much you can win with this. So one way to solve this problem is by not passing all of these things separately but creating an object and grouping them and that's basically what the context object pattern is. So the context object then contains everything you need to do whatever you need to do and that's what you pass around through different areas in your application. So what would this look like here? Well, in this case, what we could do is create a class that contains basically these things, right? Or perhaps uh not the article ID because that's maybe something you want to pass directly into the function. So that's clear that this is what it's going to render, but the other things like the user ID, the database session, the logger, uh the API key, that's all things that, you know, we could group and that you can also use in other areas of your code. So in order to do that I'm going to use a data class and then I'm going to create a class. Let's call it app context. And there we can put in the various things that are currently passed as an argument to render article. For example, the ID of the login user could be in here or we could have the database session. We could have the logger and we can also add the API key here. But let's say there are even more config settings that we want to add here. So let's say we have a config dictionary which is a string to let's say of type any like so. So now we have our app context data class. And what we can do then here is create a context like so. And then in this context we can have our various settings and ids and whatever we need. Then this we also don't need anymore right here. And now what we can do in render article instead of passing all of these things as an argument here is we can refactor this so that we just pass the article ID and we pass the context like so. And now we can update the body. So this uses context user ID. Here of course we have context db for the session context.logger. It's kind of annoying that you have to write context dot in front of everything. Of course, you could extract those uh variables again out of the context object at the start and then define them as local variables basically or alternative you could actually make a context tuple and then you still have the grouping and then you can access them here as well. There's various ways in which you can do this. I'm just using a class in this example. But one thing that it does solve as you can see is that the context is now passed here as an argument. So we lost those uh five different arguments. We just have two. Now of course I also need to change that here. So we no longer need to pass the user ID and we simply pass the context and here as well like so. Let's run this again and see whether it still works. And it does. So that's the context object in assets. We group things which reduces the number of arguments. And then we can also use this in other types of functions. For example, let's extend the functionality by also allowing us to publish the article by sending the HTML to some uh external surface. So let's say we have a send to external service and we want to send the HTML and again we have the context like so. Let's first extract the API key and then we can print that we're sending something to an API with this particular API key like so. And now let's say right here we will also send to external service the article and we simply pass the context like so. Let's run this. And now we see that this is being sent to the API. Now nice thing about this is that because we have the context object, we don't have to worry about what kind of things we need to pass along to other types of functions like so. And there's a disadvantage here as well that I'll come to in a minute. But another thing you can do now for example is let's say we also have a publish article that actually again relies on first rendering the article and then sending it to the external service. And then what we can do in publish article is that we pass the article ID and we also pass the context like so. And now we could do for example is extract now the uh user ID check here from render article because we just want this to render an article like so and we put it in here. And then next up what we could do is render the article by passing the article ID in context and then send it to the external service like so. And what we can do then here is we just call publish article and we want that for article ID 1 with the context. Let's run this. There we go. So now it's rendering the article and sending it to the API just like before, but we created a higher level function and I reorganized things a little bit. But as you can see, working with context object like so is, you know, it's pretty nice. But there is actually a problem if you continue doing this. And that's one of the disadvantages, one of the problems with this particular pattern, which is that now each of these functions is dependent on this app context. And that means that potentially each of these functions will know about the user ID, the database session, the logger, the config. And if we add more things here to the app context object, then all of these functions are going to have access to that information. And that means you can accidentally introduce a lot of coupling, which well, it's not really a good thing. It just means that it's much easier to introduce problems if you change that context object in the future. Then a bunch of functions may no longer work because you uh added all this coupling to the code. So that's a that's a problem with this pattern. And you also see for example here like uh this particular function sending to an external service. It needs the HTML and it needs the API key. It doesn't need to know anything about a app context. It doesn't care. It just needs an API key in HTML. So in this case, the context object doesn't solve any particular design problem. It just introduces more coupling. So what you can do instead of trying to do this is to actually not pass the context object in this case. So in this case, let's say we just have an API key, right? And then we turn that into a string. And then this line, we don't need that anymore. And now of course we uh we have to change the code here. Um so now we have to access the API key from the config object like so. So this already better because this lower level function that's basically what it is. It doesn't need to know anything about highlevel concepts such as the context object. So it can simply pass the arguments that are needed more lower level arguments. Arguably, you could also say that about uh the render article function, right? It doesn't need to know a lot of things that are in a context. It needs access to the database and it needs access to the uh to the logger and that's basically it. Well, there is the the API key here that it's uh printing. But since I separated out the sending it to an external service, then maybe this is not even needed here anymore. So, let's refactor this a little bit. So one thing we could do for example is that a render article doesn't get an article ID and a context but it simply gets an article and a logger like so and that should be logging. Yeah. So it gets an article and it gets a logger. And what we then need to do is this part. We could create a function called retrieve article. And this gets an article ID and a database session. This will return an article. This also needs a lower. I'll come to the point in a minute. So we get the article. If it's not there, there's an error and it returns none. We could also raise an exception potentially, but let's change the annotation so that this can actually return none. Article ID should be an integer like so. So that retrieves the article. Then we have rendering the article. So this takes the logger and it uh renders the article. It has the article ready. So it can simply print the ID. I'm not going to print the API key anymore. Like so. And then it creates the HTML. Now, publish article then first retrieves the article and we get the article ID and it needs the database and the logger like so. And if there's no article then we return this and otherwise we render the article and we pass along the article and the logger and finally we send that to an external surface. So now I've refactored this a little bit, reorganized this a little bit where a publish article knows about the context, but the lower level functions don't. And depending on what you need, you can still decide to pass the context to a lower level function. Perhaps retrieve article, you think, well, maybe it might be helpful to actually pass a context object here because we have both the database session and the logger. But I feel like if you have three arguments, that's still acceptable. And same for rendering article. I think if we just have the article and the logger, to me, that's uh pretty okay, right? You could also decide as an alternative to the context object to put things like the logger just simply on the top level of the code. So we simply define it here. It's what you also quite often see in Python script. And then you don't need to pass it as an argument everywhere. So depends on the kind of solution that you're looking for. Another thing you can do to reduce coupling a bit is by using abstraction mechanisms in Python. So for example here I have a database session and I have a logger but now I'm directly dependent on these object types here. Instead what you can do is define a bunch of protocols using Python's protocol mechanism that can look like this. just import the protocol type. So in that case we have for example a database protocol which is basically a session. So that has a query method and we have a logger protocol that has info and error and other types of logging mechanism that we need. And now what you can do is we can change let's say render article so that it gets a logger protocol and you can change retrieve article so that it gets a database protocol as a logger protocol as well. So that further reduces coupling. It removes the direct dependencies on these specific kinds of objects. Overall though the way I typically use the context object is that I will generally use this in highlevel functions or methods like a publish article in this case because to me the context object is like a highle object that brings together a lot of different things. So you want to be careful until what level you pass it through in your application. So I normally stick to the higher level functions and methods like publish article but then the lower level things like in this case rendering an article or sending into some API or uh retrieving an article I don't want to pass the entire context object around because that just introduces a bunch of coupling. So typically what I will do I will simply pass as an argument whatever these functions are going to need and in most cases as you can see here that's just a few arguments and then if you want to further reduce coupling you can use uh protocol classes like I did here for the logger or for the database session and then you can later on replace it with a mock if you want to write some tests for it for example. So overall when to use these context objects in particular if you're passing the same values through various layers of function calls context object is a great way to group those things. If the values are conceptually related like app configuration runtime info or direct dependencies then it makes sense to group them. If you want to simplify testing, mocking, refactoring, a context object can also be helpful. But there are some cases where you should avoid it because it does encourage sort of lazy design. Just, you know, putting everything into the context object just just in case you need it somewhere. If you pass a context object to low-level utility function, it just introduces a bunch of coupling. So don't do that. Those should only take simple inputs. And another issue that can occur with context objects that it becomes a sort of god object. And that's exactly what of course we try to avoid. So they're not a silver bullet. They're a tool that you can use. You can use them to simplify complexity, but you don't want to hide that. So the pattern is quite common, and I hope by shown you the pattern today that you also find some ways to use it to improve your own code. And by the way, if you're enjoying these deep dives into design patterns in Python, hit the like button and subscribe to the channel. This really helps me a lot and this also ensures that you're not going to miss the next video where I'm going to clean up yet another messy code base. Anyway, context object pattern helps reduce repeated parameters, improves testability, but it can also introduce coupling, which is why tools like protocols help you keep your design pretty flexible. And in general, highle functions can work with a context object, but for low-level utility style functions, I think it's better to be precise and focused and just pass the arguments that it actually needs. So, keep those lowle as well. But I'd love to hear what you think. Have you used context objects like this in your codebase? Have you written code that's designed in this way? Let me know in the comments. Now, if you enjoyed this video, you might also like the video I did about the builder pattern. Especially if you have a complex object, this pattern can be a lifesaver. To learn more, click right here. Thanks for watching and see you next

---

## 17. The Best Way to Share Code Between Python Apps
**Channel:** ArjanCodes | **Views:** 40K | **Date:** 3 months ago | **Duration:** 18:28 | **ID:** N_ypJwV8Q8I
**Link:** https://youtube.com/watch?v=N_ypJwV8Q8I

### Transcript:
If you're managing multiple Python apps in one project, one repository, that can be quite messy. You'll have multiple virtual environments, log files, different versions of dependencies, and so on. But with UV workspaces, you can actually manage multiple apps with shared code, shared dependencies pretty cleanly. So today, I'll show you how that works. And I'm going to do that by building a CLI tool, a fast API app that both fetch headlines from hacker news and summarize them using an LLM. And I'll set it all up using UV workspaces to keep everything fast and dry, which by the way, it's not at all in the Netherlands at the moment. It's raining quite a lot. So at least my coat is going to be dry. That's something that I have to hang on to. Now, here's the example that I'm going to work with today. I have this command line interface. You see there is a bit of a mess here with the imports. Uh I'll clean that up in a minute. But actually the code here is uh pretty basic. I'm using some uh libraries like hpx and beautiful soup to get some news from uh hacker news. So there's a function here that fetches headlines from that site and then it parses the text using beautiful soup and it tries to get some titles from it. Then what it does is it creates a command line interface using typer which by the way is also an awesome package and I define a command headlines that gets these headlines and then simply prints them nicely using rich which is another nice package. Lots of nice packages here. Now the setup for this code is also pretty simple. I have a pi project file that just has a couple of dependencies. I can install these dependencies like so and then I can run my main file. Now there's no requires Python. There's a reason for that. I'll talk about that in a minute. But basically not get some headlines from hacker news like mpm malware. Yeah, don't use that. Well, if you're doing web development, you probably have no choice uh hosting website and so on and so on. So this seems to work as expected. Now, the problem comes when you're not just dealing with one package, but if you're trying to add more things to this, let's say you also want to build an API that actually retrieves this for you. And since this is so closely related to the CLI, you're going to want to put it in the same repository. So then you would create another folder which would have your fast API app. And magically, I have actually already done that. So as you can see I have an API folder here which has a main file uh that's a fast API app uses uicorn to run the server and it has a headlines endpoint which well fetches the headlines and then returns that as JSON data. Now the problem here is that we had to copy a lot of code. Uh this fetch headlines function had to copy that over. uh I had to also copy the dependencies because now this API also needs beautiful soup and httpx and that's obviously not ideal. Now you could try to turn this into a package and publish it on pipi or something and then use it here but that's also kind of annoying. You want to be able to just do that locally, right? Uh you could uh create like one big Python package and then have uh undumber in it files everywhere which is maybe also not ideal. And this is a place where the UV workspace comes in very handy. The first thing you can do with this is that you can define shared dependencies in a root pi project tol file and then these dependencies are installed once in a single virtual environment and that is then used by all the sub projects and that also avoids a lot of version conflicts and duplication. So let's say I want to take uh some of these imports that I have here and I want to move them to a workspace level pi project file. So the overall structure of the folder and you see I already did some previous work here is that you can now have a pi project tol file here where you can then add those dependencies. So this is in principle the root of our workspace and I can add these dependencies here. For example, if I go to my API, I have a fast API and a UIORN, for example. I can put those in my root PI project file. There we go. And now these dependencies are here. And now in my API folder, I can remove these. The only thing I need to do is indicate to UV that this is actually a workspace. And in order to do that, I need to modify my root by project file. And what I'm going to do is I'm going to add a tool uvworkspace and the members are any folder in the packages folder. So that's in this case both the API and the CLI. And now what's going to happen is that you saw that before it created a virtual environment inside the CLI folder because it just work directly from there. But what we can do now is create a virtual environment at the top level. So I'll just remove this folder. And then what I'm going to do going to move up to the top level. And now I'm going to do UV sync all packages. And now it's going to create a virtual environment for all the packages in my project. And as you can see, there's now a VM folder that appears at the top level. And what I can do now is I can do UV from packages CLI non.pi. pi and now it's going to run my command line interface but using the top level virtual environment and dependencies. Similarly, I can now also start the API and let me just start a new terminal here. And now I'm just going to do a call request to this URL slash headlines. And now you can't really see this clearly. Let me move this to the top bar. Now you can see we have our JSON data as a response from this API. And also as you can see there's no virtual environment folder here. It uses the virtual environment at the top level. So that's really nice and this already very helpful like uh I have a N here for example which is being used by the API to define things like the host and the port. Well, actually I now also have access to M from my uh command line interface which is really cool. And actually uh in order to solve all these type issues, I can now select this top level workspace and now I also get correct type annotations in VS code which is really nice. So it's really cool that we now have this place where we can put shared dependencies and that solves a lot of potential version conflicts between the code in these different packages. By the way, making the right architectural decisions at the start of your project is really important. It's easy to make costly mistakes if you don't know what you need to consider. So, I created a free architecture checklist to help you get organized. Go to our code/checklist to grab your copy. This contains a ton of practical advice and a list of important things to think about you can takeick off so you don't forget anything. Iron.code/checklist. Link is also in description of this video. Now, there is still one thing we need to fix. duplicate code in our project. We have fetch headlines in the CLI and we also have a fetch headlines in the API. So what I'm going to do now is create an internal package where I can store this duplicate code and then import it in the various places where I need it. And also that I'm going to do under the packages folder. So what I'm going to do is create a folder called core. And that's going to be the name of my package. Not a really great name, but well, we'll just have to do. And then inside that, I'm going to structure this more like a true package. So, I'm going to have a source folder. Inside that, I'm going to have the core folder. And inside that, I'm going to have news.I and I'm going to take this function fetch headlines and these dependencies as well. I'm going to put that right here. And of course, I'm also going to need a by project file. And and inside that, we're going to have something like this. So there's the hpx and beautiful soup dependencies that this particular library needs. And now we have our package here. And now what I'd like to do is I'd like to go in here and then I'd like to import this from the core package. So, I'd like to type something like this from core top.news import fetch headlines. And as you can see, this doesn't work yet, but I'll come to that in a minute. So, that's the idea, right? And then we also don't need this, and we don't need this either. So, how can we make sure that core news is now recognized? Well, obviously, what we need to do is we need to add core as a dependency, but it's not a package that we can install from Pippi or something. What we're going to do instead, and we can do it here inside the CLI pi project file, or we can do it in the global pi project file, the workspace level, and that's where I'm going to do it. And I'm going to add core as a dependency. Now, of course, if you do it like this and just try this, then it's going to try to install this from Pippi, which is not what we want. But what you can actually do in a workspace is that you can indicate that some of these dependencies are actually local and they are inside the workspace. Oh, that's uh great. Okay, apparently Toml doesn't really like my style of typing. Anyway, uv sources is what we then need to add to our pip project file. And what I'm going to do here is I'm going to say that well core is and uh as you can see AI is making up a bunch of stuff which is not great but actually what we want to do is workspace is true. So now UV knows that oh core is actually something that I don't need to get from pipi something it's like a local thing. Let's sync this all up again. As you can see, it actually took the core package from a file. So that seems to work. And now what we can do is I can run my CLI again. And now it uses the fetch headlines function from our internal core package, which is really neat. And of course, now that we have it here, it's also really easy to add it to the API. So in case of the API, what I'll do is I'll just delete this because we don't need that here. We don't need beautiful sbx. And in fact, we're going to import the fetch headlines function from core. News. Now, unfortunately, as you can see, uh the VS Code import system doesn't recognize this uh workspace thing yet. At least it doesn't do it correctly in my setup. Maybe I need to do some changes here and there. But when I run this, this should work without any issue. So I'm starting the server again and now I'm going to do my cur request one more time and then it uses the API and then it accesses the API which in turn uses our fetch headlines function from the core package which is really neat and now you know once we have this set up we can also start thinking a bit about okay where do we put certain dependencies for example in the API I currently don't have any dependencies these are at the top level but you know it might be helpful to actually have these two fast API and uicorn in my API dependencies and not in the global dependencies because they're not being used in any other packages. Right? So I'll remove them here and then I can go back into my API project file and I can add them here like so. And let's see we have our CLI. So this has typer rich HTTP and beautiful soup. It doesn't need these two anymore because these will be part of our uh core package. As you can see, they're right here. So now we have these dependencies that are specific to each package. Core has hpx beautiful soup. Our CLI has typer enrich. Uh the API has fast API u and our top level pi project file has python.n and the core internal package. Also, as you can see at the top level, I've defined the Python version that is required. This case, that's 3.13 or higher. I mean, you could use an older version of Python, but at least this way for all the projects, this value is going to be defined, which is a really neat way to do it in my opinion. Now, just to complete this example, let's add one more package to this that actually uses an LM to summarize text. So I'm going to create a new package here called summarizer and I'll set it up similarly to the core package. So there's a source folder inside that it has summarizer and then we have a file summarize pi and inside this I've already prepared this like a Swedish chef. I've created a very simple function called summarize text that gets text that gets an API key creates an open AI client and then it sends a request to GPT5 and returns the output text. Of course, we can make this as complicated as you want just a basic example. Of course, in this new library, I'm also going to need a pi project file. And this is what the contents of that file looks like and has a single dependency open AI. So now once we have this the nice thing is that because in the top level tone file I've defined that our members is every subfolder in the package folder now actually our summarizer package is also available and I can add it as a dependency like so and then of course I also have to add it here to my sources so I can use it in the code. And now when I go back to my CLI for example, I can import from summarizer dots summarize import summarize text like so. It's maybe not an ideal name for a library. It's a bit long to type but doesn't matter. But now we can call this function and the only thing we're going to need is a open AAI API key which I have in this file. So what I do here is from uh end I'm going to import log.enf I'm going to call that. So I have the API key available as an environment variable and then I'm going to import OS and then the API key is open AI API key and then what I'm going to do is uh call summary on summarize text on the headlines and I pass the API key as well and then I'm simply going to print that summary. So now let's run this. That's going to build the summarizer package which of course it's going to need and then we have the top five headlines. Now it's sending it to OpenAIL LM and then I have a summary of that. So that's really easy, really easy to add more uh functionality to this. And now I have a pretty neat setup where you know I can add more uh packages here, more tools. Uh I can decide where I want to store these dependencies. Do I want at the top level? Do we want them locally in a package? It's very flexible and it's really easy to use. And of course, now if I wanted to summarize a package, I could also use that in the API. Or if I would add a third type of tool here, I could then also use the other modules there as well. So overall, it's a really nice setup that would be a lot messier and errorprone if you had to use many uh local dependencies and many virtual environments. Here it's just one virtual environment. So when should you use this one? In our case, we're using this for our automation. So, we have this repository which has all sorts of small automations. Basically, Python scripts that uh respond to some uh web hook or that uh extract some data from some API and then send it to another API. And these all have mostly overlapping dependencies. They're all using HTTPX and fast API and a couple of other things as well. And uh there's also some uh common uh dependencies between them. For example, I've created a small layer on top of our accounting system that all of these uh projects are actually using. So this uh workspace setup allows me to do that in a really clean fashion. So if that's something that you're dealing with uh then this is a really good solution for you. If you have global dependencies that you need to share, uh if you want a one log file uh for reproducibility, uh then this is all a very good choice. It's probably not worth it. If you just have a single script or app or you know you have a package that you want to release to Pippi independently, this maybe not the way to set it up or uh if you have a repo with like completely unrelated apps, then this is also not a really good choice. In that case, I would actually suggest you split your repo into multiple repos, one per package. Anyway, I hope this video gave you a better view of what UV workspaces are and what you can do with them. If you like this kind of in-depth look into modern Python tooling, hit the like button, subscribe to my channel. This really helps me make more videos like this. Overall though, I really like this feature of UV. makes it super easy to manage multiple Python apps in one repo and it's a very clean solution that avoids code repetition. But I'd love to hear from you. How do you manage your multi-app Python projects? Are you using UV workspaces for that? Let me know in the comments. If you'd like to learn more about UV and what other things you can do with it, make sure to watch this video next. Thanks for watching and see you next

---

## 18. I Hate Long If-Elif Chains: This Design Pattern Solved It Once and For All
**Channel:** ArjanCodes | **Views:** 332K | **Date:** 3 months ago | **Duration:** 15:24 | **ID:** g7EGMWvJ1fI
**Link:** https://youtube.com/watch?v=g7EGMWvJ1fI

### Transcript:
If your code looks like this, you're in trouble. You wanted something clean and extensible, a system where you could just, you know, plug in a new exporter or an algorithm, but it turned into a hot mess of if else statements. Today, I'll show you a simple design pattern that solves exactly this problem, the registry. It's easy to implement, and once you know how it works, you want to use this everywhere. Let's dive in. If you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get it at iron.co/designguide. This contains the seven steps I take when I design new software. Hopefully, it helps you avoid some of the mistakes I made in the past. Iron.code/design guide. Link is also in the video description. Now, what is the registry pattern? This pattern is a way to avoid hard- coding logic like those if else statements by storing that behavior in a sort of central registry. In Python, that can be a dictionary for example. And then instead of calling functions directly, you register them under a key like a string or an enum and later call them dynamically. It's like a sort of named plug-in map, one that's easy to extend without changing the core logic of your application. If you look at this example, we have an export data function that picks the exporter based on the format, which is a string. Now, you could use an enum for that. I kept it to a string also because that maps better to the example that I'll show you after this one. But the problem here is that this is like this chain of if else statements. Uh depending on the format, you call a different function. And if you want to add more exporters, you're going to add more lines of code here. This function is going to get really long. It's not really a nice design, right? And by the way, these exporter functions in this particular example, they don't really do anything. They just print a message cuz it's not really about exporting. It's more about how you uh patch everything up. And then the main function, I call export data with various formats. Now, you could say, hey, why don't just call export PDF and export CSV? But that kind of defeats the purpose because maybe you want to be able to select the exporter in a dropdown for example and then you want to call that function directly. Now if we run this example then this is what we get. You see we get some export results. So how do we change this so that here we no longer have to deal with this if else statement? Well, we're going to use the registry. Like I mentioned in the beginning, a registry is a sort of central place where you keep track of all of these different things. So let's create something called exporters and that's a dictionary of strings to export functions. That's a type that I defined here which is a collable that takes data and doesn't return any results. It's basically the signature of each of these exporter functions. So that is my exporter dictionary. Now currently this is empty but what you can do then is you can map the names to the function names. So for example here we have export PDF and for good measure let me actually define this after I actually define the functions. Now similarly we can add the other formats here as well like CSV and JSON like so. So now we've stored our export functions in a dictionary. And the nice thing about this is that you can take all of these if else statements that we have here and then basically change it to this. So then we don't need all of this stuff anymore. So as you can see we now simply pick the exporter given the format and then we call that and if we don't have one then we raise a value error. No more if else statement. The main function is still exactly the same. Let me run this one more time just to show you. There we go. And uh if we export to a format that doesn't exist, well then of course we're going to get an error as we would expect. But now it's also really easy because of this registry of exporter functions. It's really easy to add another one. So I could create for example an export XML. But I don't know why you would ever want to export something to XML, but well, here we go. XML. And then I can have another line here that exports my data to XML. For example, there. That's what it is. Well, we still get the error, but it's now also exporting data to XML. XML is probably why you should say I'm Dutch, so I say XML. Doesn't matter. Anyway, the nice thing about this is that I didn't need to change my export data function. The only thing I needed to do was add my particular exporter to the registry. This is the basic idea of the registry pattern. Now, this is not perfect yet because well, we don't only define the function here, but we also have to manually add it to our exporter dictionary. And this is kind of a low-level thing. And if you have many exporters as well then this is also going to be a huge dictionary right so we don't really win that much by turning it into a dictionary except we don't have if statements here but now we have this very long dictionary now one thing that you can do is actually let your exporter functions register themselves and you can do that in Python by using decorators. So what I'm going to do is I'm going to import wraps from font tools cuz that's what we'll need for a decorator function. So I still have my exporters dictionary but now I'm going to add a register exporter. As you can see this code is already generated for me. So the exporter it registers a function for a particular format that's a decorator and it simply stores that function into my exporters dictionary. And now instead of defining my exporters here manually what I can do is I can go to my export PDF function and I can do register exporter PDF. And again I have to remember to define things here in the right order. So, I'm simply going to do this. And let me also move that up. There we go. So, now I can register this as a PDF exporter. And I can register this as well. And register this one as well. And this one as well. Let's run this code one more time. I'm going to comment this out because I don't like getting that error all the time. And now you see we get our various exports. And this is actually a pretty neat setup because now we have our uh registry logic which is basically here we have the dictionary and a decorator to register an exporter. And when I define my export I can simply use a decorator to make sure that this function is actually registered. It's actually added to the registry. And now this opens a lot of possibilities. Uh, for example, I could define these things in different files and then simply use the uh decorator to register them. I could even make something that scans a directory of scripts and then adds all the functions that are defined in modules in that directory to the registry which makes it really easy to extend the code. You can basically just add a file to directory. you don't change anything in the main script and you have your extra plugin or your extra exporter and that's a really nice thing about the registry pattern. Now you may notice I'm not using classes and inheritance here. It's not really about how you build this whether you're doing this with a decorator or with a class that has methods. In the end, it's about setting it up in a way so that it's easy to add these things on the fly. And this is one way to do it in Python. This is actually my preferred way of doing this. Next, what I want to do is show you an example that's a bit more involved. So, let's say we have a command line interface that's called text utils that can do stuff with text like counting words, reversing text, uh shouting text by turning it into uppercase. And it will also support adding more commands in the future without ever touching the main script. And the example that I have here actually uses the registry for that. Here I have file registry.pi that contains the implementation of the registry very similar to what I showed you before. Now it's slightly different in that the registry here is a list. It's a list of tupils with two strings and a callable. So this is basically the function and the strings they are the group that the command line belongs to and the name. So you can do things like text shout or text reverse group name and name of the actual command. Now this all is very similar except we simply append the item to the list instead of storing it in a dictionary because we have both group and the name. Then I also have a get registry helper function that simply returns a copy of the registry. So we can work with it locally without accidentally removing things from the registry. So that's the registry implementation. Obviously you can improve this in various ways but for now this is what we have. Keep it really simple. Now what I'm doing in the main file here is that I use typer to create a command line interface and I do that in two steps. First I have a function called load text commands that imports the text module from the commands package. If you look at the actual code we see that there's a folder structure here. So have command then I have text that's the group and then I have my action. So we have count and we have reverse. As you can see I'm using the decorator here to register this command. So this has the text group and the count command and then it simply splits the text and counts the number of words. What it exactly does is not really that important. And we also have a reverse text where we also register that as a command. So these are basically imported here on the fly as you can see by using import module. Now I also have load plugins. So there is another folder here called plugins and these are set up very similarly. So these are basically other commands that are sort of extensions to our standard text commands. So here I have shout for example that turns the text red and turns it into uppercase. So in principle, there's not really much difference between what is a command and what is actually a plug-in. You can move these around. They're implemented in exactly the same way. The main reason I put it in a separate folder is that then it's clear for you as a user where you should put your extensions and that they're different from let's say the built-in commands. Now the final step in the main file is to actually register all of these commands with typer. And that is actually what's happening here. So I'm using typer slightly differently here. I'm not using the decorators. I'm simply registering them directly by using add typer but the idea is the same. And then in my main function I'm loading the text commands, loading the plugins, registering those with typer and then running the app. So you see when I run this then of course I get missing command because I didn't add any commands but it is a typer app. But now that we have this I can actually call commands like this. Here I'm counting the text from a simple little string and then I'm getting this as a result. Or here's another example where I'm now reversing text which is actually a palen drone by the way. And finally I can also call the plugins. So in this case I have my shout plugin and here is what we get. So this is an example of more reall life implementation of a plug-in system. So if you have some sort of CLI and you want to be able to dynamically add commands to it, this is a good way to do it. Now, to show you that you can actually add commands to this without making any changes in the rest of the code, let me add another plugin. So, here I'm simply going to add a whisper plugin. I'm going to copy this, call this whisper. And then here, of course, we also need to change the name. And then this is going to be whisper. And apparently, that needs to be blue. And we turn text into lowerase. So now I'm not going to shout, but I'm going to whisper. Note that I didn't change anything in the main script. I just added a file to my plugins folder. And then let's also use some uppercase here. And now you see this actually turns it into lowerase and it has added this plug-in dynamically. So this is an incredibly powerful design pattern and I use this type of thing a lot. Now, in particular, what I like about the registry pattern is that it's really easy way to kind of group behavior and choose behavior depending on what you need by putting all of these functions into a dictionary. So, you don't need to go all the way with the decorator and uh loading the the things dynamically from different modules. You can also use part of this pattern and still benefit from uh removing all those if else statements from your code. In summary, what registry allows to do is to add new features without touching the core logic. It's decoupled. The registration happens where the logic is and it's flexible. It works with many different types of frameworks. And also, it's pretty easy to write tests for this because the functions in the registry are just that they're functions. And the registry can be easily inspected or simplified for running tests. Now, some pitfalls of this pattern is a you need to be careful of hidden logic. If you overuse this implicit registration system, then it can be hard to figure out what is actually being registered when you run the code, uh, the order of import matters. If you forget to import a module that registers a command, it's not going to show up. And don't overuse this. I mean, not everything needs a registry, right? Sometimes having an if else statement is okay. Now, overall though, it's one of those patterns that is actually really simple, but it's incredibly powerful if you use it in the right place. But I'd love to know how you might use this. Have you built something that could benefit from a registry in your design? Have you used this before in your own code? Let me know in the comments. This video is part of a series I'm doing that focuses on design patterns. If you want to watch more videos covering all sorts of patterns and there's some real gems in there, check out this playlist next.

---

## 19. My 4 Favorite Python 3.14 Features
**Channel:** ArjanCodes | **Views:** 60K | **Date:** 4 months ago | **Duration:** 12:29 | **ID:** S_shhw4VV68
**Link:** https://youtube.com/watch?v=S_shhw4VV68

### Transcript:
Python 3.14. Python, it's here. Ah, I've been waiting for this my whole career. Python. Oh my goodness. But I'm also a bit sad because now what do we wait for? Version 3.162, the square root of 10. It's just not the same. Or Python 6.28, you know, 2 pi. That's not going to happen in my lifetime unless, you know, AI makes us all like invincible and impossible to kill. And then I'll be here in 500 years still making Python videos. Or maybe AI does all the coding for us and we don't need to make Python videos anymore. Or uh maybe Rust actually becomes really easy to program in and then we'll move to Rust. I I have no idea. Python 6.28, I can tell you it's not going to happen anytime soon. Anyway, let's stick with the here and now, shall we? So, Python 3.14, it has actually some really nice new features that I'm going to talk about in this video. But what actually matters depends also on what kind of developer you are and how you use Python. And we all use Python in different ways, right? So, in this video, I'm not going to give you a complete overview of everything, but I am going to show you four features that I personally really love in 3.14 with, of course, some nice code examples. Now, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you at iron code/design guide. This contains the seven steps I take when I design new software. The link is also in description of this video. Now, Python 3.14 is scheduled for final release on October 7, 2025. And there's a long list of changes going from free threading support that started in the previous version of Python sub interpreters, new debugging tools, UUID upgrades, and syntax highlighting in the shell. If I start the Python interpreter, so this is Python 3.13, and I start typing a function something like this. As you can see, there's no syntax highlighting whatsoever. It's literally just typing text. However, if I open the new Python 3.14 shell, this is release candidate 2, and I start typing. Hey, there we have nice looking syntax highlighting. Isn't that nice? I really like this. I really, really do. I think it's a great improvement. But this is not like one of the main things I I want to talk about in this video. This is one of the things that I quickly want to show you. Like I mentioned, I won't go through all of the changes in Python 3.14. Check out the release notes that I linked to in the video description where you can read what's part of the latest Python version. But for this video, focus on four things that I think are worth highlighting. And the first one is exception syntax. Here we have some Python code that raises exceptions. And there's various types of exceptions here. A value error, key error, some runtime error. And in my main function, I have a try except block that cause the risky function. And as you can see, I'm catching various sorts of errors here. And then doing something. And when I run this, then well, we get that we caught one of these two errors. What Python 3.14 adds is that if you catch multiple exceptions like here, you no longer need these parentheses. So you can simply type this. You see AI has not been trained yet on Python 3.14. So it suggest that we put the parenthesis back. No GitHub copilot. We're not going to do that. As you can see when I run this again, we get the same result because Python 3.1.4 can handle this. Now arguably this is like a really small thing, right? Uh but it's still a quality of life improvement. I mean, if you're catching multiple exceptions all over your code, you've probably typed these parentheses a couple of times. And of course, typing parenthesis as developers costs energy. We have to push those buttons on our mechanical keyboards. In fact, it saves us about 17 calories a year if we're working on a large codebase where we're typing thousands of these parentheses. And that's enough to take one bite of a donut. I'll take it. In fact, I already did. So now I have to switch to Python 3.14. Oh, another fun thing that happened to me actually is my VS Code updated. And if I type clear, you see that little thingy right there? That's a bug. Now I could restart the terminal and then this is going to go away and then it will reappear after some time, but I think I'm just going to leave it in there. I like it kind of broken, you know? It's nice broken IDE. Anyway, those are exceptions. The second thing that I want to show you is compression, zstandard compression that is now built into Python. And that's really nice. It's pretty fast uh compression algorithm. Like here, I create a little script that compares this new compression algorithm that's right here to a gzip and bzzip 2. And I just made like a simple benchmark here where I'm doing a compression and a decompression. I'm uh measuring the time and I'm just compressing some random data. So, let me run this in my broken terminal and show you what happens. So, as you can see, uh Zstandard actually compresses this down to a really small size. It's also really fast. Uh decompression is a bit slower than GZIP in general, but the size is much smaller. And if you compare it to Bzip 2, it the difference is even uh bigger. So normally maybe in the past you would opt for Bzip 2 because it has smaller size as opposed to gzip. But then you would have a much slower compression and decompression time. And I get this kind of result every time I run this benchmark. It's not a complete benchmark and there's probably other types of data that you want to compress and decompress to make a true comparison. But it does look like this is really fast and it works really well. So this is perfect for I don't know you need to store like a uh cache or log files or anything where both performance and size are important to you. The third thing that I want to mention that's new in Python 3.14 is template strings. And these are like fstrings but they're a sort of generalization of fstrings. The issue with fstrings is that they are interpolated directly. So basically you write your fing and it's interpolated and you don't you can't do anything with the uh the way that it's being interpolated. It's just part of the interpreter. Now with templates this is different. These basically uh don't return a string but they return a template object. And there you can see what the interpolations are and you can allow your own custom behavior that you can put on top of those. Here's an example of using these templates. So I'm importing template and interpolation from the template lip that's part of the string module. And then I have a bunch of functions. So what I'm doing in this code is I take some evil script code that I try to inject into a template. In essence, templates give you full control over how expressions are handled. And then you can uh do all sorts of things. For example, you can use this for safe HTML rendering and sanitize inputs or for putting automatic filters on log so that passwords are not rendered, things like that. In this example, I've written an F function. So, this sort of simulates F strings. This actually taken from the PEP that introduced the template. And the only thing that's added here is a sanitize option. So, what sanitize does is that it escapes the values. That's what you see right here. And then what you can do is have a two HTML function that takes a template and then uh turns it into a string and sanitizes it. So that allows you to define your own logic in this way. And then what you can do use the template by well first let's create some evil script that does something and then we're trying to inject it into a string. And as you can see I'm using a template string here. That's the T in front of the string and not an F string. And then we can print the result of turning that into HTML or just print it as a regular non sanitized string. Let's run this. And then as you can see in the first version, we simply get the injected evil script. And in the second case, we're getting an escaped version. And that's what templates allow you to do. So they're probably not something that you will use regularly in your Python code, but they might be part of some libraries that allow rendering HTML or doing logging or stuff like that. Templates really nice addition to Python 3.14. The final thing I want to show you is deferred annotations. Until now, if you had a class that referred to itself, like here for example, I have a node class and a node can have a next node. So that means that the node type is part of the class definition itself. Now if you're using an older version of Python, so let's say I'm using Python 3.13.7, you see that we get an error here, right? Because node is not recognized. So what you had to do to solve this is you needed to basically uh type it uh like this which would kind of work or uh you could use uh self which you import from typing or what you had to do was add from future import annotations. Let me just comment that out. So like this and then it's fine. But that's an extra import that we have to do. Right now, the nice thing about Python 3.14, let me go back to my virtual environment. Uh, I can now remove this from future import. And this still works just fine, which is really nice. So, in Python 3.14, these annotations, they're deferred by default. So, in essence, the future is not what it used to be. And what's even better, there's now an annotation lib module and that allows you to inspect annotations without actually evaluating them. Here you can see how that works. So let's say I have a uh function, my function that takes an int and that takes a node or none, returns a boolean. Doesn't really matter what this function actually does, but then you can use the get annotations function from annotation lib and pass it some Python object. In this case, a function. And then we can print the result of that. If I run this, you see that we get a specification of these annotations. Now, is this something that you'll use daily in your Python code? Probably not. But if you're working on a library that relies on type annotations to do things like paidantic or fast API or uh tools in general that introspect your code, this is going to be huge. Also, overall, if you have these self- reference, it just means less boiler plates, less imports, and overall cleaner files, which I really like. And by the way, if you enjoy this kind of deep dive into new Python features, give this video a like, subscribe to the channel. That really helps. Now, as you've seen in this video, Python 3.14 has plenty of great changes, but in the end, it depends on what you need as a developer. Obviously, for me, the exception syntax, template strings, Z standard compression, no more future import annotations makes this a really solid and very practical upgrade. But I'd love to hear what you think. Are you excited about template strings or the sub interpreters? Are you just happy that the version number finally lines up with Pi? Let me know in the comments. Now, one of the nice things of working with Python is that it has a really good standard library, and it's likely that you're not using that to its full potential. So, in this video, I go over 10 standard library modules that you may not know about, but that are really powerful. Thanks for watching, and see you next time.

---

## 20. Anatomy of a Scalable Python Project (FastAPI)
**Channel:** ArjanCodes | **Views:** 96K | **Date:** 4 months ago | **Duration:** 21:16 | **ID:** Af6Zr0tNNdE
**Link:** https://youtube.com/watch?v=Af6Zr0tNNdE

### Transcript:
Today I'm going to show you the anatomy of a scalable Python project. Here you see what my project looks like. This is basically the setup that I use for all my production projects. So I'm going to talk about folder structure, config, logging, testing, and tooling that together forms a project that's easy to work with and that can scale to more complexity later on. Now, before we start, if you want to learn how to design software from scratch, grab my free design guide at iron.co/design guide. This contains a seven steps that I use when I design new software. The link is in the video description. Now, when I talk about scalable, what do I actually mean? Well, first, it should scale with size. Basically, your code base is going to grow over time, assuming you're spending some time developing new features. And as your code base grows, you don't want to have to do a ton of refactoring work. It should basically be ready for scale. Another way of think about scalability is think about how it scales with your team. So if you have multiple developers that are involved in a project, you want to make sure that the boundaries are clear, that there are predictable places in the code where you're going to add or work on features. Then there's also scalability with respect to environments. So you want to make sure that things like config is centralized. Uh switching between different environments like development, staging, production should be uh boring in a good way. And finally, there's also scaling with speed. So if you have tests, they should run fast. Uh having a local environment should be really easy to set up. Uh Docker works when you need it. That kind of thing. Over the years, I've worked on many different projects, not just Python, but also uh TypeScript, Java, C++, anything. And of course, depending on the language, things are going to be slightly different. So, what I'll show you today is something that works for me really well in Python. I'd like to start with the folder structure, which should be balanced. You want things to be organized, but you don't want things to be so structured in folders that they're impossible to find. So here's how I have set this up in this particular project. Now there are some folders here at the top uh which is some caching and the virtual environment. Of course these are generated uh but if you scroll down a bit you see that there's an app folder and there's a test folder and these contain well the app basically the source code of your project and then the test of your project. If you look inside the app folder you see that there's subfolders here. Now in this particular example, this is a fast API app and this is what it looks like in the main file. So here there's actually not much going on. Some setup work. We create the app and I register a router. This is an API that handles users. That's all there is to it. And before I dive further into the code, let's actually run this app and see what it does. Now that my app is running, and I'll talk more about how all of this works in a minute. I can actually send requests to this. For example, here I have a call request to create a user. So this created a user or what I can also do is get all the users and that's going to give me that one user that I just created. Now like I said, the way that this is set up and how it's running is done in a specific way that I'll explain in a minute. But first, let me go back to the folder structure. Now this API has a single route which is the users route that you saw just before and that's actually in here. So I have API v1. You may have multiple versions of your API if you're uh developing this incrementally. And here is my user route. So this is basically the HTTP layer. And if you look at the actual code in this route, you see that it's actually pretty basic. Uh there is the uh standard routes that you want. You know, getting the users, creating a user, getting user by ID, uh doing an update, doing a delete, basically the crop operations that you would expect here. And as you can see, the actual code to interact with the database is not even here. That's somewhere else. And I'll talk about that in a minute as well. But the main thing to remember here is that this router is actually pretty empty. There's no business logic here. It's mainly there to specify what the routes are. Then I have a core folder which contains crosscutting concerns, things that are used throughout the application in different ways. One is a config. So for that I'm using pideantic settings which is a really helpful tool for dealing with configs. So here I've created a config class. It has an app name debug mode. There is some database strings users password these kind of things. This can also contain some helpful properties. In this case this constructs the database URL from the database name which is pretty helpful. And then I create an instance of this config that I can then import in other places in my code. Now the nice thing about podantic settings is that it can work naturally with environment variables. So here I'm loading environment variables from a N file and this is automatically used to fill in the values for the user and the password of my database which is nice. Then there's also logging. So this case that's also pretty basic. I just set up a simple logging system here with the time and the logging level and like a formatted message. That's all there is to it. And then I can use this in my code to do logging in a standardized way. So these are the crosscutting concerns core things of my application. Then we have the database and the database will typically have a schema and that's what this looks like. So there is some basic setup work here that I need to do for this particular uh database. As you can see, I use SQL alchemy here and then I have a base class and then I define a user and the user has an ID and a name. So that's the database schema. You could decide to split this up into two files where basically the engine creation and the session creation is actually in a different file than the schema. It's up to you here. I didn't do that because this is a pretty small file but you could do that in a more complex project. Now next to the database we have the models and this again goes back to the API. So here we have pantic schemas for request and response contracts. So this is what you need to pass when you want to create a user which is a name and when you read a user you get back an ID and a name. So these are different things and of course you can add your validation logic here later on if I don't know the user has an email address that needs to be validated or there's other things that you need to check before uh storing the user information and then finally we have the services folder and this contains the actual business logic. So this basically takes the database session and does the actual work. So in this case I have a user service which gets a database session and then it does things with that database session. So it can query it to get all the users. It can get a specific user by ID. It can create a new user update deletes the user. This is the business logic that is actually used by the API. Now you could instead of calling this a surface which is let's say more the architectural name for this you could also call this a repop story if you want to follow more the pattern way of defining things and then finally what we have in the application is the main file which I showed you in the very beginning where we have the basic setup of the API app and we register the route. So that's the application that we have here. Now there's another folder here called test which is also at the top level. So it's not under app. It's a separate folder. And that's actually important that you keep your test separate from your application code. And as you can see in my test folder, I have a test DB which actually sets up a temporary SQLite database which is in memory for testing. And then as you can see I have another folder here API v1 test user. And that actually maps to the same folder structure in my application code. And that's on purpose. I always try to keep my test folder structure structure the same as my application code because that way it's easier to find the test. And inside my test folder, I've created a simple example test. And of course, you can build on this later on. So what this does is that's it sets up a test client. This is a feature from fast API that you can you can use for testing. I override the dependency which I'll talk about more in a minute. And then I test creating and fetching the user. And now if I run piest you see that this test passes because of course I prepared this project so it works. Normally this doesn't work the first time. You have to tweak a bunch of things and make sure it all works. But this time it works. What else is in my project? So as you can see next to these folders we have a bunch of files here. There is uh my project tommo file which contains all the dependencies for this particular project. It contains the required Python version reference to the readme file uh the version the name of the project etc etc. Then I have a m file which contains environment variables for my project. This is normally only used locally. So this can contain things like the database user password, other things like app name, feature flags, stuff like that. And this is what is loaded into the config object by pedantic settings. This is a file that typically you wouldn't commit to your git repository because it contains sensitive information and it's only used locally. When you deploy something like this to the cloud, there's going to be environment variables set in the cloud container instance, which is done differently for each cloud provider. And as you can see in my git ignore file, it's also mentioned here that the m file should not be committed to the repository. Then there is the python version. So this keeps track of uh the python version for this particular project which is helpful for consistent local uh and development environment. Then I have a docker compose which is used for local orchestration. This works with Docker as you can see. It also sets environment variables here. But this actually connects the Docker container of my app. So I can run and use it locally which is what you saw me do in the beginning by calling docker compose up. And then there's the docker file which contains the actual build information for my particular app. So in this case as you can see this works with a UV. So as one of the first steps in my docker file, I actually install UV. I set up the environment path correctly. So UV can be found when you uh want to run it. And then I run UV sync. So that's going to sync the dependencies. Finally, I define the path to the virtual environment. So it knows things like UV corn. I expose the port and then I run the command to start the surface. So that's the docker container. And this directly maps to let's say if you want to deploy this to the cloud, you can simply create a container from this and that's going to start the server in the cloud. And then finally there's a readme file with some information like hey how do you actually start this uh server? How do you uh use it? Some very basic information for your uh developers. There is the uh in this case SQLite database that's been generated by this app. You could also connect this with a database in the cloud later on if you want to. And there's a UV lock file which is generated by UV from the PI project tunnel dependencies. Now why does this structure work so well? Well, uh one thing that's nice is that the API is thin. So it's really only about the routing. The business logic is in a single place, namely the user services. There's uh data concerns that live in my uh database folder and the cross cutting concerns live in a core folder. And then it means you can add features now to this app without having to touch many different files in places you wouldn't expect. Few other things about this structure. So first the config. I mentioned I'm using pideantic settings. Now one thing I did here is that I set a number of values here inside my config. But what this actually also allows me to do is add all sorts of other things here which don't necessarily come from a N file. And that means you can have a bunch of settings here that are maybe less sensitive like the app name for example and you don't have to put all of that in environment variables. You can put them right here in your configuration. The logging part like I said is pretty basic and I simply call this function to set up logging from the main file right here. That's also really simple. But later on maybe you want to move to a structured logging solution where it's actually JSON and we need better observability. Another thing that I want to briefly talk about is something that you see in the router. Uh namely that there is some sort of uh dependency injection here. As you can see the user service is actually created in this function and that gets itself a session from the database. So there's all sorts of dependency injection going on here. The session is injected into the user service and the user service is injected into each route. So we can actually use that surface. So this depends as something that's built into fast API which is why I use this. Alternatively, if you don't want to use this, there are also dependency injection framework like inject for example. These help you connect things in your application and that makes it easier to do the let's say the wiring of your application in a centralized place and you can easily swap out things mocking for test. Uh there is clear uh scopes for lifetime. Dependency injection frameworks can be quite powerful if you have large apps with many crosscutting dependencies. But there are some cons like more boiler plate code. It's an extra mental model for junior developers and can feel quite heavy and uh overengineer for small or medium surfaces. So my rule of thumb typically is to start with the dependency injection that's built into fast API. for this part that works perfectly fine. And then only when it gets really complex and things are connected in many different ways then maybe you can introduce a dependency injection container but only then until that point keep it pretty simple. The surface layer you can basically define these per resource. So in this case there's a user surface and then the routes become very thin translators. they get an HTTP request, they simply call the service and they do an HTTP response. And that means you can also write tests using the user service directly without the actual uh HTTP endpoints. I didn't do that in this particular test. Uh but that is actually something that you could now do. Another thing you can also do pretty easily now since this is a separate service is to swap persistence later. Maybe you want a different database or an external API and then you do the changes here and the uh routes don't have to change at all because it only uses the user service. You can add validation here uh domain rules logging uh basically everything is in the business layer place which is really nice. For the testing bit like I said I'm using an inmemory database and that's all set up in this single file so that these tests are completely isolated. they don't use the production database and then what happens in my actual test is that as you remember I had this dependency injection actually there is also an easy way in fast API to override that dependency for testing so in this case I create a new function called override get user service which creates a testing session but then it can actually use the normal user service because that's injected in there and then I'm overriding the dependency to get the user surface by the override function. And now my test runs independent from the actual production system which is nice. And now of course you can add more test to this setup. So typically what you want to do is first start with a couple of happy path tests like creating and getting a user but then you can also start adding more edge cases like uh validation errors or 404s or things like that. If you write tests like this, avoid sending things to external services in your test themselves, but add integration tests separately. So mock those out basically. In terms of tooling, I'm using UV in this particular project. I'm actually using that on all my projects. Now you can see it being used here in a docker file. If you look at the pi project to file, there is something that is important which is this python path. And this makes sure that Piest knows what the root is of your Python code. So in this case, that's this folder. So that means I can actually import things from the test folder. But also that if I have my user test, I can actually import it like this. And that means that my imports are exactly the same as in my source code where I also import from app models user or whatever. So that way the test import are now exactly the same as the app import which is nice. The docker file sets up the actual surface. So I'm using Python 313 here. I'm installing build essentials. Maybe this line isn't even necessary actually. You could also use a smaller image here like 3.13 slim bookworm for example. And then I'm adding UV setting up the UV environment path. Uh copying over the files syncing dependencies. setting the path for uv exposing the port and then running the uvorn command and the docker compose file actually uses this docker file to run it locally. So the nice thing is that when you call docker compose up like I did in the beginning then actually this creates the exact same environment that you would have in a cloud setup. So your local setup is now really similar to your cloud setup which is pretty nice. So how does this all flow? So let's say a request hits a route. So that happens here, right? My router gets a request. For example, uh listing the number of users. A user service is created with a new session that's injected into the route. And then this calls the list users method of the surface which in turn runs a query on the database session and then returns that as a result which the router then returns as a response to the request. What this structure allows to do is to start really small. You have one service, one router, a simple database setup and then later on you can add more complexity. The boundaries in this setup are really clean. We have the API that does HTTP request services do business logic. Database layer does the persistence and the core layer does cross cutting concerns. Now this is not a let's say fully complete setup. There may be other things that you need. Of course that depends very much on the project that you're running. Right? You may need a deployment script for example to deploy this app to the cloud once it's committed to a specific branch. Uh you may need IDE settings that are specific to this project that you want to share between developers. Maybe you have a scripts folder with custom scripts for data migrations, export, analysis, reporting, those kind of things. But at least this could be a nice starting point for you for your new projects. The example code that I showed you here is in the Git repository. There's a link in the video description. Now, if you enjoyed this video, make sure you give it a like and subscribe to the channel. This is actually a small thing for you, but it really helps me out a lot because that gives me more reach here on YouTube. It means more people can find me and also learn from these videos as as well. But I'd like to hear from you. How do you typically set up your projects? Does it look anything like this or is it completely different? What suggestions do you have for your project setup? Let me know in the comments. Now, I only touched very briefly on software testing here, but there's a lot of things you can do with libraries like Piest. If you want to learn how to set up your tests, this video goes over the basic setup, and it covers a few more advanced things as well, such as parameterized tests and fixtures. Thanks for watching and see you next

---

## 21. SOLID: Writing Better Python Without Overengineering
**Channel:** ArjanCodes | **Views:** 61K | **Date:** 4 months ago | **Duration:** 21:56 | **ID:** uxwjXLjJOoM
**Link:** https://youtube.com/watch?v=uxwjXLjJOoM

### Transcript:
This video is brought to you by Squarespace. You may have heard of the solid design principles. In fact, you may have heard of them from an old video done by me. Robert Martin, aka Uncle Bob, came up with these principles in the early 2000s to help engineers write better software. But are they still relevant? And do they actually help in Python or do they make things worse? To test that, I'm going to refactor a piece of Python code using these principles. and we're going to see what we end up with. And along the way, we're going to figure out what actually improves the code. So, here's the example. I have a CSV file with sales data. There's customer names, addresses, items, uh, dates, price, taxes, basically your standard sales data. Some of these rows are actually returns. So David Pon for example from Christensen Land he ordered a monitor but sent it back and got back his money and his taxes and that's indicated by a negative price. Now what we want to do is generate JSON reports that includes some useful information like the number of unique customers in a certain time period, the average order value, percentage of returns, the total sales, and being able to generate that report for any date range that we want. So here's a first stab at this in Python. I have a class called messy sales report. So that's already indicates something, right? And it has a generate method that gets an input file, gets an output file, a start date, and an end date, which is the period that we want the report to be of. And then this is the code. We read the CSV file, we apply the filter, uh, we check the average order returns, the total sales, and then we create the report, we dump it into a file, and we're done. So when I run this then we now have a file here called sales report that actually created a report for 2024. There were 33 customers. This was the average order value. The returns percentage and the total sales in this period was this amount. Now of course because we have this start date and end date. I can also do this from uh only for the second part of 2024. And then when I run this again and I look back at the sales report now you see we actually have 20 customers. There are no returns and we have of course a lower number of total sales. So that's our messy report. Now you can already see that there's plenty of problems with this code. It's like a big mess. Thus the class name. So what does that to do with design principles? Well, design principles help you tackle this kind of code and make it better. And a very well-known set of design principle is solid. What does that actually mean? Well, each letter stands for a specific principle. The S stands for single responsibility and that means that each part of your code, each class, each function should do one thing. Then you have open closed the O in solid. That means that code should be open for extension but closed for modification. In this example, it should be easy to add another thing to this report, another metric. So open for extension, but it should not be easy to completely modify the structure of the report. Then you also have the L that stands for list of substitution. And that means that subtypes should behave like their base types. So if you have a superass that specifies some sort of method, then you don't want subasses to interpret arguments of that method completely differently cuz then it's going to be very hard to swap out a subtype for another subtype. The I stands for interface segregation. That means you should prefer small focused interfaces over large ones. So don't create huge abstract base classes or protocol class with hundreds of methods in them but keep them relatively small and focused on what you actually need it for. Then we have the D which stands for dependency inversion. That means that highle code shouldn't depend on lowlevel details. Now in Python you typically don't follow these like commandments. They're also very much based in objectoriented design and well, Python supports object-oriented programming, but I don't feel like object-oriented programming itself is very Pythonic. However, they still teach us valuable lessons. For example, if you look at this messy sales report, well, one problem is that this generates method here does basically everything. It reads the file. It fills the dates. It calculates the actual metrics. It generates a report. Writes it to the file. So, of course, this works, but this piece of code has way too many responsibilities. Testing this is going to be incredibly hard. If you want to extend it, that's going to be very annoying. And the logic here is very tightly coupled. So, that violates all sorts of principles. in particular the single responsibility principle, the open closed principle, but also the dependency inversion principle because you need all these implementation details in this one method. Now, before I show you how to apply the solid principles to this code, I'd like to take a minute and talk about today's sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking and boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever try to get a new dev blog or documentation site index properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something, or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vioded, Squarespace makes it incredibly easy. Head over to squarespace.com/ircodes for a free trial. And when you're ready to launch, use offer code RM codes to save 10% off your first purchase of a website or domain. Now, back to the video. When you're refactoring code like this, you have to do it step by step. So, where do you actually start? Which part is actually the easiest to sort of extract from uh this whole mess that we have here. So, one part that is actually relatively easy is these metrics that we have here. the number of customers, the average order, the returns, the return percentage, uh total sales, etc. Because these, if you look at the code, it basically only works directly on the data frame. So what we can do is now generate a bunch of classes that each represent these different metrics and then later on we can add more metrics. So what I'm going to do is first I'm going to use a protocol class to define a simple interface. So we have the I for like small focused interfaces from the solid principles and I'm going to define a class metric enhance protocol and this is going to have a compute method and the only thing that gets is a data frame. So AI already generates this for me like so. But I don't want this to return a float. I want this to return a dictionary. So uh we can later on combine all these dictionaries that we get into a single report. And this is from a string to let's say any because a metric can be more than just a number, right? It can also be a string or boolean. Now you can also be more precise here in terms of the type of thing it returns but for now I'm just using it like this and in the protocol itself of course we don't supply the implementation. So then we can actually code up the actual metrics. So we can have the customer count metric. We don't need to inherit because it's protocol class. And there we have the compute implementation where the number of customers is the number of unique entries in the name column. So that's basically what we're doing here. Similarly, we can also have an average order value metric like so. And we can have a return percentage metric and a total sales metric. And what you can then do in your messy sales report is that we can add an initializer where we actually pass a list of metrics like so. And then what we're going to do is we're going to take this code right here and we're going to change that so that we actually run the compute method on all these metrics. And then this is what we end up with. Like so. And now what we can do here is actually pass these metrics as an argument. And let's run this and see if we actually still get a valid report. And that looks right to me. So as you can see, this already improved things a lot by applying the principle. We reduced responsibilities of this generate method right here by moving out metric computation to different classes. We injected metrics as a dependency in this sales report generator. And that also means this class no longer needs implementation details of what the metric actually computes. Also, it's now much easier to add another metric. I can simply create a new class. I can add the instance here and I'm going to have a new metric that I can add to my report without having to change the generate method right here. So that already helps out a lot. But there's more things we can do because currently generate also reads the input file explicitly and stores the output file. So this is also something that we can extract from the generate method and put somewhere else. And we can follow the exact same procedure that we did for the metric. So what I can do is I can create a sales reader protocol and this is going to read an input file and give us a data frame and then I'm going to define a CSV sales reader which actually uses the read CSV method from pandas and similar to what I did here with the metrics I can actually pass a reader in here and then instead of doing this I can actually use the reader to read the data frame. So now generate doesn't need any more implementation details of how the file is actually read and I can even decide to create another type of reader here. For example, I could replace this with a JSON file reader or an Excel file reader and I wouldn't have to make any changes in the generate method. That's the power of splitting responsibilities and injecting the dependencies. And of course, I can do the same thing for writing. So I can create a report writer which gets an output file and data. And then I can create a JSON report writer that then writes JSON data. And I can later on replace this with an Excel file writer or CSV file writer or whatever kind of writer I want. And again in our sales report, we're going to pass this as an argument. So we have the writer which is a report writer. We store it here. And this we're going to replace by writing the report to a file like so. And it's already getting less messy. So maybe I should also rename this class to let's say sales report generator like so. And of course this doesn't work yet because I have to update my main function as well. I create a reader and a writer and I add them as arguments to the initializer of the sales report generator. Let me run this again. There we go. And we have our sales report as usual. So this is how step by step we separate things more. We make things more reliant on abstraction. You inject dependencies and you slowly step by step clean up your code. Another thing that you start noticing here is that in main function we now have all these objects that we create, right? We create the CSV sales reader, we create the report writer, the report generator, we create all the metrics. And this is actually a good sign. It means that you end up with this one place in your code where you're creating all the objects where you're patching up everything and that also makes it easier to change things because I now also have a single place where oh I actually want to write it to an Excel file. I know where to change it because I just go to the main function and that's where all the objects are being created. So that's where I will make the changes. So this is much easier to manage than what we had before. On top of that, because of all the dependencies that we have here, we can now very easily swap them out for a mock when I want to test something. For example, if I want to test the sales report generator, I can supply a mock reader and a mock writer and check if it actually calls the metrics correctly. Now, there's one more thing you might want to do here. For example, here we directly apply the filters. That could also be something that you put in a separate class. Again, I'm following the object-oriented approach here, right? So I'm using classes but I'll show you something else later on. So what we can do for example we can create a class called date range filter where we supply a start date and end date and then we actually apply the filter and then in my sales report generator I can actually apply the filter right here. And then if there is such a filter then we apply it to the data frame like so. And then of course I also have to clean it up here. Let's also apply the date range filter here. And now when I run this again, we get again our sales report. And maybe just for fun, let's go back to January to make sure that also still works as expected. And as you can see, we now have the values for the whole year. So is this perfect? Well, maybe not entirely because actually I don't really like how uh you need access to the start date and end date from the filter object here. So you could maybe design this slightly differently. And in fact this whole thing we can simply put it here like so. And then we don't need this. We can simply update the report directly and then write it to the file like so. And this is going to be a bit shorter. But you might want to consider designing this filter slightly differently so that we don't have to access the start date and end date right here. So let me know in the comments how would you design this? How would you set this up so that the filter and the metric is as clean as possible? I'm curious to hear. Final thing that I want to show you is a more functional version of this code. Arguably a bit more Pythonic. But if you want to learn how to design clean software and know when to apply all these techniques that I cover in this video, I have a free guide. You can get it at iron.code/design guide. This contains the seven steps that I take when I design new software. Get it for free. It's going to help you avoid mistakes that I made in the past. The link is in the video description. So here's a functional version of this same code. So instead of having classes all over the place, I'm using mainly functions here. So as you can see first I define a type which is a metric function that's a callable it's a function that takes a data frame as input just like what we had before and returns a dictionary then I've created a data class here called report config which has the input file it has the start date it has the end dates it has the list of metrics then I have a function to actually read the data there's a function to do a filter based on the start and the end time similar to what I had before, but now it's a function instead of a class. And now each metric is not a class with a single method. It's actually a function that returns a dictionary like so. And now generating the report, we don't need a class where that method is a part of it. Simply a function that gets a data frame. It gets a report configuration and then it generates the data. We then have a function to actually write the report. So again, all of these things are very similar to what I had in the class-based solution before, except now these are all functions and not classes. And then in my main function, I create my configuration, which is the input file, the start date, the end date, and the metrics. So the matrix that's now not a list of objects, but a list of functions. Then I'm going to read the sales data, filter it based on the start date and the end date, and then I generate the report data and write it to a file. So you could also again put this into a separate function, move it out of the main file. So the only thing that's happening here is patching up everything. But this is another way of setting up things so that you're not using classes as much, but you're using mostly functions. And what I like about this is that it's actually a lot shorter. So we don't need all these objects but it's still kind of nice and especially like this part where you call all the metric functions and update the report accordingly. Now again we have kind of a similar problem here where the report start and report end that's we need to get that data from the config object which well maybe not ideal but overall it's a design that I really like. So this is still modular. It's still testable. It's just there's just less objects here. In Python, this is typically the approach that I would follow. I wouldn't create all these objects. I would use functions like this. Now, sometimes it's still helpful to use classes, right? So here, for example, I have a report config. So I find that class is actually kind of helpful to group all that information so that I have a single object here report config where I have everything that I need and I don't need to have a bunch of variables and arguments to functions. I can simply pass the config and the solid principles even though they're more based in objectoriented design I think they still fit also with this type of code. For example, the single responsibility principle. You could say that well, we have to make sure that each function doesn't have too many responsibilities. The same idea. Or the list of substitution principle, which means that if we have a metric that gets a data frame and returns a dictionary, we expect it to use the data frame as the data source and not as something else. We don't change the meaning of a metric function in something that follows this particular type. So that also still applies. And of course we inject dependencies like these metric functions. So the uh generate report data doesn't know anything about specific methods. There's the opencloed principle that we can easily add more metrics to this code without having to change anything here. So the same principles apply and they're still useful to keep in mind. So it's just that you don't need to use classes and objects all the time. However, it's still a useful set of principles to help you think more in terms of responsibilities, dependencies, and isolation and not so much in terms of classes and inheritance. I'd love to hear from you. Which version of the code do you prefer? Do you prefer the class-based approach that arguably more strictly follows the solid design principles? Do you like the functional version more? Do you like the messy version more that I had at the start? Let me know. Do you ever use these principles explicitly in your Python projects? Do you think about them? What's the most important one? Let me know in the comments. Now, if you enjoyed this refactoring, you might also like my recent video where I shared three design patterns that will help you build better AI systems. Check it out right here. Thanks for watching and see you next

---

## 22. The Real Reason the Singleton Pattern Exists
**Channel:** ArjanCodes | **Views:** 41K | **Date:** 4 months ago | **Duration:** 15:31 | **ID:** p_UQ7tzUFLo
**Link:** https://youtube.com/watch?v=p_UQ7tzUFLo

### Transcript:
Singletons are an antiattern. You've probably heard this before. The reason is that it introduces global shared state. And yeah, that's dangerous. But there's another often overlooked reason why developers use the singleton pattern. One that doesn't involve global state at all. Now, in this video, I'm going to explore both sides of the pattern, the antiattern part and the maybe useful reason. And at the end, I'll show you a practical example of where that second part of the singleton can actually work. I'm going to be doing a lot more design pattern videos in the coming months. So, make sure you subscribe so you don't miss those. And check out the design patterns playlist I've linked down below. It contains all my videos covering design patterns so far. Now, before I show you the code, every time I do a design pattern video, there's always someone in the comments saying that I shouldn't turn Python into Java or that these design patterns are useless in Python or some variety of that. Now, here's my response to that. I'm not doing these videos as standard design pattern explanations. My goal is not to convince you to use these design patterns all over your Python code. In fact, I chose the singleton pattern on purpose here because actually I don't think you should use it. But like I said, there is an interesting aspect to the pattern and that is exactly my goal with these videos to go a bit deeper than just a tutorial. Also, I might not always exactly follow the gang of four book version if there is one because to me that's not what these patterns are about. To me, it's about understanding the problem that a pattern tries to solve and then hopefully that helps you come up with a better solution when you're running into a similar problem in your own code. So, whenever someone complains about me turning Python into Java, please refer them to this video. Now, let's start with a very basic example of a singleton in Python without saying whether you should use this or not. All right. So I have a config class here and as you can see there is a class variable instance. So that is actually going to be the singleton instance. There's also an initializer that sets some values in the instance uh in this case a database URL and whether or not we should uh include debugging information or not. So the whole idea of using a singleton here is that we have one config object that we can use everywhere in our code. And how does it work in this case? Well, actually what I've done here is override the new dunder method and there I check hey if there is no instance yet attached to the class then I'm going to create a new instance by calling the new method on the superass because this one is overriding that original method otherwise we would get an endless loop. If there already is an instance I'm not creating it and but simply returning the existing instance. So that means that if I create a config object here and then I seemingly create another one actually these two objects are exactly the same because this one creates the first instance and this one simply returns a reference to that already existing instance. So here you see the output. We create a new instance. The comparison s1 is s_ub_2 gives us true. And these are the ids which are the same. There is one object. So a config object that's a good example of where you might want to use something like a singleton or some other global resource like a database connection or logger something that you need in multiple places in your code and where you only need one of those things. And that's also where things start going wrong. You now have invisible coupling. All these parts of the code that use this shared in this case config object they depend on this shared state and it's not explicitly declared because they will actually create an instance like so. And that means you get all sorts of unwanted effects. For example, if one area of the code changes something in the config object, you don't expect it to affect anything else because it's a locally created instance or so you thought. But of course, that's not true. So it's that change is also going to be reflected in all the other places. Now sometimes you might want that, but with the singleton pattern, it's not explicit that this is what happens. And that's the problem with it. And testing also becomes a nightmare because each test may then affect the global state which then screws up basically everything. Now why does the singleton actually exist? Well, it mostly came about because of languages like Java. So Java is very different from Python in that it doesn't allow you to just create a variable in a script like you can do in Python. In Java, everything has to be inside an object and then you have the uh entry point of the application which is a static main method in a class. So that means if you need some sort of global state, you need a class and you need an instance and that's what the singleton sort of tries to solve. On top of that, you can add some security mechanisms like hiding the constructor, making it private, exposing a static instance, make sure that no other instances can ever be created. You can do that in many objectoriented languages including Java, C++, C, etc. Now, Python itself doesn't support private constructors. So if you try to do that uh the approach kind of feels clunky and unpythonic. Now if you really wanted to build a singleton class that is like generic that you can reuse then alternatively what you can do is use a meta class to do something like that in Python. Here's an example of how that would work. So I have a class singleton that's a subclass of type. So that makes it a meta class. This has instances in a dictionary and then this class has a call dumber method with a class passed as an argument. If that class is not yet in the instance list, then we're creating an instance of that class. So the singleton metaclass takes care of all the singleton instances of the various classes that use this meta class. And then what you can do is import that singleton meta class and use it in the config class like so. And then you simply use it like a regular class. And then you create your config objects as usual. You can print them and you're going to get exactly the same results as before. Well, obviously the ids are now different. So this is a more let's say Pythonic way of doing it. But like I said, it's not 100% safe. Python basically always allows you to do whatever you want. So if you do want multiple instances, the way you can do instead of just calling the initializer here is that you can call the new dunder method directly and then pass the class name and then call the init dunder method as well. So when you do this now you actually circumvent the whole metaclass singleton thing and you still get multiple instances. So in Python there is no way to actually avoid this type of thing. It's in the end it's the developer's responsibility. However, if you want a real Pythonic singleton there is actually a builtin mechanism for that and that's called a module. A module obviously is managed by Python. It's going to be loaded only once and then will be reused everywhere. That's exactly what a singleton is. Here's what that looks like. I have fileconfig.pi pi which has some variables in it like a database URL the debug status whatever and then in my example I can simply import it and use it that's it so if all you need is shared global state in Python use a module you don't need a class this is simple obvious it's testable it's uh thread safe now of course that's still the issue that there is global state and that's something that you want to avoid so what I do is whenever I end up with global state in my code in some way. I see if I can modify my design to remove it without affecting usability too much. And in many case that's actually possible, but you'll still likely need to create some object somewhere that other parts of your code need to access. So what I do is I try to group those things as best as possible in one place. preferably that's the main file where you then create the actual objects and patch up everything. Bob Martin calls this the dirty place in your code. And then once you have that, that one place where you create all those dirty, nasty objects, then you can do dependency injection, organizing things smartly so that you don't end up with 10,000 arguments that you need to pass along everywhere. Dependency injection, by the way, is another pattern that I'm going to take a closer look at in a separate video. I won't do that here because otherwise this video becomes way too long. Now like I mentioned, one of the biggest issues with the singleton pattern is that this introduces shared mutable state. If your singleton holds any state, it can easily leak between tests, especially if you forget to reset it. So that leads to tests that only pass or fail depending on the order that they're run in or some unexpected side effects from previous test runs. You get tight coupling between test and implementation details. is not good. Instead, prefer explicit dependencies. Pass what you need via constructors or parameters. It makes testing easier and keeps your components more loosely coupled. Another problem with the singleton is that it's not thread safe. In multi-threaded code, two threads could enter the singleton creation logic at the same time and then you'll end up with multiple instances. Here I have an example of that. So I'm using my simple meta class singleton. I create an unsafe subclass of that. And then I'm starting a bunch of threads. And then we're going to see how often this initializing is going to be printed when I run this. As you can see now, it creates multiple instances of this singleton. And depending on when it's run, it's going to create just one or uh two or three or more. We we never know basically what is going to happen. It's actually pretty neat random number generator but not really what you want. And of course if this happens that defeats the whole purpose of the pattern and actually this exact thing happened to me. I remember when I was working for the university, I was building some sort of uh game graphics engine that we would use for research and we use singleton patterns because I was an old-fashioned researcher and I use that for things like managing resources in our environments, loading textures, that kind of stuff. And you only want one of those. But games and game engines are typically the kind of thing where you will have multiple threads, right? one for rendering, another thread for updating the UI, another thread for listening to user input, etc. And it was actually a huge problem because we ended up with loading resources 10 20 times when we really didn't understand why that was happening until we realized, oh, wait a minute, we're using the singleton pattern and that is actually not thread safe. So we actually have multiple instances of that and that kind of broke the whole thing. Now if you want to fix this it means you need to add some sort of locking or synchronization mechanism inside the meta class and this is actually possible. Here I have a version of the singleton meta class that has locks included in it. So I have my instances just like before and then I have logs and I have a global lock. So the global lock ensures that we have one lock per class. And then next we use that log to create the instance of the class that we're looking for. And then when we use the safe version of the singleton and I create my threads again and I run this, you see that we always get a single instance of the singleton. So it's thread safe. But of course now we had to add like a bunch of locks and everything and it just slows down the code even more. So it's possible to do this. But if you're working in a multi-thread environment, be very careful with global state like this. In fact, I highly suggest you avoid them entirely unless you're absolutely sure that you need it. Now, like I mentioned at the start of the video, there is another aspect of the singleton pattern that has actually nothing to do with global state. But before I show you that, if you're designing a project from scratch, there are some things you shouldn't forget because they'll come back to bite you later. I have a free guide that walks you through the seven steps I follow when designing software. Grab your copy at r.code/design guide. The link is also in the video description. Now, what is that other aspect of the singleton that's actually useful? It's not global state. It's that you have control over instantiation. Instantiation, that's a weird word, but it can sometimes be useful. Here's an example. Let's say you have some expensive resource that you want to load only when it's needed. and only once. For example, say your code depends on a large language model. Of course, you don't want to load that large language model every time you want to use it, right? You want to do that only once. So, here's what you can do. You could create a model loader which is a singleton class uses the meta class and then uh in the initializer it actually loads the model. Now, I'm just printing something but there you could actually load that very large model which takes a long time. And then what you can do in your code is you can create instances of this model loader and do a prediction based on the data. So the model is now loaded only when you use it for the first time. So that means you don't have to load it when the app starts up which is going to slow down things. It will only load when you actually need it. Basically lazy loading. So there's no global state here. Model loader doesn't have any instance variables. There's no testability issues here. And there's no magic. This is what controlled instantiation is and that can be quite useful. And next to that there is a more general version of the singleton pattern that could actually also be useful and that's called the object pool. And that's basically a pattern where you don't have one instance but you manage a pool of reusable instances rather than that single one. If you want me to do a full video about the object pool, let me know in the description. Now I'd like to hear what you think. Do you use singletons in your codebase? Have you used them in the past? Do you use the pattern in other languages than Python? What are elements of this pattern that you find interesting? Let me know in the comments. Like I said, there's a playlist with all my design pattern videos up until now. Link is down below. One that I found particularly interesting is the builder design pattern. If you want to learn more about that, check out this video next. Thanks for watching and see you next

---

## 23. Avoid Messy Code: Design Patterns for AI Agents in Python
**Channel:** ArjanCodes | **Views:** 24K | **Date:** 5 months ago | **Duration:** 18:19 | **ID:** 8_liatgLkLc
**Link:** https://youtube.com/watch?v=8_liatgLkLc

### Transcript:
This video is brought to you by Squarespace. If you're trying to build something with large language models, you know things can get messy pretty quickly. You start with one prompt, then you add a few tools, then you pass some data to another model, and before long, your logic is going to be scattered and all over the place. So, the question is, how do you structure AI systems the right way? In this video, I'm going to show you three design patterns that I've adapted for LLMs and agents that are going to help you build cleaner, more modular AI systems. Let's go. The example that I'm going to use in this video is a travel agent. And this agent can help you find hotels and flights, etc., etc. Now, there is one prompt that I'm going to start with, which is this. I want a rainy city trip within Europe. I love towers. I don't want to cross water to get there. And this is Maria who lives in Berlin. So, let's run this particular example and see what happens. The agent has decided that Maria needs to go to Paris, France, which is wrong. It should be Utre, the Netherlands. Anyway, okay, Paris. Fine. So, then the agent books a flight from Berlin to Paris, which arrives at 6:00 p.m. It looks for a hotel, which is the highest regency in Paris. I don't know if this is a good hotel or not. And then uh but it is quite expensive so hopefully it is. And then there are some activities for early evening explorers. You can visit the shopping mall, take an evening walk, uh enjoy a dinner, etc., etc. So the Asian has done like a bunch of different things here. And that's typically something that you're going to encounter in AI application, right? It's not just I send a prompt and I get back a response. I mean, we can just use a chat agent for that. We want something that's a bit more uh involved. And of course, in this particular situation, you don't want to handle everything in one massive prompt because that's simply not going to work. You need to break it down into separate agents and then each is responsible for a single step. So we have choosing a destination, we have planning the flight, recommending a hotel, and suggesting things to do. The way I've built this is that I'm implementing each of these steps as its own function. And then you can execute them in order by storing them in a list. More about that in a minute. So how is this code actually set up? I'm using byic AI for the agents. So first I load the environment variables. In this case, there's just one which is the OpenAI API key. Then we have some dependencies which is we need to know the username. We need to know the origin city and then there is a context that's basically the information that each of these agents is going to need. So things like the destination, the uh origin city, the arrival time, the hotel name, etc. And this going to be filled in step by step by the agents. Then for each step I have an agent. So in this case I have a destination agent that helps users find a ideal tra travel destination based on their preferences. Then I have a flight agent that can look for flights for a particular trip. Then I have a hotel recommendation agent that suggests a good hotel near the airport or city center. Then we have an activity agent that suggests local activities. So you have all these different agents that can do each a specific thing that also have their own system prompts that to adapt them. And then the pattern that I'm using to perform these tasks in order is the chain of responsibility. And what that pattern entails that you have a bunch of different functions that are executed in order. Now you might say, hey, design pattern should use classes and inheritance and things like that. But I'm sort of taking a bit of creative liberty here and using my own take on these patterns in this particular example. So instead of using classes here, I'm just using a function because I just think that's simpler. Now the important thing here is that each of these functions, so I have handle destination which gets uh arguments and then uses the agent. We have handle flight, we have handle hotel and handle activities which all do a part of the job. But the important thing is that the arguments here are actually the same. We have the user input, we have the dependencies, we have the context of the trip that contains information about the trip. And as you can see the context is being filled in by each of these agents in steps. So for example, this destination agent sets the destination in the context object which is then again used by the flight agent to plan a flight to that particular destination and then same for the hotel. So the hotel also is recommended in the destination place. So all of these steps use results from the previous steps. And what makes it work is that the arguments here are always exactly the same. user inputs, dependencies and the context. And then once you have all this, then we can have a plan trip function that actually executes this chain. So it creates the trip context. It has a chain of functions and then I'm using a for loop to call each of these functions in turn with the particular user input. Now, this way of setting it up is actually really flexible because we have these specific things that are done in these functions, but the structure is actually somewhere else. So, that's actually really nice. Also, it's really easy to add another handler here. You simply define the function and you insert it into the chain. You don't have to change anything else. You could even decide to make this dynamic and uh basically let the user select which things uh you want the AI agent to do by I don't know ticking some boxes or something and you can simply enable or disable them in the chain without having to do something somewhere else in your code. So that's the chain of responsibility. Now this is a very basic implementation of that. You can do more advanced things as well such as uh handling errors for example and make sure that if I don't know handle destination has an error that the chain isn't completed anymore. Or of course you could generalize this to more like a graph structure which is actually already built into pideantic AI. But typically what you're going to often need is a simple chain of things that needs to happen. And this is a pretty neat way of setting it up. So, that's chain of responsibility for AI agents. Now, there are two more patterns that I'm going to show you. But before I do that, I want to talk about this video sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking. And boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever tried to get a new dev blog or documentation site indexed properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something. or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vibecoded, Squarespace makes it incredibly easy. Head over to squarespace.com/rincodes for a free trial. And when you're ready to launch, use offer code iron codes to save 10% off your first purchase of a website or domain. Now, back to the video. The next pattern that I want to talk about is the observer pattern. This has to do with visibility. When you're calling multiple agents in a chain, just like I showed you before, it's actually pretty helpful to know like what prompt was sent, what response came back, how long did it take, was there a problem with the response. So, you can solve that by using the observer pattern. And what that allows you to do is that instead of scattering logging code throughout your app, you can define an observable agent. Basically, an agent that logs things. It can measure response time. It can call attached observer objects with structured info about the call. And that allows you to more easily inspect what is actually happening in your agent system. So here's what I did. I have another example here which again uses pyic AI. In this case, I just have a single agent because I just want to show uh very basic example of how to set this up. But we have again our travel dependencies, username and the origin, city, and we have a travel response. So in this case, I'm just looking at the destination agent. And then what I've done is implement a sort of observer pattern. So the way to do that is that you have to specify what an observer actually is and how it's structured. So in this case I'm using a protocol class to define an agent observer and this has a single method called notify that gets things like the agent name the prompt that was used the current dependencies the output the duration etc etc and then I've created a class console logger that implements this protocol but of course you can add other classes as well you could also send it to a logging surface or store it in a file or do whatever you want. That's the power of the observer pattern. And there what I'm simply doing now is just printing the agent call log and that's all there's to it. Then what I've done in order to make pyantic AI compatible with my observers is that I define a function called run with observers that gets an agent prompt dependencies and the list of observers. Now you could also go the more objectoriented route and actually create maybe a subclass of agent that is an observer. But in this case I've uh done it with a function because to me that also works pretty well. Like I said I'm not following design patterns to the tea here. I'm using my own interpretation of them. And what I've done here is that I record the time that it takes in order to do that request to the agent. And then for each of the observers in the list, I call the notify method with the information that it needs. And then finally, I return the output. The agent itself is the travel agent that basically recommends a destination just like in the previous example. And then I have the main function. So it creates the dependencies. In this case, it's Nina who's in Copenhagen. Copenhagen. And there's a prompt. Well, I want to escape to a cozy place in the mountains for the weekend. So that's definitely not going to be the Netherlands. And then we're going to call this run with observers function. And as the observers, I provide the console logger that I just defined. Let's run this code and see what happens. So as you can see, we now get a log that's provided by our console log observer. And the nice thing is that because we've now abstracted this away, uh there is just this simple uh function call. If you have multiple agents, you can simply call this run function with the agent that you need and the prompt that you need and you supply the observers. And so it's pretty straightforward to do this. Like I mentioned, you can define any type of observer here. It can log to the console. It can log to a file, send it to database or logging service. The main thing here is that this setup, this pattern keeps your agents observable without coupling them to any specific logging implementation. And by the way, if you're serious about learning how to design better systems, AI or otherwise, I've put together a free guide at arn.code/design guide that walks you through the exact seven steps I use when designing software from scratch. The link is in the video description. The final pattern that I want to show you that is useful in AI systems is the strategy pattern. Let's say your travel assistant should behave differently depending on the user. Maybe one user wants a formal professional travel agent, another one wants a more quirky or fun experience. And there is a third type of person that cares mostly about budget. Now instead of hard coding that behavior, you can actually use the strategy pattern to use different personalities. Now the strategy pattern again if you follow the Ganger 4 book it uses classes and inheritance where each class has one method. We can actually do it a bit simpler and then uh use a function instead that supplies the thing that we need. So, what I'm going to do is implement each travel agent personality as a function that returns a preconfigured agent. It's going to have the same input, the same output, just the system prompt is different. And this is what that example looks like. Again, by AAI, I'm loading the API key from the N file. I have my travel recommendation. I have the dependencies. And then I have my strategy function that each returns a different type of agent. So, I have the professional agent, I have the fun agent, and I have the budget agent. Now, like I said, the only thing that changes here is the system prompt, but you can imagine that you also want to have other settings in your agent that depend on the type of agent that you want. Maybe, I don't know, the temperature should be different for the fun agent because it should come up with more creative solutions or something like that. By using functions in this way, we can swap them out dynamically, which is really nice. Then how do you use that functional version of the strategy pattern? Well, that's what this other function does. Run travel strategy. So this gets a strategy function which is a callable that returns an agent. Now you might say, why not simply pass in the agent? Well, that's a fair point. But the nice thing about the strategy pattern is that the agent is created here and not before. So you don't have to create all the agents before you actually run the strategy. And you can use this function to pass arguments. So you can even modify the behavior of how the agent is being created by calling the function here. So we pass this function as an argument to this function. We create the agent and then we run it with the prompt and the dependencies. And then what you can do is depending on the type of agent you want, you can run the strategy with that particular agent. So in this case I call run travel strategy with the professional agent function or here I call with the fun agent function or the budget agent function and we're going to get different results each time. So let me run this and you can see what the difference is. So as you can see we have different types of responses. This is a pretty professional response. Then we have something that's a bit more fun with exclamation marks and yeah I don't really like that. Uh and then there's the uh budget agent that uh looks at cost of living which is very affordable etc etc. The nice thing about this pattern is that the run travel strategy function doesn't need to know anything about the particular agent that's being used. You can uh keep your business logic basically what is in here consistent while allowing for flexible behavior. Now one thing you probably notice is that these patterns don't really change. They're not different for AI systems than for other systems, but some of these patterns are quite useful in particular for AI systems like the ones that I've shown you today. Again, I've used my own take on these. So, these are not like strictly following the gang of four book or anything like that. I typically tend to use functions, protocols, whatever works for me, whatever makes it simple and easy to work with. That's just my style. I know there are people that like more strict implementations of these patterns. I typically take a bit of freedom with them. I do think that patterns like this, designs like this, help you build systems that are a bit more scalable. They're going to let you decompose complex task into clearer steps. You can add observability without polluting your logic. You can inject behavior dynamically and cleanly with the patterns that I just shown you. When you when you combine this with pantic AI, you're going to get strongly typed validated outputs, fewer runtime errors, easier testing and reuse. And to me, this is how you go from, let's say, one-off hacks to scalable, maintainable AI systems. But I'd like to hear from you. Are you using any of these design patterns in your projects? Do you implement them in this way as well? or do you use the more traditional implementation from the GA 4 book? Is there another design pattern that you use a lot in systems that work with LLMs or another pattern that you'd like to see applied to these LLMs? Then I can do a video about that. Let me know in the comments. Like I said, I've used Binantic AI in this example in a very basic fashion, but it's actually really powerful. If you want to learn more about how to use Binantic AI in your projects and what you can do with it, check out this video next.

---

## 24. Python Has the Best Standard Library Ever: 10 Modules You Need to Know
**Channel:** ArjanCodes | **Views:** 98K | **Date:** 5 months ago | **Duration:** 15:39 | **ID:** eZ9RqnkJxsk
**Link:** https://youtube.com/watch?v=eZ9RqnkJxsk

### Transcript:
Python's standard library is crazy powerful. Most people know about JSON or OS, but there are modules that can save you hours of work and you're probably not using them. Today, I'll show you 10 powerful standard modules you should absolutely know about. Before I start, if you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get it at iron.code/design guide. This contains the seven steps I take when I design new software and hopefully it helps you avoid some of the mistakes I made in the past. The link is also in the video description. First module I want to talk about is data classes. Now you are probably already using data classes, but I still think it deserves a mention on this list. You might also say, "Hey, didn't you do a video a couple of weeks ago where you said you were no longer using data class? So what is this all about?" Well, it's not that I'm no longer using data classes. I actually still use them, but I don't use them in production systems as much because then I notice that I often end up using things like Pantic or whatever ships with the specific framework or library I'm working with. Now, data class themselves are still quite helpful to me. I really use them quite often for quickly coming up with some ideas in my Python scripts, creating a few classes that store a bit of data, making some relationships, and seeing how everything fits together. And for me, data class work really well for that because they're just so easy to use. Here you see an example of how they work. So you simply import the data class decorator from data classes. You put that on top of your class and then you specify the attributes of the class by using type annotations. You can do default values like this which is really helpful. And there's a ton of other things as well. For example, you can also make a data class frozen. And that way if you do that and actually type booleans correctly, then you can also make read only data class, read only objects, which is quite nice as well. Also, data classes add a bunch of default behavior. For example, in this case, as you can see, there's no need for initializer, but we still have one where we can pass the arguments uh as we want. Uh there is a wrapper. So, if you print a in this case an instance of that data class, it's going to give us something useful instead of just the memory address. So, overall data classes are really helpful for that. Here you see what we get as a result when I run this little piece of code. The second library that I think more people should use is path lip which basically supplies a modern way of dealing with file path. It replaces os.path with an object-oriented approach to file path. And the nice thing about path lil is that you can use the slash operator to construct more complex paths. So in this case I have my base which is the my project folder. That's a path object. And then I can use slash to create subfolders and then print that. So when I run this, as you can see, we get this. And now this is also a path object. So it's really intuitive to use it. And they have a bunch of helpful methods. For example, you can check if a file exists. And then you can print the file size. Uh you can also create a directory, a parent directory, then write some text to a particular file. So as you can see now that I run this code uh here we have now my project and there is config/settings.toml and that's exactly what we wrote here in the code. So now because it exists it's also going to print the file size when I run this for a second time. So pathflip is incredibly helpful. It also works consistently across different operating systems. Third module I want to show you is funk tools. This contains powerful decorators and utilities for functional programming. Here you see a couple of examples. I've written a simple power function that takes a base and an exponent and then it computes the power. That's very basic obviously, but the nice thing is that we use the cache decorator so that this computation only happens once for each combination of input parameters. So in this case, what you can do is I create a bunch of powers. But when I call this one again 2 to the 10 then it's not going to actually compute. It's just going to directly return the value. So for uh systems where you need to compute things that are CPU intensive and you don't want to do double duplicate work then caching is actually really helpful. It's really easy to use. When I run this you see that it computes 2 to the 10 3 to the 5 but then here it simply uses the cached result. So that's a really nice feature of funs. A second thing that I also use quite often is partial function application and this allows you to take a function like power for example and already apply some of the arguments. So in this case I'm doing that for the exponent two and for exponent three. And a nice thing about partial function application is that it doesn't call the function it creates a new function with that argument already applied. So now we have a square function and that is actually a function with one argument the base and the exponent is set to two and we also have the cube function and now we can simply call this with that one argument and then it's going to use these values which are already applied and since there is also cache that still works as expected and you can also see that here in the output a fun tools has a couple of other useful tools as well by the way there's LR RU cache which is similar to cache but has limited memory usage but there's a reducer function to apply a function cumulatively to a sequence and there's other things as well fun tools really nice try it the fourth package that I want to mention is tomol lip and this is included in the standard library since Python 3.11 this allows you to parse toml files without having to install a third-party package here's an example so I have my pi project file and I'm opening that here and then I do tomolib.load and then it's going to give me a dictionary with the data in my project dottoml. Now in my tommo file right here there's not much here. There's a project with a name and a version and a description etc. But in my example I can simply access this through the data dictionary. And when I run this then this is what we get. Standard. What is that? Well, actually that's the name of the project as you can see right here. I can simply change this to something else and then it's going to print that. So toml is really helpful and it's actually quite useful to have a library for that in Python because kind of the standard for configs in Python projects nowadays. Next module I want to mention is graphlib and this helps you sort things based on dependencies. Graph lil has some useful classes as well such as the topological sortter that helps you sort things based on dependencies. For example, here I'm creating a topological sortter of strings and then I'm going to add tasks and dependencies. So compile but I can only compile if I fetched the sources. Test is dependent on compile. Package is dependent on test. Deploy is dependent on pack and and etc etc. I'd simply add these things and then I can create a static order and then print that as a list. When I run this then this is what we get. So as you can see I first need to fetch the sources and then I can compile etc etc. This is really helpful if you're using Python to I don't know create a build system or trying to resolve some dependencies in a project and basically any scenario where you need to follow steps in the right sequence. Related to that, there's another module that you might find helpful, which is heap Q. And this is a dynamic priority Q built on the heap, thus heap Q. And what this does that it keeps the smallest element always at the front even when you add tasks dynamically. Here's an example of how this works. So I have three tasks. Sending an email to clients, writing documentation, and fixing a critical bug. And these are simple uh tupils with a integer and a string. Then I can use heapify. It's like Spotify but for heaps. I can heapify tasks and then I can process them. So I'm popping a priority and a task from the task list in y loop and then I'm processing that task. And as you can see in the code, I can actually dynamically add task. So here if the task is fixing critical bug then I'm printing that there is a new urgent task that has arrived namely deploying a hot fix and that has priority zero and if we do that then actually uh the rest of the task writing documentation sending email is going to be done after we deploy the hot fix because this has higher priority. If the task is writing documentation, I'm adding another task called refactor old module. But that actually has a lower priority. So that will be done after we send an email to the client. So when I run this, this is also exactly what you see. So we start by fixing critical work priority one. Then I get a new urgent task deploying the hot fix. Then we're processing that. Then we're doing a documentation writing which has priority two. uh that adds a task refactoring an old module which is priority 4 but first we send the email to the client. So this is helpful because with heapq you don't have to sort manually or scan a list yourselves. It keeps everything in a priority order very efficiently. Module seven is secrets which are cryptographically secure random numbers. Secrets provides safer random values for things like tokens, passwords, security keys, and a source. This is how it works. Simply import the module, have a main function, and then it has a bunch of functions like a token hex for example, which is a secure hex token or a URL safe token that you can put in a URL or just some random bites or randomly choose an item from a sequence. So, there are several options here. And when we run this then this is what we get. As you can see the random choice was Apple. Well, I would never have guessed that. So it's completely secure. Basically the idea is that this doesn't just directly rely on some sort of uh seed table or something. So it's a bit harder to predict what is actually going to be the choice. For example, if you're randomly picking a recovery question answer or password element, then random do. Choice could be predictable, but secrets. choice is not. Next module is shut. Is that how we say it? I'm I'm never sure how I should say this. Shut. It's weird. Shut. It also feels like something where you know you're supposed to know how to say this, otherwise you're some kind of noob or something. I'm just going to say shut. We're just going to call it like that. Shut. It helps you copy, move, archive, delete files and directories directly from Python. Here I have an example. I import Shuttle and then I use it right here to copy a file, my PI project to file and I copy it to a backup or we can use make archive to create a zip archive. This is pretty helpful if you're dealing with I don't know managing files or doing automatic backups in a Python script or something. Then using Shuttle is really helpful. Let me run this. What has it done? Well, as you can see, there's now a backup by project file which is a copy and we have a zip file that FS code can't read. Unfortunately, there's probably an extension that can do that. Now, I have a few more modules for you, and the last one is particularly powerful. But before I continue, if you're enjoying this video, don't forget to give the like and subscribe to my channel. This really helps my channel grow. It also helps me keep making deep dive videos like this one. Next up is text wrap, which helps you format text neatly. This is helpful if, for example, you're developing a CLI or you're printing logs or you're writing reports. And there's a couple of options here. So I'm importing text wrap. In this example, I have some long text. But then what you can do is use text wrap.fill to make wrapped text. And you can also add indentation and you can also shorten it. It's just some examples. With indentation, you can specify a prefix so that everything will be written behind that. And shorten allows you to specify a maximum width and then add a placeholder. So you can do something like this which is quite helpful in uh longer text. Let's run this and see what we got. There we go. Exactly what we wanted to have. Final module I want to mention is it tools. This is incredibly powerful. It has a bunch of very efficient tools for looping, combining, processing data without loading everything into memory because it uses generators. Here are just a couple of examples of what you can do with it tools module. Uh combinations for example allows you to create all sorts of possible pairs from a list of items. So I have items A, B and C and I'm going to generate all the combinations of two items. Turn it a list and it gives me all the pairs. As you can see, we have them right here. But there's more. There's also a counter which can also be very helpful. You specify a starting point and a step and then you just use next to retrieve the next counter value as many times as you want. And this is also what you see back in the output of the program. Another thing that's really helpful is that you can cycle through items. And this is something that happens quite a lot in like user interfaces where you need to like shift through different options. And then with cycle you don't have to care about that the list has ended and you need to go back to the front cycle actually takes care of that. As you can see I simply get my various cycles without have to worry about the list length. If I change this to, I don't know, let's say uh 40 and I run this again, we get 20 ons and 20 offs. Now, if you were doing this with a light switch, you would probably get a headache, but in Python, it's no problem. Other useful thing is group by that clusters consecutive identical elements, which can be handy for data processing. So if you have this kind of list for example with A, B's and C's, you can simply use group by to get a list of those things that you can see right here. Now it tools has a bunch of other very useful generator functions as well. It's just the starting point. If you want to dive deeper into IT tools and find out how it can help you in many cases write much more concise code, check out this video next. Thanks for watching and see you next

---

## 25. Build Production-Ready AI Agents in Python with Pydantic AI
**Channel:** ArjanCodes | **Views:** 111K | **Date:** 5 months ago | **Duration:** 15:50 | **ID:** -WB0T0XmDrY
**Link:** https://youtube.com/watch?v=-WB0T0XmDrY

### Transcript:
This video is brought to you by Squarespace. What if you could integrate GPT4 right into your Python app and make it aware of your business logic, your data, and your models? Today, I'll show you exactly how to do this with Pantic AI, a really nice toolkit that connects AI with real Python code. And don't worry, it's not just theoretical. I'll walk you through an actual example. Let's dive in. First, what is Pyantic AI? It's basically an extension of paidantic that helps you build AI powered agents. These agents can use your domain data models. They can access dependencies like databases or APIs and they can return structured outputs with validation. Thus, the Pantic extension. So, an agent in that sense is an LLM that's aware of your business context. And Pentic AI integrates with popular models including GPT4. Why is this helpful? Well, if you're just using a chatbot directly, then you're going to get unstructured text back, which is going to be hard to deal with in production systems. And what Pedantici adds is that you have type safe outputs that are validated by Pantic. You have a clean way to inject uh contextual dependencies like a database for example, and you also have the ability to define tools, custom functions that the agent can call. And this helps you build AI systems that go beyond just being simple chat bots. So let's take a look at a concrete example in healthcare. That being said, I'm not a doctor. I don't recommend you actually start using this in your hospital, but theoretically this is possible. Let's say you're building a triage system that helps nurses respond to patient questions. So here is the starting point of this example. It doesn't use any AI. So as you can see, I'm using async.io because well why not it's helpful if we're sending requests to an API I'm using a data class to uh represent a mock database I'm importing environment variables from aend file in this case this contains my open AAI API key I'm not going to show you that file but then I have a simple mock database very basic it has a patient class using a data class for that one a patient has an ID a name and vital which is a dictionary of strings to whatever. I'm not saying that this is how you should model patients, but it's a simple example. Then I have my database, which is nothing more than a dictionary that has two patients with uh each an ID, a name, and some vitals. Then I have a helper class that helps me access some of that data. So here, given an ID, this is going to give me the name of the patient, and this method gives the latest vital of a patient by ID. Extremely basic, of course. If you have an actual database, you would fill this with way more information. You'd have way more capabilities here. It's just a basic example. And the only thing I have now is a main function that creates a database connection and gets a patient name and vitals and then prints that. So when I run this, then this is what we get. Very basic, right? So now as the first step, let's set up a bit of scaffolding for our agent. One thing that we're going to need is some class that represents the things that the the agent depends on. Let's call that triage dependencies. And for simplicity, I'm going to use a data platform. And what we're going to need is a patient ID. And we're also going to need a database connection, right? So that for any patient, we can access then the database and do something with it. So that's what the agent is going to be dependent on. Next, we're also going to need to decide what the output of the agent should look like. So that's going to be triage output. Not sure why it's indented here. And since we're using Pyantic AI, this is where we should rely on Pyantics base model. And of course, we're going to need to import that. So what the output is going to be actually AI already sort of indicates that here is that we're going to have a response text from the chat agent. We also need a boolean indicating whether or not this should be escalated and we're going to need to indicate the urgency as a number between 1 and 10. So these are the three things that we are going to need and we're going to use a field to give a bit more information about this. So in this case, the response text is going to be a field with a description and that's the response text for the triage. And maybe we can actually write that a bit clearer by saying this is a message to the actual patient like so. And then we can do the same thing for escalate and for urgency. So whether we should escalate this case and what the urgency level of this case is to a human nurse. Don't forget about the humans. AI. So now we have created our sort of context for the agent. So this is what the agent depends on which is the patient ID and a reference to a database connection and we defined what the agent should output response text whether to escalate and what the urgency is. Now before I show you how to create an agent, first want to take a minute to talk about today's sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking and boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever try to get a new dev blog or documentation site index properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something, or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vioded, Squarespace makes it incredibly easy. Head over to squarespace.com/ircodes for a free trial. And when you're ready to launch, use offer code iron codes to save 10% off your first purchase of a website or domain. Now, back to the video. Now, next thing that we're going to do is define an agent that uses GPT4 that takes our dependencies and then returns a structured output. And we also need to supply it with a system prompt that explains the role of this particular agent, which is a triage assistant. So I'm going to create a triage agent which is an agent and that is actually something that we're going to need to import from paidantic AI. going to import the agent and then the agent is going to use the open AI GPT 40 model. We're going to need to supply the dependencies which is triage dependencies. We're going to need to specify the output type which is triage output and we're going to supply a system prompt. You are a triage assistant helping patients. I still can't type there. provide clear advice and assess urgency. There we go. That's our system prompts. I'm missing closing parenthesis. Oh yeah. Wow. Copilot even corrects my spelling. Very helpful. Okay. So, this is my agent. And now what you can do, let's change this main function here, is we can create the dependencies. So, we're going to do that for patient 42. And we have a database connection. And then we're going to get a result from the triage agent. And we can then specify a prompt supply dependencies. There we go. And then we can print the result.put. Something went wrong here with the parenthesis. I know if you also have that problem if you have these kind of editing AI kind of tools that they always screw up my parenthesis for some reason. Not sure why that happens. Okay, so now we're actually running the agent. And let's see what happens if we run the file again. So now you see we get back an object with a response text. We have escalate is false, urgency is two, which is probably right in this particular case. Seems like it's not very urgent, right? But the thing is, of course, that's it's not really using any of the patient information. Right? Now, before I show you how to make this more patient dependent, if you're enjoying this video, give it a like and subscribe to the channel. This is a small thing, but really helps and lets me keep making these types of videos. Now let's say we want to personalize this more to the patient instead of this very generic uh thing that we get here as a response. So what you can do then with the pyantic AI agents is you can add things like for example you can add a system prompt triage agent dot system prompt. So I'm going to use the system prompt decorator and then we're going to add the patient name and prompts in pantic AI they're going to get a context. So in this case that is a run context which well we need to import that from a pyantic AI like so. And run context is a generic type that gets dependencies like so. And this is going to return a name because we want the patient name of course. So then we get the patient name from the database providing the patient ID and then we can do is simply write the patient name. Patient name is like so. Now this is going to add this particular system prompt to our agent which is nice. And then what we can do in the agent for example is to also indicate always mention the patient name when available like so. So it's more personalized. So we're going to get generic advice but at least it's going to be uh personalized to you. So when I run this let's see what the response text is. There you go. John for your symptoms etc etc. So now it's personalized to the patient name because it uses the uh system prompt and adds it to my agent. So that's a really helpful tool. You can of course add more of these prompts if that's helpful and get uh useful information from the dependencies. Now instead of just getting data, you can also add tools. For example, calling a function that does something. So in this case, I'm going to keep it really simple and I'm just going to add a tool to get the latest vitals for a patient. Now this is simply a database request, but you can also imagine that the tool is actually I don't know augmenting some patient information in a database or or do basically whatever you want. But here I'm just going to get the latest vitals. So now when I run this again, it's going to take the latest vitals into account when creating the response. So as you can see, it now mentions the vital signs and the blood pressure. And if I change these things, so in this case, we're talking about John. Let's say I'm going to give John a heart rate of 30. It's not very high. and also like a very low blood pressure like so not sure if this is even realistic but just to see how it influences the result. So now you see that whereas in the previous responses we got uh escalate is false right we got that here and we got a pretty low urgency now even though the patient is saying the same thing it uses the information to actually change these values so now escalate is true so a nurse should take a look at this and the urgency is also higher so what's really cool about this is that you can combine LM reasoning with your domain knowledge the outputs you get are validated by Pyantic and they're structured so you can trust them and use them in other parts of your application. You can extend this agent by adding more tools or prompts or anything you like. And in my opinion, this is a big step in incorporating AI in your Python applications in a more useful way. Now, predentic AI's other possibilities as well. on particular graphs are kind of interesting. Although they say in the documentation themselves that well don't use it if you don't really need it because of course graphs can potentially complicate things. But they do mention different ways of organizing your agent workflows. So the one that I showed you today was a single agent workflow. That's also what most of the documentation shows. But you can also combine agents. For example, you could have one agent that then defines a tool that uses another agent. You can have an agent run something and then the application code could do something with the data or call another agent. Or you could go full on graph uh based control and then you can define these graphs with nodes and edges and make like a really complicated setup. But only do that if it actually uh adds something to your application. If you can use one of the simpler methods I mentioned just now, I think that overall is better. But I'd like to know what you think. Do you use pideantic AI? Could you see yourself using it in your own projects? Maybe in finance, healthcare, or something completely different. Let me know in the comments. Now, there are, of course, next to Pyantic AI a bunch of other libraries that are incredibly helpful to building stuff in Python. You probably already know quite a few of them like pandas or fast API. Now, if you want to learn about more libraries that are really helpful, but only few developers know about, check out this video next.

---

## 26. We need to talk.
**Channel:** ArjanCodes | **Views:** 27K | **Date:** 5 months ago | **Duration:** 8:12 | **ID:** 1THzmkEFQKA
**Link:** https://youtube.com/watch?v=1THzmkEFQKA

### Transcript:
something is likely holding you back from reaching your full potential. And this is something that I've struggled with my whole career. And I've seen many other developers do it, too. By the way, if you're watching this on the same day I published this video, I'm actually on a holiday. But through the magic of scheduling, there's still a video for you today. How neat is that? Anyway, so it's going to be slightly different video. Not really a code example video like I regularly do, but yeah, maybe something more of a story. I worked in academia for a long time. Basically, my whole career has been in academia. And in 2016, I was kind of fed up with it and I wanted to go into the startup world. So, I was going to launch a startup and we I actually did that. So I left the university and I created a company together with uh an ex-colague from the university and we wanted to create a sort of uh website builder for musicians. The actual product doesn't really matter. Um and of course at that time I had to make then a lot of technology decisions. So I did a lot of programming before that mostly in an academic context making uh tools to support uh research and stuff like that. Um but I was also kind of full of myself you know I'm the guy with the PhD so I'm smart I'm going to figure out everything I don't have to listen to anybody. So when I was looking at technology to use for that website builder, I came across React and uh it was kind of new at that time was not really the default choice for things. So I took a quick look at it and I basically dismissed it kind of right away without really properly evaluating it because I thought, well, you know, we're doing something that's so special. I know better how to do that than the people who develop React. So, um, I'm going to build that myself. So, of course, what happened is that while we built the product, but it ended up being way too complicated. Uh, it was so much work building everything that was not the core of the thing that as a new company we wanted to deliver. So, it it one of the reasons it failed was because of that. It was just too complicated, too much. So, I definitely learned the lesson then. But there is something deeper going on here and that's something that affects many developers and that's assumptions. We so often make the wrong assumptions or we make way too many of them and that happens all the time. You know, you you get used to a certain way of working, a certain kind of tool set that you're using and you're not open to change it anymore. It's not so easy anymore as you get older. you assume basically if you see something that's different from what you use that it's wrong and I do it myself as well. I mean I still do it. I was very uh skeptical for example of AI coding tools for a long time because I just assumed they wouldn't work properly. And when I now do a front-end project I tend to use React by default because that well that's what I know. But there are plenty of other frameworks out there that might actually be a better choice. In general, it's the problem that as people, but as developers specifically, we tend to stick with what we know and what we're comfortable with. Perhaps you also recognize that feeling when you hear someone talk about a topic that you're enthusiastic about, but you don't agree with them. you just assume that they're wrong instead of placing what they're saying outside of your own context. But that is actually scary doing that. It's easier to just make the assumption that they're wrong. And what makes it easy is that we use labels to put things into little boxes that we understand. Uh I I see this in the comment section on my videos all the time. When I do a few videos where I use a more functional style of programming, I get Arian hates classes comments. So, you put me in the box. Arian doesn't like classes. He only likes functions. Or when I explain design patterns, I get comments saying that I shouldn't turn Python into Java. In the end, those are all labels. And what they actually do is they make you feel uh comfortable because you don't have to question your assumptions and the labels simply make it easier to understand things because you just simplify things. Basically, it doesn't matter if that's accurate or not. And I also get it like assumptions, they are actually important. They're a survival mechanism, right? I mean, in prehistoric times, we we had to make assumptions because that actually saved our lives. We assumed that that tiger is hostile, so you know, let's stay clear of it instead of walking up to it and saying, "Oh, you're a cute little tiger, aren't you?" Also, we didn't have to change them all too often. You know, a tiger is always going to be dangerous, and that berry is still going to be poisonous 10 years from now. But what's different from the past is that we are in a period where things actually change all the time, especially for us software developers. In order to survive nowadays, in order to keep our jobs, in order to stay relevant, we need to make sure to question our assumptions because they may no longer be valid. And being a software developer, being in this industry means that you need to continuously identify or be aware of where you're labeling things, where you're oversimplifying things, and what your assumptions are. This is actually what's makes you learn. It's what makes you grow as a person. And it's also not something that you just do for a few days and then you're done. It's a lifelong struggle. I'm still working on that and I will probably need to do that until the day I die. But I do have one huge privilege and that's you. When I post something on this YouTube channel, you point out where I missed something or why something I said is not accurate. It's important that you keep doing that. It not only made me learn more than I could have imagined. I mean, it's been an amazing journey so far, but it's kept me sharp and it has tremendously helped me in making better videos. So, I want to thank you for showing up, for watching the videos, for being critical, and sharing your thoughts in the comments. I don't always respond, but I do listen. My videos, they're not perfect, and they're never going to be. But here's my promise to you. I'll always try to make the next video better than the one before. Now, before I finish with this video, I have a question. I'd like you to think about an assumption you've had for a long time that you recently had to re-evaluate. Maybe you realize that you've been using a programming technique that's been holding you back or perhaps someone you know is a different person than you thought. What did it make you feel like when you realized that? And how did letting that assumption go feel like after the dust settled? Let me know in the comments. You know, sometimes it's good to take a step back from our daily work and think about stuff like this. I'm going back to my holiday to do just that and enjoy some time off with my family. Next week, that's going to be a regular video again, so stay tuned for that. Thanks for watching and see you

---

## 27. How to Write Great Unit Tests in Python
**Channel:** ArjanCodes | **Views:** 39K | **Date:** 6 months ago | **Duration:** 22:57 | **ID:** EIV_ixKGPmc
**Link:** https://youtube.com/watch?v=EIV_ixKGPmc

### Transcript:
This video is brought to you by Squarespace. Here's a simple code example. This class is a weather service that retrieves weather information from an API, weatherapp.com. Now, you might wonder, how do you actually write tests for this? It's kind of hard, right? Because this does an HTTP request. So, you can't really test that without actually doing the HTTP request. This is probably part of the code that you actually want to test whether you're getting the correct JSON data and that it has the uh current object and the temperature object. So, how do you test those? In this video, I'm going to show you how to write tests for code like this, what monkey patching or mocking is and when you use it. And I'm also going to cover a few advanced testing strategies, as well as a few tips to help you write better tests for your Python code. Because writing tests for your code might seem daunting or not necessary, but actually software tests are really helpful, especially once things get serious in production. Let's go. One of the main types of tests that you hear a lot about is unit tests. And what these do is that they validate the behavior of a small isolated piece of code. That's usually a single function or method. These unit tests are typically fast, deterministic, and easy to run. Now, why would you actually write unit test? Well, of course, they catch bugs if you write them well. Uh they make refactoring safer because they provide a sort of safety net for you uh in order to make sure that when you make changes that things don't break that you didn't expect to. And often that can happen that you change something in one part of the code but you didn't realize it actually affects a completely unrelated part of the code or actually it was not as unrelated as you thought it was. Another thing that unit tests do is that they also document how your code is supposed to behave. So there's sort of a specification of the behavior of your code and that's of course also very close to what happens with test-driven development where you actually write the test before you write the code and then the test give a specification of what the code is supposed to do. Are unit test the only type of test you should do? No, of course not. In production systems, there's plenty of other different types of tests. for example, end to end test to help verify complete user workflows or security tests to help you protect against vulnerabilities or data consistency tests to make sure that your data stays reliable. So, unit tests are just one type of testing, but they're not the whole thing. And something else I want to mention is that writing software test is not an excuse to write sloppy code and then just tweak it until it works. It's really important that you think things through before you actually start coding. I'm not saying you should go full-on waterfall methodology, but you know, use your brain a bit and make sure you understand how everything is supposed to fit together. Good design is really important and actually often leads to code that is easier to test. So they go hand in hand, good design, good test. If you look at Python specifically, it comes with a built-in unit test module. So technically you could don't even need anything else to start writing tests for your Python code. However, I always recommend using Piest. This lets you write your tests as simple functions in modules. It provides uh more options with assertions. Overall, it's much more capable and pleasant to use than unit test in my opinion. Now let's take a look at this weather surface class. So if we want to test this get temperature method, it means we need to do something about this HTTP request because we don't want to do that request every time we call that method in our test code. So one thing that you can do to avoid that problem is by using something called monkey patching. And that basically means that you are dynamically replacing a function or attribute at runtime. Why is this called monkey patching and not elephant patching or uh raccoon patching? Well, I'm not really sure, but it kind of comes from the idea that you're sort of doing something that you're not really supposed to be doing. But for tests, sometimes you can't get around it. So, how do you then write a test? Well, here's an example of how you could do it with Piest. So, I've created a test file, test weather patch, because we're using monkey patching, and I'm importing the weather surface class from the weather module. That's the code I just showed you before, importing piest and some typing things that we're going to need. And then you can see we have a single test function, test get temperature with monkey patch. And interestingly, this gets an argument of type piest dot monkey patch. And that's the object that we can use to do monkey patching while we're testing. And you see this in actionly here. So I'm using the set at method in the monkey patch object to replace httpx.get by another function and that's actually a function that I defined here. So this kind of a fake replacement function, right? And what this does very simple. It gets urls and parameters just like the regular get function in hpx. It creates a class fake response that has arrays for status and JSON just like the object you would regularly get and then it returns an instance of that object. As you can see there is already a temperature prefilled here in the JSON data. So this doesn't actually call any other REST API. This purely sends a response back. So we replace HBX.get by fakeet. Then we can proceed as usual. We can create the weather service with the API key and then we can get the temperature in Amsterdam. It doesn't really matter if it's not being used. And then we assert that the temperature is 19. So now we've actually written a test that tests a part of processing the response from the weather API. And let me run this test. And as you can see, the test passes. So this works as expected. And by the way, if you call the weather API for real, uh we can do that by just running the main script like so. And now we can see that the current temperature in Amsterdam is 23.1°. Now I'm not based in Amsterdam actually. Maybe I should replace this by Utre. Let's see if we get anything different. No, it's the same. Netherlands are really small. Anyway, that's monkey patching. replacing something with a fake something else so that we can run the test accordingly. Now, as you can see, it's not that hard to do this, but it is a bit annoying to have to do that. Also, you need to keep track of the things that you actually replace uh not forgetting to replace something that shouldn't be called in the test. So, it's yeah, I wouldn't say this is the ideal way to write test. And actually, here there is a different way to write tests for this. But before I show you how to do that, first I'd like to take a moment to talk about today's sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking. And boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever try to get a new dev blog or documentation site index properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something. or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vioded, Squarespace makes it incredibly easy. Head over to squarespace.com/rcodes for a free trial. And when you're ready to launch, use offer code ran codes to save 10% off your first purchase of a website or domain. Now, back to the video. Now, the next thing I want to show you is a slightly different way of writing the test. So, here I also wrote the same test. Well, it actually checks for a different temperature, but doesn't really matter, but it's using slightly different features. So, as you remember in the original test with the monkey patch, we had to create a fake response class with methods and then return an instance of that class, which was kind of annoying. What you can do instead is using a mock and the unit test module from Python actually has already nice support for that. It has a class called magic mock that allows to do all sorts of cool things when you're writing tests. So here I have another version of that same test. And as you can see there's still a function, a fake get. We're still replacing httpx.get with that fake get function. So nothing changed. We're still doing monkey patching here. But instead of creating this class with methods and everything, I'm now using the magic mock object. And as you can see, this is really easy to set up. So I create an instance of magic mock. I then set some values. So race for status.turn value is none. JSON.turn value is and then some JSON object. And then I'm simply returning this object. So, matchmark allows you to set up an object with different kinds of methods and properties and responses in any way that you like, which is really good. And then I do the same thing. Create the surface, get the temperature, and then assert that it's 25. Now, you might say, well, that's not that much easier than just using a class, right? But the nice thing about magic mark that is that it also has assertions. And if you want to use that, you need to set up your test slightly different. So here I have another version of again that same test but now I'm not using a fake get function but I'm doing this slightly different by using the patch function from unit test and then this is how you would do it. So I again create this mock object. I set some return values and other things that we're going to need. But then I'm using patch which is a context manager and I'm patching hvx.get get width and then I'm indicating what the return value is going to be and that's this object and then I also get an object that I can use inside my context and this is where I can add extra asserts which you don't have with regular classes. So here you see again I create the surface I get the temperature. So that's going to uh send me back the mock response when I call hx.get get then it's going to do an assertion that the temperature is 25 which is what we expect but mock get has other methods that I can use for example assert called once so now I can make sure that we only get the temperature once and that there is no double API request or something and as you can see if I type mock get dot there's many other options here as well which gives me a lot more control over uh what is actually happening with my mock object. So I can really inspect this in depth and that's of course very valuable when you're writing test. So this is why if you need some kind of fake object well you can create it yourself with a class like I showed you in the first example but using a mock gives you a lot more flexibility. The next thing that I want to show you that also helps you write tests more easily is by using something called a fixture. And a fixture is basically something that helps you set up and tear down states for your test. Later on, I'll show you how fixtures combined with parameterization can make your test even more powerful. But here's an example of how you can use a fixture in the test that I just wrote. So again, we have our test. And as you seen in the previous test, the most work is actually setting up the weather surface with the fake mock response object. And this is something that you can put in a fixture. And here's how you do that. Piest has a decorator, a fixture decorator. And you put that on top of a function. So here a function weather surface that uses a monkey patch. Then I'm defining again this fake get function just like I did before. And I'm replacing HTVX by fake get. And then I create the actual weather surface. So this is a fixture that creates a weather surface. And then the way I can use this in pieest, I can simply pass this as an argument. And then the test itself becomes really simple. In this case, that's just a really simple assertion. But the nice thing now is that I can run different test with that same object. So for example, I can run another test where um bot already generates something like this where uh you know we try to get the temperature from a city that doesn't exist and then it should get an exception. So also there you can then reuse that object and that's why fixtures are actually really powerful. Now, there's one more thing you can do to make your test easier, and that's actually you can't always do that. Depends on the code, but if you can, this is a really good thing. You can actually refactor your code to make it better, make it easier to test. Now, the main problem here with the weather service is that we have this direct call to httpx.get. That's kind of annoying. We have to work around that with monkey patches and mocks and whatnot. But what if you can refactor this so that you kind of take out this direct dependency on httpx and make it a bit more generic and then the test also become a bit easier. How can you do that? Well, you could for example separate the hpx part into a client and then use that client instead. So in weather service what I'm do now is I create a weather service instance that has a client and an API key and then when I get the temperature I simply access this object and the object is passed in via a dependency injection mechanism when I create the service. So now get temperature no longer directly calls some function from some module. It calls a method on another object and that object is something that you can replace or mock or anything. So that makes it a bit easier now to write test for this refactored weather service. And here you see what that looks like. So I create first a mock HTP client in my fixture. I then set the return value of get to some other object that has arrays for status and a JSON method. And then I return a weather surface where I set that particular client. No need for monkey patching anymore. And now I can test this as usual by simply uh using this surface that we passed as an argument. We got it via a fixture. So, as you can see, there are different ways in which you can write your code and set up your test. And some are just easier than others. And being able to do some refactoring work to make your code easy to test is really helpful. Now, I have a few more advanced pyest feature I want to quickly show you. But before we look at those, I want to help you take your own project even further. And to do that, I've put together a free software design guide that contains the seven steps I take whenever I design a new piece of software. It's very practical, very to the point. You can apply it to your own projects right away. You can grab your free copy at iron.code/design guides. The link is also in the video description. All right, now let's wrap this up with a couple of pi test features you might not know about. A couple of hidden gems, so to say. The first is parameterization. And this allows you to test multiple inputs without having to write multiple test functions. You can see how that works. So I'm using piest.mmark.parameterize and I'm parameterizing the city in the expected temperature. London 15°, Berlin 20, Rome 18. Doesn't really matter what the cities and the temperatures are, but as you can see, we have multiple inputs here. We're supplying a list. And then in my parameterized test, I still get the uh monkey patch solution here. I could have also used the mock, doesn't really matter, but I'm passing the city and expected temperature as an argument. And now this test gets run with each of these different parameters. And I simply write the test as I would do it usually. So parameterized tests are really helpful if you want to test quickly multiple inputs without having to copy paste the testing code. Another thing you can do, I already sort of showed you that is that you can run a test that a particular call actually results in an exception and you can use piest.raises to do that and you can even specify the type of exception that you expect. So in this case I have a fake response that there is an API error and then we replace hbx.get by fake get create the weather servers and then we check that indeed we get an exception. Now you might say, aren't you just testing that your test code works? And that is kind of true, but in this case it is of course possible that get temperature maybe catches that exception or does something else with it. So the test is still helpful in making sure that if an exception happens here that it actually arrives when you call the method. So piest.races you can use that. Another thing you can do with piest is skipping a test. That's something you might want to do uh temporarily. Maybe you know your test only applies on a certain platform or you know it's going to be fixed soon so you just skip the test for now so you can reenable it later. So that's mark.skip. And there's also a conditional version of this skip if and this allows you to depending on some condition skip the test. In this case, for example, if uh the platform is Windows, well, then we're going to skip the test because this fails on Windows. It's not really true. Assert true never fails. Also, not on Windows, but this is how you would typically use that. Finally, something you can do with pi test is X fail. And that means you can indicate that you expect a particular test to fail. So, the test is going to fail, but that's expected. it's not going to fail your build, but it still is going to show up in reports. So, in this case, we know that there is some API bug. So, this is going to fail. We still write the test for it, but then um at least it's clear and it's in the reports that way. These features are all very handy when you're refactoring something or you're working on crossplatform code and they help keep your test suites green while you can fix things incrementally. Now, a few final tips. I typically try to keep a single assert per test. Uh you want to keep each test focused on one outcome. Mostly that's a single assert. Sometimes there are multiple asserts if an object is complicated. For example, um another thing I do is I want my tests to be clear and descriptive. In this case, I'm calling this test get temperature with monkey pad because of course these are examples. But if you name the test clearly then it's also easier in the reports to see what you were actually testing. And finally it's good practice to keep test in separate files and also sort of try to follow the structure of your codebase in your test file. So if you have uh source/fu/bar.pi then in your test folder you're going to have test/fu/ testbar.pi. So that if you have your code, it's also easy to find the test for your code and you don't want to mix your tests uh directly with your production code. This should be in a separate test directory. Uh that's what I also did here in my uh code example. Now I did put the code right here at the top level. You could also put that in a separate source folder which might be a bit better. I kept it pretty simple here. But in a bigger project, that's typically how we would do it. a source folder and a test folder. Now, there's one thing you have to make sure you do. Uh, it's really important, otherwise your tests are not going to run correctly in your PI project file. You need to make sure to indicate what the Python path is. In this case, that's the root folder. If you have this in a source folder and this is going to be source, but if you don't do that, then your test files are not going to be able to find the uh modules. It won't be able to find weather service for example. For example, if I run test in the current setup, then this is what we get. And you also see that we get the uh skipped and the x failed test right here. So you can see them in the report. And when I open one of the test files, then it has no issue importing the weather service. But if I remove this Python path from my PI project file and then I try to run the test again, then it doesn't work because it can't find the modules. So, make sure to add Python path to your PI project file, otherwise it doesn't work. But I'd like to hear from you. What are your favorite tips for writing unit test? Do you prefer mocking, monkey patching, something else? Do you even write tests? Share your thoughts in the comments. Now, if you're working with VS Code like I am, it's important that you properly set off things for Python development. Otherwise, you'll be struggling with imports not being recognized by the VS Code uh language server or test files not finding the modules and so on. If you want to learn a simple way to do it right every time, watch this video next. Thanks for watching and see you next

---

## 28. UV Just Got A Serious Upgrade
**Channel:** ArjanCodes | **Views:** 45K | **Date:** 6 months ago | **Duration:** 9:55 | **ID:** CJJzrtHeLGI
**Link:** https://youtube.com/watch?v=CJJzrtHeLGI

### Transcript:
If you've ever felt Python dependency management is slow, fragile, just clunky, UV has been evolving fast to fix that. In this video, I'll walk you through how UV works, how to set it up, and also what's been recently improved in the latest version, which at the moment of recording is 0.8.0. And if you enjoy writing clean, well ststructured software, I also got something for you. I've put together a free software design guide that helps you improve your software design skills in seven simple steps. You can get this for free./design guides. Link is also in description of this video. Now, before I take a closer look at UV and what's possible in the latest version, you might wonder why do we actually need a tool like that? You can simply install Python on your machine and then add dependencies with pip, right? Well, in principle that's true. when you're just working alone on your own machine and you don't really care about transferring your work to another machine, working together with different developers, deploying it somewhere in the cloud, then yes, you can basically use Python as is on your local machine. However, if you're not a pure standalone Python developer, then it's helpful to have a better solution for managing dependencies and virtual environments because if you need to work on some piece of code and you need to share it with another developer, you need to make sure that they have the same environment as you. Otherwise, it won't work. Python might not find the dependencies. there may be a version mismatch between the different Pythons that you and the other developers have installed on their machine. So you need some sort of virtual environment option for that. Now there are tools for that already in the Python ecosystem like pip and virtual etc. But these are actually quite clunky. They're kind of hard to use. A couple of years ago, we had poetry, which also solves that problem of dependency management in virtual environments. Works pretty well. But nowadays, I highly recommend working with UV. It's built in Rust. It's really fast. It has everything that you need. Now, let's start by walking through basic Python workflow using UV. Now, you first need to install it on your machine. And you can use a curl command to install it on your machine. I'm using Homebrew on my Mac, so I've installed it with Brun. That also works pretty well. and it replaces a bunch of tools like pip virtual and pip tools and more. To initialize a project, simply type uv in it. And now this has set up a basic project with a my project tunnel file that you can see right here. Now, as you can see, the dependencies in the file are empty. So adding a dependency is really easy. For example, let's add httpx package. That's really helpful in many different situations. So this now installs the package. And when we print the pi project file, you see that httpx is right here. Now, if you need to remove the dependency again, that's also really easy. Simply type uv remove httpx. And this has removed the dependency. As you can see, it's no longer here. In a new project, UV also creates a very simple main file that you see right here. And running this is very easy. You simply type uv run main.pi. And this is going to run the project in the virtual environment that belongs to your project. So as you can see, this is really easy to use and it's actually also really fast because UV is built in Rust. Now the interesting thing is that there have been a ton of new releases of UV. I think they basically release something like every week. If I scroll down, you see that for example here we have 0.7.19 3 weeks ago and then if we go back we have again three weeks ago more 3 weeks ago last month uh more last month. So they release regularly new things and as you can see there's like a ton of bug fixes, enhancements, changes every time. So UV is very much a moving target at the moment. It's improving by the day basically. And of course for new tool that's really important. So it shows that the team behind UV is really serious about it. And in particular in the latest releases there are a couple of things that I think are interesting to quickly mention. I'm not going to do like a in-depth UV video today. If you want me to dive deeper into some of the features of UV simply let me know in the comments and then I'll maybe do a dedicated video about that particular feature. Now, one thing that is interesting is if you take a look at um some of the more recent releases that there's simply been more support for the various types of Python. For example, of course, we have the upcoming Python Python 3.14. So, since 0.716, there's support for the 3.14.0 beta 3 version. And adding more support for different Python versions is something that you clearly see in all these release notes. For example, here in 7.18, we have ARM 64 Windows Python version support. And if you go to 0.720, we see there's support for beta 4 and PI 7.3.20 as well. Now, this all great. Of course, it's not really relevant what the precise versions are here, but as you can see, it's pretty clear that UV focuses on being compatible with a wide variety of Python versions. And I think that's important to know. Now, next to being compatible with lots of different versions, there's also been several enhancement and performance improvement. For example, here you see performance improvement make code resolves about 10% faster. or in 0.7.16 share workspace cache between lock and sync operations which also makes it more efficient or in 0.7.21 improve the cache key performance. So there's many of these small little tidbits that just overall make UV more efficient and faster which is nice. Now there's one kind of bigger thing that's been recently added to UV which is this workspace flag. And what this actually does is that it allows you to manage dependencies for multiple projects at the same time. I can do a standalone video about that specific feature if you want to. Just let me know in the comments. But the idea is that if you have multiple projects going on and they roughly have the same kind of dependencies, I mean that happens quite a lot. Like if you're building I don't know a bunch of automations that all rely on the same things then having a workspace for that with a single virtual environment and dependencies and everything can be really helpful because it will just simply be easier to manage. You can also find more information about workspaces in the UV help pages. So workspace is basically a collection of packages that are managed together. And if you have a large codebase then you can split those into multiple packages with common dependencies. And what happens that you do have per project your own pip project tol file but the workspace shares a log file. So it operates with a consistent set of dependencies. So that definitely helps with performance. But there are more of these small things like GPUaware installs for Intel or AMD. There are clearer error messages, safer locking. All of these small things help. Now, another area where UV has improved a lot is simply fixing bugs and simple quality of life improvements. Basically, every release has a bunch of these bug fixes. And I actually recommend you taking a look at the release to see what's in there. It's kind of interesting to see. Now, the biggest thing to me is the new build backend, which has been added in one of the recent releases. It has massive impact. But before I talk about that, if you find this helpful, give this video a like, subscribe to the channel. It really helps more developers discover modern tools like UV and helps me keep making videos like this one. Now, let's talk briefly about the new build back end. As of version 0.8.0, UV build is now the default build backend for UV. And what does a build backend actually do? Well, this is basically what Python uses to turn your project into a distributable package like a wheel or a source tar. Traditionally, this was done by setup tools or hatchling. But UVB build is a much faster and cleaner alternatives. It validates your metadata. It respects the modern standards. It integrates directly with UV's locking and syncing. And as of 0.8.0, you don't even have to think about it. It just works. it's selected by default whereas before we had to use the hatchling build back end and this uh new UV built back end also requires no configuration for most Python projects. Now how can you see what the build back end is? Well, basically if you're using the most recent UV then default it's going to be UV built but you can actually also add that manually and that's by specifying the build system option in your PI project to file. So in this case, we explicitly state that we're using the UV built back. Now I haven't done extensive tests, but according to the documentation, this results in 10 to 30 times faster builds, fewer bugs, and a modern workflow that's easier to reason about. Anyway, that's just a quick look at everything that's been happening in the UV universe through version 0.8.0, but I'd like to hear from you. Have you already switched to UV? What's your experience with? Let me know in the comments. And are there features of UV that you'd like me to do a more complete overview of? Now, already a while back, I did an introductory video to UV. If you want to watch that, check it out right here. Thanks for watching and see you next

---

## 29. Why Are Not More People Using These Python Libraries?
**Channel:** ArjanCodes | **Views:** 65K | **Date:** 6 months ago | **Duration:** 24:12 | **ID:** F09EK4ztG34
**Link:** https://youtube.com/watch?v=F09EK4ztG34

### Transcript:
Today I'm going to explore 14 Python libraries that you probably haven't used, but you should. And at the end, I'll include a bonus one. It's not a library, but it's really nice and you should definitely check it out. Before we dive in, if you're building a new project or you just want to get better at structuring your code, check out my software design guide at iron.code/design guide. It's a practical checklist I follow myself when designing new software. It's totally free and the link is in description. The first library that I want to talk about is tools with a Z or PI tools. Already when a library ends with a Z, you know it's going to be pretty cool. Actually, PIT tools is a functional programming toolkit for Python and it enables things like uh compose, query, partial and high performance data manipulation pipelines. Now some of these things like partial function application already available in fun tools standard library package but compose isn't and having a compose function can sometimes be helpful. For example, here's a really simple piece of code that imports the compose function from tools and then it composes stripping a string and turning it into uppercase and then that gives us new function and that we can use to then strip and convert a string to uppercase. And when you run this then this is what you get as a result. So tools is really nice just for writing functional code without going full hasll. The only disadvantage is that there's not really any type annotations for this library and I feel like that's sorely missed here. It would be really helpful to actually have proper type annotations. There's been an open issue for adding a static typing to tools since 2020. So that's been a really long time. So, I hope they'll get to it eventually because it would definitely be helpful. The next library that you should know about is Tabulate. This is a really simple, easy to use library that cleanly formats tabular data for the console or markdown latex. It can do a lot of different things. This is really nice when you're developing a CLI and you just want to print some data and see what is going on or if you just want to debug some output. Using tabulate is really easy. You have some simple data here, a list of lists. And then I can simply call tabulate. I pass a table. I supply the names of the headers. And that's going to print this data as a table in my terminal. There you go. Really straightforward. As you can see from the documentation, there's a ton of other options as well. For example, you can add row indices. You can change the format of the table. So it can be, for example, latte output if you need that for, I don't know, paper or something. And it can also print your tables in a grid like this or like this. There's tons of different options available. In short, tabulate really simple, really nice, highly recommended. Now, talking about printing stuff to the console, another library that's actually really nice is rich. And this allows you to create really nice looking console output. This also includes tables, but uh syntax highlighting, trace backs, progress bars. It's kind of a drop-in replacement for print but with a lot more power. Here's very basic example. So I simply import print from rich and then you can add these types of tags to for example change the color of the text. So when I run this then this is what you get. Now rich can do way more than just change the color. For example, it can pretty print JSON by using the print JSON function or you can do easy alignment of text like so. Uh there's also support for paging which is also quite interesting if you have pretty long input. And like I said, rich can also print tables. This piece of code that shows you how to do that. This is kind of a different approach from tablate which is really about taking some data and then just being able to quickly print it as table. Enrich there is a more structured approach to it where you have to create a table object and then add columns and rows which you know also works but table 8 is a bit simpler if you just have some data you want to quickly print. The next library that I want to show you is hypothesis. I've talked about this library before. So if you're doing software tests, like you're writing unit test for example, you often have to deal with different types of inputs, right? The empty string, zero, negative numbers, positive numbers, etc., etc. Now instead of manually defining all of these cases, you can also approach it slightly differently by using something called propertybased testing where instead of providing all these separate inputs, you describe the shape of the inputs and then this is going to generate test cases for you. And this is really nice for discovering edge cases or even thinking of edges that you didn't even consider. So here's an example of how that could look like. So you have to do a couple of imports from the hypothesis package. But as you can see I'm I have a simple test function here that checks whether sorting is important. Then what you can do with hypothesis is use a decorator kind of structure to indicate what kind of thing you're supplying to a particular test. So in this case given is various lists of integers. This already shows you the power of what you can do with hypothesis. So this basically generates a bunch of different integer value inputs and this generates a bunch of different lists including probably empty list with one element uh all different kind of cases and then this test is not going to be run once but it's going to be run for all of these different cases and then it's going to check this assert. So how does this work? Well, if you're simply using piest and this is somewhere in your test folder you simply run piest. Now in this case I have an explicit example. So I call pi test on that particular example. As you can see it collected the test uh and the test passed in this case because of course this will always uh be correct right. So if you want to level up your unit test hypothesis is a really great place to start. Next up is Pantic settings. This is actually a really cool little extension of pideantic. It adds built-in support for managing environment variables and application configuration. I mean, you always have to deal with some level of configuration of your app, right? You may need to load a database URL from somewhere or use environment variables and you don't want to be writing a boilerplate Python code in order to load those uh settings or retrieve them directly from environment variables all over your code. This is going to be a mess. And you also want to maybe do some validation like making sure that certain settings are there before you actually run your script. And that's what pyantic settings allows you to do. It's just a simple package that you install separately. And then what you can do is define a pyantic model and then use that as a setting. So I've created a really simple example here of what that could look like. So, as you can see, I'm importing base settings from identic settings. And then I'm going to create a class in this case called settings that has a bunch of different settings. In this case, just two, a database URL and a debug flag. And then this actually the interesting bit. I created a config class where I defined an end file or you can define any file that you want here. In this case, I have settings. In my settings. So this is typically something that uh you store in your server or it's in any case not something that you will store in your git repository and that's where we have the actual settings. So there's a database URL and there is a debug flag and what this part does is that it says that basically you take the default values from this particular file. So then when I create an instance of this settings object, it's going to take the values from this file and then I can simply access them directly. I get type annotations and everything which is really nice in my IDE. So it's really easy to use. So when I run this, you see that we now have access to these values that are from the end file, which is really nice. And on top of that, there's like a ton of stuff that you can modify like uh whether it should be case sensitive or not, how you should parse certain environment variables, etc., etc. So, I recommend you try this for your settings. It works really well. Next up is HTTPX, which is an improved version of request. It adds in particular async support. There's connection pooling and overall better performance. And in many cases, it's actually a drop in compatible with request. So I've now switched completely over from using request to HTBX. Works really well and it's really easy to use. Here's the most basic usage which doesn't use any of the advanced features, but you can simply do hbx.get and then you have your URL. So in this case, this retrieves information about one of my courses from my uh business API and then you can simply print a response JSON data and here you can see the result. So this was actually my software designer mindset course but there are way more things that you can do with HTTPX. So this is a single uh connection request but you typically want to create a client and then use that client because that's going to improve the performance and that's actually really easy to use. For example, in H we actually have an async client that you can create and you can uh if you create that what you can see here. So this actually use a context manager but then you can actually use a weight to uh do get request and do multiple requests at the same time etc. and get all the advantages of concurrent code. Now like I mentioned there's ton of other things you can do with HPX as well like connection pooling for example. If you're still on request I highly recommend you switch to HTTPX. Next off, and this is something that I've always been struggling with and I'm really happy that there's a library for this, it's fast API pagionation. Now, if you're building an API and it accesses resources, like I don't know, you have an endpoint that searches for resources, for example, and you want to support pages because you don't want to send back thousands and thousands of uh objects in a single response. Well, you need to write some boilerplate code to deal with pagionation. like uh you may want to set like a skip and a limit for example or you want to do differently where you're actually sending back some key that allows you to then navigate to the next page. A fast API pageation adds support for these things for fast API. So it's uh really cool and here you can see a simple example of how you're supposed to use this. So in this case your uh get request in this case for getting in users gets not a uh list of users but it gets a page object and then you call pagionate on the result to actually deal with that. I also created an example of this. So let's say if you have a pantic model of an item which has an ID and a name I then created a fake database containing uh lots of these items doesn't really matter what they contain. And then I created a pagionated endpoint for these items. And then finally I can run the script as a UV corn server. So when I run this example like so now I have my server. What I can do now is navigate to the items page. And as you can see this gives me the first 50 items. So that's already really nice that page and it adds this automatically. But if I pass query arguments like page is three and size equals 10, then I'm getting this as a result. So all of this is handled by fast API page. Really, really nice. If you're building APIs, adding this is a no-brainer in my opinion. Next library that I want to mention, I don't have a specific example for this because it requires some extra setup, but it's fast stream. And this is an async event-driven framework that's built on top of async.io, But it makes working with services like Rabbit MQ, Kafka, Reus, etc. Reddis, Reis, Reddis. I never know how to pronounce it, but it makes it much easier. I like Reus actually because it's kind of the counterpart of Reus. Yeah. What do you think? Anyway, it's pretty cool and works with the Python decorator mechanism to add some features. Like for example here this Kafka example where we create a broker and you pass the URL of the Kafka server. Here for example this decorator allows you to consume data from this incoming data stream and you can output it to another one via this simple returner statement. That's actually pretty nice. And another thing that's nice is that it actually integrates with Pyantic as well. So you can define messages with a pyantic base model and then fast stream will use that to pass in the data. You can add validation and anything you want. You get all the advantages of pyantic. So fastream really nice if you're using services like radius, kafka or rabbit mq. Next up is nice and this is a full stack python framework to build modern user interfaces just with plain python. It has built-in updates, charts, tables. You don't need any JavaScript and it supports both web and native deployments. And here's very simple example of how this works. So I have imported UI from nice GUI and I can simply write UI.LE I add a button. I have an on click handler uh lambda function in this case and I can add a notification and then I simply run the app like so. And when I run this script, it already automatically opens the browser for me. And then I have my nice little guey and I can click this and I get the notification here at the bottom. So already by just defining these basic objects, it already looks pretty good and it's already pretty easy to uh get started with. So to me this is a really important aspect of a good user interface library that it's actually easy to use and nice gooey has most of the features that you actually want in a user interface including uh very nice layout possibilities. So, for example, you have all the standard things that you would need like um menus and cards and uh columns and rows in a grid, a list, there's a slide, there's accordion style things like this. Basically, all the elements that you want. But if you don't like the way that nice Google works, there are alternatives like for example flat which allows to build multiplatform apps in Python. And in the back it's powered by flutter as opposed to nice guey. Flat is a bit more focused on building native feeling apps. And then uh you can build apps with Python that run on your desktop, your mobile or on the web. And here's an example. Welcome to Here's an example of what using flat looks like. So it's slightly different from UI, but you kind of get the feeling of this as well. So you get a page in your main function and then you can add elements like uh text in this example and then you simply run it like so and as target you pass the function that needs to run and when I run this you see that it doesn't open a website it actually starts a desktop app with the text element in it now flat just like nice gooey has access to all these different kind of uh controls that you have like uh all sorts of uh buttons for example example, there is dialogues, there is uh navigation elements like an app bar or a menu bar, etc., etc. Basically, anything that you need. So, if you want a more native experience, try flat. Now, if you also don't like that, maybe you like something that is a bit more React style. I don't know why you would want that, but if you do, then actually there is an alternative to flat or nice gooey, and that's called Reflex. and allows to build fullstack web apps using a React style component tree in Python. And this combines state management component rendering using a declarative approach. Here's the uh reflex website. We can go to the documentation of how it actually works. And again, you have all the components that you would want, right? So again, you have your layout things like a sidebar or navigation bar. Uh there is checkboxes, there are uh speed dial, pricing cards, lots of different things. And when you take a look at some of these uh user interface, you can also see the code that actually powers it. And it's very much reminds me of React. So you have your function where you uh create a component and that component contains other things and that's basically how you define everything. Personally, this is not my preferred UI library. I like nice gooey in particular, but if you're really used to working with React, then Reflex might be a good option for you. Next up is textual and this is a terminal UI framework for Python. And this actually built by the creators of Ridge. Now I believe they stopped the company actually, but the open- source libraries textual and rich they remain. So you can still use these. And this is basically kind of like a nice gooey. And what this allows to do is create these sort of textbased user interfaces. And this is nice if you want, I don't know, some type of terminal dashboard or for admin tools or whatever. And the way this works is pretty simple. You have an app class and there's a compose method and there you yield the actual thing that you want to render on the screen. You see a slightly more advanced example where we have a clock and you can supply some CSS. That's what you see here. Uh there is uh there is this compose function that I just mentioned. And you can also do interactive stuff like here we update the clock and then we set an interval so that the clock updates itself every second. And then there is the actual update method. So maybe I should just uh run this example and see what happens. Well, and there we go. We have our clock and it's almost lunchtime for me. By the way, if you're enjoying these videos and you like to keep updated on the latest Python news, check out my weekly newsletter, The Friday Loop, where I share news about Python and software development in general. You can sign up for free at the Fridayloop.com. I want to finish this video with a few AI frameworks. I mean, we're all building with AI nowadays. Uh, it's really important and it's going to be integrated more and more in the things that we do. So, what are some of the libraries packages that you can use for that? Well, one of them is Lang Graph. This built on lang chain, but it's more focused on defining agent flows as graphs. So, you build the logic of multi-step AI workflows with nodes and edges. So, this is great for orchestrating AI agents with branching logics, conditionals, and loops. Here's an example of the kind of thing you can do with this. So, let's say you have a bunch of these different agents uh that communicate with each other. So uh you can actually define all of this in Python by creating these different agents and then you can create for example a supervisor agent that uses these other agents. But next to supervised there's also other options like a swarm of multiple agents or you can uh interface with an MCP server. Uh there is a memory management. Lots of different possibilities. Now, Lang graph is nice, but actually Pantic AI, which is next up in this list, might be even nicer. And this is um you could say it's tooling for integrating pideantic with LMS where you can use structured validated schemas to define the shape of LM responses. But it has other things as well like for example uh dependency injection. There is support for graph. It integrates with bantic logfire. It's type- safe uh Python ccentric designs. These are all really nice things. The basic way you set this up is really simple. You just create an agent. You supply the model that you're going to use. There's a system prompt and then you can simply run that agent. Uh but you can do other things like uh dependency injection. So here's another example of an agent and there you supply dependencies. So in this case, these are support dependencies which is a customer ID and a database connection. And then you can add tools to that like a function that retrieves the balance of the customer or the sample system forms for adding a customer name for example. And on top of that you can supply the output type. So in this case that's a support output model. Uh so that means you also get a structured schema for the output of an LM. So there's no more magic strings that you have to deal with. So overall, I feel like this is also really nice design to deal with agents and perhaps you don't need very complicated agent structures, but you would like to have validation, some custom prompts, some integration with toolings, pideantic AI is a really great way to do that. And by the way, if you want me to do a more in-depth video just covering Pyantic AI and what you can do with it, let me know in the comments. Now, I told you there was a bonus and it's not a library, but it's actually a tool that is really useful if you're working a lot in Python. And that's Marimo. Marimo. I'm not really sure. I'd say Marimo. Marimo. Marimo. Marimo. Marimoo. Marimoo. H. Anyway, whatever we're calling it, however we're pronouncing it, this is a fresh take on interactive Python notebooks. There's no uh hidden state like in a Jupyter notebook uh UI updates uh reactively based on the code. And what is actually really nice in particular, what I like about Marimo is that it's doesn't use some custom format or something. It's just a piece of Python code. And I can actually show you that in their online playground. So let's take a look at one of the tutorials for examples. This is all done in the uh web interface but you can also run this as an extension in VS code. But what you can see here we have a notebook style setup. So we can add uh elements to this or edit this in any way that we like. But if you look at the files you see that there is not some custom file. There is actually a Python file and you can take a look at the actual code in the Python file. And as you can see here is a markdown element that's reflected right here. We have uh cells with uh defined with a decorator. There's a slider. The all the elements are here. This is completely defined by this piece of Python code. And that's nice because that means you can also put this in a Git repository. You actually work with Python code. So you can maybe even write some custom code that interacts directly with this in some way. There's a ton of built-in components for inputs, sliders like uh what you can see here at the top. So, this just works, which is really cool. Uh, there's charts and there's more. So, if you're frustrated by Jupiter notebooks and how to use them, Marimo might be a really good option for you. And like I said, Mario also has a VS Code extension. So, if you're using VS Code, they can simply install this extension. And then you can use Mario directly in VS Code. Yes, we trust the publisher. Hopefully this video gave you a few new interesting tools and libraries to play with, but I'd like to hear from you. Which of these are you already using? Do you have a favorite Python library that I didn't mention? Would you like me to do a deeper dive on any of these libraries? Let me know in the comments. Now, in order to effectively use these libraries, you need to know what to do and what not to do in Python code. And this video shows you exactly how. Thanks for watching and see you next

---

## 30. The Builder Pattern in Python: Finally Explained!
**Channel:** ArjanCodes | **Views:** 56K | **Date:** 6 months ago | **Duration:** 14:19 | **ID:** 2f1FkGQTarc
**Link:** https://youtube.com/watch?v=2f1FkGQTarc

### Transcript:
Sometimes creating an object gets messy. If you have a config file, a report, a database query, or even a simple HTML page, all need a ton of options, flags, and nested parts. And before you know it, your constructor turns into a monster, or you're dealing with layers of deeply nested dictionaries. That's where the builder pattern comes in. It lets you build complex objects step by step in a clean and flexible way. To my own surprise, I never covered this pattern on the channel. So, today we're going to fix that. Now, before I dive in, if you want to learn how to design software from scratch and when to apply certain design patterns, I have a free guide for you. Go to ion.code/design guide. This describes the sevenstep process I use when building production systems. The link is also in the description. As an example, today I'm going to use the builder pattern to build an HTML page. So this is going to be the product that the builder produces. Now before we can even do that, I want to start with a really simple data class that represents an HTML page. So let's say just get started HTML page has a title and a body. And then what we can do is add a render method that then creates the actual HTML content. Now, of course, this is still a very simple thing, right? But let's import this HTML page and then in our main function, let's now create a page like so. So, we have a title and we have a body and then we're going to print the rendered page like so. when we run this then this is what we get as a result. Now we can make this a bit nicer for example by making the data class frozen so that this is a simple immutable object. Now you could say well this is totally fine. We don't need any particular pattern to do this and in a sense you're right. I mean in this case the HTML page is of course very simple. Body is just a string right? Uh so uh we just put it in here. So it's very basic but of course that's not really how HTML is set up. HTML is really can be a really complex structure. So if you wanted to properly represent an HTML page and retain the full structure, you would get a pretty complicated set of variables here and it would be really hard to actually initialize that properly or set values to it because that's like a deeply nested thing. But even if you wouldn't do that already, HTML of course has lots of other things as well apart from the title and the body such as metadata and maybe other things. Now the builder pattern is a creational design pattern that separates object construction from the representation. Here's a page from website called refactoring guru that shows the builder pattern in more detail. This by the way is a really helpful resource if you want to learn more about design patterns in general. As you can see, as part of creational patterns, we have the builder pattern here. And this image showcase the reason a builder pattern exists. And when you scroll down, you see various examples of where the builder pattern could make sense. So in this case, there's a house builder and we have build operations for all of the different parts instead of having one huge initializer that builds everything all at once. So basically, you configure your object piece by piece. And once you've done that, once you're ready, you build the final object that's often immutable. And you may think, well, that's kind of a very specific use case. You wouldn't use that all that much. But actually, you've probably already seen the pattern used by other libraries. For example, Pandas uses this pattern as well. There's a style option on data frames where you can set various style configuration things like uh the caption and the format and a background gradient and a bunch of other things as well. and then finally you convert it to HTML. This is a clear example of the builder pattern. We define step by step what we want to build and what the conditions and the configuration is. And then finally we build the thing. That's what's happening in this last step. Another example is math plot lib where we're also constructing a chart in different steps. So we first start with a bar chart, then we set a y label, then we set a title, a legend, etc. And finally we build the plot and show it on the screen. And this by the way is very basic map plot lip example. But this is also an example of the builder pattern. We build we set up the thing step by step. And finally we build and show the thing that we want to have. So that's what the builder pattern does. And you can also imagine that if you have all this information like uh the different fruits, the counts, the the labels, the colors, the y label, the title, the legend, that's like a whole bunch of stuff. And you don't all want to pass that to an initializer. It's going to be a big mess. So that's why you opt for a builder pattern like this. Now, back to my HTML page. Let's create a builder that allows us to create an HTML page object step by step. So I'm going to create another class here called HTML builder. And this is going to have a title and the content of the body. And then what you can do is add various methods like in this case AI already generated a bunch of things for me like uh it allows me to add a heading or a paragraph. But for example, we could also add a title. And then let's simplify this a bit by just having an empty initializer like so. So now we've created a builder class that allows to add a title. It adds headings uh by automatically formatting it. It can add a paragraph to the body content. And then we're going to have a build method that actually creates the HTML page object like so. So if we now go back into our main function. So instead of having to do it like this, what we can do now is we import the HTML builder and then we're going to create the builder like so. Then we're going to add the elements that we want. So we have the title which is my web page. We can add a heading like so. I need to learn to type properly. And we add the paragraph. And then we run page is builder.build. And that is going to give us an HTML page. So I think there is still an issue here. So build it should actually not directly call render but this should actually return an HTML page like so and then we can print the page. So let's run this and see what we got. So as you can see we get more or less the same result as we had before slightly differently formatted but this is basically what it looks like. Now there's one thing we can do to make this a bit easier to use which is to use self typing. So at the moment the HTML builder class when you call these method they all return none but actually it's helpful if it returns the object so we can chain uh operations on it. So in this case we're going to let it return self and we're going to do the same thing here. like so. And what we can do now is instead of doing builder dot builder build, we can actually do it in as a chain of operation. So we can do dot add title dot add heading dot add paragraph and this makes it even simpler to use. So that's the core of the builder pattern. Again, you might wonder why not uh use just a big constructor in this case. Well, in this case, the HTML page is still relatively simple. It has a title and a body. But what if it has uh metadata, buttons, a footer, uh theme, CSS classes? Uh if you have to pass everything into one single constructor, it becomes really hard to maintain and errorrone. And the builder pattern here gives you a lot of flexibility as you can add or remove all these optional parts over time and also keeps construction logic out of your data model which improves readability because currently HTML page remains relatively small. We don't have to worry about how it's being built. We simply worried about how to use it. So that's also a really nice thing. And as we add more complexity, HTML page can remain simple. If we add more features to how you can build an HTML page, the that doesn't affect this class all that much. It mainly affects the HTML builder class. Now, one thing we could do is add metadata, which is a dictionary of strings to strings like so. And now we can also extend the render method to take care of that. And this is how you could implement it. So we're adding a meta item for each item in our metadata dictionary. And now in the HTML builder, we could add metadata. And then of course we would also need that here. Or another thing we could do is add a button like so. So this is a simple method that gets a label and an on click link and then it's going to add this to the body content. So once we have this we can create a much more complete page. So in this case we have a builder, we add a title, we add a heading, we add a paragraph and then uh we add a button with a link. And now let's create an HTML page and then render that page data into that file. Let me run this. And then, as you can see, we now have our page HTML with the button and the paragraph and the header and everything that we added. As you can imagine, without the builder pattern, this would result in a really big constructor that's very hard to read, or a very big HTML page class that's responsible for both construction and rendering, which is not great from a single responsibility principle point of view. Now, as a bonus, how can we view this HTML page easily? Before I show you that, if you're enjoying these kinds of deeper dives into real design patterns like the builder pattern, hit the like button and subscribe to the channel. It's a small thing, but it really helps me know which design patterns to cover next and gives the algorithm a little notch. Now, in order to view this HTML page, ideally, I'd like to open a browser and then host that HTML page locally. And that's actually what this little class does here. This is an HTML viewer class. It gets a file name that it should display which is the HTML page that we created. It gets a port and it gets a server and then it has some helper method. So there is surf for example which creates a TCP server and then opens the browser and then navigates to a particular URL and then it's serving the file at that particular URL. And finally, there's a start method that sets the directory to the path of the file and then starts the HTTP server from that spot. So what you can do now is actually run the script and then it's going to open the browser and show the HTML page that was created by the builder pattern. Back to our IDE, I can simply cancel and stop the server by a keyboard interrupt. This is a really clean, simple way to show the page that we created locally. So before I end the video, when should you actually use the builder pattern? Now, of course, I talked about the use case of when you're building complex objects with lots of optional parts. That's a great place where the builder pattern can help. Things like configuration files, nested queries, UI components, stuff like that. It's great when you want a clean, fluent API or you need to really separate construction from logic. And the nice thing about the builder pattern is that because you built the object step by step, it's very readable. It's very flexible because it's really easy to add or skip parts. It's also quite safe because it avoids giant constructors or fragile nested dictionaries. But the builder pattern also introduces some boiler plate. Uh it's more boiler plate than just have a simple data class. And if you have an object that has just a few fields, then of course it's going to be overkill. Another problem, potential problem with the builder pattern is that it's easier to make mistakes. For example, in the main file, if you forget to add the build operation, then it's uh not going to work. Uh also, if you have many many many different elements that you need to set, then you may forget one and you have to sift through all of these method calls to figure out which one you forgot. So if something is really complex then you might want to shift to uh storing the thing in a separate JSON or configuration file and then read from a file instead of calling all these separate methods. But overall though if your object has five plus optional fields and you expect it to grow over time then the builder pattern is actually a good choice. Otherwise I'd say don't use it but keep it simple. But I'd like to hear from you. Have you used the builder pattern before? What other design patterns do you regularly use? Are there any patterns that you'd like me to cover on this channel in more depth? Let me know in the comments. Now, next to patterns, there are also anti patterns, things you definitely shouldn't do. Check out this video to learn more. Thanks for watching and see you next

---

## 31. Is SaaS Dead? Here's a Senior Engineer's Perspective
**Channel:** ArjanCodes | **Views:** 17K | **Date:** 6 months ago | **Duration:** 13:20 | **ID:** 70uORDj3Lzc
**Link:** https://youtube.com/watch?v=70uORDj3Lzc

### Transcript:
This video is brought to you by Squarespace. Software as a service SAS platforms are basically dead. Oh no. They're all going to be replaced by micro SAS platforms that are vibecoded on the fly by companies themselves or by AI agents that you can just deploy for free, almost free, and then let them do all the work for you. Now, this could happen for a specific kind of SAS that I'll talk about later on. But in general, I don't think it will. Before I talk about why, let's explore this premise that SAS is dead. I've seen several posts online saying that SAS is dead, you know, on platforms like LinkedIn, other social platforms. Maybe I just shouldn't go on those platforms in the first place, but well, we were I was doom scrolling and I came across them. Now where I believe this started with is something that Satya Nadella the CEO of Microsoft said in a podcast in the beginning of this year notion that business applications exist that's probably where they will all collapse right in the agent era because if you think about it right they are essentially CRUD databases with a bunch of business logic. the business logic is all going to these agents. His premise is that most SAS platforms are basically crowd applications, a database with a bunch of business logic on top of it and that this all collapses and becomes AI agents. Another discussion is that companies will simply vibe code their own platforms and not use the existing ones. And this is actually already happening in particular for internal tools. Now, it's clear that over the past months and years, there have been a lot of changes and improvements in how AI helps us write code. I mean, it's more integrated in our IDs. For example, VS Code has a great connection with GitHub Copilot. I use that all the time. It can suggest edits. It has built-in chat interface. Works with your entire codebase. And of course, next to VS Code, there are other options such as cursor, which itself is a fork, VS Code, or Windsurf. And then there have also been a lot of improvements to agents in particular with the MCP protocol that allows you to directly connect LMS with various APIs and other systems. And then I'm not even talking about the newer companies recent companies like Lovable or Replet that allow you to basically just create a fully hosted application just by prompting. So the trend is clear. More coding tasks are going to be simplified or even taken over by AI. more things going to be done by agents and not by humans. So you might say, well, I mean, and if that's true, in that case, what's the purpose of software as a service in the future? We don't need it anymore. Right? Now, on the surface, that may sound reasonable. But when you dive a bit deeper and you look at more specific SAS solutions, then you'll notice quickly that there's a problem with that line of thinking. Let's take a simple example. A SAS software as a service that does accounting for your business. Every business needs this. I used it in my own business as well. Now, an accounting system is nothing more than a CRUD application. It's working on a database that stores your invoices, bank statements, transactions, anything. So, you could say, well, you know, you could vibe code that yourself or, I don't know, let an AI agent do your bookkeep keeping for you. So, no need for an accounting platform, right? But the problem is that there are rules around how you're supposed to do accounting as a business. An accounting platform needs to follow these rules by law and they're different per country. But here in Europe, we have rules like or uh policies like GDPR. There's ISO standards for security. You need to do audits. And on top of that, you also need to couple your accounting system with the bank. Now, bank is not just going to let some random AI agent or an application that you vibe code yourself to directly connect with their APIs. Plus, a rule in most countries is that an independent accountant needs to check your numbers once a year. At least that's how it is in the Netherlands. And these accountants only support a limited number of platforms and sometimes they even work just with a single one. Uh simply put, you always need an established accounting software as a service. And now will those platforms change uh rely more on AI to automate things for you? Absolutely. But that's already been going on for many years. It's nothing new. For example, the accounting platform that I use has some pretty basic rules and system in place to automatically map transactions to bookings and vice versa. So automation is already there. that's not really going to change. Or take another example, uh, website design and hosting software as a service. It's more than a crowd with a database. There's NIS-2 compliance. You may need to have an agreement with econ in order to sell domains to your customers. You need cloud architecture to actually host a website. It's not something you vibe code in an afternoon or that an AI agent can do for you. But of course, website design and hosting will change. It changes all the time and will rely more on AI in the future. That's what you also see with today's sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking, and boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever try to get a new dev blog or documentation site index properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something. or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vibe coded, Squarespace makes it incredibly easy. Head over to squarespace.com/ironodes for a free trial. And when you're ready to launch, use offer code iron codes to save 10% off your first purchase of a website or domain. Now, back to the video. So clearly an accounting service website hosting is not that easy to just vibe code or let it be taken over completely by AI agents. But there are more examples like this. Take a payment provider service like Stripe. That's not something you can just vibe code into existence. You're going to need to make agreements with payment services such as credit card providers, banks, Apple Pay, CLA or even services that are country specific such as Ideal in the Netherlands or mobile pay in Denmark and Finland. And that assumes that you know what the right prompts are to make AI create something like Stripe. It's just very hard to do. It's easier to just use Stripe. It's all about convenience in the end. Now, will AI be more integrated in Stripe? Absolutely. Stripe already offers an MCP server to access their API. So that's already something that you can use right now. So we'll still use these software as a service platforms. That just be slightly different. They'll rely a bit more on AI. They offer different types of integrations, but at the core, they still provide a surface to you, and that will not change. Or a final example, a music software as a service like Spotify or Apple Music. I mean, yeah, you can vibe code a music player. You might even be able to vi code something that stores an MP3 in the cloud and streams it to a client, but of course, that's not really the core of the surface. The core of the service is music. And if you want music, you need to make a deal with a music label, and that's not going to happen just like that. We can't do that as individuals. So, we're going to need to rely on a service for that. Uh, I'm not even sure how agents fit in here. I mean, we just want to listen to music. Maybe we could let an agent listen to music for us. To me, what these examples clearly show is that often the problem that a SAS solves is not code related. I mean, writing the code is actually the easy part when you think about it. Accounting software solves being able to follow rules and regulations in your specific country and providing a transparent view of the finances of your business to an accountant. Website hosting solves domain registration, making sure that the hardware is there to host your site and that it's fast. Stripe solves payments, connections with banks, credit cards, payment systems, tax compliance, offering a payment flow, etc., etc. And a music service like Spotify being able to listen to any music you like without getting fined for copyright violations. Now, all of these things are way more than just the code. And of course, they might each offer integrations with AI systems so that agents can work together with these platforms. I mean, maybe it would be helpful to have an agent that can search for music, particular kind of things that you like or keep an eye for uh bands or musicians that are close to uh your interest so that when they uh I don't know, release a new album or something, you get a notification. But we don't even need an agent for that. And if you look at what an agent actually is, that's not even that clear. We have the LM component, right, with the MCP server components, which are basically APIs that an LM can use, and some other systems that surround that, like uh scheduling, for example. And of course, that allows us to do lots of very powerful things, but it's not really different from what software as a service companies are already doing. you're still going to need software as a service platforms. They just offer different integrations and might themselves use agents to do certain parts of the work. Another thing to realize is that the chat interface is not something that works for everything. I mean, a graphical user interface is useful. It's it's not all going to be replaced by chat bots. So all of these tools will exist next to each other except that with LMS and agents, we just have a new way of doing things for us. So you'll still use Stripe, Spotify, or any other SAS because that's just cheaper and more efficient than trying to vibe code your own payment platform or your own music listening experience. These platforms don't just solve an IT problem. They do much more than that. Now that being said, there are SAS platforms out there that solve like a very small, very specific IT thing that are really nothing more than a CR application. Uh for example, a SAS that connects to API so you don't have to write your code yourself or I don't know simple internal uh data dashboard SAS. I think these type of products they are going to have a hard time surviving in the next couple of years and that's also where we see a lot of disruption. I mean many uh companies are now starting to use these code generation tools to very quickly build internal tools and dashboards and that's you know almost as easy as just using these existing software as a services. So, is software as a service dead? I don't believe so. I mean, it's going to change, but as long as you solve a real problem like payment, accounting, a vehicle rental, or if your product relies on content that's hard to access without it, Spotify or even YouTube, you still have a reason to exist. And those companies are still going to need engineers to work on those complex products. Now, will the work that engineers do change? Of course, it will. But that's nothing new. It's always been like that. We might need less engineers in the future, but we will likely work on way more interesting products. But what do you think? Do you agree with me? How how do you think we'll be using SAS platforms in a couple of years? And how do you think AI LMS and um the integrations that it offers? How how do you think it's going to affect SAS platforms? Let me know in the comments. Now, if you are working on a SAS platform that's dying and you want to shut it down properly with minimal headaches, check out this video next where I share my own experience doing that. Thanks for watching and see you next

---

## 32. I Designed a Python SDK That Relies on… Deep Inheritance?!
**Channel:** ArjanCodes | **Views:** 18K | **Date:** 7 months ago | **Duration:** 22:58 | **ID:** Ov8JsEnNLCM
**Link:** https://youtube.com/watch?v=Ov8JsEnNLCM

### Transcript:
This video is brought to you by Squarespace. Have you ever written something like this? I mean, it works, but it's also messy, repetitive, and easy to break. In this video, I'll show you how to turn that into this. No manual headers, no messy JSON, just clean, expressive code that feels native to Python. That is what an SDK offers. I'm going to show you a really cool design that works incredibly well. And you're not going to believe this, but it uses classes and multiple layers of inheritance. Blasphemy. Let's dive in. Now, in order to show you how to build an SDK like this, here's a simple fast API app that I'll use as a test back end throughout the video. So, the API is very basic. It supports a basic CRUD for users. So, I have a simulated database, just a dictionary, and then uh it expects some sort of API key. Of course, that's all hardcoded here. Normally this would not be here but you probably get this from a database or something. Then there is some code to check that the API key is there. I have a pideantic model for creating a user and for representing a user. So user has an ID, name and an email. And then I have some endpoints. So you can post to the users endpoint. So that's going to create a user. We have a get request to get all the users or get a user by ID. We have uh an update user and we have delete user. There's nothing special here. And as you can see, this particular API is secured with a simple bearer token. Now, without an SDK, this is the kind of code you would need to write to interact with this API. And like I mentioned, there are several problems here like you need to set these headers manually. Uh you need to parse the JSON. Uh there's no models or validation here. If you do this for multiple different types of resources, not just users, but maybe also other things, then you get a lot of code duplication. And overall, it's really easy to forget something or misname a key or make a typo in the URL or something like that. And we want to avoid that. Now, an SDK is typically a Python package that provides a layer around an API like this. So, you can interact with the API in a more Pythonic way. And most bigger software as a service platforms, they have SDKs in different programming languages and I use them all the time. For example, Stripe has an SDK that I use regularly to get customer or payment data from their API. I don't directly call the REST API. I use an SDK and that's typically a better way of doing things. In this particular example, how would we get started with building an SDK? Well, one thing that you're going to need that is going to be very helpful is to have some model of the resource. In this case, that's a user. And let's use pideantic for that. So from pyantic, I'm going to import base model. And then I'm going to create a class user which is a subclass of base model. And the user has an id, name, and an email. And currently we say that email is a string. Uh you can also use the email string type instead. that will be a bit more precise. Now that we have the resource class, we can add methods to that to make retrieving a resource easier. So for example, I could create a class method that's called find. And find is going to uh perform this call for us uh the one that we have here. And then it's going to return a list of users for each of these user types. Not sure why my AI is generating that, but for now it doesn't matter. So now we have a find method and we can even add a return type which in this case is a list of users like so. Now of course we also need to pass a header. So what you could do to solve that is basically supply a token here and then now when we do the get request and we pass this as a header. So now what we can do is we can use user.find find and then we can pass the secret and then we can print the users and we have now user objects. So let's see what happens if we actually run this code. So again I have my test API here. I've started a simple uh server using a UV cord and now let's run our example code and see what we get. And indeed we get a list of users. In this case there is a single user because this API that we have here has a database with just a single user. So this is really the core of what an SDK is. So instead of having to call directly uh endpoints like this, what we do is we supply a class with uh data ID, name, email in this case and we have methods that do the low-level operations for us. However, uh this is not really a great way to organize the SDK. The problem is that your API is typically going to have multiple resources. There may be users, there might be orders, there might be products, there might be other things depending on what type of SDK you're developing, right? And if we create a class like this for each of these uh resource types, then well, we're going to need to add all of these methods. They're going to be almost the same except for this little bit right here. Uh there might be varieties in what it returns. Maybe we'll have to do some custom conversions. I mean, you never know. But that means we are going to have a lot of code duplication in this case. So then you might say well why don't we move all of those methods then into a separate client class which kind of makes sense but also there you want to be careful that you're not creating some sort of god API client class that has a get user create user update user delete user uh get invoice update invoice delete invoice etc etc and 30 more methods like that. If you have a client class that looks like that, it's impossible to test that in isolation. It violates a single responsibility principle which by the way is already an issue here because our user class is used to represent some resource but it also has a responsibility of interacting with the API. So that already arguably violates a single responsibility principle. And on top of that, if you have big client class or a big user class, that's hard to document and maintain. Now, before I show you a much better way to do this, I'd like to take a minute to talk about this video sponsor, Squarespace. Squarespace is an all-in-one website platform designed to help you stand out and succeed online. Whether you're just starting out or scaling your business, it gives you everything you need to claim your domain, build a professional site, grow your brand, and get paid all in one place. I've used Squarespace myself to launch websites for my businesses. And as someone who builds software and teaches software design, I really appreciate tools that are thoughtfully designed and just work. It's really easy to get started by using Blueprint AI, which generates a fully custom website based on just a few prompts. There are a ton of templates that all look really good. After that, it's trivial to add sections or change the appearance to your liking, and boom, you have a full professional website. Squarespace comes with integrated SEO tools, so you don't need to worry about optimizing meta descriptions or generating site maps. It handles all of that automatically. If you ever tried to get a new dev blog or documentation site indexed properly, you know how frustrating that can be. With Squarespace, it's baked in. And with the built-in analytics dashboard, you can track traffic, engagement, and even revenue if you're selling something. or you can use to figure out what landing pages are working and what needs to be improved. Whether you're building a site to showcase your portfolio or launch a software as a service that you just vioded, Squarespace makes it incredibly easy. Head over to squarespace.com/rincodes for a free trial. And when you're ready to launch, use offer code iron codes to save 10% off your first purchase of a website or domain. Now, back to the video. So, the first approach that I want to show you is to build a simple reusable low-level HTTP client wrapper where we are basically going to encode this type of behavior in it without being too specific about the type of resource that we're dealing with. Here's an example of what a class like that could look like. So, we have an API HTTP client class. So, that's the low-level HTTP client. It gets a token when you create it. So, you only set the token once. It has a base URL. In this case, this is a locally uh running API, but you could replace it, of course, with your own API. And then we create the client based on that from uh by using the HTTPX package. And there we set things like the headers. Then we have a couple of helper methods to make interacting with that client a bit easier. So we have a request method which uh gets a method. So that's get, put, post, delete, etc. It gets an endpoint. So that's something that's being put after the base URL. And there's extra data, JSON data, other things that you might want to pass. Then it calls the request method on a client and it returns the response. And then on top of that, I've created a couple of smaller helper methods uh that basically can do the most common HTTP verbs like get, post, put, and delete. And of course, you can add more if you wanted to. The nice thing about doing it this way is that all the low-level HTTP REST API stuff is now in this HTTP client. And at the same time, the client doesn't need to know anything about specific resources because it only deals with endpoints and URLs. What you can then do in the user class is instead of putting all this low-level stuff in the find method, we can simply use the client. And this is what that looks like. So we have a new class method find which doesn't get the token anymore because it's not needed. It simply gets a client. And then it uses that client to retrieve the users and then turns it into a list of user objects. So this already removes a lot of the duplication. Now if you want to use this in your main script or notebook or wherever, then this is what you're going to need to do. You need to import the client, create an instance of that client, and that's where you pass the token. So this would typically be something that you read out of an environment variable. Here it's hardcoded just for simplicity. Then you can use the user base model to interact directly with the API. So we just call user.find. We pass the client as an argument and then we get a list of users and we can print that. And when you run this code, you see that this actually works. Now it looks slightly different here because I'm printing each user individually. Of course, I can also just print all the users like so. And now when we run this, we get exactly the same result as we had in the very first version of the code. Now, like I said, this is already much better than the first version. It's very explicit. We have to create all these different objects and whenever we call user.find, we pass the client around. So that also makes it really easy to test. But having to pass the client all the time also makes things a bit more cumbersome because that means that anywhere we interact with this API, we also need to have a client or create a new client uh find a token from an environment variable etc etc. So that's kind of annoying and what you could do kind of a different approach to passing the client around is to go for a sort of global client kind of singleton style import. So in that case, what you can do is take that API HTTP client class that we built and then change it to a module with functions and import that directly in the user module. Here you see a new version of this low-level HTTP client. It's no longer a class, it's a module with functions. And as you can see, I'm storing some helpful information like a reference to an HTTPX client, the token, and the base URL. Then I have a function called set credentials that gets a token and then creates a client. Now of course you could add more functions here to also change uh the base URL and things like that. I haven't added that here but you could do that. Then I have functions to do all sorts of interactions with that lower level client. So there's request method just like before but it uses this client variable right here. We have a get, post, put, and delete function. So there's no need anymore to create an HTTP client. And what you can then do in your user model is directly import this module. And then we simply call client.get. And we don't have to pass it as an argument to the find method. And when you use this, well then initially when you launch your application, that means the first thing that you need to do is initialize the client with your API key. That's what you do by calling the set credentials function. That's where I pass the token. This sets up everything, creates the hpx client and then you can call use.find or uh any other resource dot whatever helpful method it has and then get the result directly. So that makes it a lot easier to work with the API. Another nice thing about this that you configure the client in a central place in this case in the main file and then you use it everywhere else which makes it really easy. Now of course there are also some cons because this means we've now introduced a global state and that means this is now harder to swap out in tests and this is also less explicit than actually passing the client to the find method like we had before. So that's clearly a trade-off. Do you centrally configure the client as a sort of singleton but that then introduces lots of dependencies makes it harder to test but makes it easier to use for the end user of your SDK or do you explicitly need to pass the client around which makes it easier to test but it might be a bit harder to use for your users. Now whether or not you choose to pass the client there is another issue with uh the methods that I added to the user class right here and that is that well like I said most APIs have multiple resources and if you follow this design this means that even though we made these uh methods shorter you'd still need to copy paste those methods to all the different resource models because your users or invoices or orders or whatever you have in the API you all want them to have a kind of similar setup. They will all need maybe a find method or a update or a save method. And you want to be sure that all of those things follow the same name and the same uh overall layout, right? Because we don't want uh one uh resource type to have a find all method and another just a regular find method. It's going to be confusing. Now, there is a really cool thing you can do, which is rely on inheritance and generics to define standard behavior for these things. And I kind of find this funny because I'm always giving off on inheritance and they have to be careful using it that I almost never use inheritance. But this is actually a really good use case for it. Now in this version of code actually nothing changes on the client level. It's still the module with the functions to interact with the API. But our user is no longer a subclass of base model directly from Pantic but from another class called base API model. And this is what that looks like. Now base API model is a subclass of base model. So we have this layered inheritance that we all hate, but it actually works really well here. And on top of that, this is a generic class. So let's break it down. What's happening here? So the first interesting thing to note is that we have a number of standard methods. For example, there is a save method. So what this does is that it either uses post or put to either create a resource or update a resource. So that's the save method. And as you can see this is independent of the particular type of resource. It just gets the data. It then checks if there's an ID. If there is one then we already have a resource in the database. So we simply use update. If there is no ID we create the object. And then once we've done that, we make sure that the ID matches the ID that we get back from the JSON data in the response. That's what's happening here. This is independent from the resource type. Then we also have a delete method. So if there's no ID, we can't know that the object is actually being created in the database. So you can't actually delete it. But if you can, then we call client.delete. So again, client is something that we imported. That's the same thing that we had before. So also delete is independent from the resource type. Right? Then now this is where we start to use the generics aspect of this particular class. We have a class method called load and this gets a class of a particular type. That's the generic type and it's going to return an instance of that type. So this loads a resource by ID. It again uses the client. it constructs the resource path from this class variable that we defined at the top. So for example, in the case of our users, that's going to be the string users. It constructs the endpoint from that by adding the ID of the resource that we pass as an argument. And then it returns the JSON data of that into an instance of that particular class. Similarly, we have a class method find that returns a list of these generic type thingies and that does a get request on the endpoint as well but without the ID and then we turn that into a list by uh converting each JSON object into a class instance similar to what we had here. So in a sense what we did here is we created a generic version of the find method that we had before on the user class. And now the user class is actually really simple. This is what it looks like. So we import this base API model that we just created. And then I have a class user that's a subclass of base API model. And we pass the user as a type. So that's the generic type. And then an important thing that we do not need to forget is that we need to set this resource path. So what is the endpoint and that's in this case the string users. So this is all we need to do. And now when we've done this we get all of this behavior from the base model for free. And that means you now get full CRUD behavior out of the box. So this is what using that class looks like. So again we initialize our API with the key. So then we can use the save method to create a user. We can change that user and save it. And then we can also find all the users. And because we're using type annotations, we actually get the correct types here. So uh user.find for example gives a list of users. So all of that works really well. And let me show you what happens if I run the third version of this example. So what that does, it's going to create a new user. But of course, this older other user is also still here. So, we should see a list of two users at the end. Well, there you go. We still have our first user that was in the database, but then we have the second user, which is this one, and that's uh the user Alice Smith with this particular email. And why is that? Well, first we created this user right here, and then I changed the name to Alice Smith. Let's create another user. And then let's change the email address to Ironcos.com like so. So when we do this, we now have our Aron user with the correct email address. You might wonder where the Alice user went, but actually the uh test API was restarted because I changed the file because it watches the folder. So, uh, it's maybe a little bit confusing, but in any case, this almost looks like some sort of OM, but in the background, it's just calling a REST API. And what I really like about this is that the interface is clean. It's very Pythonic. It's really easy to reason about and to use the full advantages here of Python's type checking. So, at this point, I'm actually quite happy with this design. All the CRUD logic is centralized in our base API model. It means that uh all the resource models are going to be really simple to define. It's basically just the pyantic style base model and we define what the resource path is. So it's easy to scale. Uh it's fully type safe because of pyantic and generic uh return types and it's also super intuitive. Now there are trade-offs here. One is that we are directly dependent on the client. So there's more tight coupling. uh that means it's harder to test without mocking the HTTP layer or monkey patching. Also, you can't really easily switch APIs, right? If you want to switch from a REST API to GraphQL, for example, that's going to be harder. And of course, when you define these resources, you still need to set the resource path. You shouldn't forget that because if you do that or you set the wrong path, then well, it's not going to work as intended. And of course, there are things you can do to take this to another level. You could abstract this further and for example uh auto discover this resource path based on the class name and adding an s or something. You could add query parameters to the find method and make it more generic. You could make it support pagionation either via skip and limit or via the last ID of the page. You could add a cache layer. You could add population if you have resources that are related. The key is that this pattern gives you a really good foundation to iterate on. The full code example of this as usual is in the Git repository. So feel free to check it out and use it for your own things. Maybe I should release this as a package fast SDK or something you know. Uh if you call something fast then by default it's going to be popular. Maybe I should build in Rust as well. That's like a recipe for success. Anyway, I'd like to hear from you. What do you think of this design? Would you design an SDK in this way? What would you do differently? Let me know in the comments. Now, in the end, this is all about keeping things clean and simple. And there are a few basic things you need to know that will allow you to do that almost automatically. Check out this video where I show you exactly what they are. Thanks for watching and see you next

---

## 33. This Is Why Your Python Code Turns Into Spaghetti
**Channel:** ArjanCodes | **Views:** 37K | **Date:** 7 months ago | **Duration:** 21:10 | **ID:** SNqwNILX1Gg
**Link:** https://youtube.com/watch?v=SNqwNILX1Gg

### Transcript:
Take a look at this example. It's a Python script that takes an input image and then applies a couple of filters on it. Now, as you can see, there are some imports here with different types of filters and then there's a process image function that then uses these filters and then creates an actual image out of that. Now, this looks pretty innocent, right? But if you take a look at the process image function itself, you see that again we have these filter imports and then we have the actual process image function which then checks what kind of filter it's dealing with and depending on the filter it's going to apply different settings to the filter. Now you might be able to improve this design a bit by maybe I don't know moving all this code to the main module but then your main module becomes larger. You may try to improve the design of these various filters to make them more the same. But the problem remains that this function and the the main module as well, they both depend on specific filter imports. And if you add more filters to the system, that basically means you're just going to get more and more and more imports. It's fragile and it's hard to test. And if you need to add new filters, you need to edit these files over and over again. And this is how you get spaghetti code. So in this video, I'll show you how to fix all of this with abstractions. Python doesn't force you to use them, but once your project grows beyond a few basic modules, abstraction can actually be really helpful to reduce imports and therefore coupling and also make your code a lot easier to test. Now, I'm going to use image filters in the video as an example, but this is about making any Python code easier to work with, extend, and understand. If you want a deeper guide on software design to learn the seven steps to come up with a great software design, get my free design guide at iron.co/design guide. The link is also in this video's description. Now, abstraction. Why is that so important? Well, in a sense, what you're trying to do is you want to shift your code from describing what it does to kind of how it does it. So instead of specifying concrete implementations, you try to bring that all together under one thing and that basically describes the process of how those specific implementations typically do things. Now when you rely on concrete implementations everywhere just like I've showed you in the example before that means you create a lot of uh tight coupling your code becomes harder to reuse uh you may end up with circular imports. Basically, it quickly degrades the quality of your code. Now, Python lets you avoid this by using various types of abstractions. So, then the question is, how can you set this up differently? Now, before I show you a few different ways to do this, let's first take a look at the original code. So, I've already showed you the main file and the process image file. So, that's what these look like. But, we also have a folder with filters. Uh, this is at the moment pretty basic. We have a a grayscale filter class and we have an invert filter class. And this is what these files look like. So you can see these are basically classes with some properties and attributes. Like in the case of the grayscale filter, we have an intensity, we have a name, and then we have an apply method that basically applies this filter with a particular intensity. And then we have a configuration method. So you can actually set the intensity of the filter with a dictionary of values. And the idea is here that you can basically configure this with different types of dictionaries. And in the future we can add different types of attributes so that we can uh change various properties of this filter. And in case of the invert filter there is no special behavior here. It simply has a name and it inverts the image and that's it. And I'm using the pillow package to do all this work. I didn't code this up from scratch obviously. Now let's take a look when we run this code. I'm using UV by the way to manage this project. But basically what I have here I have an input image which is um this very handsome guy. And we're going to apply these filters by doing a UV run main.pi. And now it has done this. And now we get the inverted and we also have a grayscale image. So that's what this code is doing. Now the first thing you might want to consider is using abstract base classes. This is kind of the classic way in Python to do abstraction. So what happens in that case is that you take these filter classes and you try to abstract away from them. So we have the uh classes at the moment they're very specific, right? We have grayscale and we have invert. But what you would do with an abstract base class that you provide a sort of contract for these things. So what I've done is I've created another version of this example that uses abstract base classes instead of this just randomly defined classes and that's in this folder. So in principle my main file still looks roughly the same. We still create these specific filters and we import them from these modules. But if you now look at the filters folder, you see that there's a new file called base and this is the base of what a filter is supposed to look like. it's a contract and that is exactly what abstract base classes do. So in this case you see there is a property which is called name. So each filter needs to have a name. Uh we also uh have an apply method. So that basically applies the filter to an image. And we have a configure method that sets configuration parameters for this particular filter. That's what this looks like. This is the contract. And now what you can do is modify these specific filter classes so that they follow this specific contract. So grayscale filter is now subass of filter base. It has an intensity which is an internal thing. That's why I added a underscore here. Uh we have a name just like before. And then we have an apply method that applies that filter with a particular intensity. And we have the configure method. This is not very different from what we had here because basically the interface stays roughly the same except in this case we made it a subclass of filter base. We now have a contract that we have to follow. Now the invert filter looks a bit different. This of course also a subclass of filter base. It implements this contract. And in this case I just uh added a simple setting. You don't even need to do that. But now instead of in the previous version where we had a do invert which is of course different from apply in this case I changed it so that it follows the contract more strictly. So it also has an apply method now and it has an enabled option. This by the way might be something you could even move into the abstract base class so that any filter can be disabled and enabled anyway. And I've added a configure method to this. So now the invert filter also follows exactly the contract. What's interesting though is if you look at the first version of the process image module. So that's this one here. Basically my uh filter object had a type any because it could be basically anything. And then I check with is instance call whether we're dealing with grayscale filter or with an invert filter. Now this is not a good example of great code right. If often if I see is instance calls, it's kind of an alarm bell and I try to figure out why they're there and if there is a way to avoid uh using them altogether and maybe replacing it with some abstraction as I'll show you in a minute. But uh of course in this case you could fix this kind of without abstraction by basically making the filters more the same. But then why not just use an abstraction and make it official. So if you do that, if you do make it official, then this is what it looks like. So as you can see, this is way shorter. What I've done now is I've imported the filter base. There's no more is instance checks. I don't need to do that. And I supply a filter object here. And then instead of using any, I can now actually specify the type. And then I apply the filter. Simple as that. Now I made another little improvement here which is that the process image function in this first example actually directly sets configuration things. Now that doesn't make a lot of sense and that's more of a design issue of where you should do certain things. So instead what I'm doing in this version of process image is that I don't apply any settings. I simply apply the filter and save the image. That's the responsibility of this function. But then in the main file you see that that is actually where we set the intensity. Now by doing this by introducing this abstraction it has a couple of nice effects. So one thing is that it kind of forces you to make sure that there is one place where you're basically putting all of this together and that is in the main function. So that's where we have the specific imports. That's where we're saying hey we want a specific grayscale filter or we want an invert filter that is enabled. And then the other methods, other functions, other modules, they don't know that much because they're relying on abstractions. So a process image for example, it has no idea what kind of filter it applies. It just knows I just get an object that has this contract of filter base so that I can call methods on it or that I can print the name of this thing because that's specified in a contract. And that is exactly what abstraction achieves. And as you can see now also in terms of imports, I don't have to import specific filters here anymore. Of course, I still need to do that in the main function right here. I still import them because well, that's where I'm patching up everything. And that's typically something you want to strive for that in your codebase. You're going to end up with basically one place where you're putting together everything. That's the like the dirty spot in your code, right? every clean room maybe perfectly clean, but there's also always a little corner that is a bit dirty and we need that. We need that dirty corner and that's also the case in code. We have one place where we patch everything up and often that's going to be the main file. That also gives you a kind of measure of how successfully you've decoupled your code and applied abstraction. Is there a single place in your code that is messy and the rest is clean? If that's the case, then you'll probably do a pretty good job. Now, this is abstract base classes. And one of the things that you see with abstract base class is that we need to have this inheritance relationship, right? That's how it works. That's how Python verifies that you actually implement the contract. For example, if I remove this subclass so now it's just a class. But if you look at the main file, you see that we now get an error here that inverts is not assignable to filter base. It's not a subclass of filter base. even though it implements the methods and the properties with ABCs you need that explicit relationship that inheritance relationship uh that also has advantages for example it means that you could store some attributes in the base class and then that attributes is accessible in all the subasses that can be helpful uh for example in the case of this enabled attribute you know if you want to have that for all filters then you would move this to the abstract base class but you might say, well, it's maybe not so Pythonic to do it this way. I mean, Python has dock typing that works different than this system of sort of static type checking almost. So, as an alternative, what you can do is use protocols, which is more closely aligned to how Python's typing system works. And I know there's people who don't like them. There's people who don't like abstract base class. I'm kind of in the middle. I use both, but I typically tend to use protocols more than abstract base classes. So, how are they different from the abstract base class examples that I've showed you just before? Well, if we take a look at this folder, which has a protocol version of this, if you take a look at the filters folder inside that, you see we're back to two files. There is no base class here. And if you take a look at the actual class definitions of invert filter and grayscale, you see that there's no inheritance relationship. Now, that saves you an import, which might be nice, but at the same time, it's basically up to you at this point to make sure that when you're writing this code that you're actually following the contract of how filters should work. On the other hand, this is a key difference in the process image module. You now see that we have a protocol class that specifies, hey, what does a filter base look like? And this is then what we pass as an argument. Same as what we had with the absolute base class, except that we're defining the protocol here because we're purely using it to specify the contract of this particular argument of the process image function. But there's no inheritance relationship and that means that I can create filters using different classes with different methods. However, uh of course when I go to my main file and I try to use them, well then at that stage we're going to get a problem. Right? So in this case for example uh let's say I go back to my invert filter class and I'm going to rename this to apply invert. I don't get any problem here because well this is just a class it's completely independent but in the main file we now see that there's an issue that oh actually this is incompatible with the filterbased protocol because the apply method isn't there. So that's when the check takes place. So that's quite different from how ABC's work. Final thing I want to show you is that I've now shown you how to use abstractions with classes but it might make sense to use functions instead. In this case, a filter is basically nothing more than a function. Right? So, here's another version of that same example, but now I'm using purely functions. And you might think, oh, that's actually much better. There's definitely some advantages to using functions, but there's also some limitations as you'll see in a minute. So, with these filters, I've added the third one because why not? Now, actually, the definitions are even simpler. I have my uh grayscale function that's simply a function called apply grayscale. It gets an image. It gets specific configurations for particular filter and returns an image. And same for invert. So this basically only gets an image and then I'm applying that filter and that's all there's to it. And then I have another example here which applies a sepia filter. Doesn't really matter what it do. But as you can see, the nice thing about using functions here is that these are actually really simple. But how do you then proceed to use them? Well, in my process image functions, so function, what you can then do is kind of abstract away by using the callable type. So that's what I'm basically doing here. So here I'm saying, hey, my image filter function is a function that takes an image and that returns an image. That's basically all that it does. And then process image. Well, it gets this image filter function and it applies that function to the image and then saves it. And that's all it does. And probably this needs to be image dot image. Yeah, that's probably better cuz if I go to the definition here, there is no definition. Okay. Tens in Python, they're not perfect yet. Anyway, this is how I've set it up. It's quite similar to the protocol version in the sense that we define the type of the thing that we need here locally. It specifies the interface of process image. That's interesting. These are both quite quite close to how Python's typing system works. However, of course, now we have a problem because uh grayscale is actually not a valid image filter function because it gets an intensity. That's where the magic of functional programming comes in. I'm back in the main file and what I've done here is I've imported these filters. So I have my grayscale filter which takes an intensity. So it's not a valid uh image filter function. But now I created another function called make grayscale filter and this returns an image filter function. And how does it do that? Well, it uses a closure. So defines another function that is actually a valid image filter function because it gets an image and returns an image. But then inside that function we use the intensity that I'm passing as an argument here and then I return that function as a result. So this gives me a grayscale filter function. So I can call this and then I have my function. And same for the invert filter. So I can have an enabled setting if I want to and then I can define a filter function that is exactly an image filter function and then return that as a result. And then basically what you can do here is create your grayscale function by calling make grayscale filter with a particular intensity and then I can call this function and same thing for the invert filter. So now I'm using these functions but well it's a bit cumbersome that we uh have to do this right it's a bit annoying. So another thing that you can do is use something called partial function application and what this does is basically the same thing as make grayscale filter or make invert filter is that it defines a new function that it returns as a result and it does that by already applying some of the arguments. So what you can do in this case is call partial which is partial function application from the fun tools package and then we supply it with the function which is apply grayscale. So that's our function that gets an intensity and then we can pass an intensity as an argument. And now what happens is that we don't need this make grayscale function anymore with the closure because basically what partial does is exactly the same. It takes apply grayscale. It sets the uh intensity and then it returns a new function where this intensity is already applied as an argument. And now we can use that to call process image. And I just realized that actually I copy pasted uh all this code here. But actually of course the way that you would do this is by calling a return apply gayscale with the image and the intensity. And for the invert filter, it would be exactly the same. So that's a bit more correct. So this is also pretty nice in terms of design. Now there are some limitations to partial function application. For example, if you go back to the process image function and you want to print the name of the function. So normally an object has a dunder name property that you can print, but actually partial doesn't return this. So you don't have access to that information. And that's just some of the limitations that you run into when doing something like partial function application. And by the way, if this video helped you clean up your Python thinking, hit the like button. It helps me a ton if you do that. And while you're here, why not subscribe to the channel so you won't miss any future videos. In short, it's really easy to end up with spaghetti code if you're importing modules all over the place. It starts small, but it becomes complex very quickly. So if you encounter this in your code, think about applying abstraction. If you need to abstract from functions, use callable. If you need a structive class hierarchy with maybe a base class that has already some instance of variables with values that are used in subasses, use ABCs. Or if you need something that's closer to Python's typing system, use protocols. And if you're really into functional programming, use things like closures and partial function application. But be careful because if the developers in your team don't have a lot of experience with functional programming, then it's maybe not the best solution either. But now I'd like to hear from you. Do you use abstraction? Which form do you prefer? Callables, protocols, ABCs? Let me know in the comments. Now, I used partial function application in the code example. If you want to learn more about that, what you can do with it, as well as some of the other features of the fun tools package that I imported that from, check out this video next. Thanks for watching and see you next

---

## 34. Why Dataclasses Disappear in Real Python Applications
**Channel:** ArjanCodes | **Views:** 112K | **Date:** 7 months ago | **Duration:** 14:30 | **ID:** NRIgLNzmnYY
**Link:** https://youtube.com/watch?v=NRIgLNzmnYY

### Transcript:
Let me show you something interesting. This is our main repo with a bunch of uh different automations. It's basically most of the Python code that we write at RN code is in this backend repo. This does a bunch of stuff. It integrates with APIs, databases. It's uh listens to events from payment providers, etc. Let me search for data classes. Now, as you can see, there is my examples repo as well that has uh what is it? Almost 800 uses of data classes, but my back end repo doesn't even show up here. It looks like I'm not using data classes at all in our back end. So, when I first noticed this, I was very surprised. Like, what? I'm promoting data classes all over the channel. Basically, really like the feature in Python, and when I look for it, we're not even using it. what's going on? But then I realized I am actually still using data classes, but probably not in the way that you think. But before I talk about that, let's do a quick recap of what data classes in Python actually do and how it differs from other third-party packages such as Pyantic. In short, I've already talked about this in lots of other videos. A data class is a simple way to create classes that store data, and it's part of the data classes module. And this is part of the Python standard library. So what you can do is you can specify a decorator and then you can create a class let's say a book. And that book has a title and it may have an author, it may have a year, it may have other things as well. And the nice thing about data classes is that you can specify the instance variables in this way. And then the data class decorator is going to automatically create an initializer for you. It's going to create a wrapper for this particular book class. It's going to do object comparison and a couple of other things as well. So because this is now data class, I can simply create a book and then I can print that book and when I run this then this is what we get. So it gives me a nice useful representation. Whereas if this is not a data class, so now there's all sorts of things that are not there. For example, the book initializer is basically empty because it's not being generated. So I need to do that manually. And also when I print it, it's not going to give me a lot of information because if I create a book like so and uh then I run this then it gives me an book object very low-level thing at some memory address right so data class solve that problem and make it easier to deal with classes and object that contain data. Now, there's lots of other cool features that they add such as a frozen option read only. You have control over what should be added as an argument to the initializer. Uh being able to use slots for better performance and a bunch of other things. Like I said, it's part of the standard library for quite a while since Python 3.7, so there's no external dependencies. It's tied to Python's own versioning, which can be helpful, and that means it's easy to maintain across different environments. That's actually a huge win. um especially compared to third party libraries that sometimes well let's say disagree with each other. In short, data classes sound like a great feature. So why am I not using it? Well, before I talk about that, let's first go over some of the alternatives. And the main one that I want to mention is Pyantic. Pyantic is a bit like data classes. It's not part of the standard library, but the main thing that Pyantic adds is validation plus a couple of other features as well. That's why it's used often by frameworks like fast API where you know validation is kind of useful if you have an API. Now in the past we had some compatibility issues because of that like fast API depended on paidantic v1 you had SQL alchemy for database access was also changing quite a bit and at the same time pantic v2 changed a lot of APIs and managing these versions wasn't always fun. If you were around in 2023, 2024, you probably remember like I mentioned, paid is deeply integrated into fast API in the request and response models. There's validation and parsing. Uh there's a type coercion. That's also feature of pideantics of converting a string one two three into an int one two three automatically. So that means it's really natural to use fast API and pideantic together. Here you see an example of that. So I have a fast API app. I have my book class for which I'm not using a data class because well it's easier with fast API to use pyantic because that's what it integrates with. We have a function to generate some ID. I just built something that looks a bit like a MongoDB object ID and then you have a bunch of endpoints right creating book getting a book listing books etc. Pretty basic. And then we can run this API. And this is pretty straightforward using UV. And what you can do now is send a request. So in this case, I'm sending a request to this API that receives books. And as you can see, of course, uh when you create this, there is simply an empty dictionary. There is no books whatsoever. There's nothing here. But then what I can do is post a request to create a book. And then later on when I retrieve the list of books then this is what I get. So very basic way of how an API works. Right now an issue here is that book right here is used for both representing a book and for representing what a request like creating a book expects right it's also being used here. And that's not great because you would expect a book in a database for example to have some sort of ID. And it also results in a weird design where if you look at create book, we have to return this dictionary containing an ID and the actual book. And same for get book. So it gets an ID and it gets a book from the dictionary. And that's kind of confusing. Ideally, you'd want to return purely a list of book objects, but that's not possible because a book here doesn't have an ID. I could add an ID, but then I'd need to supply it when creating a book. Okay, you can maybe make it optional, but the list of books that you store in your uh database or in your memory, well, books need an ID, so it's not optional. So, it's it's not really a Python problem actually or a fast API or a financing problem. This is actually a data modeling problem. So, you might wonder, well, is that maybe a place where you could use data classes? And the answer is yes, maybe. So here's another version of that API that now uses a data class book class to model the internal representation of a book right and as you can see it has an ID field now which uses generate ID as a default factory so when you create a book instance it's going to create an ID and then on top of that we have now new pantic models for creating a book which has a title author and pages and for a book response which is an ID, a title, author and pages. I have my fake database again and then I have my routes and now I can do uh this where I'm basically supplying a response model. So that gives my fast API app the information about what kind of thing this response and same thing for getting the books. The response model is a book response and for getting all the books we have a list of book response objects. So that's very neat. So you might say okay great. So that's what we can use data classes for. But of course in a real application this is probably not what you would do. In a real application you'd probably use a database and the package that you might commonly use for this is SQL alchemy. And this is the same example but this time around it works with a real database or at least it works with a SQLite database but you can change this URL so that it's a real database in a cloud. So very simply said, I have my database engine setup stuff here. And then I have my SQL alchemy OM model. So that's basically what we had as a data class before where I have an ID, I have a title, author, and pages. And then again, I have my pyantic models for creating a book and returning a book. And then finally, well, we have some uh boiler plate code for getting the ID, which we're going to inject in the endpoints. That's what's happening here. And then finally, we interact with this database to uh create books, retrieve books, uh and everything that we want to do with books. So, what happened here is that we got a bit closer to what a production application would look like. The data classes disappeared. Now, it's possible that you actually still like the data classes approach of having a data class decorator, right? instead of this uh inheritance relationship that we have here. Well, Pideantic does support two ways of defining the models. So, this is the classic way of doing it, but there's also a newer way of doing it, which is using paidic style data classes and that's what this looks like. So, it's very close to how the built-in data classes works as you can see right here. So, you can use either of these two. There are some differences between pideantic style data classes and the base model in a sense base model has more features because the pyantic data class follows the standard library data class very closely. Here have another example where I'm using both uh data class and the base model to define various types of models. So I have a book model for example using the podantic data class as type one pages and it has a validator to check that pages is positive. That's one of the things you can do with pyantic, right? And then I have an author class that is a base model. So that's the other way of defining the models in pyantic. It also has a valid data by h. Now there are some limitations to pyantic style data classes because it closely follows the standard library implementation of it. One of them is that it doesn't do type coercion. So if you create let's say a book uh with uh string pages that doesn't work. Whereas for an author uh you can actually do that. So for example, if I run this code where I try to create a bad book with a string as pages, then it's going to result in a validation error. As you can see, it wants input to be a valid integer. However, if I have the author here, which is a base model, bantic model, and I change age to be a string like so. So of course, I get a type issue here. However, if I run this, then I don't get any error at all. It simply just converts the type into an int. And there's more missing features from the paid style data classes. For example, you can't do model dump which creates a dictionarial JSON style version of the object because model dump is not implemented on that. It's only implemented on the base model. So even though pyantic data class may feel more Pythonic, they do lack a lots of important features. Finally, these pyantic style data classes don't work all that well with fast API. For example, if you try to change the response model to a pyantic data class, it doesn't work. By the way, if you want to learn how to design better software from scratch, check out my free design guide at code/design guide. This includes the seven steps that I use for building real project. Links in description. So even pyantic style data classes are actually too limited. So it looks like that whatever I do it I seem to end up with code that doesn't use data classes. What these examples show is that when you're looking at production code you're often dependent on data structures that are used by your favorite libraries. Fast API, SQL Alchemy, Pandas, etc. And typically just the standard library data classes don't have the required features that those libraries need. So they use something else. Fast API needs extensive validation. So it uses binance and not the built-in data classes. Pandas has its own data frame structure. It doesn't use a data class for each row in a data frame because that's not efficient enough and it's missing important features. And since we often rely on libraries like this to write our Python code, you're going to use those structures instead of data class from the standard library. So it seems like data classes are mostly useless then. But here's the interesting thing. I actually still use data classes. And how I like to use them is not for these types of scripts or automations. I actually use them a lot for prototyping. For example, if I'm trying to come up with a model for a certain domain and uh get an understanding of how everything works together, I could of course make a bunch of diagrams with mermaid, you know, write some stuff in markdown or whatever or I could uh get a paper and a pen or do it in KFA or whatever or use some of these online drawing tools. Now instead what I like to do is define a couple of data classes, one for each concept in the domain and then I use chat GPT a lot to then discuss the domain and iterate over that Python code. And for me that actually works really well because I find data classes is really easy to read. It's very quick to just write a data class, code up some relationships between classes, write some simple example code to figure out how it all fits together, etc. So to me data class are really useful for well let's just call it what it is vibe domain modeling and of course I use data class a lot in my examples because they allow me to give you a very simple representation of something without having to depend on a database or other things. But I'd like to hear from you. Do you use data classes in production code? What's your main use case for them? I'd love to hear your thoughts. Now, I've talked about pedantic and data classes before, and there's also at the package that data classes are based on. If you want to learn more about that and see a deeper comparison between the pros and cons of each, check out this video next. Thanks for watching and see you next

---

## 35. Serverless on Google Cloud: What You Can and Can’t Do
**Channel:** ArjanCodes | **Views:** 11K | **Date:** 7 months ago | **Duration:** 19:36 | **ID:** EYYhDnPAPcw
**Link:** https://youtube.com/watch?v=EYYhDnPAPcw

### Transcript:
If you want to deploy a Python function to the cloud and run it locally in seconds, well, here's the entire thing. Save this as main.pi, install the functions framework using uv for example, and then run it as follows. And now you've got a serverless function that's running locally on port 8080. This is what it looks like when you navigate to that page. Functions framework is something that Google offers. So, if you actually want to deploy this to Google Cloud, well, things get a little bit more complicated, but it's not even that hard. So, in this video, I'm going to look at what serless actually means and how to work around some of the limitations of serless functions. I'll talk more about that in a minute and then also cover when you should use them and when you should stick with another solution together. This video is sponsored by Palumi. More about them later. Now, what is a serless function? It's nothing more than a piece of code that runs in the cloud without you managing any servers. It's typically triggered by an event like an HTTP request or a popsup message, a file upload. And when something like that happens, the cloud provider handles basically everything, the provisioning, scaling, including to zero billing, which of course you will only pay when that function actually runs. And it's not just Google Cloud that offers this. There's similar offerings across different cloud providers. AWS has Lambda functions. Azure has functions and then there is Google cloud functions. Or if you're looking for a cloud provider in Europe, Scaleway also has serverless functions. Each of these has its own quirks and differences. Today I'll focus on the Google version of that. And like I mentioned, Google offers the functions framework so you can easily test your cloud function locally. And this is obviously very helpful because this helps you build and test your functions locally before you actually deploy it. Now there are some limitations. But before I talk about that, let's actually deploy this function to the cloud. For that, I'm going to use Palumi, who is also the sponsor of this video. Now, when you're deploying something to the cloud, there's lots of different ways that you can do it. For example, you can directly create resources in the cloud provider's dashboard or you could install the CLI by the cloud provider and then create your resources that way. Now, Palumi is a bit different. This is an infrastructure as code tool and that means that you can define what your cloud infrastructure looks like and handle provisioning resources and deployment by writing code in your favorite programming language like Python. And to me actually infrastructure as code makes a lot of sense because you specify what your infrastructure looks like and that's directly part of your codebase. Getting started with Palumi is really easy. If you're using a Mac you can install directly with Brew for example like so. But as you can see Palumi is already installed but it's outdated so it's currently upgrading it. Now like I mentioned when you're deploying a cloud function there's actually a couple of other things you need to do as well and that have mostly to do with security. uh for one you need to specify how the cloud function may be accessed. Is it a v yeah HTTP endpoint and who can actually access that so there's lots of options to think about. So it's not as straightforward as you think but in any case what you're going to need is that cloud function is going to be packaged as an artifact that's going to be uploaded to the cloud. So that artifact needs to be stored somewhere. So for that you need a storage bucket. then you actually need to create the actual cloud function resource and then you also need to specify access. So if you do this using Palumi and Python then this is basically what that looks like. So we create a bucket then I'm creating a bucket object which contains the actual Python resource the asset. Then I'm creating a cloud function with a specific name location. You can specify a build config like which version of Python do you want to use and where that asset is going to be stored that the cloud function is based on. You can configure the service like how much memory will it use uh what is the timeout what's the maximum number of instances uh things to do with scalability obviously and then finally like I mentioned you need to specify how the function is going to be accessed so in this case I'm saying that there is a invoker of this particular cloud function and that's available to all users in principle this means that the function is public and anybody can call it now what's nice is that Palumi actually directly works with UV. So you can specify your dependencies and create a virtual environment with UV. And then Palumi will create a requirements .txt file for you when you deploy the cloud function because Google Cloud Functions don't know how to use UV. They have requirements.txt. Now once you specified your infrastructure in Python, it's really easy to actually deploy that by using Lumi up. Now you see after deployment, actually I made a little mistake, so I had to redeploy it. That's why this looks slightly different. But as you can see, it outputs the actual Python endpoint and that's actually also in the infrastructure is code specification right here at the end. So we get the URL after we deploy this. So when I click this, so now you see we actually calling the function in the cloud and it gives us the same result as we had right in the beginning. Now what is interesting is that if you make changes to this infrastructure then Palumi is going to provision the right resources in the cloud to match those changes. For example, maybe I want to change my surface configuration from a timeout of 60 seconds. I maybe want to change that to only 30 seconds. So I make that change and then I simply run Lumi up and it's going to detect which things are going to need to be uh changed in order to uh do that. So in this case the only thing uh we need to do is update the actual cloud function and then I can perform that update. Now once you've deployed this with the settings that you want you can actually also see it in your Google cloud console. So as you can see that function is right here and I can click that and then we have the HTTP URL. You can attach custom domains to this. You can view the logs to see what's going on. You can add other things like environment variables etc etc. If you want to try Palumi yourself and organize your infrastructure by simply writing Python, go to palumi.com, get started for free. The link is also in description of this video. Now, like I mentioned, there are limitations to serverless functions and in particular if you're running them on Google Cloud. For example, one limitation is that these functions, they use Flask. So if you want to use something like fast API, which is reasonable nowadays, one of the most popular API frameworks, well, you're out of luck. Also, typically Google Cloud offers only a single entry point per function. Now, there is a way to simulate sub routes with a trick that I'm going to show you in a minute, but that's not native. It's not built-in. Another thing is that it uses a fixed execution environment and this is true of serverless functions in general because you're typically dependent on the runtimes that the cloud provider provides. Uh Google for example provides uh runtimes for most of the Python versions. But uh as soon as a new Python version comes out, you may not immediately be able to use that. It's going to take a while before that runtime is available. And if you want something custom, um maybe a different OS or very different setup, well then that's unfortunately not supported. So there's no uh Docker files, no custom base images and of course those are things that are available with other types of solutions. Also, Google Cloud doesn't support UV yet. So there's no PI project to file. Uh it even doesn't support poetry. You need a requirement.txt file. Now with UV it's actually possible to generate a requirements.txt file from the pipe project file. And this is how you do that. So when you run that you see that we get now a requirements.txt file that's being generated. And actually if you're using Palumi for deployment together with UV then it's actually going to call this directly for you. Another issue with cloud functions is that there is no native async support. So even if you use an async function, there is no actual running event loop. And you may also encounter cold start. So that means you have to wait a while before the function can actually run. And that's because uh in particular if you have larger dependencies then well the startup is just going to be slow on the first call and then of course Google is going to keep that uh function alive for a while. So the subsequent calls are going to be faster. But after a while the function will be put back into storage again and then when you recall it you come to need to wait. Finally there's no persistent file system. So there's like a limited number of space. It's mainly uh for you know source code and things like that and it's typically wiped each time you invoke the function. So if you want file system you're going to need to set up something uh separately. An important thing that you need to consider with serless functions and any cloud application is of course security. And that means you need to make sure you use environment variables for things like config, secrets, API keys, those kind of things. And typically your uh cloud provider is going to offer some sort of service for that. For example, if you take a look at Google cloud, there is a secret manager. So the secret manager is a place where you define your environment variables. And then there's also an easy way to access those variables in your serverless functions. For example, let's create a secret uh database credentials. And then I'm going to provide a value like I don't know, let's say some sort of password or a secret value. It doesn't really matter what it is really just to show you. And then we're going to create the secret. As you can see, there's built-in versioning. You can also uh specify how long you want a particular secret to be available. But now we have our database credentials secret. And then what you can do in your uh cloud run function is that you can actually specify access to that. So if I uh click on this function and then I'm going to deploy a new revision and this is going to be similar in most other cloud platforms as well. Then actually I can go here to my variables and secrets and then I can refer to a secret and that's going to be my uh database credentials and for the secret this is the one that I'm going to pick and then you can even specify the version for example the latest version. There we go. And now when you deploy this then this secret is going to be available under this environment variable name in your Python code which is pretty nice. And that also means that you don't have to store this in a git repository or send it to your colleagues via a team's chat or something like that. You just store it here securely. Another security thing is to make sure you restrict access. I've showed you how to create a public endpoint. that's not very secure because once you have the endpoint and basically anybody can call that and do that many times which well it's going to cost you a lot of money. So avoid those unless that is actually the feature that you actually need. And you can use um uh access management in these cloud providers to control access like who has access to these things. Is that everybody or is that like a particular type of user? Um to be honest though, I I do think this is one area where cloud providers can do a better job. Uh I found uh documentation in Google Cloud for permissions and roles typically quite limited. That's also the same for uh for example AWS. I I tried setting up a similar serverless function in AWS. Writing the code was the easy part, but figuring out what roles and permissions to assign to the various task was really a pain in terms of who can deploy it and who can actually access the function. And it was the same thing on Google Cloud. So I feel like a lot of the expertise in using these cloud systems comes in like knowing how to control access and making sure that you're not just you know opening it up to everybody just because you can't figure out how to actually properly control the access. Now on top of that uh Google Cloud currently has two versions of cloud functions. There's the first gen and the second generation and they have different roles and different permissions and well the documentation is sparse and the same goes for the permissions related to deployment. For that you need a service account and you also need to make sure that this has minimal privilege only has the roles that it actually needs. But again, it's kind of painful and you have to resist the urge to just make it an admin so that at least it works which uh defeats the purpose of having such a system in the first place. Cloud providers make this simpler. Okay, rant over. Now I've told you that one of the main limitations of cloud functions is that there is a single endpoint which for basic stuff is nice. But what if you want to have multiple endpoints in a single function? And this is actually possible because of a feature of flask which Google cloud relies on. And the way it works is that you can define an internal mini flask app inside the cloud function and then route the request to your particular internal flask app. That's what I'm doing right here. So here what I did is I have another cloud function that basically allows reading information about a couple of different YouTube channels. So that's all in a JSON file and what I did is I created an internal flask app and then I defined this internal route which is / channels slash and then the ID of the channel and then basically I retrieve the channel from the JSON file but of course you could do this from a database or whatever you want and then in the end we have the HTTP cloud function. What that does, it creates a new app context for this internal app and then it uh dispatches this request to the internal app and then returns the response. So I've now deployed this function to the cloud and when I simply go to the endpoint then this is what we get because you need to specify a channel ID and here I pick my own channel and you see we get a JSON response. So basically this will allow you to develop a fullfledged restful API in a single cloud function which is interesting and this can actually be quite handy for smaller APIs. Well I wouldn't use this for full-fledged APIs. I mean uh I'm not sure how much this extra indirection cost for example but I'd say that's not even the main issue. I mean for a full-fledged API I'd want to use something like fast API. I maybe I want more control over the execution environment. Maybe I need some extra storage. And that means that at the moment at least you're going to need another solution than a simple serverless function. So what are those other solutions then? Well, the thing that we've been using now in Google cloud is called cloud run functions. That's the second generation. It's as you can see it's pretty easy to set up. Uh the scaling is fully managed. Uh like I mentioned the run times are limited with only flask no fast API and one route only though you can get around that and there's other issues like uh cold starts that you may have to deal with. Now an alternative to cloud run functions is cloud run and that is uh more flexible that supports any container. So you can basically create your own Docker container with your favorite frameworks. Install fast API, Django, Flask, whatever you want. You have full access to everything that Docker offers like uh Linux versions, Python versions, use your own base image. Uh you can do anything you like in terms of routing or using a full web framework. Now it is slightly more work to set up. you need a docker file and it's also going to be more expensive because what happens is that you basically give your cloud provider a docker file and say hey host this somewhere and you would typically have one running instance of that uh image at least. So that means you basically paying an amount per month for uh that surface versus uh the surfless functions where you're only paying when they're actually being invoked. Now, you can also do it that way with Cloud Run, but then you will have more cold start issues since you're supplying a custom Docker container and that's just going to take longer than one of these ready to go runtime environments of the serverless functions. Now, if you want to go all out and have a way more complicated uh system that has also uh storage and uh you want full control over orchestration, you have a large system, then you would use something like Kubernetes that also gives you full flexibility over networking uh runtimes etc. Now, of course, a Kubernetes cluster is a lot more complex than just running a serverless function or even cloud run because you need to manage uh scaling, security, updates, etc. I've done this actually in one of my previous companies. It's not even that hard. Uh but uh it is a lot more work than using one of these ready to go things. Now, that being said, it's always a trade-off, right? I mean uh you may not always need complexity and especially in the beginning when you're just starting with a new project having something very simple that's not very expensive but maybe not as scalable in terms of cost uh that might be a very good start but then uh as you progress and things become more complex well you may want to have more control over the aspects of how your application runs and that's when you grow more into the uh hosted docker container solutions or even a fullon kubern Kubernetes cluster. Now, like I said, these serverless functions, they're great for faster deployments, but there are some limitations. For anything uh that's more complex, if you need asynchronous uh if you need concurrency, custom runtimes, full APIs, consider deploying a full Docker container or even a Kubernetes cluster if you're up for that type of challenge. On the other hand, if you only need a really simple piece of code that runs when something happens in another system, let's say you want to process an invoice or something, well, you may want to go for something even simpler like NAM. But I'd like to hear from you. What kind of cloud resources do you typically use? What's your go-to solution when you start? Is that something uh like a serverless function? Do you just deploy a Docker container? Do you vibe code something in uh Devon or Lovable or any of those tools? What what's your go-to solution? Let me know in the comments. Like I mentioned earlier, most cloud providers roughly provide the same types of resources, but there are some differences. If you want to learn more about that, check out this video next. Thanks for watching and see you next

---

## 36. MCP…. So What’s That All About?
**Channel:** ArjanCodes | **Views:** 36K | **Date:** 8 months ago | **Duration:** 13:28 | **ID:** r0QIuI1wpes
**Link:** https://youtube.com/watch?v=r0QIuI1wpes

### Transcript:
I'm interested to see some videos about UV and Python. And as you can see, this LLM actually searches for videos on YouTube. So, there's a tutorial by uh Corey. There's also one of the videos by me. High quality Python content and practical examples. Very much appreciated. Anyway, I'm going to show you today exactly how I built this and how you can build integrations of your own tools with LMS. As you'll see, there are multiple ways to structure this architecturally, and you actually do need to make a choice, but more about that later. But first, I want to talk about the standard that makes all of this possible, and it's called MCP. According to Chat JPT, MCP was introduced by OpenAI, which is completely false. It was anthropic. Interestingly, if you ask Claude by anthropic for what MCP is, it makes even less sense. Multicontroller processing architecture. Seriously, I don't think we're all going to be replaced by AI just yet. What MCP does stand for is model context protocol. It's an open standard that allows AI models like LMS to integrate with external tools and data sources. Now you might think to yourself, why is that even necessary? Don't we have restful APIs already? Why can't LMS just use that? Well, most language models like Jad GPT, they operate in a safe sandboxed environment. They don't have network access by default. They can freely call some arbitrary HTTP endpoint. And of course, a chatbot also needs to understand what your API does and what inputs it takes and what responses to expect. So even if a model could directly call an API, it wouldn't know when to call it, how to format the parameters, what to do with the response structure. And that's where MCP comes in. According to the website about model context protocol, it's like a USBC port for AI applications. And you can also see a little image here about how this is all supposed to be structured. So on the left you have your host with an MCP client. So that's uh cloth or an IDE or different types of tools. Basically anything that can use an MCP and then via an MCP protocol that communicates with MCP servers and MCP servers then again communicate with their own internal things. So in the case of the YouTube videos, you would have a data source, a source of YouTube video information, and then the MCP server provides endpoints that an MCP client can use to search for those videos. I'll show you in a minute how to do that. It's actually pretty easy. But if you're working on architectural decisions for your own projects and you need to consider these type of things, I've put together a free architecture checklist to help you avoid costly mistakes. Just go to r.co/checklist. codes/checklist to grab it. It's full of practical tips, covers all the key things you need to think about. Iron/checklist link is in the video description. Now, let's see how I built this YouTube video search example I showed you in the beginning. Now, the first thing that I did is define a simple module that does the work of searching videos by keywords. And this is basically what that module looks like. Now, it's nothing more than a simple wrapper around package called YouTube search, but it has a function to search for YouTube videos. So, there is a query. So, that's where you would define your keywords and there's also a maximum number of results. And then what this does is that it uses this YouTube search class that's from the YouTube search module to actually do the query and get the results, turn it into a dictionary, and then return that. And then finally, there's also another little helper function that takes a video ID because each view video on YouTube has a specific ID obviously and then it can construct a URL from that ID by simply putting it in this string right here. Now, if you want to then build an MCP server, then this is a server that basically exposes the functionality from the module that I just showed you. And you can do this really easily in Python by using the MCP package. So in this case, what you can see is that we import a class called fast MCP from this package. We initialize this server and I'm just going to call it videos. Could also call it YouTube videos, whatever. It doesn't really matter. Then of course an LM in the end needs to work with strings. So what I'm doing here is that I format a particular video feature into readable string. So I put the title, the channel, duration, description and so on and so on. Uh views might also be interesting and the URL which I construct using that function that I just showed you. And then we go to the actual implementation of the MCP server and that's where we see this and that's actually an interesting thing to talk about for a minute. So basically MCP servers they can have three types of capabilities. They can have resources. That's basically data that can be read by an MCP client. It's similar to a get request in a restful API. The second thing is tools. So these are basically functions that can be called by the LLM. In this case, searching for videos. I've made that a tool. You could have also done it as let's say a resource, but in my opinion, search is maybe a bit more than just uh retrieving some data because it can actually get quite complicated. even though this example is pretty basic. So, we have resources, tools, and the third type is a prompt. That's basically a pre-written template that helps users accomplish specific tasks. That's really easy to actually install this in a locally running LM. So, what I've showed you at the start of the video is claw desktop and you can basically open a desktop config file. So on the Mac that's under library application support clause and then it's a JSON file and there you can define MCP servers. So in this case I have a server YouTube videos command is uv and I'm providing it with arguments namely directory where we have this service defined and then we're calling run on mcpv videos.pi which is this file. And when you do that then in claude you're going to have this little menu here where you can select these different MCP servers that you have. So in this case we have YouTube videos. If I click on this you also see that we have the get videos tool right here. And of course that is exactly what this MCP server exposes. Now what I'm using here is the free version of clot. If you get the paid version there's more capabilities and integrations. In that case, you can also connect with remote MCPs for services like uh GitHub, Stripe, etc. And now that I have my YouTube videos MCP, I can simply interact with this directly in the LM just like I showed you in the beginning. And as you can see, we get a bunch of different video suggestions. If you look at this little block, you see that there's a request. So this is actually how it interacted with our MCP server. And then in the response we have all of this uh data that was formatted by our MCP server. Now it didn't mention the URL uh which I think uh would have been helpful here. So you can probably improve the prompt so that you actually get uh URLs actually from cloud because it does get that as a response. So there's definitely some things to improve. Now if you look at the architecture of this then basically my MCP server currently directly uses the YouTube helper module. In terms of architecture this might actually not be the best way to set things up because that also means that there's now various direct function calls throughout the code. For example, when I'm formatting the video I directly call constructing the video URL right here. And in my MCP tool endpoints I have to call directly these functions. Now obviously this is a very basic example but you can imagine this can become quite messy once you start adding more features or you have a bigger service that you want to expose as an MCP server to your LLMs. Now on top of that if you are let's say building some sort of bigger service it's likely that you already have a restful API that exposes functionality to other clients such as a front- end app. Now, before I show you another version of this MCP server that is architecturally slightly different, give this video a like if you're enjoying it so far. This really helps me out because then I know whether or not to make more videos about this topic. If you don't want to miss those videos, make sure you also subscribe to the channel. Now, back to our MCP server architecture. you want to avoid duplication and therefore it might make sense to not directly call these modules but call your API instead. Now for the MCP server that adds an extra indirection but it also means that if you make an update to your API or fix a bug then the MCP server benefits from that immediately as well. So here let me show you another version but now it relies on an API. So what I did in this step is that I have a new file called video API. So this is kind of my restful API and here I have a simple fast API app that does the video searching for me. So I have a get request for getting videos. This actually searches for uh videos and then returns that as a dictionary using the video base model. And as you can see it also directly calls this video URL helper function to construct the full URL. Then once you have this API, you can create another version of the MCP server that then calls that API instead of relying directly on the modules. And that's what you see here. So I'm using HTTPX as a package to uh do HTTP requests have my uh URL API URL. And of course you can change this to if it's hosted in the cloud or something. But then what we have is just a piece of code that formats the video. So we had that in the previous MCP server as well. But as you can see it here just directly gets the URL from the result. And then we have the tool that actually uses an HTTPX async client and then it does a get request on my API that gives back some JSON data and then you can use that to actually format each of these entries that you get back into a uh response that an LLM can actually work with. So this is an architectural choice that you need to make, right? Uh do you directly call the modules in your MCP server or do you actually build your MCP server as a layer on top of your REST API? By the way, there is a third option with fast MCP. You can actually automatically convert an Fast API application into an MCP server. Here you see an example of that. Now, of course, this only works if your original API was already written in fast API, but then this is an easy way to convert it into an MCP server. You'd still need to think of the architecture for the case where you would like to have both the API and the MCP server. So, this may not work for you, but at least it's possible. Now, I'd say if your service is really simple, then maybe there's no need for a separate layer. Also, if you never plan to use an API that's separate from your MSP server, in other words, if the things you're building is specifically and only for integrating with AI models, yeah, then just keep it simple, obviously. But in many cases, it's a good idea to build the MCP server as a layer on top of your API instead of directly calling functions from various modules. For example, if your company offers a tool that has a REST API, then you can build an MCP server that accesses that API. And in that sense, you can consider the MCP part of your system as simply another user of your API very similar to a web front end. Now, if you're building tools, you're working on back-end systems, think about what kind of functionality you want to expose to AI models. This is all very new, but in the coming years, I expect we'll see many MCP implementations. And I expect that soon offering AI access to your tool via MCP is going to be expected. If you don't do it, then your competitors will. Fortunately, as you've seen today, building an MCP server is not hard, as long as you set it up so it's easy to maintain for you. In most cases, that means letting the MCP talk with your REST API if you already have one. But I'd like to hear from you, though. Did you already build an MCP server yourself? What's your experience been with them? Do you have any tips you'd like to share? Let me know in the comments. If you'd like me to dive deeper into these type of topics, system design and architecture that involves AI, let me know what you'd like me to do more videos about. Now, API design in general is something that's hard to do right. If you want to learn how to do that, make sure you check out this video next. It covers a few crucial things you need to do. Thanks for watching and see you next time.

---

## 37. Writing Python Functions Like a Mad Scientist
**Channel:** ArjanCodes | **Views:** 33K | **Date:** 8 months ago | **Duration:** 15:51 | **ID:** OdDI-5PBpSk
**Link:** https://youtube.com/watch?v=OdDI-5PBpSk

### Transcript:
In Python, you usually define functions use the def keyword like I'm doing right here. You've seen this probably a million times. And on top of that, it's a really basic function that just takes a name and then prints out a greeting to that name. So, when I run this code, then this is what we get as a result. But Python actually has many ways to create functions, some practical, some questionable. Learning about them gives you a much deeper understanding of how Python actually works under the hood. So today I'll walk through a bunch of them, starting with the ones that I use every day and ending with one that's completely bonkers. Trust me, you're going to want to stick around for that one. The first way that I want to show you to create a function is a lambda function. And this is a oneliner function using the lambda keyword. As you can see here, a lambda function is a very simple oneliner that takes one or more arguments and that then evaluates an expression. And you can actually store this function in a variable even though uh that's not really a common way to do it. Of course, if you need a named function, then you should just use the dev keyword. But lambda is great for little anonymous functions that for example you might want to use for simple throwaway functions that you need to pass to functional programming tools like map and filter. Now there are some limitations to lambda functions. uh one is that it can only contain one expression, no statements, no multiple lines, etc. So don't use these for complex logic because it's going to be really hard to read. But ultimately, this code results in exactly the same thing as the very first version that I showed you. The second way to create a function is by using partial function application. I've talked about the fun tools package before, but in particular, funk tools has a partial class that allows you to take a function, apply already some of the arguments, and then it provides you with a new function. So in this case, I have a power function that takes a base and an exponent and it does base to the power of exponent. Very straightforward. Now, what you can do with partial is that you can actually take that function and apply already some of the arguments. So let's say I define another function called square that is always of course an exponent of two. So then I use partial to supply it with the power function and the value of two for the exponent argument like you see here and it gives me a new function square that I can then call with a single argument and then that's going to compute the square of the number. When I run this then this is what you get. partial function application is actually quite nice especially if you need to for example preconfigure some function and you don't want to write a special wrapper for it. Uh it can help you reduce some boiler plate. If you're writing higher order functions in other words functions that get or return another function as a result but if you're not familiar with partial function application it might be a bit harder to read. And of course this is not as flexible as defining a another full regular function. So take that into account when you use this. But in my opinion, partial function application is actually a nice tool and it's kind of underused in languages like Python. A third way to create functions is by using decorators and that allows you to define functions at runtime. So with decorators, you can dynamically modify the parameters or the result of a function by wrapping around it. And this is basically what that looks like. So what I did here is I created a decorator called print result or actually print result is a function that creates a decorator. Print result gets an argument in this case a formatted string and then it defines a decorator that basically prints the formatted string and provides it with the result of calling the function that it wraps around. If that makes any sense. Well, basically here is how you see uh how it works. So we have a double function that takes an integer, returns another integer and then performs some computation. What it does exactly doesn't really matter but I use the print result decorator supply it with an argument so that I can uh print the result of this function and then when I call this then it's going to print the result for me which is nice. So when I run this then this is what we get. But the nice thing is because now this is an argument I can now change this to something else like so for example and then when I run that again then it's going to use that formatted string instead. So decorators are pretty powerful for modifying behavior of functions that we use quite a lot for uh caching values for logging things. Now an issue with decorators that it's can be hard to follow. it's kind of hard to debug especially if you use multiple decorators in a row. It can introduce some unintended side effects. Uh it can mean that you do complex computations even if you're not aware of it. So that's another thing you need to be careful of with decorators. Now designing your functions carefully is really important. Not just what they do, but how they behave, how flexible they are, and how they connect to each other. If you want to learn more about designing clean and scalable Python code, I have a free design guide. you can download. It covers the seven steps that I use when designing new software from scratch, including how I think about designing functions and responsibilities. You can get it at r.co/design guide. Link is also in the video description. All right, the next way to create a function in Python is by using a class with a call dunder method because in Python basically anything is an object. So a function when you define it it's actually an object with a call dunder method and you can actually just define that yourself. So uh if you have a class in this case I have a greeter class and I define the call dunder method that means that an instance of this class is now callable and that's what you see happening here. So I have my called dunder method. I've supplied the implementation here. is the same as in the previous examples but and then in the main function I create a greeter instance and because it has the call donor method implemented I can actually call it as a function in fact it is a function in in Python this is actually how a function is defined it's it's an object with a call down method so this is how you would do that and let's also run this and as you can see we get exactly the same result as in the first examples now there's some interesting consequences of this. This means that functions can actually store state. So you can for example use it to keep track of calls or something like that. In fact, you can even take a function like this and actually assign values to it. So I could do something like main blah equals uh whatever and then I can uh print that and then when I run this then you can see it actually uh prints this as a result. So I'm storing now values in the main function kind of because it's an object just like anything is an object in Python. And you can even add methods to a function which makes zero sense but you can actually do it because it's an object. Now one reason why you might want to do this is that you may want to define a certain type. For example here I've defined a greeter function type that takes a name string as an argument. If you would normally define a function in Python, you would probably do something like this. My function equals and then we have callable that takes a string and that returns a string and then of course collable we need to import from the typing module. So that's how you would normally do it and then you can use this type throughout your code. But the disadvantage of defining a type this way is that you can't supply names to these things. So we don't know what type of string this is. Whereas if you do it this way and then you can use for example a protocol class or something like that then you can actually specify the name of the argument. Now it's a kind of contrived way of figuring out why this would be useful because of course this does add a bunch of boiler plates. This is much shorter obviously and defining a function this way is way less intuitive but you know there is some argument for doing it in this way. Now, we're already getting kind of in weirder territory. Now, the next few ways to define functions are still sort of normal, but keep watching because the last one is just completely crazy. The next way to create a function is by using XAC, and that's what you see right here. Now, it's kind of cheating because of course I'm defining or creating a function in the regular way except that I'm calling XAC, which calls the Python interpreter within the Python interpreter. So what happens here is we simply execute this source dynamically and then uh we can uh call that function because we defined it right here. So when I run this you see that this actually works. Now should you define functions in this way? Uh no probably not but it does work. It can be kind of useful for meta programming. There are some use cases where you want your users to be able to define custom code. For example, in an analytics dashboard, you may want to have the user write custom code to visualize data or something like that. But generally, it's not really recommended because this is pretty hard to debug and quite risky. Also, it's slower than a normal function definition because you're doing interpretation on the fly within your script. So, it's not really recommended, but it does work. The next way you can create a function is by using Eval. You can use eval for simple inline functions like what I'm doing here. Again, it's kind of cheating because I'm just defining a lambda function here, but I'm using eval to actually run that code. So, I'm evaluating this expression which is a lambda function and then I can store the result of that in a variable which is then a function and then I can call that function and that gives me a particular result like so. So basically the same pros and cons as with exac apply here. You need to be careful with these things because there can be security issues especially if this function is not defined by you but by the user of your application. The next way you can define a function is by using new class from the types module. And it's getting a bit weirder already now. So instead of writing a regular class with a call donor method, you can actually dynamically create one at runtime. That's what you see here. So I import the types module and then I'm calling types new class and I'm going to call that dynamic function. Uh there are some other arguments that you can supply here. Uh like the basis of the class if you if you need a superass for example. You can also specify the arguments. Uh and there is an exac body which is a piece of code that you can define where you specify what should be the body of the class. And it's a bit complicated how you have to do that. So uh of course because this is not like a normal way you would define a class or a function. But basically what you need to do is supply a function that then does something. In this case it updates the class object with a call dunder method and that refers to a lambda function that then does a certain multiplication. And since this is called dunder method that means that dynamic function is now callable when we create an instance of it. And then I can call that as a function. Complicated. Let's run this. There we go. It works. It's very contrived, completely useless, but you can actually create a function in this way. Now, maybe there are some situations where you need to do this. for example, if you need to generate many similar callables programmatically or something, but I'm not really sure. Uh, in any case, I definitely wouldn't call this readable unless you really need this type of behavior. Um, so if you know a use case for this, let me know in the comments. I honestly don't except for some really low-level internal thing. But this is child's play compared to the final way to create a function that I want to show you. Before I show you that, if you're enjoying this video so far, give it a like. It's a small thing, but it helps me a lot if you do that. It really does. All right. So, the final way you can create a function is by doing it manually from a code object. So, normally when you write a function in Python, you just use defaf, right? Easy. But deep down, every function in Python is actually made up of two parts. There is a code type object which contains the compiled byte code and all the metadata and there is a function type which wraps that code and it provides a callable interface. Now technically you can create a function manually by constructing a code type and passing it into a function type. Let's see what that looks like. So here I'm importing the types module again. Show you later on why that is. But what I'm doing is first I create byte code that loads a variable then loads the constant 42 adds them together and then returns the result and then I wrap that bite code inside a code type that is actually what is happening here very complicated finally I'm using typesf function type I suppli it with the code type object with globals. I give it a name and then we're going to store that in a variable. And now we have a callable Python function. And then when I call this, this behaves exactly like a normal function. And it what it does is that it adds 42 to the input. Let me run this. As you can see, it works. But there's a caveat here. This only works in Python 3.10 10 and earlier. In Python 3.11 and newer, Python's internals changed completely. There's new resume op codes that are required at the start of every function. Many instructions now have inline cache entries, and if you miss even a single bite, Python is just going to crash your program. Believe me, I tried creating a version of this code for new Python interpreters. I failed. I couldn't get it to work. If you figure out a way to do it, post it in the comments. But unless you want to learn more about how Python works internally, please don't do this. Leave raw bite code writing to the C Python developers or stupid YouTubers like me who accidentally fell into a Python bite code rabbit hole. Now, this was not an exhaustive video. For example, you can also define a function by importing code from a pickle file. You can directly import compiled pyc files. And maybe there are other options. I'm curious though, how many of these ways to define functions that you already know? Do you know any other ways that I didn't mention? Does this video entice you to start writing some bite code in your Python script? Let me know in the comments. Seriously though, when you're writing functions, there's actually a lot to consider. In particular, you need to design your function signatures correctly. I made a full video covering everything that you need to know. Check it out right here. Thanks for watching and see you next

---

## 38. Stop Using map() and filter() Like This in Python
**Channel:** ArjanCodes | **Views:** 97K | **Date:** 8 months ago | **Duration:** 16:23 | **ID:** ylzo04lU9Xs
**Link:** https://youtube.com/watch?v=ylzo04lU9Xs

### Transcript:
If you're still using a for loop in your code, you're an amateur. I mean, why would you use that if you can just use map and filter and be a functional badass? It's much better, right? Right. Well, let's find out. In fact, I will show you four cases where using map or filter instead of a for loop actually makes things a whole lot worse. Let's dive in. Let's start with the obvious. You've seen this a million times. This is basically a for loop that combines filtering and mapping into one. So I have a class user. It's a type dictionary. I then create a list of users. And then I iterate over that list using a for loop. And if the age is over 18, then I'm going to add an uppercase version of the name to that list of adult names. So that's very basic usage of a for loop. And when I run this example, then this is what I get as a result. Now, it's very nice. We can immediately see what is happening by simply taking a look at the code. It's pretty clear and readable. But it's also, you could say, kind of verbose. It's more an imperative way of programming how things happen, not what is happening. So, let's rewrite this, but now use filter and map. So the first thing that I'm going to do is I'm going to get my adult users and this is going to be a list where a user is filtered by age. And similarly what I will do for the adult names is then I can use map that actually uses the adult user list and then maps each name to an uppercase version of the name. And then I don't need to do this anymore. So I can simply remove that. And now when I run this same code, of course, we're going to end up with exactly the same result, but now I used filter and map to do the same thing. So this is a more declarative way we describe what we want. We want a filtered list where the age is equal or less than 18. And here we want a mapping from this list of users to the uppercase names. Depending on specific logic, this can also be more compact and potentially more readable once you get used to it. Another thing that's nice about filter and map is that these things are lazy because they return iterates. So, it can be more efficient. If you only need to uh filter out part of the list, then this will allow you to do that. Whereas with the for loop, that's not really possible. Now, next to the for loop and the filter and map solution I just showed you, another option is to use a comprehension, which is a really nice Pythonic way to solve the same thing. So, in that case, what you would do is we would have my adult names and then I can use the list comprehension to achieve the same result. As you can see, it's an interesting combination of a sort of kind of a for loop with a conditional statement. uh and we put all of that into a list. So that's a comprehension, but it's also a really short nice way to do this particular job. Under the hood, Python just executes this as a loop, but it's a bit more expressive and concise than the for loop that we had at the start. So those are kind of the options, right? We have the for loop, we have map and filter, and we have the list comprehension. Now, before I cover some issues with in particular map and filter, let's just benchmark this example. I've expanded here slightly. I still have that same list of users, but now I just generate a huge list of users so that we can measure some performance differences. And then what I do is I have three versions of the code. I've already showed you these before. We have the for loop where we go through these users and only append the adult names. I have the map filter version of the same thing and I have the list comprehension version of the same thing. And then I'm using Perf counter from the time module to measure the start and the end time. So when I run this then this is the result that we get. So as you can see the for loop and the list comprehension are actually quite a bit faster than the map and filter solution. Now maybe you get different results if you try different types of operations. And of course if performance is really important to you don't use Python but it's still interesting to see the difference. Now, this difference honestly will rarely be critical unless you're of course processing millions and millions of elements. So, the question is if you don't care about performance and you'd like to write in a functional style, should we just replace all the for loops with map and filter? Well, definitely not. I want to cover four clear cases where trying to go functional with map and filter actually makes your code worse and potentially even buggy. The first case is complex logic. So if your logic includes nested conditionals, default values, multi-step processing, etc., map and filter can actually make it pretty unreadable. Here I have an example of a piece of code. So this is a function called transform that takes a collection of data, list of data, and then does some work on it. So uh as you can see, depending on if there is a flag in the item, it's going to try and do something. So this is a relatively complex piece of code. Of course, if you take a look at it, you can figure out what is going on, but it's still kind of complicated. Now, that might also be a reason to actually split this up more. But for now, let's just keep this example as is. And in my main function, I then have a list of data, sample data, and then I transform that and print the result. So when I run this like so, then this is what we get as a result. Now, like I said, even though this logic is kind of complicated, you might want to design it differently, it's still relatively readable with the for loop. But if you take a look at what the map version of this code looks like, then this is what you get. And actually, when you look at it, this is actually pretty hard to understand because we have these uh lambda functions here. Uh we have this sequence of get operations. There is an if statement here that's also kind of adds complication. So it is shorter than the version we previously had, but it's very hard to understand by looking at this code. And if you want to debug this while keeping the code kind of in place, it's going to be a nightmare, especially with these nested structures and implicit defaults. It's almost as bad as debugging regular expressions. And on top of that, the version that I have here actually introduced a bug. If you remove this B element from the data, actually this is going to crash because we're trying to access it here directly. Whereas in the original version of the code, we catch the key error and then we simply append minus1. So actually didn't even precisely map the behavior because that's actually kind of hard to do with the map version of this code. And I'll talk more about that in a minute. And actually this argument also applies to list comprehension. So here I have a list comprehension version of that same code. Now it's even shorter and more compact than the map version that we have here, but it's just as hard to understand. It's not much better. Now, of course, like I said, this is contrite example. Maybe you want to split up things more anyway into separate functions. And then again, it might make sense to use map. But as you can see, map filter and even list comprehensives can be hard to read. By the way, if you want to improve the way you structure and design code in general, I've got a free design guide that might help at ion.code/designguide covers the seven steps I use when designing new software from scratch. The link is in description. Now, the second case I want to cover is exception handling. So, like I mentioned, there's an issue with exceptions in the example that we just took a look at. Now, you can solve this by splitting things up more and then using a separate function. But here's another example of where handling exceptions inside a map or filter can lead to messy London logic and unclear failure paths. I have a parse numbers function here that gets a number of inputs which are strings and then for each of these strings it's going to try to convert it into an integer. If that works it adds it to the list of valid numbers and then it returns that as a result. If it doesn't work, I wanted to print that there was an invalid input. In my main function, I have a list of strings. So, one of these is invalid. As you can see, I try to parse it. And then I'm going to print the valid numbers. When I run this like so, then you can see that we get indeed that one of the inputs is being skipped because it's invalid literally. How would you convert this into something more functional? Let's say we want to avoid this for loop. Well, in that case, what you might want to do is that's what you see here. I have another version where I have a safe convert function that's defined inside parse numbers and that actually handles the exception. So, we try to turn the value into an int in this function. So, that's for a single value. If there's a value error, we print it and then we return null. And then what I do here is that I'm using map and filter in order to create the list of valid numbers. So I'm mapping safe convert onto the user input. So it calls that function on every element in user input. And then with filter I remove everything that is none and then I turn that into list and return that as a result. That's how you would do it with map and filter. But now suddenly what we're doing is we're returning non to indicate failure and then trying to filter it out later. It makes it much harder to follow the flow and debugging kind of gets awkward. So this clever functional code actually does more work and it reads worse. On top of that, this actually confuses the type system. Are we dealing with list of ints or list of int or none? Now, if you want to turn this into more truly functional code, you could use the maybe monad from the returns package or do real world oriented programming with a result type which requires another dependency. It's all not very Pythonic. So, honestly, that wouldn't be my preferred choice. The third case I want to cover is side effects such as writing to file or doing some logging. Using map or filter when you're not actually returning anything meaningful, just performing side effects breaks the whole idea of functional purity. Here I have an example that uses a for loop which is a loop that's used to write warnings to a file. As you can see, this gets a list of log entries and then for each entry it's going to get the timestamped message and then if it finds a warning then it's going to print that warning. Seems like a pretty reasonable thing to do as part of logging system. And in my main function, I have a list of log entries and then I'm passing that to the write warnings function. When I run this, then this is what we get as a result. There's a single warning in this code and you can also see it right here. If you want to replace this for loop with map and filter, well then you will probably get something like this. So I still have the write warnings function but inside that I define a process function that takes that does the work for a single entry and then I map that function to the list of log entries and within that process function I then create the timestamped message. I open the warnings text file and then write the message and if there is a warning there then I'm going to print it. Now, this looks clever until you realize that this is actually reopening the file for every single entry. Also, in order to actually perform the operation, you need to turn the result of map into a list because map returns an iterator. So, we're creating a list just to force evaluation. That's a code smell. You could of course partly solve this by moving out this context manager to the top level and define a process function inside that. But it's still not pretty because there is all this print side effects and we still have to turn it into a list to actually work. In short, don't use map for side effects cuz that's a functional antiattern. The final case I want to show you is when you have early access with break or continue. Here I have another example where there's a function that scans an list of event for suspicious login and it calls a another function is suspicious on a particular event and that just checks that there is an unusual keyword being used in the details and then what it does is that it goes through this list of events. If it's suspicious then it's going to print something and then it's going to break because then you want to take action. You don't want to process the rest of the events. Let me run this just to show you what it does. In this case, it stops because there was a suspicious login that was detected. How would you actually implement this in a more functional way? So, you would try to be clever maybe by using filter. So, we could take events and filter it on suspicious events. And in this case, what we're doing is that we're calling next so that the iterator is actually being called. So, what this does is that this is going to give us the first suspicious entry. And then if that event exists then we are simply going to print it. So this works but it also loses the clear step-by-step logic that the loop had. Also this doesn't support continue skipping entries or logging why something was skipped. In short if your logic is simple and pure map and filter can make it concise and efficient. If you need lazy evaluation and fits well within your design, then map and filter are also great choices. But when the logic becomes complex, you need to deal with exceptions. There are side effects like printing things or you require fine control like exiting early, then the humble for loop is actually a great option. Coding is not about being clever. It's about using the simplest, most readable solution to a problem. And to drive that point home, I just want to show you something really interesting. So here I have the RN code workspace, which is where I have most of my repositories related to my YouTube channel. So in particular, there's the examples repository that contains all the code examples of basically any video that I ever made on the channel. And what's interesting is if you look for when I'm using the for loop, well, we're going to get like lots and lots of results. Now there is some uh false positives like uh using four in a readme file and things like that. But as you can see there's actually quite a lot of for loop usage in my code examples. Now if I look for map cost to map I see that there is only 90 results and filter there's only 44. So, as you can see, I'm using for loops way, way more than map and filter. And that's for good reason. I use the for loop so often because in most cases, I just find it easier to read and easier to work with. But maybe that's just my own limitation. I don't know what what do you think? Do you prefer using map and filter over for loops in your own code? Or do you stick to list comprehensions and classic loops? Let me know your thoughts in the comments. Now, I mentioned the maybe monad and the returns package. I'm not saying you should or shouldn't use that, but learning about those concepts, learning about monads is actually really helpful. It's a good thing to know about as a developer, especially if you want to learn other programming languages in the future like Rust. I did a full video about this package a while back. You can check it out right here. Thanks for watching and see you next

---

## 39. If I Were Starting Today, This Would Be My Tech Stack (2025)
**Channel:** ArjanCodes | **Views:** 23K | **Date:** 8 months ago | **Duration:** 19:56 | **ID:** 1g8nwmDvRhE
**Link:** https://youtube.com/watch?v=1g8nwmDvRhE

### Transcript:
I like to build stuff. SAS platforms, automation tools, educational softwares, games, even a long time ago. In fact, already when I was a kid, I built the world's first fully autonomous wooden robot. Look at that. Right now, I'm teaching it how to vibe code. So, even that job will be taken away from us in the near future. Anyway, today I thought it'd be fun to show you the exact tools and technologies I'm using right now. This isn't a you must use this stack kind of video, but you might get some inspiration or ideas to apply to your own projects. This video is sponsored by Acflow. I'll talk more about them later. Now, the first question you might wonder is how is Python involved in your system. So, that's actually the first thing that I want to get out of the way is that we're not only using Python, even though of course I do a lot of Python content on the channel. In fact, before I started this YouTube channel, I was using almost no Python at all, but mostly TypeScript. It was quite an obvious choice for us at that point in time at my startup company, we were building a web-based SAS platform. Now, of course, you could use Django or something like that, but it's really not the obvious choice. We used what most of the industry was using at that time, which is React and NodeJS. Still, a lot of people use that obviously. Personally, what I really like to use Python for is backend stuff, APIs, automations that connect various other APIs or that handle events in various systems. And that's also how I'm using Python mainly at the moment. I'm going to show you some of the automations that I built so you have a kind of a real life view of how this stuff is all working. But before I dive into that, I want to show you something that's deceptively simple but very effective. And that's our static website. Now, you might have taken a look at my website already. This is what it currently looks like. In fact, we are going to publish an updated version of the website very soon. So, by the time you watch this video, that version is probably already live. So, it will look slightly different. What we use for our website is a technology called Astro. This is a modern static website generator that lets us write markdown and modern components and it compiles everything to fast, minimal HTML and JS. Because this is all statically generated, we basically host the entire website in a git repository and there we have all the files. So in this case, for example, for my uh software design course, I have this single Astro base that basically contains all the components, all the text, etc., etc. And the nice thing about Astro is that it's built on top of React. So you can basically spit up everything into components and then in the end it all compiles down into pretty minimal HTML. And then the only thing you need to do is host it somewhere. Now I'm not going to go through the entire website with you. As you can imagine, there's plenty of different pages here and each are set up in a similar way. The place where we host our website is on Cloudflare using Cloudflare pages. Basically, the only thing we needed to add was then some sort of deployment system and for that we're using GitHub actions. And here's what the deployment script looks like. So when we push to the main branch and then it looks specifically at certain types of files then we're going to publish the website and the nice thing is that there is actually all sorts of ready to go actions already that you can use uh like a Cloudflare specific script that allows us to publish everything to Cloudflare pages. Just need to pass the secret and the account ID and then you deploy it and that's basically all you need to do. So this is pretty straightforward. Why did we pick a static website builder? Because actually for most of our pages like the homepage, the course pages, even the blog, we just don't need a dynamic back end. And by having everything static, it means just fewer moving parts. The load times are much faster and less maintenance work. And it's also a nice uh contrast to some of our more complex projects. Now, before I dive into the automations we've built, because that's honestly one of my favorite parts, I want to talk about a tool that helps make your managing of this way less chaotic, and that's today's sponsor, Ailiflow. AFlow is a productivity app that brings all your tasks, calendars, and tools into one single place. When you're working on multiple projects like me, juggling content, automations, courses, the websites, the actual work isn't the hard part. The context switching is. And this is what a flow looks like. As you can see, I have several projects here. I created one for the main things that I do. So, using AUFlow is really simple. Let's create a task and then add it to my calendar. So, I'm going to write a video script. One of the nice things about AUFlow is that it's smart to use AI, for example, to map automatically your tasks to different projects by using AI. And now what you can do very easily because it integrates task in the calendar. I can simply drag this to let's say on Thursday I want to work on writing video scripts. So now this is being scheduled right here. It's really easy. I've also connected Microsoft Teams to AIFlow which is the tool that we use for communication. And that means for example that I now have an option in Microsoft Teams to directly create an AIFlow task. For example, here there is a new landing page for me to review. And now when I go back to AFlow and I'm going to look at my inbox, we see that now there is a new task for me that is ready to plan. So I can simply click plan and then I can plan to do that task for example tomorrow. There's also support for GitHub. So you can connect a flow with GitHub and automatically turn issues into tasks so you don't forget about them. Aciflow follows an inbox zero philosophy, but for your tasks, you can quickly capture stuff as it comes in from various tools, organize it later, and stick to what you planned for the day. This is super helpful to time block the work, which is incredibly important for software development. You don't want to be switching between tasks all the time because you won't get anything done. If you're a developer, a data scientist, a researcher, you're juggling multiple projects, check out Acuflow. Click the link in description. Now, back to the video. I want to take a look at some of the automations we built. And I'm going to show you two of them. One is related to accounting, my favorite subject, and the other one to show the latest video on the website. I'll start with the last one. So, what we did is we put most of our automations in a single git repository. It's called backend. Not very descriptive name, but well, it works for us. But for example, here I have my latest video automation. And very simply that's just a Python file that connects in this case two APIs which is the YouTube API and the Cloudflare API. So basically what we wanted to do with this automation is that I have my static website and if you go to the homepage and you scroll down well there's my face but what you will also see is my latest video which at the time of recording is my video about decorators and how you probably shouldn't use them all that much. But how do you do that if this is a static website? because we don't want to deploy a new version of the website every time I post a new video, right? So, what I actually did is I get the latest video ID from YouTube. So, this is an automation that we run once every week and then I let a custom subdomain that I host on Cloudflare, Cloudflare's DNS and point that to the latest video and that is what this automation does. So this runs as a Google Cloud Run function and there's some task scheduling involved. So if you look at the code, this is actually really basic. Uh I have a function to update the Cloudflare rule. So basically that's a forwarding URL because if I go to that URL for my latest video, it's going to forward to the YouTube URL. And then I'm simply going to send the new URL to uh Cloudflare so that it updates this page rule. and then going to that URL points to the latest video. And the second part is of course that we need to get the data from YouTube. So there is a YouTube client that we use for this. So as you can see that's part of the Google API and that's what we use here. And what we're doing there is that we search for my latest video. So I'm providing my own channel ID and then I get the last one. I construct the URL from that and then I update the rule. And as you can see, there's two rules because we have uh one which is like the just the page of the video which I use for example on my Instagram account and there's also an embedded URL version that I'm using on my website. So this runs once a week on Friday I think a couple of hours after the video is published or something and then basically my website will always show the latest video. Pretty neat, right? Another example is automating accounting. So we use Stripe as payment provider for courses. Uh so when you buy a course from us, the payment is actually handled via Stripe and then this allows us to accept all sorts of different payment methods, credit cards, uh bank transfer, etc. And Stripe can also generate invoices, receipts, it computes tax, all these things that are really important if you're running a company. The only thing missing is that we need to make sure those invoices are correctly booked in our accounting system. Every business needs to do that. Now, there's lots of different types of accounting platforms. Of course, they kind of differ per country because each country has basically its own rules. I'm based in the Netherlands, so we use a platform that's called Moneybird. And this does the job for us. And they also have a REST API. So what we built is an automation written in Python that connects Stripe payments and invoices and books them correctly in our accounting software Moneybird. And there's basically four different automations that we built for this. We have the consumer side which is that if you are an individual developer, you go to my website, you buy a course, then that's basically the automation that's run. And we also have a business side which is that if companies buy one or more courses for their teams then these are the automations that are run but they're very similar. And basically there are two types of automations. That's one which catches the web hooks from Stripe. So if somebody does a purchase a web hook is being launched and then we can do something like book the invoice. Another one is that Stripe charges fees. It's a payment platform. They need to make money. they charge fees and these are sometimes independent from uh individual purchases. For example, if you use invoicing or tax computations, this is like a separate amount that's written off your account every day or something like that. And we also want to of course capture those so that we can also book those cost in the accounting system. So what we did, let me just show you quickly the web hook for uh the business is that we have again a main file. So this is again a Google cloud run function and that basically handles this connection. So we have different event handlers here. For example, when an invoice is paid and this is the code that's run when that happens. So if I go to the definition, let me show you what it looks like. So there's some type issues here. I haven't installed all the dependencies. It's not a big deal. But basically what we're doing is we retrieve the stripe data as you can see here. And then uh we get the customer information and then what we do is we try to find if there's already a invoice in Moneyboard our accounting system and then if it's there we're going to update that invoice with the correct information. So that's all the things that are here. So in this case we create a transaction for example and then we book the invoice to that transaction. It's a whole accounting thing. I don't want to bore you with that. I don't really like doing accounting. So I just want to automate it so I can just you know forget about all of that. Now like I said we are using moneyird as an accounting system and that's basically an API. This a restful API that you can use. But if you look at the code you actually see we use a package called money snake and this is actually one thing that we were missing in our accounting system is a proper SDK in Python. So we actually started building one of our own. This is called money snake. It's an open-source SDK that we're building and this is basically a layer on top of the REST API, but it uses classes and methods with type annotation. I just want to quickly show you what it looks like. Now, what we wanted to do is make this incredibly simple. So, there's basically just a few things you need to do in your code. First, of course, you need to import the package, install it, and import. And then, what you can do is set the admin ID and the token. So that initializes the API clients kind of similar to how the stripe SDK works as well. But then what you have is these classes that you can create objects of like uh contacts and external sales invoices. So for example here this is some code that syncs contact information between Stripe and Moneybird. So we have a contact class and then what you can do is when you have a customer from stripe the payment platform you can actually use the contact class to construct a contact in the moneyird system. So I'm supplying it a company name address etc. So you just make a mapping basically from the stripe data to a money bird contact. And then since you then have an object with methods, you can do things like setting a custom field such as the Stripe customer ID. So we can find back the customer in Stripe if we have the customer in Moneybird. And you can also simply call save and that's going to update the customer in Moneybird by calling the API with the correct methods and arguments. So in short, an SDK like this really makes your life a lot easier. And again, Money Snake is an open source package. If you happen to be based in the Netherlands and you're using Money Bird for accounting, feel free to use this or to contribute to the project. Now, there's one more thing that I want to show you, but if you're enjoying this video so far, give a like and subscribe to the channel if you want to watch more of my content. This is a small thing, but it really helps me a lot. Now, one of the most impactful things we built this year is the Iron Codes business portal, and that's really how we manage access for companies and teams that buy our courses in bulk. So the automation that I shown you before for mapping Stripe invoices to Lonnie invoices the accounting system that's basically connected to our business portal and the key feature of this is course licenses for teams at businesses. So uh company can purchase multiple licenses for a course and then they can invite team members to join and this is basically what that business portal looks like. Our business portal runs on Google cloud as a cloud run instance. So this basically means hey Google here's a docker image. Please uh run the docker image and give me a URL. That's basically what it is. Now in this business portal like I said companies can buy multiple licenses for a course. They can invite team members to join and then the portal takes care of granting course access and billing and basically having a dashboard where you can manage all that stuff. And on top of that we created a public API so that companies can integrate all these things into their own systems and this is what the documentation looks like. So this is being generated from the actual API. Now for the business portal back end we actually recently moved to Nexjs which has builtin API. So as part of the source code of the portal we have an API folder with routers where we have the various things in our API. So for example, we have a course router where you can get course information and so that's all part of the same system. So I'm not even using Python for this anymore. What's nice about using the built-in API from NextS is that this is very lightweight. It's really easy to develop with and uh we don't have to switch between Python and TypeScript for this particular uh portal because the app which is basically the whole front-end application is built with uh TypeScript and React. So we have all these different pages that contain basically all the implementation of the the whole front end dashboard. Now this portal also relies on a database because obviously we have to store information like how many licenses a company bought or who those licenses for courses are assigned to and for that we're using MongoDB and the reason I'm using MongoDB is that it gives us a lot of flexibility as the data model evolves. So there's no rigid schemas. We can roll out new features without having to do many migrations. Now, of course, MongoDB has its issues. I mean, the lack of structure means that things can get inconsistent. And if you do need to migrate to a different structure, that might actually be harder. And on top of that, if you need strong consistent relationships between documents, MongoDB might actually not be the best choice for you. But for us, it actually works quite well. I think in particular when you're dealing with a system that's changing a lot and that's often the case in uh a new thing that you're developing having a flexible database is actually quite nice. Now again nothing against SQLbased databases I've also used those quite a lot but for this project we opted to go with MongoDB. Now just like all the other things we're building the portal is also deployed with GitHub actions. So as you can see I have a YAML file here with the release. So basically whenever we uh publish a release then we're going to also deploy a new version of the portal and that means deploying a new docker image. So we have to uh create the artifact uh we have to set some uh environment things like the image name and stuff like that and we actually build the artifact with uh docker and then we push it and deploy it to cloud run. And then finally uh some cleanup. And that's basically all there is to it. So this is pretty simple actually. So as you can see the stuff we built is actually pretty straightforward. So for automations it's just simple Python scripts that run on Google Cloud Functions with a bit of scheduling in the cloud. For the website we use static website hosting. We can just edit some markdown files and we can update the website. For our uh business portal for companies, we use a single front end built in Typescript and NexJS which is also pretty straightforward. So for us working with these different tools works actually pretty well. Uh we're a pretty small team and for us speed matters. We need to keep things simple. So Python is the most simple solution I feel for automations and for some backends. Uh, TypeScript is really nice for front- end stuff, especially for uh, interactive things like the business portal dashboard. We use serverless things like uh, cloud run and cloud run functions because that just reduces our operational overhead. It's much simpler to manage. We use GitHub actions for all the deployments. And using this stack, it lets us move pretty fast. We can automate the boring stuff and we can focus on uh, doing things that matter most to us. Now, I'm curious. What do you think? Are are these the same technologies that you're currently using in your own stack? Is Python your go-to? Or are you using something else entirely? Let me know in the comments. I'm curious to learn what you're building your apps with. Now, I haven't talked much about how we use Git. If you want to learn more about branching strategies and how to use merges, fast forward, and rebasing, check out this video next. Thanks for watching and see you next

---

## 40. 10 Python Anti-Patterns That Are Breaking Your Code
**Channel:** ArjanCodes | **Views:** 63K | **Date:** 8 months ago | **Duration:** 25:16 | **ID:** ts38mSIUPSg
**Link:** https://youtube.com/watch?v=ts38mSIUPSg

### Transcript:
What if I told you that some of the code patterns you're using in Python are actually hurting your project more than helping it? They might look clever. They might even work, but I can guarantee they're going to cause headaches later. So today, I'm going to take a look at 10 Python antiatterns that I've seen in real world code bases, some of which I may have written myself. This video is sponsored by Localize. More about them later. Let's dive in. The first antiattern is to use exceptions for control flow. Here I have a simple script that fetches data from an API. So you can see I have function here fetch from primary API that gets a city and that then returns some data about that city. Now of course there's no real API involved here. It's just a test. But as you can see that in some cases there can be a timeout. So there's a simulate timeout function that does nothing else than just call sleep. And then if a random number is less than five, then it simulates a timeout. Basically, something that can happen in a real API, right? If you're hitting a rate limit or maybe there's an problem with your internet connection or whatever, a timeout. If there's a timeout, this raises a timeout error that this primary API that we're calling here timed out. Now, there's also another function here called fetch from backup API. It also gets a city and returns weather data. And this basically does the same thing except it's supposed to call another backup API. Now in terms of design, this is not perfect. There is some duplication here. Obviously, it's also just meant as a simple example. But what I really want to show you is this function which is get weather forecast that actually uses these fetch functions that I showed you before. Now the issue here is that if you look at the logic here is that it tries to get data from this primary API and then if there is a timeout error it tries to get it from the backup API and then if even the backup API fails so we're going to catch another timeout error then we're going to return something with an error status that both of these APIs timed out. Now this is something that's quite common in Python. Instead of checking a condition before doing something, we just try it and catch the exception. It's basically the idea that it's easier to ask forgiveness than permission. So that makes this a tempting solution. And in this case, well, it might still be kind of okay, but it is very dangerous. Don't overdo this a because exceptions are kind of expensive performance-wise. They're not really uh designed for this kind of thing. They're designed for exceptional situations. But second, if you have many different types of exceptions, which is very well possible in different types of libraries, then your code is basically going to turn into a mess because you you already see here we get these multiple levels of indentation where we're trying to figure out where in which part of each try except block we are. If there are different types of errors, they're just going to get more and more complicated and it's just going to lead to code that's very very hard to read. Instead of exceptions for control flow, we could just directly use the status that we already have here. So this line here, you would then replace by a return statement of an error status that this API timed out. And we can do the same thing here. So we replace both of these timeout errors with a simple return value, a different return value. Then we can also refactor this get weather forecast function. You already see that copilot actually does this. And now you see we have a result that we get from the primary API. There's no exception that we have to deal with. If the status is error then we're going to fetch from the backend API and in this case just return the result. But you could even include another if statement here. So if again status is error then we're going to return that both of these APIs timed out. And actually I didn't run this code. Maybe I should just do that to show you what actually happens when you call that. So apparently the weather in New York is sunny. Second antiattern is to use classes with only static methods instead of just modules. If your class looks like this, you see data prep-processing utils with a bunch of static methods that just do something, just some helper methods, then actually there's a much simpler way in Python to do this. You could just use a module with functions. Python is not Java. There's no benefit to wrapping functions in a class here with static methods. It just adds an extra level of interaction with basically zero payoff. This is especially bad when you use static method, class method, comos everywhere without clear intents. Modules are namespaces. You can just use them for that and you don't need to use a class. So what you can do instead is this. Here I have another file called data processing.pi. So this has these three methods, but they're just functions. They're not in a class. And then instead of defining this class here and calling data prep-processing utils dot whatever, well, you just use the module. So from data processing you import all these functions that you need and then you simply call those functions. It's much simpler. Third antiattern is to override dunder methods in surprising ways. Yes, Python of course lets you override almost anything. It gives you a lot of freedom. Doesn't mean that you actually should use that all the time. For example, this is a pattern that I have seen in some code bases and I really think you should avoid it. There's a payment class here. it has overridden the new dunder method as you can see right here and that method then gets a payment type as a string and then depending on what you pass like PayPal card whatever you want it's going to create an object of that particular type so we have the class payment that handles that it has uh subasses PayPal payments and stripe payments and each of these subass has an implementation of a particular method this case paying some amount. And then what you can do with this pattern is that you can then pass the type of thing you want. In this case, a card. And then this is going to call the new dunder method. And then depending on the payment type, that's going to return a different type of object. And then my payment is of that type of object. You call the pay method. And then it's going to call the pay method of that particular type. So in this case card, it's going to return a Stripe payment because that's handled for credit card payments. And then when I run this, you see we get that we're paying 100 whatever kind of currency you like using Stripe. This might seem like a clever solution, but it's also very unexpected surprising behavior because normally you would expect if you call the payment initializer that you're getting an object of type payment, but actually you're getting another class instance. Of course, stripe payment and payable payment are of type payment. So, at least there's that. But if you call an initializer of a particular type, you expect that the thing that you get back is of exactly that type and not some subclass. Good luck debugging that if that's all over your codebase. What you're doing here is that you're changing basic behavior of a language and that's not really a good thing to do. These dummy methods, they're there to be overridden only when necessary and do it in a way that is clear and predictable. This kind of magic breaks expectations and confuses tools, llinters and your future self. Now in this particular case actually there is no need to do all this inheritance magic at all. What you can do instead is you could have a payment method enum. So that's a PayPal or card or whatever you want and you could have payment functions. So in this case I have a pay PayPal function. I have a pay stripe function. We can add more functions if we wanted to. There's no need for classes at all. And then you can decide on different designs of how you would get access to this different payment methods. For example, here I have a function get payment method that gets a payment type that's very similar to what we had before with the payment type that's part of the new dunder method. But here what happens is that depending on the payment type, it just returns one of these functions. and I've used type annotations to specify what this function actually returns. And then if you want to get a payment function that has a type that's not implemented, well, in that case, you simply raise a value error. And when you use that function in your code, it's actually also really simple. You just call the function and you pass the thing that you want. And then you get a function back and you call that function. As an alternative, if you don't want to create a function with a bunch of if else statements, I mean that can also get kind of complicated, then what you can also do is store these things in a dictionary. So in this case, I have a dictionary payment methods that maps a payment method enum value to a function. So that's why you see here. So we have card, we have PayPal and then what you can do instead of having this function here is that you simply access the dictionary directly or you can use get or whatever works in the same way and actually now I'm seeing that I'm passing the payment type as a string here. Of course this should be a payment method object like so because that's an enome and then here I should of course also update that so that looks a bit better. The next antiattern is using hard-coded values throughout your code. Let's take a look at this example. I have a streamlit app here. And this retrieves some data from some URL. Uh that's what this function does right here using pandas. And then I create my streamllet app. This happens to be about Uber pickups in New York City. So there is a title there. We load some uh we have a loading state. Uh there's a checkbox. There's a couple of other things as well. And then we basically build up this simple stream net app. So let me let me run this and see what happens. As you can see after loading this is what the app looks like and then I can interact with the UI as we used to in Streamlit. Now there's nothing in particular wrong with Streamlit. However, if you take a look at this code, you see that every string, every user interface string is hardcoded directly into these streamlit calls. And that means that text fixes are basically just going to be really annoying because you have to look through the code to find them. If you want to change Uber pickups in New York City to ride share activity in New York, well, better hope that you remember where I actually hardcoded that. And now if that same app supports multiple language with all these hard-coded strings scattered throughout the code, that's going to become a total nightmare. And that's also where today's sponsor localize comes in. It lets me move all the text out of the code in one place. And that's where you then manage all your translations. And I can even sync them automatically into my app. Let me show you how this same app looks like once we've integrated a service like Localize. Here's what that looks like. actually setting up the connection with localiz key in the interface and then create a client in your app. So as you can see I retrieved the information here from environment variables and I've also written a few helper functions to quickly get a translation from a key. So these are in this class. So I have the localized translator that initialized the client etc. and that has a get translations method that retrieves the translations and then actually if I create an instance of this class then this has a call dunder method. Uh that's a smart way of overriding a dunder method. So I can call this as a function and then I get my translation. And as you can see with this function it's really easy to get the different translations and the only thing I need to know is these IDs. As you can see, one line of text hasn't been translated yet. So, let's add that. Here's the localized interface, and I'm going to define a new key called number of pickups by hour. And I'm going to give it a default English translation. I can now also add the Dutch translation. And what's nice about this is that AI is directly integrated. So that you can translate with AI directly in the interface. Can simply click translate with AI and that's going to give me a translated text. And then I can simply use that as the key value. Now that the key is defined, I can directly use this translation in my dashboard. And we simply use the same ID here. Here's the new version of the dashboard that uses localized translations. And what's nice is that switching to a different language is now completely trivial. I can simply change the language ID that I use here to let's say Dutch. And then when I reload the page, we see that we now have nice Dutch translations. It's all being handled by localize. As you can see with AI powered translations, you get results in seconds. Literally 10 times faster than doing it manually. And it's not just fast. Localized AI also keeps things accurate with glosseries and context aware translation so your content actually makes sense in every language. You can assign review approved translations in one place. It's no more messy spreadsheets or endless back and forth just a streamlined workflow. And when it comes to tracking progress, localize gives you real insights like how much of your content is AI versus human translated and predictions on when translations will be ready so you can launch on schedule. Try localize for your project today using the link in description. Now back to the video. The fifth antiattern is to write custom decorators just to inject something. Decorators are really powerful, but and I've talked about this in earlier videos, sometimes developers use them to inject stuff like configuration data or database connections. I have an example here. So in this case, I have a decorator called inject config that gets a file name and then returns a very complicated thing which is a collable containing a collable that returns a collable. Well, already we're getting kind of confused here. And the reason this is complicated because of course the decorator wraps around another function and itself returns a function. That's why you also see here and uh as you can see so if we drill down in all these different functions in the end what happens is that we take this file name we load it uh as config and then we call the original function that the decorator wraps around with that particular config value. And then because of the way that the decorator works, what we can do then is we have inject config which gets an argument and then it runs this code reads the config and then it calls main with the data that read from the config file and then we can do something with it. So when I run this code then this is what we get training with a particular config file. This adds a lot of complication. on top of that it's confusing because the main function gets a config but actually we have to call it without any arguments. So instead of all this confusion why not just call a function. So here I have another example that is basically doing the same thing. We have a main function and we just call a function to load the config based on a JSON file that we pass and then we use the config. There's nothing really going on here in particular that requires the use of a complicated decorator. And this of course if I run this works in exactly the same way and it reads the same config file. It's much simpler, much cleaner solution. The sixth antiattern is to overengineer with design patterns you don't need. Just because you can implement the abstract factory or the visitor pattern in Python doesn't mean you should. Python is a dynamic, very expressive language. You often don't need all that extra stuff. And that may sound weird coming from me since I have talked a lot about design pattern in the past, but in my opinion, the most important design pattern is actually a principle, which is keep it simple. Here's an example. I have a report class that has title and content. And then I have different data exporter. So I have a CSV exporter, which is a class that's a subclass of exporter. I have a JSON exporter for JSON format export and then I have a factory that given the format gives me a particular type of exporter and then in my main function I create a report I create an exporter factory or actually this is just a static method in this case if you want to truly follow the uh abstract factory precisely you'd have to create an instance so I'm already cheating here to make things a bit shorter but then we get an exporter and we export the file in a particular format that we like. So this adds like a bunch of classes and things that are really not just waiting for the coffee machine. Now instead of using all these classes, we can actually find a much simpler solution. Just use functions. Actually that's I should maybe make a sticker just use functions or a t-shirt. Let me know if you want a t-shirt that says just use functions. Anyway, let's do that. So here what we can do is basically we take this and we call that export CSV and I'm going to remove the self because that's not needed. I'm going to remove that. No more ABC. And here I'm going to do exactly the same. Simply going to call this export JSON. No more self remove the class. And the factory is instead can be just like a dictionary. So we can have exporters which is dictionary maps CSV and JSON to each of these functions. And now I can just remove this like so. And then what I can do here is the exporter we retrieve it like so. Yeah, that's fine. And then here we can simply call it directly. And then maybe for clarity I shouldn't call this an exporter but I can call this an export function like so. And now this does the exact same thing but it's much shorter. We just have these two functions and I have a simple dictionary and that's all I need. Of course you can do the same thing I did in one of the previous examples where you use an enome instead of a string. So there's definitely things you can improve here but already this is much simpler. There's no need for complicated design patterns to solve this particular problem. The seventh anti pattern is what's called inappropriate intimacy. Taking a look at this exporter example, as you can see, these export functions need intimate knowledge of the structure of a report because they need to know that a report has a title and a content. And if reports become more complicated, it means that these exporter functions also become more complicated. That's what we call inappropriate intimacy. It's not great design. So instead, what you can do is refactor this so that some of the dependency on the report structure is actually moved to the report class. So what you would do in this case is that you would add functionality so that report can be represented into different formats. For example, we could add a to CSV method that basically does this work that we normally have here. And then we can change uh this exporter function to simply call that method. And similarly, we can add a to JSON method that converts the report to a JSON format. And then in export to JSON, I don't have to do all of this, but I can simply call report dot to JSON to get actually the data that we need in the format that we like. And finally, there's something else that can be improved in this code example. Now, before I show you what that is, give this video a like if you're enjoying it, and make sure you subscribe to the channel to watch more of my content. It's a small thing for you, but it really helps me a lot if you do that. So taking another look at this example, we've now removed the inappropriate intimacy, but there's still direct coupling between a report and the export functions because as you can see, report is passed as an argument. But there's no need for these functions to only work with report. As long as they get some object that can deliver a CSV or a JSON representation, it can be exported. It doesn't really matter what it is, right? So let's instead use a protocol class to introduce some abstraction. So I'm going to import that from the typing module. And then what we're going to do is we will create a class. Let's call that exportable. This is simply going to be a contract that says an object needs a to CSV and to JSON method in order to use it. So here we changed report to be exportable like so. And we're going to do exactly the same thing here. So now what's nice is that these two functions no longer need a report. As long as the object that they get has two CSV or two JSON method, we're good to go. So for example, what you could do now is introduce another class. Let's call that uh okay, budget. Why not? And that has a title and an amount. we can have a CSV representation of a budget and a JSON representation of that. And now what I can do here is I can simply create a budget and then for example as a title I don't know my recording budget and the amount is going to be let's say 1,000 like so. And now what we can do is we can get a CSV exporter and then simply export that budget. And as you can see I still get a type issue here because I forgot to update the exporter function type. So this simply gets an exportable type as an argument. And now that problem is solved. The protocol class here helped to decouple these functions from the actual object specific objects that they work with. And now what you can do is for example move this to a different module. You no longer have to be dependent on a specific type such as a report or a budget. Of course, Python ignores type annotations at runtime. So basically, you could pass any object and as long as it works, it works. But these are really tools that are there for the developer to get a better understanding of your code. And that's also how you should use them. The ninth anti pattern has to do with imports and that's importing everything with a wild card. So instead of doing this, you would simply import everything from the data classes module and then you could actually use it throughout your code just like you did before, but then you have also access to everything else. Now you already see that my type checker gives all kind of warnings about this and that's for good reason because you're now really importing everything into your script and that can lead to all sorts of conflicts. If you're doing this from multiple places, it simply pollutes the name space. It hides where the dependencies are coming from and it also makes static analysis harder. So what you want is you want to be able to take a look at the top of your file and have a clear idea of what is actually being used in the code. So that means you should typically go for explicit import. So in this case from data classes, we only use the data class decorator. So that's what we import nothing else. The final antiattern is to not use built-in tools and libraries. Don't reinvent the wheel. Python comes with a lot of batteries included. So don't write your own file walker with recursion. Just use os.walk. Don't parse JSON manually, but use the functions from the JSON module. Before building your own thing, check the standard library or mature thirdparty libraries like Padantic, Click, Typer, etc. There's a lot out there and that's actually one of the main advantages of Python. The ecosystem is huge. So much has already been built so you don't have to do that anymore. But I'd like to hear from you. What do you consider antiatterns in Python? Let me know in the comments. Now, you've seen me use type hints in all these examples. Type hints are incredibly useful. In fact, I'd say it's almost an antiattern not to use them. If you want to learn why they're so useful, and it's probably not what you think, watch this video next.

---

## 41. I’m in prison...
**Channel:** ArjanCodes | **Views:** 16K | **Date:** 9 months ago | **Duration:** 5:39 | **ID:** VxiLGc5A7Ho
**Link:** https://youtube.com/watch?v=VxiLGc5A7Ho

### Transcript:
I'm in prison, actually I'm going to be here for quite a while. Not because I did something bad, I actually got away with that. But this is the new Arjen Kool's office. This prison was built in 1856, it's a really cool building, isn't it? This is located in Utrecht, the Netherlands, which by the way, if you are in the Netherlands and you want to visit some nice places, Utrecht is actually a really nice place to go. If you want to go a bit outside of the standard touristy stuff like Amsterdam or Zaanse Schans. And I like walking out of my office and basically being here in Utrecht. There's tons of cool shops, places to eat, nice buildings. If you have the opportunity, come to Utrecht. Now since 2015, this is no longer used as a prison, but it's used as a sort of office building for smaller creative companies, actually a really inspiring place to be. Let me show you what it looks like on the inside. Heavy doors. It's hard work living in prison. Now we're actually walking in the corridor towards my office and there's a couple of nice places here as well, like a really cool little coffee place right here. You can even sit outside if you want to, it's really nice. Some toilets, everybody, even prisons need toilets, right? And then we arrive in sort of the main area, which has the A-wing, the B-wing, lots of different cells. And what's actually really funny is that in each of these cells, there is like a small company, like just a person working on something, doing some creative work. There's lots of different businesses here. There's, I think, somebody who makes jewelry, there's somebody who does a garden architecture, there's lots of different people. It's really inspiring. So this area of the prison, there's actually still some actual cells that still look like cells. And what you see here is kind of the area with the isolating cells. So this was for the people who did some really bad stuff. Actually what's interesting is that there were also quite a few famous Dutch people who stayed in this prison, including, maybe you know him, Herman Brood, who's a well-known Dutch artist. And now let's take a look at the new Arjan Kootz office. Welcome in the new office. So actually, this is where I normally sit and try to do some actual work, like video editing, answering questions from my students about the courses, emails, anything. Basically we have a nice kitchen with the pièce de résistance, which is this, obviously this coffee machine, very important to me. And then let's see, what else do we have? Oh, we have a bunch of boxes here, which I still need to sort through. That's kind of the disadvantage of moving and the advantage because now at least I get to sort this out finally and not leave it like a big mess. And then we have this little setup here, which is what I'm using for recording. So here is where I have basically my whole setup with the camera, some lights, a microphone, and this is where I normally record all my tutorials from now on. This is not the point of view that you normally see as the audience. You'll see the other side, but that's what I use. And then finally, we have the logo that I still need to put up. This is kind of a mess. I think I'm going to put it on the wall right here. But yeah, so that's it. And that's the new place. I hope you're going to enjoy it. Over the past year, my team has become quite a bit smaller. Now, that may sound like a bad thing, but actually it also means it's a lot easier for me to manage. In fact, I feel like I'm starting to get back to get some balance in my life, which is a good thing. You know, in the past I was continuously running around trying to scale the business, implementing crazy ideas, trying to follow the latest trends in AI or whatever. Honestly, I'm kind of done with that. I want to go back to something simple. Just making my weekly videos, doing some coding, doing things that I enjoy, showing you things that I think are useful to you. Basically, just more videos that cover concepts like software design principle, a Python thing or a programming concept in general. Then I have a code example and I do some coding, just editing everything to show you how it works or how it doesn't work. Covering some design principles, showing you how to implement it in different scenarios. Basically, useful things to know if you've vibe coded yourself into a corner. So I am wondering, what topics would you like me to cover more? What would you like me to focus less on? What do you think of this new office space? Let me know in the comments. Anyway, I just want to show you the new place. It's going to be a little while before I'm fully settled in, but it feels good. A fresh start, you know? Anyway, thanks for watching and I'll see you for a regular video next week.

---

## 42. The REAL Reason You Should Use Type Hints in Python
**Channel:** ArjanCodes | **Views:** 33K | **Date:** 9 months ago | **Duration:** 13:01 | **ID:** 0oBLMwHdZ2Y
**Link:** https://youtube.com/watch?v=0oBLMwHdZ2Y

### Transcript:
Today I'm going to show you why type hints in Python are useful and it's not for the reason you think. Sure, they help with catching type errors early, but the biggest benefit is actually something else. And that also leads to a very interesting property of types that really surprised me. More about that in a minute. First, let's take a quick look at where type hints came from and how to add them to your code. Type hints were introduced in Python 3.5 back in 2015 through PEP 484. I've talked about that PEP before in my videos. Python is dynamically type which makes it flexible. It also means it's easy to pass the wrong type and only discover it at runtime. The idea behind type hints is to provide optional typing to help with code clarity and tooling support without forcing Python to be statically typed like Java or C++. Since then, type hints have evolved significantly. Python 3.9 introduced built-in generics, lowercase list and dict, etc. instead of the uppercase list dict from typing. Python 3.10 brought type alias and improved type inference. Python 3.12 introduced a new generic syntax. But again, real power is not what most people think. So, let's look at an example. I'm going to start with a very simple function that calculates a discount for a list of item prices. And this is what it looks like. As you can see, there is no type hints here at all. Like I said, it's not required in Python, even though I have all these little red squiggly lines because I have my typing set to strict in VS Code. But actually, when I run this code, this works perfectly fine because Python ignores type hints at runtime. So, this works. But what exactly is items? Is that a list, a tupole, a generator and discounts? What is that? You can probably figure that out by analyzing the body, but in general without typins, you have no idea. And what does it return? Well, we also have to look into the body to figure out what it actually returns. And this is of course a very basic function, but in case a function is longer, more complex, well then it's going to be really painful that you have to look into the body of the function every time to figure out what kind of thing it expects and what it returns. So let's add some type hints here. Let's say items is a list of floats like so. And for the discount, we can also define that. God, I need to learn how to type. We can also define that as a float. And then you see that copilot already suggests the correct return type for me. So here now items is a list of float, which is great. Now it's much clearer what it does. And that's also something where typeins help already, right? You understand better what your data structures are. So here I changed this to a list of float. But now you see that there's an issue that items is actually integers. So there's a mismatch. So we can either change the type or we can change the values depending on what you actually need in your code. Right? So let's run this. And of course we didn't really change the code in any way. So this still works correctly. Now the thing is that in this particular approach, there is actually still something missing. I've explicitly defined items as a list of float. However, you might wonder why should we limit this to a list? What if somebody wants to pass a tupole or a set or even a generator? So, more flexible approach in this case is that you actually use an iterable instead. And that we're going to need to import from collections.abc. And then instead of making this a list, we're making this an iterable. Now we still have the same code right here in my main function. There is no issue whatsoever. If I run this, of course, this works exactly in the same way. But now if I want to, I can change this into a different kind of data structure like so. And I can save this and now this also works without any issue. And not just that, I can also turn this into a tupole and that's also going to work just fine. In fact, I can even use a generator. Like so. And now this also works. In other words, it's the same function, but we get way more flexibility. All because typins made us think about our data structures. And this is actually the real power of typins. They don't just tell you how to use a function. They actually make you think how it should be designed. And that can actually lead to a better design of your code. For example, because of this change, moving from a simple list to an iterable, you might redesign an order class to change from a list of line items to a set so you can avoid double occurrences of items. Or perhaps you want to retrieve these items dynamically using a generator from the database and then use calculate discount in that way. It all changes how you design your code. And the reason you figure that out is because you thought more about your code in generic terms. And that is the biggest benefit. Now, if you want to improve your ability in general to spot design problems like this in Python code, check out my free code diagnosis workshop. I'll teach you how to review code quickly detect problems. Sign up at ar.code/diagnosis. The link is also in the description. So one thing you can conclude from this example is that you want your inputs to be as generic as possible. If we would have stuck with a list of floats, we would have unnecessarily restricted the function. Using iterable means that it can accept list tupils, generators, any other iterable. In type theory, you would say that these inputs are contravariant. They accept a wider range of types. Let's take a look at another example. Imagine you're building a reporting system where data can be exported in multiple formats. So here I have an example of some code that does that. In fact, this just has a JSON exporter and there is a function to generate a report. So that function gets a specific exporter as an argument. In this case, that's a JSON exporter. Now the problem with this approach is that generate report only can work with a JSON exporter. Let's actually also run this and see what happens. So it actually exports it in a JSON format. But let's say we want to create a new class called a CSV exporter. And there it also has an export method. But then it actually joins things by uh commas. So now the problem is I need to refactor generate report to also work with the CSV exporter which is not very flexible. It violates the open closed principle. On top of that, it means it's very easy to now create a CSV exporter that is slightly different from the JSON exporter by I don't know using another method name or uh organizing the arguments differently and that means it just leads to code that is basically easier to complicate and we want things to be simple. Now better way to approach this is to actually use an abstraction like an abstract base class or a protocol. So I'm going to use a protocol in this case and what I'm going to do is I'm going to create an exporter class and this is going to have a single method called export and generate report is going to expect an exporter instead of a JSON exporter. And now what I can do is I can actually generate a CSV exporter here. I don't need to change anything in generate report because it accepts any exporter. And then I can simply run this and then I'm going to get my data in CSV format. In other words, this is way more flexible. I can add more exporters here without having to do anything in this part of my code. So it's more flexible, more maintainable. Overall, just a better design of the software. And this is another example of making sure that your input types are generic. So instead of having a specific JSON exporter which limits me, I'm now using an exporter which is more generic and accepts any type of export. Now, interestingly for return types, it's actually the other way around. You should be as specific as possible. Let's say we have a function that computes a discount per item. So that's very similar to the first example I had, except it does it for all the items separately. So we have my uh items which is a nice generic iterable like we talked about before. We have a discount and then I'm returning a list of items where I apply the discount to each item separately. Now what I've done here is that I've also made the return type generic basically following the same procedure as for the arguments. Right now in principle this works but there is also a problem. this iterable is actually too broad and even though the function actually returns a list in my main function it treats discounted items as an iterable of floats and this means that actually you won't have access to list specific things such as the length like if I try to print the length of discounted items here you see I now get a type issue because there is actually no lamb donger method whereas actually when I run this this will actually on totally fine because it is actually a list and Python simply ignores these type hints at runtime. So the fix is that you need to be more specific in your return type. So instead of making this an iterable, you should actually make this a list because that is actually what it returns. And now also this type error that we got here is gone. And let me run this one more time. You see that it does exactly the same. So, as I mentioned before, inputs should be as wide as possible, as generic as possible, contravariant. Outputs should be co-variant. In other words, as specific as possible. Now, before I show you a final example, give this video a like if you're enjoying it so far, and subscribe to the channel. It's a small thing, but it really helps me a lot if you do that. Let's look at another example where the return type is too generic, and that causes issues. Imagine that we're designing a system that interacts with a database. So I have my database connection class here which is a protocol. So that's let's say an interface for connecting with the database and then you might have different types of databases that follow this particular protocol. Specific one that I have here is a posgress database that has at least these methods. So it follows this protocol. Then I have a separate function that's called connect to DB that creates a database connection. In this case, it creates a posgress connection. The return type is generic. It's a database connection. And then basically I use this in my main function to execute some SQL query. Again, not for real. It's just an example. When I run this code, this is what happens. So, works perfectly fine, right? But here's the problem. This function connect db claims to return a database connection protocol but it hides the fact that it's actually a posgress connection. The caller of this function loses access to any posgress specific features like for example this method start transaction that is not in the protocol. So if I try to call this method here like so you see that we actually get a type error. So what's the solution in this case? Just like the previous example, we have to be specific. So instead of returning a protocol, we are actually returning something of type posgress connection and we're specific about that. And now also the typing issue here is again gone. What I've shown you today is that typeints are more than just documentation or help you avoid type issues. They do something really important. that make you think really carefully about your data structures and help you come up with better more generic design. And a really good practice in approaching how to use typins effectively is by thinking about the idea that inputs should be as generic as possible and outputs should be as specific as possible. That's really for me at least the guiding principle that I use when I think about types and when I think about how I organize my code and how everything communicates. But I'd like to hear from you. Are you using typins? I sincerely hope so. And what do you think makes them useful? And did you know about this idea of generic inputs and specific outputs? Let me know in the comments. If you want to go beyond the basics with type hints in Python, you need to learn how to use generics. They're very useful. Like I mentioned, since Python 3.12, there's new syntax which makes them really easy to use. To learn all about that, watch this video next. Thanks for watching and see you next

---

## 43. The Hidden Dangers of Python Decorators
**Channel:** ArjanCodes | **Views:** 22K | **Date:** 9 months ago | **Duration:** 15:05 | **ID:** dVnNc9oEnF8
**Link:** https://youtube.com/watch?v=dVnNc9oEnF8

### Transcript:
Python's decorators seem like magic until they turn your code into a nightmare. They look like a clean, even fun, way to add functionality. But they come with some hidden pitfalls that can make debugging harder, break type safety and create unexpected issues. Before we get into why I think you should mostly avoid them, let's first break down what decorators are and how they actually work. Let's jump in. So, what is a decorator in Python? It's a function that takes another function as input and modifies or extends its behavior. And this is actually done without changing the original function's code, which makes decorators a form of metaprogramming. Python introduced decorators way back in version 2.4 in 2004, and they have been widely used ever since. One of the most useful built-in decorators is DataClass, which automatically generates special methods for a class. So you've surely seen and used DataClasses before if you've been using Python for a while. But basically how that's done is you import from DataClasses the DataClasses decorator. And then what you can do is use the DataClass decorator on a class. And in this case, you can simply define some fields like name, age, etc, etc. Now this is not the normal syntax for adding members to the class. You should normally do that in an initializer. But with DataClass, this is a much simpler way of doing it. And also it generates other methods like wrapper, for example. So if I have my main function, I can now create a person like so, and then I can simply print that. And then when I do uvrun main.pi, then basically it's going to print that person. This is an example of a good use case for decorators. It saves boilerplate code. It doesn't introduce weird side effects. Now next to using these types of built-in decorators, you can also create decorators yourself. Let's, for example, create a simple logging decorator that prints a message before calling a function. What you need to do then in Python is create a function wrapper. So let's say I have a log function wrapper that gets another function as an argument. That's the function that it wraps around. And then within that, we have a wrapper. You see that my AI actually already generates that for me. So I have my wrapper function here. The only thing that this does, it simply prints that this function is being called with certain arguments and keyword arguments, and then it returns calling that function. So the only thing it does is wrap around that function. And then the result of that is that we return that function. And then the only thing that the decorator syntax in Python actually does is call this function that is getting returned here. So how do you use this then? Well, you could create a log function decorator call, and then I'm simply going to do that on top of another function. In this case, I'm adding a and b. And then let's see what happens if we're going to print adding one and two. I'll remove this part because that's not really relevant at the moment. Let me run the script again. And now you see that it calls this logging function because we added the decorator on top of it. And this is a pretty good use case of a situation where you could use a decorator. There's a problem though in the current implementation, which is that the function name and the doc string of the add function are now actually wrong. So if I print add.dunder name, like so, so you would expect that to be the name of the function. So let's see what happens if we do that. So you see that actually now that is called wrapper. And why is that? Well, that's because that is actually the function that we return here. Now you might say, well, who cares, right? Because I never actually used this value, but you know, it is kind of dangerous if that's not correct because in other places in your code, it might be used. It may be used when you automatically generate documentation or whatever. So you really want to make sure that this is neat and as expected. Now this is actually a pretty easy fix. What you can do, there is a wraps function in functools. So I can import functools and then I can write another decorator, functools.wraps, and then it wraps around that function like so. And that's the only thing you need to do to fix this because when I run this again, now you see that actually the function name is correct. In short, if you write your own decorators, make sure you also do this so that it works as expected in all situations. One thing you might want to do is make sure that you add proper type annotations. You see that my type checker already complains about this. So let's just use AI to add these annotations. And there we go. And now we have a nice little decorator with annotations. Great this AI stuff. I don't even have to use my brains anymore. Now knowing how these decorators work is great, but understanding how to design software from scratch is even more important. I've put together a free design guide that walks you through the seven key steps I use when designing software. It covers everything from structuring your code to avoiding common pitfalls, things I wish I had known earlier. Get your free copy at arjan.codes/designguide. Link is also in the description. Now that we know how decorators work, let's look at why they can actually be harmful. The first problem is that the order of decorators can actually cause some unexpected bugs. Many web apps use authentication, caching, and other things for faster responses. But if you use decorators for that and you stack them in the wrong order, then you can actually break your app. Here's an example of what that could look like. So I have an authenticate decorator that actually does user authentication before calling a function. I have another function here that fetches a user from a database. Not for real, it's just for showing you this, but this function could potentially connect with the database and retrieve the data. So you might also want to cache that, right? So I've added this so that if you call it twice, it actually does the database request only once. And then I have a main function where I called that function a couple of times. The issue here is that authenticate is run before the caching, so it's now also cached. And you can actually see that happening if I do uv run deckorder.pi, then you see that I'm authenticating the user, and then I fetch it from the database. But the second time that I fetch it, actually this is now cached, so the user is no longer authenticated. And that's because in Python, decorators are evaluated from bottom to top, because each decorator wraps around the one below it. So in that case, that means that this thing is going to cache whatever comes out of this thing. So that means that it will also cache authentication. However, that is typically the case. Not even 100% sure that this is what's going to happen, because the decorator can also decide to run code after calling the function. For example, here I'm running this code before actually calling the function, but I could in theory do something like this. So I can print something else, and then I'm going to return the result, like so. So now, when I run this again, you actually see that the user authenticated part is run after we fetch the user from the database. In short, it's not always clear in which order things are going to be run when you're using multiple decorators. Now, to fix this, let me actually return just the function result and not do this anymore. To fix this problem, you can swap these things. So we are going to cache the result of fetchUser, but then we're going to authenticate on top of that. And now, when I run this again, you see that we're authenticating the user every time I'm retrieving something from the database. And caching actually still works properly. So it only retrieves the user from the database here and here when we're changing the ID. So that's the first problem. Decorators and functions and how they interact can be confusing. The second problem is that decorators can actually break your code analysis and typing. And for that, I want to show you this example. Some decorators modify function signatures in unexpected ways, which makes it difficult for IDEs and type checkers to understand your code. So here I'm using Hydra. This is a config management library I talked about a while back in one of my videos. But basically how it works is that you add a decorator, hydra.main, and that gets some information like where is the config file. So I'm reading a config.yaml file here. And then it injects an argument into that function. So in that case, this is the config. Let's actually run this to see what happens. like so. So as you can see it reads some configuration settings from a config file. It doesn't really matter what it does. Now you might say oh that's actually nice so I can just use this decorator and then I'm just going to get an argument that then contains the values that I need. In principle sounds like a great design right? But the problem here is that it introduces something weird in your code. Namely that we have this function here that I need to call without arguments and then Hydra is going to inject that argument for me. So you might run into all sorts of issues where your type system might complain that it's expecting an argument here but it didn't get one. It's just very confusing and it also makes it hard to debug because you're calling a function differently than what you would expect if you just take a look at the signature. A much better solution here in my opinion is to not use decorators at all but simply have a function that reads the config. Then when you need to read a config value you call that function. Simple as that. And if you're worried about performance you could even add caching so it only reads the config files once. Honestly it doesn't have to be more complicated than that. I think using a decorator in this way is overkill and it has more disadvantages than actual advantages. So I would recommend against this. Now third problem that's actually kind of related to this is that a decorator can introduce hidden function requirements. Some decorators expect specific arguments in your function even if you don't actually use them. A great example is if you're using FastAPI with the slow API rate limiting decorator. Here's an example of that. So I use FastAPI and SlowAPI which is the rate limiter you can use for FastAPI. So I create instances of these things right here and then I have a route that is limited by two requests per second and I use these two decorators here. So I have the limiter decorator and I have my app decorator that's generated by FastAPI. So purely by looking at this you would expect well this would work perfectly fine right we just add this simple decorator right here. However if I run this code you'll see that this actually crashes because actually this limiter expects that there is a request object. So the way to fix this in this particular case is that we are supposed to add a request object which is of type request from FastAPI. Like so. And now when I run this again and now when I run this again it works without any issue. But here's the problem. Request is actually not being used in the function. You even see that my type checker gives me a warning like hey this is not accessed. So you might think okay maybe I can then replace this by an underscore which is the standard way in Python of indicating that you still need an argument but you're not going to use it. But then actually it crashes again because it actually wants a request argument. So that means that if we're using it in this way we are required to have a warning in our type system just to use rate limiting. I think that's a really bad design decision and it's one of the issues that decorators can introduce. Now to solve this particular issue I think honestly it makes way more sense that limiting is built directly into FastAPI since it's so directly coupled to it. Now of course that's not the reality. We live in reality so I have to deal with it. But if I were to write a rate limiting library I would probably write something that extends the FastAPI decorator with rate limiting. So I would basically create a new version of this that behaves exactly like the FastAPI decorator but then you can use that instead of the one from FastAPI. And this means you don't have to use an extra decorator with all the issues that it introduced and you can keep it simple. And then if my hypothetical library works well maybe Sebastian is going to incorporate it into the FastAPI package in the future. Who knows? By the way if this video saved you from an unexpected decorator induced headache hit the like button. It helps me more than you think. And if you enjoy deep dives into Python and software design like this video subscribe so you don't miss the next video. So we've seen problems with the order of decorators. With decorators introducing arguments to functions that are not there. Decorators that expect arguments to be there even though it doesn't make sense from a typing perspective. And on top of that you're going to run into other issues as well if you use decorators a lot in your code. One is harder debugging. When something goes wrong in a decorated function you often see errors inside the decorator not where the actual problem is. So stack traces become harder to read, debugging takes longer, and second you're going to also have to deal with unit tests that are going to be harder to write because you might need to mock or patch decorators to test your functions which add unnecessary complexity. And finally since decorators wrap functions that adds an extra layer of execution and if they do some expensive operations that you don't clearly see by just putting decorator on top this can actually considerably slow down performance of your code. So overall in my opinion Python's decorators look elegant but they also introduce a lot of hidden complexity and it's really easy to make bad software design decisions. They do have some valid use cases. Think of the data class decorator or basic things like a property or a static method decorator. In most cases they actually make your code harder to read and maintain and my recommendation is to avoid them if you can. But I'm curious what you think about this. Have you used decorators a lot? Do you regularly build your own decorators? What are the kind of issues that you encounter while doing that? Let me know in the comments. If you want to learn other ways to help you become really good at Python make sure you check out this video next. Thanks for watching and see you next time!

---

## 44. 10 Tips to Keep Your Software Simple
**Channel:** ArjanCodes | **Views:** 45K | **Date:** 9 months ago | **Duration:** 21:07 | **ID:** 0U-RwnWaFIM
**Link:** https://youtube.com/watch?v=0U-RwnWaFIM

### Transcript:
Have you ever looked at a piece of code you wrote a while back and thought what was I thinking sure happened to me today I'll show you 10 essential tips to keep your code simple maintainable and easy to work with which is what everybody wants, right? And of course, I'll show plenty of Python examples. This video is sponsored by monday.com more about them later Let's dive in in this video. I'll use this code example. There's a employee class. It's a data class and it has a couple of Instance attributes name role and a number of vacation days and then there is a method take a holiday But that's an employee take a single holiday or pay out five holidays Then if you scroll down you see that there are then subclass of this. There's an hourly employee There's a salaried employee. There's a freelancer There's an intern and they each have different extra instance attribute depending on the type of employee Then we have a company that has a list of employees where we can find various people we can pay employees so quite important and then we have a notification system to inform employees of Payments and holidays and things like that. So notification is an abstract base class But then we have email notifications currently These are just print statements just for an example, but we also have SMS notifications and I used a factory pattern to Create instances of these things based on the notification method that we want I know this is rather complicated, but that's the whole point of the video. I start with a pretty complicated example that we're going to simplify and then finally I have a main function that creates a bunch of Objects and calls some functions to see what actually is happening So creating an employee here adding it to the company adding a few more employees. I find some Different roles of people and I pay an employee and then I'm going to take holiday and then send the employee a message So when I run this we see that there are some results here like various employees have been found. I'm paying an employee An employee takes holiday and we're sending an email. Now the first tip when you're looking at code like this is Yagni, you ain't gonna need it and that's actually a common trap for developers to just build features You know just in case of the years I've become more and more strict with myself of not writing code that I'm not actually using at the moment Because in the future you probably won't need it because you're refactoring and changing things Anyway, because writing extra code also means you have to write an extra test and you have to make sure that conceptually it fits within The framework of what you're building you might need to do refactoring on that code that you don't use So there's a lot of work that then goes into code that is actually not being used in this case Well, it's not that bad because it's a relatively simple example, it's just a single file, but We have a few classes that are actually being used. For example, the freelancer and the intern These are both not being used now It's possible that the company wants these classes because they know we're going to have freelancers and interns soon So we want to have that support But at the moment these classes are not being used so unless you have a very good reason for them being there Instead of just in case then you should actually remove these and that already removes quite a few lines of code Another thing that happens once you remove class that you don't need you can probably remove more code That's not needed. For example here. I have pay employee which pays employees depending on different employee types well in this case, we can also remove all of this code because that's no longer needed and Of course when I do that, it still works as expected because that code is not being used Second tip is to not repeat yourself. Don't repeat yourself dry That's a pretty common software design principle If you repeat yourself, it leads to inconsistency and maintenance nightmares If you're in a really bad situation If you copy paste code instead of trying to come up with a more generic solution You also introduce more places where you might need to fix bugs later on an example Here is the company class which has find managers find vice presidents find support staff, etc And all these methods they basically have the same line of code Except that the only thing that changes is the role So it means that if there is some bug in this list comprehension Then you're going to need to solve it in each of these different methods instead of just a single one So it pays off in many cases to try and come up with a more generic solution so you can avoid these type of issues so in this case a simple way to solve that would be to have a find by role and that's going to take self and It's going to take a role as a string and then we'll return a list of the employees that have this particular Role and now I can basically remove all of this stuff So now that I have my find by role method I can update the main function so that find vice president becomes find by role and then I'm going to check the vice president like so and Similar I can do find Find by role manager Actually vice president should have dash and we can also do support Let's run the code again, and we see that the result is exactly the same But now I considerably simplified the company class by removing duplication And by the way, you can't always remove all duplications Sometimes that can actually backfire if you try to find a generic solution for something, but that makes it too generic Which means that you just end up with a bunch of boilerplate code So you always have to find a balance there The third tip is to avoid over engineering if you have a simple problem, you don't need a really complex solution for that So don't introduce design patterns framework extra layers, etc Unless you really need them an example of this is I already mentioned it at the start of the video the whole notification system So I have an abstract base class here. I have email and SMS notifications and a factory that gives me instances of those things That's pretty much overkill for what is supposed to be a Relatively simple system a much simpler solution actually just uses functions instead of this pattern So what I would do is actually take This method here and I would turn that into a function called send email. I remove the self I Remove the class here and also the notification class no longer need and same thing for SMS I will do send SMS so that the function name is clear self is no longer needed and the class is no longer needed and I also don't need the factory in this case including this particular case where we're trying to handle an invalid notification method So in this case what we can do instead is not do all of this, but simply do Send email and then I don't need to do all of this work Let's run this again, make sure that this works as expected, and it does. So now again, we've made this a lot shorter and simpler. And in general, that's actually my suggestion. If you want to keep your code simple, is to actually start with functions, because often they're simpler than classes. And then if you notice that you need them, if you notice that your code needs a way to group some data and apply some operations on that data, only then add classes. The fourth tip is that you need to organize yourself better. If you want to create simple software, you need to also have simple processes for organizing and managing your software development projects. For example, at Ironcodes, we use Kanban boards for everything to keep it pretty simple, and it works really well. Also, if your projects are scattered across a bunch of different platforms, it's going to be really annoying. You know, your deadlines start slipping, you're not using your resources, your team effectively, and time and money is wasted. And that leads to software that's just not as good as it can be. With today's sponsor, monday.com, your entire development team can collaborate effortlessly. Think of it as your sleek all-in-one workspace where you can assign tasks, set deadlines, and track everything all at a glance. You can plan roadmaps, manage your sprints, release projects fast on one flexible platform. In my own experience, setting this up well makes a massive difference. When you need Kanban boards, Gantt charts, calendar views, monday.com has you covered. Switch between different views to see your projects from every angle and stay on top of everything. You can automate engineering work so you can focus on developing your product instead of doing repetitive administrative work. And on top of this, monday.com integrates with GitHub, Jira, Slack, and more. monday.com's dashboards give you a clear snapshot of your projects and team's performance to give you instant insights without having to dig through spreadsheets or reports. And using a platform like monday.com means that as a team, you can work more asynchronously. You'll need less meetings and you'll get more work done. If you're ready to take your productivity to the next level and keep your projects and teams organized, head to the link in the video description to try monday.com for free today. The fifth tip is to keep your functions short and do one thing. In other words, cohesion. If you have long functions with many lines of code that do lots of different things, they're going to be really hard to understand, hard to test, and also hard to work with. Each function should have a single clear responsibility. An example here is that we have in our employee class a take a holiday method. Now this actually does multiple things. It lets the employee take a single holiday or it pays out five holidays and does this if else statement that basically handles these two different cases. In this case, you can actually detect already in the signature that there is an issue with this function because the payout Boolean already indicates that there is too much going on here, that there's two options, two different things that this function does. You can also take a look sometimes at the name of a functional method and if there is or inside it, take holiday or pay out holiday, then you know that the function is doing too many things and you should probably split it up. So here what I would do is I would add a new method called payout holiday. And then you can take this like so. I remove the Boolean here and then I'm going to call this take a single holiday because that is what the method is actually doing. I remove this code as well and I'll de-indent this and then of course I have to change here. So here I'm going to change this to take a single holiday and I don't need the Boolean anymore. And now let's run that code and we get exactly the same outcome except now if you look back at the employee class, you see that we have two methods now that each do one specific thing. So the next step is to avoid hard-coded values and that's actually another problem with these methods right here and that they have hard-coded values. For example, here payout holidays has the number five, which is hard-coded. You could even say that take a single holiday has a hard-coded one. So you might also want to make that more generic, especially here with the five days that appears here and here. So if you want to change that, you need to now modify it in two places and make sure you don't forget about it. So ideally these type of constant values need to be defined separately from your code and that can be in a database, you can do that in a config file, etc. Now the simplest solution if you don't want to create a config file is to at least put these type of hard-coded values in a constant and put that at the top of the file so that it's easy to find and modify. So here I can create a payout holidays days and that's equal to five and I'm going to take this constant value and then I'm going to replace the five by that. So now if I change this, I only have to change it in one place. So that makes it much easier. Tip number seven is to use meaningful names. If you use good names that eliminates the need for comments. For example, if you take a look at this freelancer class that I removed before, there are some instance variables that don't really have a clear name. For example, unclear what amount and retainer actually means and what are the units, right? So instead of this, you could write something like hours per month. And then we know, oh, it's about hours and it's on a monthly basis. And for the retainer, you can do something that is very similar. So we could instead call this monthly retainer. So we know that this is about one month and not for a year or something. This is already much clearer, but of course, you might still think that those names are not very clear yet. It's kind of up to you to decide how far you want to take this. In short, actually naming things is really hard and there's not always a single best solution to it because developers will have different opinions about names as well. Tip number eight is to document your thought process and not just what the code does. That means that if you add comments to your code, you need to write a short explanation that explains why something is done, not just how. If you take a look at the code right here, you see that there's a comment that checks whether self.vacationDays is less than one. Now this is not really a useful comment because we can actually see that right from the code. Instead you want to write comments that explain why we're doing certain checks. So here we can do a check whether the employee still has holidays left. And now this is a much clearer explanation of why we're doing this particular check right here. So don't just describe what's happening, but actually explain why you're doing it. By the way, if you want to get better at diagnosing code and detecting issues like this, check out my free code diagnosis workshop at rn.code.diagnosis. This teaches you how to spot design problems like this and help you improve your code more effectively. The link is also in the video description. Now I have two more tips, and then there's a bonus tip that actually makes a big difference. Tip number nine is to not overcomplicate your module structure. Flat is overall better than nested. You want to avoid unnecessary folders and layers and only introduce structure when it's actually needed. As an example, I created a modular version of this employee system, and that's right here in this folder. So as you can see, there is the Ops folder. We have company, inside company there's company.pi, which is the actual company that we had before. We have a config folder that's currently mostly empty. Then we have employees. There's an employee class, then there's types, which has the different types of employees. So if you take a look at this structure, there's way too many folders and subfolders for a code that is of this complexity. I mean, the example is not that complex, but it does look very complex by having this structure in place. And that also means that at the moment, importing things is quite cumbersome. For example, if you take a look at the main file, you see that we need to import company from ops.company.company because, well, we put company inside a company folder. So in this case, that extra level is not needed at all. We can simplify this, for example, by simply moving this and then removing this company folder. And now I can just import it like so. And that's much simpler. And same for employees. I mean, do we really need a different folder of employee types? I don't think that really adds anything in particular. So let me just move these up to the employees folder, remove types. And that also means I can clean up my imports right here, like so. And in my main file, I don't need to include this either. Now, you can, of course, explicitly import these in an init.dunder.py file to make at least the imports a bit simpler when you use the package. But overall, it's simpler to keep the structure a bit flatter and not add more complication than necessary. Finally, the config folder is currently empty, so I'm simply going to remove that. You ain't gonna need it. Start simple and refactor only when necessary. The final tip before the bonus is to write tests for critical code. So, simple code is code that doesn't break. So, once you organized everything into modules, not too many, you kept things simple, then writing tests is also much easier. In essence, writing tests actually helps you keep things modular and clean because you have no choice, you have to organize things. And that's one of the great benefits of writing tests. So, how do you do that? So, if you have your ops folder right here, then you typically would have a test folder that follows the same structure. So, here I have, for example, test notification. And that actually tests the notification file right here. So, this sends emails and sends SMSs. This is not a full suite of tests. I just added this as an example. But as you can see, I do keep the same structure. I write test underscore in front of it, as well as the function. And then I'm writing a simple test. So, in this case, I'm testing sending an email, and I'm using Capsys that captures the system out. That's something that's built into PyTest, which is really nice. And what I do is simply create an employee, I create a message, I send the email to the employee, I capture the standard out, and then I assert that this is actually the message that we got. And when I run this, you see that this particular test passes. And then what you would typically do is for each file, you would add a file right here in your test folder, and you would also create subfolders for the various tests. So, for example, there is an employee subfolder, so now I would also create an employees subfolder in the test folder. That way, if you're working on employees, then it's also easy to find it back in the test. Finally, the bonus tip. Spend about 10% of your development time on refactoring. Refactoring is basically a necessity. You're always working on improving your code, and you need to always spend a bit of time there. If your code gets messy, you need to clean it up before it slows you down too much. So what I typically do is small, frequent refactors instead of working on code, adding a bunch of features, and then once in a while having to do a big update and having tons of technical debt that you need to remove. If you keep it small and frequent, then your refactors are going to help you prevent technical debt. And I typically spend about 10% of my time doing that, refactoring, improving the code. And it helps me set up for success because then later on, it's easier for me to add new features or make more improvements to the code. And actually, it helps me learn more about the domain and about how things fit together because once I have my code base, I can start moving things around, reorganizing things, and at the same time, gain a better understanding of how everything fits together. Now, here's a nice exercise that I like to do from time to time. I highly recommend you do that as well. Take a piece of code that you're not particularly happy with, something that's not too large, but also not too small, and then start working on it. Make it simpler. Reorganize things, add some abstraction. And here's the trick. Go on refactoring that code much longer than you would typically do. You're going to reach a point where you feel like any changes you make in design are just going to make things worse or they don't really change things all that much. And that's when you know you're done. Now, of course, you don't always have to take it that far when just writing regular code, but it's a very instructive exercise because this forces you to think about design alone and not about adding more features. Anyway, which of these tips do you already follow? Do you have your own favorite techniques for keeping your code simple? Let me know in the comments. I've talked a bit about software testing in this video. If you want to learn more about that, including a very simple way to set it up in VS Code, watch this video next. Thanks for watching and see you next time.

---

## 45. How to Use Git Branches Like a Pro (FastAPI Example)
**Channel:** ArjanCodes | **Views:** 47K | **Date:** 10 months ago | **Duration:** 19:00 | **ID:** viAZQjs5lHk
**Link:** https://youtube.com/watch?v=viAZQjs5lHk

### Transcript:
should you merge or rebase fast forward or not if 
git has ever confused you well you're not alone   today I'll take you through a realworld git 
example using fast API we'll see exactly how   branching merging rebasing fast forwarding works 
step by step and what the pros and cons of each   approach are i'll be using Git Kraken to show 
more of these things they're also the sponsor   of today's video now let's dive in before we start 
I'm going to create a folder and initialize a git   repository there now I have my folder i'll just 
create a new git repository and I'm going to use   the main branch the example that I'm using today 
is a fast API project that just has a simple hello   world endpoint i've copied the code right here 
as you can see this is really straightforward   i'm using UV to manage this project so 
let's start the server and see what it does and as you can see when I open a browser on my 
local host this works as expected now next to   this basic setup I've also created a separate 
folder called tests that contains a basic unit   test for this endpoint so the only thing I'm 
doing here is creating a test client from the   fast API library importing the app set up that 
test client and then have a test hello function   that does a get request and then checks that we 
get a status code 200 and that the response is   Kraken as you can see the files of this project 
have not been added and committed yet so let's   do that as a first step so I'm going to stage 
the changes and then I'm going to add a short message like so in Git Kraken you can now 
visually see what's happening with your Git   repository so this commit is right here now let's 
say you want to add a goodbye endpoint instead of   working directly on the main branch the idea is 
to create a feature branch now you can do that   with the git CLI as follows but you can also do 
it in get kraken to create the branch that case   it's really simple just click branch and then 
I'm going to enter the branch name like so and   now there is my feature / goodbye branch so now 
let's modify the main file to add the goodbye   endpoint like so and then let's also create 
a test for that while we're at it and as you   can see GitHub copout already generates this code 
for me which is great so it's basically the same   as test except we're testing the goodbye endpoint 
let me save that going to and let me run the test   and we see that both of these tests pass now when 
I run the server again we see that hello world   still works as expected and the goodbye endpoint 
also works now you go back to git kraken you see   that there is a new work in progress point here 
so what we can do is then take a look at these   files that have changed and as you can see there 
are two files there's the main file and the test   which are the two files that I modified so I'm 
going to stage those and I'll add a commit message here and let me commit that so now you can see 
that the feature branch has departed from the   main branch and I can easily switch back between 
the feature branch and the main branch like just   by double clicking on it and then you can also see 
if I go back to my code now the goodbye endpoint   is gone again so let me go back to this feature 
branch and you see there we have it back so that's   how you switch between branches in Git Cracker so 
now the question is how do you make sure that this   new feature ends up on the main branch so you 
could use a simple merge to do this if you're   using the git CLI this is what that looks like and 
this creates a merge commit it keeps both branches   histories what you do in Git Kraken is that you 
can basically drag this over to the main branch   and that's going to merge it or fast forward it or 
rebase it so we have some different options here   and I'm going to talk about these options more 
in a minute but for now let's just do a merge   now as you can see this feature branch is actually 
still there it still exists and we are still on   the feature branch let me switch back to the main 
branch and now you see what happened is that here   we were still on the main branch we created the 
feature branch that kind of created this little   side track here and then the main branch continued 
and then we merged these two and now I can start   working again on the main branch if I want to 
so the commit history as you can see here still   has that feature branch so what's nice about this 
is that with a simple merge like this you have a   clear record of how these branches were combined 
you can actually go back to oh what actually   happened in the various branches so you can also 
track more easily which features were integrated   when now the disadvantage of this approach is that 
your history can actually get quite messy if you   have many of these merge commits many different 
feature branches existing at the same time but   there are ways in git to simplify this by using 
other features such as rebasing or fast forwarding   let me show you how that works so let's say I'm 
going to add another feature the possibility to   add a name to the goodbye endpoint so let's 
create another branch called feature goodbye   name and now I'm going back into my code and 
I'm going to write a name argument here which   has a default value of world like so and then 
I'm going to turn this into an string like so let's also add a test there we go and let me run pi test again we 
see that all the tests pass and I just want to   start the app again to see if that actually works 
so here have my browser and now I'm going to add   a name article and that works as expected great 
so now again I have my work in progress here so   going to view the changes main and the test have 
been modified so now I'm going to add name option   to goodbye endpoint these stage and let me commit 
that so now I have those again on feature branch   now we have the situation where the main branch is 
right here and we have this single feature branch   that basically continued off the main branch 
so this is your situation if there's no new   commits on the main branch which is the case here 
then git can actually fast forward the branch and   that means that it basically moves main forward 
without doing an actual merge commit so now when   I drag over this feature branch to the main branch 
like so you see that I have fastforward main as   an option here so let me do that and now you see 
that actually the feature branch it still exists   the main branch it still exists but they are 
at the same point so there is no separate uh   path here anymore like we had right here with 
the normal merge so that's what fast forward   does it means that if this is your standard 
way of merging of uh taking the main branch   and moving it up to your feature branch then 
you maintain a nice linear history because the   commits have basically been directly applied onto 
the main branch so this is pretty nice especially   if you're doing pretty small independent changes 
it keeps the history clean however it's no longer   clear which commits belongs to which branches or 
features that you developed in the past because we   just have this single path going from here to 
here we don't have this little line appearing   here showing me oh in this particular feature 
branch I did this the issue is that fast forward   doesn't work if there are new commits on the main 
branch while you worked on your feature branch   so let's say we create yet another feature 
branch that renames the goodbye endpoint function like so and I'm going to call this 
goodbye name like so let me stage that change and   actually I don't think I need to change anything 
in the test let's just run pi test just to be sure   no that's not needed and now let's rename the 
goodbye function and I'm going to commit those   changes to my new feature branch but now let's say 
we go back to the main branch and someone else my   dark alter ego decides that it's a great idea to 
add the name option that I had in my main file or   in the goodbye endpoint and they also want to add 
it to the hello endpoint so let me copy this over   like so and then this is going to be an fstring 
and then this is going to be a name and let's say   that I go back to my git kraken that this person 
actually worked directly on the main branch so I'm   going to stage those changes added name argument 
to hello and now I'm going to commit those   so what happened now is that this was previously 
where my main branch was at when I created this   feature branch and then I did some modification 
and committed that so at the same time someone   else worked on the main branch and committed some 
change namely that I added this name argument to   hello and now this is all me but you can imagine 
that there's different team members that work   on different branches in your code and this can 
definitely happen now the problem is if I now try   to merge these things so if I drag this feature 
branch over to the main branch you see that fast   forward is no longer an option and that's because 
there have been changes to the main branch since I   made this feature branch so in this case rebasing 
is a great option what happens when you rebase is   that the commits that you did to your feature 
branch are temporarily removed so basically   history is rewritten so the first step in rebasing 
is to actually remove this particular commit right   here then the feature branch is moved to the 
latest main and then the temporarily removed   commits are reapplied to that let's see what 
happens when I do this in get kraken so I'm   going to move this feature branch over to the main 
branch and I'm going to rebase this so now it says   that to start rebase I need to check out branch 
feature new goodbye do you want to do this now yes   I do and now you can see that things have changed 
here actually what happened now is that we still   have this main branch here but the feature to 
rename the goodbye function is now applied again   onto the feature branch that is created from the 
latest main branch so that's what it looks like   now you can also see in the code it actually has 
combined these things so I have my hello world   endpoint that has the name argument and have my 
goodbye endpoint that also has the name argument   and now that we have this we can simply do a 
fast forward again so that main moves up to my   latest feature branch like so and now this is 
again a stable situation where we have a nice   clean history and the main branch is currently 
synced with the feature branch that I did before   or of course if you want to preserve history you 
could have also done a normal merge instead of a   fast forward so the nice thing about rebasing is 
that there are no extra merge commits it typically   leads to more clean readable solution because 
you're you can just do a fast forward merge   after you've done a rebase like I've showed 
you but there are cons like you're rewriting   history and that can actually cause issues for 
example let's say somebody makes a modification   to a function that you also worked on in your 
feature branch and then you're going to have   a conflict while you're rebasing and you have to 
solve these one by one now Git Kraken has a nice   feature called interactive rebase that makes this 
process a lot easier in short merging preserves   history but it can look messy fast forward keeps 
history clean and minimal but only works if main   hasn't changed and rebase rewrites the past but 
like I said you may have to deal with conflicts   next to what I've shown you already Git Kraken has 
lots of other features ranging from better code   change suggestions managing active pull requests 
visualizing how pull requests are merged into your   repositories with Git Kraken insights if you're 
thinking about trying Git Kraken all the tools   are free to download and get started if you use 
my referral link you get 50% off your first seat   of Git Kraken Pro so it's only $4 a month plus 
when you sign up through my link you help support   my channel there's no catch just an awesome DevX 
platform at a discount links in the description   if you want to check it out the system I use today 
for adding features has a name it's called Gitflow   gitflow is a branching strategy for managing 
feature development releases and hot fixes in   a structured way that's basically what you saw 
when I named my feature brands feature slash and   if you would have a hot fix it would be called 
hotfix slash gitflow is natively included in Git   Kraken and you can enable it in the settings right 
here so if I scroll down I can go to my git flow   settings and then do an initialize git flow and 
now as you can see there is this extra menu item   here called gitflow where I can do things like 
look at the various features i can also open   git flow and then create with a new feature or 
release or a hot fix and this is already going   to apply the right naming automatically which is 
nice so another thing that you see here and that's   also a standard part of git flow is that you have 
your main branch which is what we had before but   you also have a developer branch that's going 
to be the branch that you use to integrate new   features before you actually release it and then 
you have these feature branches so these are the   ones that I created in the example today then you 
can also have release branches so these are for   final testing before deployment and you can even 
have hotfix branches that are urgent bug fixes   you do on main branch now if you don't want to 
work with all these different types of branches   and following a standard like this then a good 
alternative is trunkbased development but that's   not a magic bullet as you'll see in a minute now 
with trunkbased development the idea is that you   basically always work off a single branch but you 
enable and disable features using feature flags   the idea behind this is that you always work on 
the main code and that you're therefore running   a way lower risk of deviating too much from the 
main branch which means you have to resolve a   bunch of merge conflicts now the problem is 
of course that you are working off the main   branch and that means that you're going to need 
to write unit test in order to make sure that   everything is working correctly so unit tests are 
a really good and necessary mechanism that helps   you make sure that the code you write doesn't 
break things for example let's say you want to   put a feature flag around this goodbye endpoint 
that we wrote so I can do this in a very simple   way let's say I make a a constant called feature 
goodbye and I'm going to set that to false and   then you can add your feature goodbye check 
right here and now I've basically created this   uh feature flag where I can enable and disable 
a particular feature simply by changing this   constant value here um I can commit this code uh 
if I make sure that this is set to false this is   not going to show up in production it won't be 
there of course I also have to make sure that   the test actually reflects that so in this case I 
would also import the uh feature goodbye and then   these two tests are only done if feature goodbye 
is actually set to true and now when I run a pi   test you're going to see that I just have one 
test but as soon as I enable the feature I have   my three tests back again so now it means as long 
as we make sure that this is false then we can   safely work on this endpoint without having 
to worry that it breaks the rest of the API   but this approach only works when this feature 
is relatively separate from the rest i mean let's   say you want to change the hello world endpoint 
to include an option to make the greeting more formal let me clean that up a bit so when 
you add this it's relatively easy to add a   unit test to make sure that this works 
as expected but unfortunately there is   no easy way to disable this formal argument 
now you could for example override this and   make sure that it's always set to false so it 
will always still have the original behavior   in this case but actually it's going to be more 
complicated if you generate your API docs based   on function signatures because then the formal 
argument is going to show up so we're going to   have to do work to remove it again uh or 
alternatively what you're going to need to   do is to actually maintain two versions of the 
same code so you might want to have a feature   uh formal like so and then uh if uh feature 
formal then this is going to be the function else and then we just have our regular hello 
endpoint but now you see there are already some   issues like there is a double function declaration 
which well is not really a problem because it's in   the if statement but we have now duplicated code 
here because we needed some code to separate these   two uh versions of my uh feature branches so 
the trade-off here is that this works but it   can also quickly become very complicated and much 
harder to read your code so in my opinion Git Flow   is still a valid way of managing your development 
process especially if you're working on code that   needs lots of changes all over the place but 
what do you think do you use feature branches   in Git Flow do you use trunkbased development do 
you use some combination of both let me know in   the comments i hope this video helped you improve 
your Git game and gave you a better understanding   of the different ways in which you can create 
and merge branches now if you enjoyed this video   give it a like subscribe to the channel it's a 
small thing but it really helps me reach more   people on YouTube thanks again to Git Kraken 
for sponsoring this video like I said check   them out using the link in the video description 
it's a really great way to up your Git game if   you want to learn more advanced Git features 
including a really cool one that allows you to   check out multiple branches at once watch this 
video next thanks for watching and see you next

---

## 46. Coupling 101: Design Choices That Make or Break Your Code
**Channel:** ArjanCodes | **Views:** 38K | **Date:** 10 months ago | **Duration:** 20:39 | **ID:** MM9VQp-k0JQ
**Link:** https://youtube.com/watch?v=MM9VQp-k0JQ

### Transcript:
you probably heard people say that coupling is bad and should be avoided the problem is that you actually need coupling if you work with external libraries apis code that separate it into modules and folders you're going to have coupling there's no way around it but there's coupling and then there's coupling I'm going to show you three examples today of bad coupling and then show you how to refactor them into not as bad couling let's dive in here is a typical example of global coupling I have these constants here API URL version ad a token which of course you never put hardcoded in a python script an account ID some helpful type uh aliases here and then I have a function make request that directly accesses these things these constants here inside the function body and that's not a good thing I mean for some of these variables it might make sense like the API URL or the version I mean that's not going to change all the much and probably the code is going to dependent on a specific version of the API so it's not that bad but of course the actual account ID and the token you really don't want that to be part of a constant hardcoded in your python script especially if you're writing an SDK layer on top of an API then you can assume that this URL is going to be fixed so it could make it a constant however anybody using your SDK would need to supply their own account ID and token you want to make that easy and not have functions directly depend on globals for that now one thing you could do is simply pass all of these things as arguments to the make request function so let's refactor the code to change that so what I'm going to do is I will pass the API URL I'm going to pass the API version and then finally I'm also going to pass the token and the account ID which is an ins if I recall correctly yeah and now I can simply use these values instead and then of course I also have to refactor this main so it's uh it s something else but it doesn't really matter it's just for the example also there's another issue now that I have a default value here but here I don't have a default value so perhaps I should also add that so you already see the kind of problems I'm running into with adding all these arguments in any case I've now removed the global coupling so and it still works yay but wait is this actually better uh honestly no I replaced the global coupling that I had with pretty bad data coupling data coupling is when your piece of code relies on lots of data and that actually makes it hard to use and that's Al something that you see here right I need to pass all this information and now when I call this function I need to always supply all of that stuff which is also really annoying now of course I could do this in a slightly less bad way by for example removing these two things and then still relying on these constants in the function body but still that means that make request is going to have 1 two 3 4 five arguments which is honestly quite a lot and then you need to pass that every time you call the function so how do you solve this well that's where software design comes in you have to take a look at the structure of the code and come up with a better solution so in this case we have this grouping of information that we need to work with and we want to use that information and perform operations on it a class is actually a great solution for that and that's also what many sdks actually use as an implementation now let's refactor this code but now use a class so what I'm going to do is I'm going to start with a data class that I just need to import and then I'm going to create a data class API client and as part of the client I want well AI is already generating that for me the URL the version the token and the account ID and then make request becomes a method of that class so let me just indent this correctly and we're going to add self here and I will remove this and now of course we don't have to pass all of this stuff anymore and what I'm going to do is this will become self. token and now what I can do here is I can create a client like so and now I can make a request and then of course I don't have to supply this anymore I don't have to supply the token and the account ID I simply do it like this let me run this so yeah this still works as expected so this in my opinion is a much simpler solution you create the client want and then you call the method on it and you can even introduce post get and other helper methods that call make request internally it's not a perfect solution though because say you create a layer on top of this to handle invoices so I would have another data class here and that's an invoice and then let's say an invoice has an ID it's going to have an amount it's going to have a I don't know a customer ID and payment view which is just a string and then I'm going to have a function send invoice and now this function actually uses the API clients to actually send the invoice so we pass the client and then we call make request on the client and we call the invoices endpoint so the problem that this introduces that you need to pass the client object to every call of a method because there may be other types of classes maybe we will also have orders and customers and other things that will also need methods that interact with the client and every time you need to pass the clients that's just annoying now solution of course could be to make client again a global variable which then introduces again Global coupling or you could make it a member variable of invoice but it doesn't really make sense to me because it's not really part of the invoice data structure in short there are no easy solutions here you're always going to have to deal with finding the right balance between Global coupling what you pass as an argument what do you group into an object and unfortunately tools like GitHub copilot or other AI code editors are of limited help here I've tried many times to use AI to introduce design pattern or help me come up with a better structure of a module it's very hard mostly because llms are just bad at Advanced reasoning and that's the most important thing you need to do in software design that's why I also don't really believe AI is going to replace all developers anytime soon it can generate codes sure but that's maybe 10% of the work we actually do that's all something I hope you realize that the world is still going to need great Engineers if you want to learn yourself how to design a piece of software from scratch I do have a free guide for you you can get this at rn. codesign guide this contains the seven steps I take when I design new software and hopefully that's also going to help you make better design decisions so under codesign link is also in description of this video let's take a look at another example of bad coupling so what you see here we have an account data class that has an owner and a balance and there's a couple of methods here so we have a deposit method that uh increments the balance with a certain amount we have withdraw there are some checks in the beginning and then it withdraws the amount from the balance we have a property and then we have a couple of other things and a function called pay service fee that gets an amount and a payment type and that modifies the account balance directly by using the service fee for a particular payment type this is an example of content coupling content coupling is when one module directly accesses or modifies internal details of another module that's what happening right here this pay service fee function which is not part of the account directly modifies the balance instance variable the reason this code was designed in this way is that pay service fee can't use withdraw because that's going to raise a value error if the amount is larger than the balance and in case of service fees we actually want to allow negative balance that's basically a rule of this fake B bank so that's why this is done directly like this and that way withdraw is still safe if you call that then there's going to be some protection that you can't withdraw more than you have on your balance let's run this and see what happens now this works as expected uh of course we do get the type issue here that balance is protected and used outside of the class which is declared which is true uh but it still works because python ignores this at a run time now one thing that we could do is actually turn pay service fee into a method so that would mean that I would take this and then I would add this to my balance data class like so and then what I can do is simply pass self and then I'm going to do self. balance minus the service fees and as you can see there is some issue here because it tries to look for the payment type so we probably want to move this up so it knows that exists like so and now this is actually solved and now of course also here we need to do account. pay surface fee and then I remove that argument because that's no longer needed so let's run that and see what happens so exactly the same results no surprises here so you could say okay that's solved I removed the coupling between these things because we turned it into basically a single piece of code single class but wait because now actually that means that if this is like a separate module the account module you also need to store the service fees there and it needs to know about payment types so accounts the account class now becomes bigger and that's a problem because the more features you tag on to accounts the bigger the account classes going to be and that is going to be problematic obviously it's not great so a better option in this case to think about the communication what information needs to be shared with the account class and how can we make sure that simple withdrawing isn't affected that that's still safe so a possible solution is actually to introduce a withdraw unsafe method that does allow for negative balances so let's add that instead so the only thing that this basically does is a simple wrapper around the balance instance variable and that allows to subtract an amount and now what you can do is we take pay service fee and we turn that again into a regular function and then we're going to call the unsafe version to withdraw the surface fee and then here of course I remove again this and I add account back as an argument if I type that correctly like so and it still does exactly the same thing so what's nice about this solution is that pay service fee now again cause a method which is what we want there's no direct access to instance variables also we still have the safe withdraw method but there's also an unsafe method that we might want to avoid calling but at least it's in the name so as developer you know that okay if I call this I have to be careful another thing that is actually nice is that I can reuse this method in the withdraw method so instead of copy pasting this code like what I'm doing here I can also remove this duplication and then basically call Self do withdraw on safe and then the amount so that now means that there's only one place where we're directly modifying the balance which is great again it's also not an ideal solution because this means that you do have an unsafe access mechanism to a bank account you might not want that so you know that's something that you have to think about on the other hand in Python there are no safety features you can always directly modify instance variable so yeah that's kind of the trade-off that you have to make it does show a fundamental issue though that you always need to think about we designing software like what is the boundary between pieces of code between modules what do you allow and what do you encapsulate by the way if you're enjoying this video you're finding it useful give a like And subscribe to my channel it really helps out and you get notified about more videos like this one all right let's go back to the examples the third example that I want to show you is this one and this is actually an example of what we call stamp coupling so you can see we have a transaction type string enum so again this is in the banking financial sector we have a transaction which has an ID and the type and an amount and time and a couple of other things as well and then I have a couple of functions one function that logs a transaction so this transaction and that logs the transaction type the ID and the Tim stamp also have a process transaction that depending on the type of transaction is processing it now it's not actually doing anything it's not really the point but it is accessing some member or some instance variables of the transaction object and then I have a main function where I create a transaction and I call these functions let me run this and then this is what we get now what is stamp coupling exactly stamp coupling occurs when one module one piece of code passes a complex data structure some for example an entire object or a larger record to another module another piece of code even if only part of that data structure is used and that's exactly what's happening here so logging the transaction gets a whole transaction object but it only needs the type the ID and the time stamp it doesn't care about the customer ID or the amount and same for Pro processing the transaction it simply looks at the transaction type and the amount it doesn't care about any of the other things now at the moment that's not really a problem but it can lead to issues if this transaction class changes in the future at the moment it's not clear if you look at the signatures of these functions what part of the transaction object they're actually using you have to dive into the code to figure that out so ideally you want to reduce this type of coupling because potentially this can result in all sorts of issues where you're modifying a transaction and then getting an error here but it's not even using that part of the transaction so how can you solve this uh one way is to only pass the data that you actually need by passing it as Arguments for example if you take a look at process transaction instead of passing the entire transaction you could actually only pass the uh transaction type and you can pass the amount like so and then you would of course also need to change the function body like so so now that stem coupling here is gone and then of course if we call it we also need to pass the transaction type and we need to pass the amount and then well when we run this this is going to do exactly the same as before in my opinion for process transaction that's a reasonable solution this adds one argument to the function which is okay okay now let's take a look at log transaction so here if we were to do the same thing if we have the same solution I would replace transaction with we have the transaction type and we have the uh transaction ID which is an integer and we have the transaction time stamp like so and now of course I also need to modify that right here like so and then I will also need to change that here like so when I run this I get the same results now here I'm not so sure that's a really good solution because now log transaction has three arguments which I think is already on the limit of what you want in a function and if you want to log more things in the future now you have to add more and more arguments so it becomes harder and harder harder to read so the question is how can you still pass the transaction object without the dependency let's use abstraction so what I'm going to do is from typing I'm going to import the protocol class I'm never sure whether I should import this from typing or from collections. ABC or whatever for now I'm just going to stick with typing because I think it's really the clearest to me it makes the most sense that I take it from there but then what we going to do now is we're going to create cre a class let's call that a loggable and that's a protocol and that's going to have a transaction type it's going to have an ID and it's going to have a timestamp and then in my log transaction function I now pass a loggable object like so and now I can do I'll just put back the code as it was before and then we have the Tim stamp like so and then here in my log transaction I can simply remove these things again and simply pass the transaction object just like I did before but now log transaction again has only a single argument but it's not directly dependent on the transaction class I can pass anything that has these types of instance variables as long as it has these three things I can call log transaction on it and if I make changes to the transaction class it doesn't affect this particular protocol so at least in this case it's explicit but this also comes with the trade-off if this kind of thing happens everywhere throughout your code you're going to end up with protocol classes everywhere which is also going to be messy now another thing you could do is turn logging into a method of the transaction class but that has other tradeoffs again like making the transaction class larger and maybe logging is not something that you want to be part of that because it's maybe a separate action that you want to take or that you want to modify for example by being able to send all logs to a file or uh to the console or to some logging surface and if that's all over the place and all sorts of different class that doesn't work either so again more and more tradeoffs in short as you can see from these example removing coupling is not just a question of doing some work and then it's all fixed or something it's mostly a trade-off you're going to have Global coupling content coupling stamp coupling these things are not great and it's best to avoid these things if you can but often you can actually replace them with a less evil type of coupling as I've shown you today but I'd like to hear from you how do you take these types of decisions do you think about these things when you write code let me know in the comments if you enjoyed this video you might also like my take on the Gilded Rose refactoring problem in that video I show you how to create a safety net of unit test and then step by step improve the code and remove a ton of coupling check it out right here thanks for watching and see you next time

---

## 47. How To Design Robust Python Functions
**Channel:** ArjanCodes | **Views:** 59K | **Date:** 10 months ago | **Duration:** 20:56 | **ID:** g-Cytq7YDCc
**Link:** https://youtube.com/watch?v=g-Cytq7YDCc

### Transcript:
this is okay and this is not okay I know this looks like I'm contradicting myself but there's a reason for this it's all about the details if you make the wrong design decision even just a small mistake you'll end up with brittle functions that easily break I'm going to show you the main principles that I use to write code that's less likely to fail with of course plenty of python examples this video is sponsored by localized more about them later because of the dynamic nature of python you don't need to use type hints so you might wonder what if you don't use type hints should you then actually check that an object or value is of a certain type and even do different logic based on that it's actually something you see quite a bit in older python code let's take a look at a few different scenarios there are basically four of them the first option is more or less YOLO no types nothing there's no indication whatsoever what the function might do when it might go wrong no checks nothing so that's what you see here we have calculate average it expects numbers we don't know what that is and then it Returns the sum of the numbers divided by the length of the numbers now of course if I run this even though we get all these little red squiggly lines it actually works because python actually ignores type annotations so even if there are no type annotations a python script will still run and this case it simply calculates the average now of course this is pretty brittle right because if we pass an empty list well then we're basically going to get a zero division error and here we do that explicitly but by accident it's possible that we pass an empty list and then we might want to give some more helpful information second option is that we do runtime checks before we call actually the function and then raise an error if basically something is wrong so that's what you see here so if my list which is in this case a list of Ines L like before but then we're going to just check that it's actually an list instance and that it contains integers for all of the things that are in the list now in this case of course that is the case but if I change this to let's say a floating point then we're going to get an assertion error that actually it's not an integer so that's another thing you could do right now the third option is that you actually do runtime checks inside the function and that can then raise an error so for example here we have calculate average again the safe version of that which still gets numbers we I did didn't add any type annotations here and then I'm just doing some checks so if it's not an instance of list then I'm going to raise a value error if not all of the elements in the list are in this case either ins or floats then I'm also going to raise a value error and then I'm returning the actual computation so here I have a save version and this is actually going to fail if I do something like this and now you see we get a value error but this is at least a bit more meaningful information now the other option of course is to use type hints and that is what you see here so here I have calculate average typed but now I've added type annotation so I have an input type here which is numbers list of integers or floats and I also have a return type of float and then I perform the calculation and of course this works exactly in the same way as before but then perhaps I should do an actual print statement here so we do a calculate average types and that is going to be the results that Jupiter automatically prints the last value so when you take a look at these options and in general the types are here either checked within the logic of the code or they're not checked but at least they're being indicated to the developer except for this very first version now if you check types as logic so that's basically what's happening in this second option where we do before the function or in the third option where we do it inside the function that is actually problematic I mean in this case they blows the code with assert statements which is just really annoying it doesn't provide any additional functionality also this is actually not really that precise because what happens for example if somebody makes a subass of list it might break these assert statements because that's an instance of another type right and thirdly by doing this outside of the function it means that every time you call the function you're going to have to do these checks which is just really annoying in my opinion this just makes your code a lot harder to use so my recommendation is to not do any of this but simply rely on the type annotations because those are kind of an implementation of these types of checks that you have right here now of course this is not going to raise an error but in any case types are mostly useful in the development phase they don't really matter during execution at least in the case of python final thing you have to be careful of if you add type hints like this is that you don't accidentally make them too restrictive for example in the case of calculate average I pass a list of in or float so this is already something I did to make it a bit more generic but actually when you take a look at what the function does this also works for let's say a set or a tupal so uh you could try to come up with a type that is a bit more generic so that's what I did here so I have a type number which is int or float and then I've defined a type number list which is a list of numbers or a set of numbers and probably this is not a very accurate name I should maybe call this number collection like so and then of course also change the type annotation right here so now because I'm allowing for both lists and sets I can also call calculate average types with a set like I'm doing right here in my opinion there's also a trade-off here like how much time do you really want to spend making it as generic as possible I guess if this is just an internally used thing you don't have to spend too much time on it however if this is part of a package that's being used by thousands of people all over the world you might want to spend a bit of time making your type annotations more precise I want to show you another example of how I sometimes see types being used so in this example I have a function process content that determines logic based on the type of the input so what process content does is that it detects offensive words in a piece of content so here if it's a string then I'm going to do this this if it's a dictionary then I'm going to do this if it's a list then I'm going to do something else Etc you can expand this function as much as you want of course so this is another example of making a function that's very brittle if you decide logic based on the type of your arguments then normally that should probably be different functions instead of putting all of that in a single function it's also quite combersome to work with because this can easily break due to Python's Dynamic feature and depending on the type strictness of your IDE type strict you will be either flooded with red marks the stuff that you see right here or maybe you just get some very vague help in terms of the typin now there are several ways to solve this one is you could simply split this into separate function so you could create a process content string a process content dictionary and a process content list and then call the right function depending on the type of thing that you have another thing you you can do is what we call function overloading where the type or the structure of the parameter determines which version of of the function is going to be called and you can do that in Python actually with something called single dispatch from the fun tools Library so if you want to do that I recommend you check out that library and it's pretty easy to do but actually that doesn't have my preference I would probably turn this type of thing into separate functions with slightly different names that indicate what what kind of thing it does alternatively you can also use a design pattern like strategy but then for example using a dictionary of function so you can point to the right function depending on the content type another thing that can make your code quite brittle is relying too much on hardcoded values a good case study of this is handling translations in software that's multilanguage this can quickly turn into a mess of hardcoded strings translations scattered all over the place and painful manual updates and that's where today's sponsor localize comes in localize is a modern translation management platform that helps developers product teams streamline localization integrates seamlessly with your codebase allowing you to manage translations more efficiently without interrupting your development workflow the API is actually really simple let me show you here's a basic streamlit data dashboard that displays Uber pickup when you take a look at the code you see that this dashboard users localize to provide the translated text in the interface I'm using the python SDK here that localize provides you with setting up the connection with localize is really easy you just need to create an API key in the interface and then create a client in your app now I simply Supply the API key that I take from the environment and I also have a project ID and then I have a couple of helper methods functions to quickly get a translation from a key so once you have this basic boiler pay code it's actually really easy now to get translations in the interface now as you can see in the dashboard one line of text hasn't been translated yet so let's add this here I have the localize interface and I'm going to define a new key called NB pickups hour and I'm going to give it a default English translation in my app I don't need to do anything I can simply restart the app and as you can see the new translation is now right here now it's really easy I can now also add other translations like for example the Dutch translation now ai is directly integrated can simply click that save that and now I have my Dutch translation ready to go switching to another language in my dashboard is not really easy I simply replace the value of this language consent by Dutch in this case and now I have a translated dashboard this is all being handled by localize so if you're dealing with complex localization localize makes things way easier with AI power translations you get results in seconds literally 10 times faster than doing it manually and it's not just fast localized AI keeps things accurate with glossaries and context aware translation so your content actually makes sense in every language faster better translations mean you can launch in New Markets without the usual headaches on top of that you can assign review and approve translations in one place no more messy spreadsheets or endless back and forth Just A streamlined workflow and when it comes to tracking progress localize gives you real insights like how much of your content is AI versus human translated and predictions on when translations will be ready so you can launch on schedule try localize for your project today using the link in description now back to the video so before I've talked about type constraints you want your inputs to be of a particular type but how about value constraints for example an integer that needs to be positive a list that needs to be not empty only even numbers Etc basically things that are not encoded in types where do you put these types of cont constraints clearly you can't use type hints for that one thing you could do is perform checks outside of the function just like I showed you with the type constraints but I don't think that makes a whole lot of sense in my opinion these types of checks should be inside the function so that whenever you call the function the function can check that the contract the things that it gets are valid for example here I have an initiate client function that creates an open AI client but it needs an API key and it wants to check whether actually that API key is is valid so there's some code here that sets the API key checks whether it can read the list of models using that API key and then if that doesn't work it's going to raise a value error otherwise it's going to create the open AI client so this is a clear example of a value constraint that you put inside the function before you actually do something and that's a typical use case the good thing also of putting it inside the function is that inside the function you have the implementation details of in this case the open AI Library so you want to encapsulate that so that if somebody calls initiate clients that they don't have to think about how to check the API key now in this case it's also pretty clear that we want to raise an exception if the API key is not valid but that's not always that clear for example here I have again a version of calculate average that also has a value constraint so it checks whether numbers is an empty list but then what should you do if the list is empty should you raise an exception return zero this is basically a design decision and while both of these approaches whether you're returning zero or raising an error they both have merits depending on the context so here is how you can think about this so if an empty list is considered an invalid input or logical error in your application then raising an exception is more explicit and it's a safer choice if an empty list is a valid input then I'd say returning zero is reasonable this is often used in systems where no data defaults to zero like in some statistical or financial calculations the problem with this though is that this can mask logic errors and that could lead to unexpected Behavior so in my case actually I prefer stricter error handling so my recommendation is actually not to return zero in this case but raise a value error and in my opinion raising a value error here is a better solution than relying on a zero division error because now when I run this code you can see that we get the value error and it gives me more information namely that the list is empty whereas if I didn't do that then I would get a simple zero division error and I'd have to figure out why is there a zero division error so by doing these checks it allows you to give more specific information to developer so that they can solve the problem much more effectively how about optional values things that can be known and default values how does that affect functions being brittle or not so in this case I'd say you need to do checks as soon as possible that's also called the fail fast principle that's what you see here so I have a new version of calculate average which has numbers which is an optional list of ins or floats and this one also has a Precision which is again an optional in that can be none so in this case there is a check in the beginning of the function so one thing you could do is then raise an exception if numbers is none or if it's not empty uh it doesn't make a whole lot of sense to me because then why would you make it optional uh alternative you can also assign it a default value like so and then probably we could uh assign it some default values inside the list so it doesn't fail here it depends on what you want to do if a certain value is not provided so in this case in the case of numbers I honestly don't think it's a good idea to make it optional because it does make a whole lot of sense in case of the Precision it might make more sense to make it optional because if it's non then it's simply going to return the average and otherwise it's going to around it with that particular prision my recommendation though is that if you don't have any logic that's tied to nonv Value simply avoid optional values and if you can let type annotations help you with passing correct values then use them to your advantages so in this case what I would do is avoid the optional values alog together and basically here I have my numbers list and I have a Precision which default I set to to now of course if you want to have the option to not specify fire precision and in that case you want infinite Precision or whatever then maybe we can do it in this way then you could still use optional but overall I would typically avoid it and another thing that's also nice about this is that if you don't have optional values you don't have to do any checks for them so in this case it means that the function is actually much smaller and concise which I like these are pretty typical things to consider when you're designing a software if You' like to learn how to design a piece of software from scratch check out my free design gues Aton c/d designu this teaches you the seven most important things you need to do link is also in description of the video now I want to go back to the example that I showed you at the start of the video which was about returning non optionally now in that example I had a function to get a user by ID that's also the function that you see right here and you could basically decide to either return the user and then raise an error if the user is not found or if there is no user you could actually return none and in that case this type annotation here would be user or non and then basically this I would then remove and then I would have a function that returns n if there is no user so those are the two typical scenarios that you see in my view I have a preference for raising the error again because I prefer stricter error handling I prefer to raise an error instead of returning some value Val that you may not find very useful and in this case in my opinion semantically it also makes sense to raise an error because you pass an ID so you expect that if you pass an ID that you are probably going to find an object with that ID meaning that if that object if that us doesn't exist probably something is wrong and you want to know about it instead of getting num and that's in general the rule that I would follow if you're looking for an object by ID but it's not there raise a not found error if you're looking for an object that you might expect doesn't exist then it could make sense to return n for example here I have another function find user by email that we pass on email address so there I can imagine that actually it makes sense to return user or n because we pass an email address we're not sure if there is a user so in this case it could make sense to return n if that user isn't there that being said I would still raise an error in this case just because I prefer raising an error instead of passing a non value i' like to avoid n as much as possible now if you're looking for several users instead of one user for example here I have a get users function that gets a filter and in this case I don't think it should return none but if there are no users I just think it should return the empty list and that is actually what this code does because if in the for Loop there is never a user that follows this fil filter then it's simply not going to be added to this list ultimately though it comes down to the sematics of a function if it's perfectly reasonable that there's nothing that gets returned then yeah sure return none if you're expecting to find something raise an exception overall though in case of any doubt simply raise an exception don't return none because returning none is going to create a more brittle situation in your python code now one thing I do want to mention here is the maybe monads this is not built into python but you can actually use the returns package and that has a maybe Monet however I'd be careful with this because it's not a standard python feature and might be confusing for some developers so what have I talked about well first don't do deep checks of types before or in a function because that's going to lead to a lot of refactoring work if you need to change things around instead use type hints optionally with a static type Checker like mypi if you want to if you're dealing with value constraints like only positive integers or non-empty lists raise value errors or custom exceptions avoid optional values if you can but instead Supply a reasonable default value and avoid returning a nonv value except for a few specific cases where it might make sense overall in case of Doubt simply raise an exception it's the best way to go about it and finally feel fast do these checks early on and fail immediately instead of trying to ignore errors or return some value that hopefully uh is usable to the caller of the function now I'd like to hear from you though how do you approach these type of things do you think about these things when you write your python code do you have any tips any things that you would like to share please post them in the comments below now I did a full video while back covering the fail fast principle in more detail you can check that out right here thanks for watching and see you next time

---

## 48. This Is How Marie Kondo Sets up Her Pytest
**Channel:** ArjanCodes | **Views:** 28K | **Date:** 10 months ago | **Duration:** 16:02 | **ID:** jxqGsJEhiAg
**Link:** https://youtube.com/watch?v=jxqGsJEhiAg

### Transcript:
Today I'm going to walk you through how to set up PyTest from scratch in a really simple minimalistic way, Marie Kondo style I'll start with code that has zero tests I'll add PyTest, show you step-by-step how to write unit tests and even cover some advanced features along the way Including one feature almost no one knows about that's actually really cool. At the end I'll share some practical tips to make your tests cleaner faster and more maintainable. Let's get started Obviously, the first thing that you're going to need to do is install PyTest. You can install it using any package manager But in this video, I'm going to use uv which is fast and efficient. To add PyTest to your Py project file simply type uv add dash test dev PyTest This dev flag here ensures that PyTest is only installed in development mode, not in production You can also see that when you take a look at the Py project file So here we have dependencies, the regular dependencies of this particular example project, what these are doesn't really matter But you also see we have a dev dependency group that contains now the PyTest package and PyTest mock If you just checked out some code from a git repository that has a Py project file You need to install dependencies. You can install the dev local dependencies only by simply typing uv sync extra dev And this is going to install all the dev dependencies local on the machine If you're using pip and you want to install PyTest, then that's going to be pip install PyTest dash test dev and if you're using a tool like poetry, then it's going to be poetry add dash test dev PyTest, more or less the same as uv. The key takeaway here though is to always install PyTest as a dev dependency and use a virtual environment. Before I start adding tests Let's first take a look at the actual project we're dealing with here So as you can see, there is a source folder right here that contains a bunch of files Now what these files are exactly doesn't matter This is actually taken from Bracere, which is an open source project that we're working on that allows you to create translations and transcripts Using AI. We're using it ourselves for our YouTube videos But the question is of course if you have a source folder like this, where do you put the test, right? Now there's some people that say well you should put it inside your source folder. That's typically not what I do I like to put my test in a separate folder called test So let's add a new folder here and let's call that test like so now if you're using VS code like I am you also need To configure it to actually recognize PyTest and you can do that by creating settings for your workspace So here I have some sample settings that you can use for testing So one of these is that we have to enable PyTest that way VS code is going to recognize that you're going to be using PyTest Another thing that you need to do is supply this folder so that VS code can actually find the test that you want to run Now there's also some other options here that I have disabled for now One is that you can actually disable unit tests So then we'll only use PyTest and another thing you can do is you can supply this setting which is an environment file And then actually when you're running your test, it's going to rely on the contents of this particular environment file I'm not using it in this project but that's actually a really useful setting to have and in VS code itself on the left or on the right or wherever else you have configured your Icons to be in the interface. You're going to have this little icon here called testing And if you click that you see that there are now zero tests actually in the past I did already run a bunch of tests here that while I was preparing a script for this video But at the moment there are no tests as an empty test folder, right? But this interface is there because we define these things like for example, if I change these into comments Then you're going to see that there is no test in a workspace not been configured yet when I enable PyTest Then it's going to see this and then it's simply going to recognize that we have a folder with some source code And if I also tell it where the working directory is then it's going to show us this test Working directory which pass because there are zero tests at the moment. So pretty straightforward But it's just something that you're gonna have to do Another thing you might want to do is in your pipe project files Make sure that your Python path is set so that PyTest can find your modules when you're running your test And by the way, if your project already uses unit test Python's standard library module for testing Don't worry PyTest can actually still run these without any issue. So you can basically migrate Gradually instead of rewriting everything all at once now testing of course It's just one piece of the puzzle a solid architecture can save you from costly mistakes down the road whether it's unmaintainable code Performance issues unexpected bugs creeping in to help you with that I've put together a free software architecture checklist covers the key principles to keep your projects scalable maintainable and free of expensive rewrites grab your copy at arjan.codes/checklist  The link is also in the description of this video Now let's move on to actually writing some tests when you write tests The first thing that you need to think about is naming Convention and you do have a sort of folder structure here already and typically what I like to do is follow this Structure to a T so my test folder is going to have the same structure as my source folder That way it's easy to find a test belonging to a particular file And other things I do is that I name the files test underscore module name So for example, if you take a look at this file timer.py That's at the root of the source folder. What I would then do is that under my test folder. I would create a file test Timer.py or if I have a file called timestamp utils, I would do test timestamp utils So your test file always contains the module name Another thing you need to do is make sure if you define a test function It must start with test underscore. Otherwise pytest is not going to recognize it Now if we take a look at this particular module here timestamp utils You see that there's a function here called to timestamp that takes a time Delta and returns a string There are some special cases here like if the duration is Negative then it's going to simply return zero and then it's simply going to compute a string version of that particular time Delta So not a very complicated function, but it's something that we might want to test Right, and of course like a YouTube chef, I have already prepared some of these test cases for you So I don't have to bore you with me writing all that code. So in my test timestamp utils I'm importing the to timestamp function from the source folder I also need time Delta from daytime and then I have a bunch of functions Like I said, each of these start with test underscore that way pytest is going to recognize them So here I have an example to check that to timestamp actually returns a string this by the way would also be a really good Candidate for some parametric type of testing which is a more advanced way of writing tests that I won't cover today I did do a video on the channel while back about hypothesis property-based testing those kind of things actually pretty Interesting way of dealing with unit test and I have a bunch of other specific tests here Like I'm going to test the negative duration. So if a negative number of seconds, does it actually return this? I also test large duration. So if we have multiple days, does that also work? how about Specific time like one hour two minutes three seconds. What happens to a time Delta of zero? Does that work correctly? one millisecond and also some other cases like checking what happens with 999 milliseconds or with 500 microseconds, which should be rounded to zero now in order to run these tests I can simply switch to my console and then I type pytest and as you can see it's going to collect all of these tests By looking in the test folder and it found a test. We got one warning This is a deprecation warning because of a specific thing that the package is using that that I'm trying to test here But all of these tests pass you can also run these tests right from the VS code interface You do that by clicking this icon If you don't see that icon by the way Just click right here and then you can select Testing here and then you're going to see it pop up and now you see we have to test folder here I can press play and it's going to run these tests right from a VS code The nice thing about the interface in VS code that you can then also explore the test So for example here, you can click through these tests and then see what kind of output the test did produce And that's what you're going to see. Let's take a look at another example. That's slightly more complicated So in the source folder, I also have a translation folder here and there is a translation API file and I Let me just move this out of the way. If you scroll down, you see that we have a bunch of functions here, like splitting by breakpoint, translating it by calling the OpenAI translator, an asynchronous version of that, and a couple of other things as well. Now, if you want to add a test for this particular file, so that's on the source translation, then translation.pi, well, then we also need to reproduce that folder structure in your test folder. So I'm going to add here another folder called translation, and then I'm going to add a file, test.translation.pi, and this is where I can put my translation test. And again, like a true kitchen chef, I've already prepared some of these things. So then here I have a couple of different tests that I can run, like checking the empty list, checking the list of different types, checking what happens if the breakpoint is out of the bounds of the collection, and a couple of other things like that as well. Something that you're also going to encounter quite a bit in test, and something that you're going to have to deal with is mocking, which means that you replace objects with mock objects, and that's actually what you see right here, right? So we don't want to run a test by actually sending some prompt to OpenAI, so actually we create a mock translator, and there we provide a mock for the return value as well, so that we get the translated fake content. So that's mocking. Another thing that you will encounter is patching, and that means that's not so much that you're replacing an object with something else, but that you are replacing an import with something else, like a function that's called from the function that you're going to test. So now that we have these tests, I can run PyTest again, and then you see we now have 14 tests that pass. And also here I can simply run all of these tests, and then you're going to see that now I have my timestamp user test, but they also have the translation test, which are right here. Then you can navigate through them and see what is actually going on there. Before I show you a few cool features of PyTest, if you're enjoying this video so far, it would help me tremendously if you hit the like button. If you want to watch more of my content, make sure to subscribe to the channel. It's a really simple thing, but it really helps me out a lot if you do that. Thank you. All right, PyTest, there is a couple of really cool things that you can do. So one thing, of course, we've already seen this. You can simply type PyTest, and that's going to run all of the tests. You can also optionally pass the test folder if you haven't supplied that in another setting. So then that also works. What you can also do is limit PyTest to only do a specific module. For example, you can do PyTest test, and then we have test timestamp utils, like so. And that's only going to run the tests that were defined in timestamp utils, which is these eight tests. You can even run a single test function by writing PyTest test timestamp utils pi, and then you put a double column, and then you write the specific test. For example, test large durations. And then it's going to run this single test. So that's all really helpful. Now, here is a really cool feature of PyTest. You can actually run tests by keywords. So you can write something like PyTest minus K, and then you can supply keywords. For example, I want the test function name to contain the word timestamp and not large. So I only wanted to test small timestamps in this case. If you've been careful with your test function naming, then this actually works. And then if I run this, you see we now have seven tests, seven were deselected. And if I just do, for example, timestamp as a keyword, you see that there's now eight tests that are being run. So if you are careful about how you name your test, testing by keyword can actually be really helpful, because then you can be very focused on what kind of group of tests you want to run and which tests you want to avoid running, which is going to make your testing process much easier, because then you can just run them way faster. Of course, here I just have like 14 tests, not that much, but in a bigger project, you're going to have hundreds and hundreds of tests. And being able to select by keywords and do some filtering is incredibly useful. If you are running a lot of tests and some of these tests are getting pretty slow, you might want to check which tests are actually the slowest so that you can do some performance tuning. What you can then type is PyTest durations equals 10 durations min equals 1.0. This is going to give you the 10 slowest tests that take more than one second. At this time, there are actually no tests that are that slow, but this is how you would use it in a more complicated project. So this is also really powerful. Finally, a couple of other things that are nice is that you can use PyTest minus v, and that's going to give you a more verbose output. So that will give you full test names. It will give you some extra information as well. It's also going to be really helpful. What you can also do is PyTest minus s, and this is going to show print statements. So for example, if I have my test translations thing here, and then I can print something, hello world, right? And if I then run PyTest minus s, as you can see, it's actually going to print hello world. That's what is happening right here. So this is also a pretty helpful setting that I use from time to time. As you can see, setting up PyTest in VS Code doesn't have to be hard. I mean, once you've configured this, it makes testing much easier and faster, and also I highly recommend that you change the VS Code settings so that you actually have this user interface for running tests, which is pretty nice. Some key takeaways from the video, make sure to install PyTest as a dev dependency, organize your tests properly within a test folder, and use the same folder structure in your test folder as for your source folder. Make sure to use test discovery and filtering options from PyTest, like filtering by keyword, and finally, stick to good test design practices. So make sure your test names make sense, that makes it easier to filter on them. Make sure you have one assert per function, that's going to keep your test more focused and readable. Also, make sure you reset state before each test so that one test is not dependent on another test. There's lots of other advanced features as well, like you can use fixtures to reduce repetition, you can use mocking, you can use patching, and don't forget, you can even use AI to generate test cases, but always make sure you review them. Now I have a question for you, how do you currently run your test? Do you actually run tests? Are you using PyTest, Unit Test, something else? Let me know in the comments. If you want to learn more about what to test, how to set up a complete set of unit tests, check out this video where I do a refactoring challenge and use unit tests to create a safety net. Thanks for watching, and see you next time.

---

## 49. This Is How You Write an Efficient Python Dockerfile
**Channel:** ArjanCodes | **Views:** 59K | **Date:** 11 months ago | **Duration:** 18:47 | **ID:** tc713anE3UY
**Link:** https://youtube.com/watch?v=tc713anE3UY

### Transcript:
With Docker, it's easy to get into a situation where your Dockerfile is a complete mess and your deployments are getting slower and slower. So in this video, I'll take you step-by-step through creating an optimized and efficient Docker image. I'll cover picking the right base image, removing clutter from your Dockerfile, making it more secure, and a few things you probably didn't know were possible. As a starting point, I have this very simple, extremely simple FastAPI app that just has one endpoint, a hello endpoint that you can send a parameter to, a name, and then it's simply going to return hello, name. Yeah, I know it's not very creative, but it works, right? Now, in order to run this locally using Docker on your machine, you of course need a Dockerfile. And this is what that Dockerfile looks like. So basically, I need to install a bunch of dependencies here. I'm using Poetry in this case to handle the whole project, so I need to install that as well. I need to set the path for that. We're also going to need some environment variables like a password and a user database host, that kind of stuff. I'm not actually using that in this particular example, but this is typically the thing you would need in a back-end API, right? I'm setting the work there, so that's basically going to pick the local folder inside your Docker container where the files are going to be. I'm going to copy the application files into the container. Then, I will install the dependencies using Poetry. I set up the virtual environment path correctly so it can find uvcorn. I expose the port, and then I start the FastAPI application. So that's my Dockerfile. So now let's actually build this particular Dockerfile. As you can see, I've passed all of these environment variables as arguments to the Docker commands, so they're not in my Dockerfile, but they are actually passed like so. Now it's simply going to run this. This is going to build the Docker image for me. As you can see, this took a while. It took 36 seconds. We have a bunch of warnings here because, of course, these variables haven't been used, so I can mostly ignore that. I'm going to address some of these things later on in the video. But now that I have this Docker image, I can actually run this like so. Now I have my uvcorn server running inside my Docker container, and I can actually take this URL, and I'm going to copy that. And when I paste this in Google Chrome, we see that we get this as a result. And I can even pass another name to the URL, like ariane, for example, and then that's going to give me this result. You can also see these responses in the console when I'm running my application. So this is all working very nicely. Now there's another thing that is interesting to look at, which is what the Docker image and container actually looks like. So as you can see, I have that particular image right here. I made that two minutes ago, so this is almost a gigabyte in size. And you also see that there is this container that we started in order to run the actual server. So this is our starting point. Now, the problem with the starting point is already a pretty big one, is that we started with the Ubuntu base image. And it actually makes a massive difference if you pick a more suitable image for your particular project. Ubuntu is pretty big, which is why the Docker image was kind of big, almost a gigabyte. So when you're creating a project, you can consider using a smaller image. There's a ton of different base images. Now you might say, well, instead of Ubuntu, why don't we simply take the Python base image, right? Well, the problem is that the Python base image is actually also kind of large. So if I run this Docker build process again with the Python base image, you're going to see it's still going to be a pretty big size. Even worse, if you look in Docker desktop, you see that the Python base image is actually 1.68 gigabytes. And that's because it actually contains a lot of bloat that we do not actually need. Now, the Python base image is actually based on Debian, which in principle is a good distribution. But there are different versions of that that you can pick. For example, you could use Bookworm, which is Debian 12 codename. Or you can use Slim, which is no documentation, debugging tools, et cetera. You also have a combination, which is Slim Bookworm, which is both of these together. If you want an older version of Debian, you can use Bullseye or Buster. If you can, though, consider using official images only because of maintenance reasons. These tend to be more frequently updated in terms of security, but also in general. Here I have almost the same version of this Dockerfile, but I replaced it with the Python Slim Bookworm image. And if we go back to the original one, you can see that here I needed to install Python. So in this case, in our Python Slim Bookworm image, that's no longer needed because Python is already installed. So let's build this version of the Docker image and see how it compares to the first one. In terms of timing, it helped a little bit. We went from about 37 seconds to 31 seconds, so that saves a bit of time. Not that much, though. But when you take a look at the size now, you see that this is now a bit less than what we had before. So it's 868 megabytes instead of 937. And by the way, as you can see, I've added this no cache tag. That's not important. It's just so that I recompile it from scratch each time so we don't get any bias in terms of. Now the next step is to think about the tag that you use. Be sure to not always use the latest tag. This is a so-called rolling tag. Basically, every time there's a new version, the latest tag is going to be updated. And since we didn't use a tag here, it's going to automatically be the latest one. If you're more specific, you can prevent unexpected changes in your base image. You'll have more control because you know the size, the contents and so on of the image that's being built. This doesn't always minimize the image, right? It's possible that a particular version of this image is larger than a older version, but for maintenance purposes, this is actually a good idea. So you always know which version you're using. For example, here I have yet another version of the same Docker image, but there I've added the 3.13 tag. So let me now build this version of the image. As you can see, this takes around the same time as we had before. And when we look at the Docker image stack, it's going to be the same size because probably 3.13 is also the latest tag. But now at least we are in control of the version that we're using. Let's further clean up this Docker file. So in this current version, we simply install like a bunch of stuff, but you can actually do that a bit more effectively. Here I have another version of that Docker file where the apt-get command has the no install recommends option. So this basically doesn't install recommended package, but only what you actually need. So only the main dependencies are installed. Talking about dependencies, by the way, this installs Poetry and so on. Actually for running the production environment, you won't actually need Poetry. There's a way to avoid that this ends up in your production image that I'll show you also in a minute. But this already saves quite a bit of space. And having a small Docker file is actually nice because that just makes your deployment way faster. So let's now build this version of the image. So this saved yet another second of build time, which is, you know, not much, but it's still nice that it's there. But again, we made our file a little bit smaller. We're now nearing 800 megabytes. So we're making step-by-step a little bit of progress here. But we can do even more. Here's yet another version of that same Docker file where I'm actually removing the files and folders that are right here. And what this actually does is that it removes metadata about package report stories that apt uses to manage software. So this includes information about the packages that are available, versions, dependencies, other details that it retrieves, something that you really don't need to have in your final image, right? So let's also build this Docker image and see how much space we save by doing that. As you can see, that removes another 30 megabytes of space. Also, we're now at a build time of 25 seconds. So from the very first version of Docker, we already shaved off 10 seconds of the build time, which is pretty nice. Now, the next change that I'm going to make is to use a different builder. Here's an example of the Docker file that actually uses UV. This is going to dramatically increase the speed of the build because Poetry's dependency resolver can actually be pretty slow. Note, if you're changing from Poetry to UV and you use a pyproject.tomofile that's Poetry-specific, you might need to change the contents of that file, which, anyway, is good practice to do. So one thing that I did have to change here is that I needed to change from slimbookworm to bookworm because UV depends on things like curl in order to install itself. So most likely, the image is going to be a bit bigger than the version before, but as you will see, it's actually building a lot faster than when we used Poetry. So as you can see, we're now at a build time of 8.7 seconds, which is a really nice improvement. Unfortunately, because now we switched again from slimbookworm to bookworm, our image size is now 1.61 gigabytes, which is not great. But we're going to fix that in the next step, which is to use a multistage build. So in order to make your Dockerfile more efficient, you can leverage multistage building. So you could use one image for downloading, installing, compiling, everything that you need to build your program. Then you copy over what you actually need to a new image, and then the new image is what actually is going to be deployed. Docker can handle these multiple stages for you. You can do all sorts of different stages, as many as you like, but also be careful that it doesn't become too complex. In my view, if we're simply dealing with Python projects like the one I have here, actually having two stages is nice. One for building, compiling, installing everything, and one for actually running the web server, the FastAPI server. So what does that actually look like? Doing multistage build is actually really easy. So here I've changed the Dockerfile so that it now uses a multistage build. So here I'm still using 3.13 bookworm, because that's needed in order for UV to run correctly. I simply follow the process as I did before. However, of course, UV only needs the PyProject file in order to install its dependencies. So I simply copy over the PyProject file to the build image and then let UV create the virtual environment and install all the dependencies. Then I have the production stage, which, again, uses slim bookworm. I set the environment variables that the server is going to need. I set the work there. I copy over the files. And from my builder stage, I copy over the VM folder, the virtual environment. I set up the path, expose the port, and then I simply run UV record just like before. So that's a simple multistage build. Let's run this and see what we got. So as you can see, this is still really fast. Actually, it's now even faster. It just took 5.3 seconds to build this whole thing. And when you take a look at Docker Desktop, you also see that this image is now really small. We saved a lot of space by not having all of these build tools installed in our image. So it's really clean, really minimal, which is really great, because deploying this is now going to be really fast. Having such a small Docker image is great. This also means it's going to save you on storage costs, because these things are typically stored in registries on the cloud. And now perhaps it's a good idea to just check that this actually is still running as expected. So here I'm running the UFCONN server with my multistage build image. So it all seems to work as expected. And let's try calling an endpoint just to make sure that that also works, which it does. But we're not there yet. Actually, we can still further improve the efficiency of this Dockerfile. One thing that you want to avoid is what you also see me do in this Dockerfile that I have right here, and that I'm still copying over everything. Actually, this is not needed. My production application doesn't need all the files. And most likely, I'm now copying over a bunch of things that are not really necessary, because there is my source folder. There might be some other stuff here, like VS Code settings. Who needs VS Code settings in a production image, right? It's not necessary. So you can actually be more specific and only copy the things that you need. That can lower your image size. Now, in this case, it's just a couple of files. It's a basic example. But it's possible that you might accidentally copy over gigabytes of node modules from a related TypeScript project or something else. So you want to be really careful and only copy the things that you actually need. So here's another version of that same Docker image where I'm not copying over everything, but I just copy over the source folder, because that's the only thing that I need. Let's build this image and see what we end up with. Now, what's really nice now is because we have already optimized the build process so much, I actually don't even need to skip the video. It's just building ridiculously fast now, which is really nice. And as you can see, this didn't really save me a lot of space in this particular instance. But if you're accidentally copying over huge files, this is going to make a massive difference. Another reason, by the way, to make sure that you're only copying things that you actually need is for security issues. If you simply copy everything, you might accidentally copy over files that are sensitive, like .env files or other things. So I'm really happy with this Dockerfile size. Final things that I want to show you is a few ways to make your Dockerfiles and images a bit more secure. So the first is to use a better mechanism of loading secrets by using secret mounting. And here's an example of what that looks like. So instead of, in the previous version, simply taking environment variables directly, what you can actually do is mount secrets by doing this. So you refer to an id, a secret id, and I get these from a call to a cat of slash run slash secrets of these particular secrets that we store. And I'll show you how to call this because that's also going to be slightly different. And doing it this way, it means that the secret is only accessible for this specific run operation. So other run operations will not be able to access the secret. So this is the value that the container will use. And this particular bit is going to set the environment variable in the image. Note that this is not like the ultimate most secure way because anyone that gets a hold of this image will still be able to access these credentials and tokens. An alternative solution to this is to actually use some sort of vault like a HashiCorp. But this is not going to be covered in this video. I might do a video about that in the future if you want to know more about that. But honestly, this is good enough if the image is not going to be shared with others. Because when I'm mounting secrets, the Docker build command is also slightly different. So here we need to specify the secrets like so. So we specify the id and then we specify the environment variable. So you need to make sure when you call this that these environment variables are defined. So let me run this. And again, we're going to have very fast build time, but now it's going to use secret mounting. In terms of Docker file size, there is no difference here. It's really just a way to make your Docker image a bit more secure. Final thing that I recommend you do, and that's also the last thing that I'll show you here, is that you should create a non-root user. And set it as the user under which the container will run. This is a security best practice. By default, Docker containers run as the root user. And that means if an attacker exploits some sort of vulnerability in your application, they might gain root access to the container. If you run this as a non-root user, that limits what an attacker can do inside your container. Even if someone gains access, they won't be able to modify system files or install new software inside your container. And this actually also helps for you because then you can't accidentally modify system files or write data where you shouldn't. So how do you do that? Actually, really simple. The only thing that you need to do is add an actual user. So here I'm creating a new user. I'm calling that app user. And then I'm going to simply switch to that user. And then actually that's how the rest of the container will run. So let's build the container. Now the final version of the container took less than five seconds, which is really nice. It's really fast to build. And if you look at the size, well, we have a slightly increased size with related to the last one because now we're, of course, creating a user. That apparently takes up some space, but not much. So in the end, what we now have is a really fast build. The image is really small. And it's also pretty secure. Now, before I finish this video, I need to vent a bit. We actually had a lot of trouble getting all of this to work like we wanted. Working with Docker honestly proved quite hard. That's, for example, no easy way to inspect the contents of a container except for logging LS commands, which makes it really hard to test. If you combine that with multistage builds, access permissions, local files versus files in the Docker image, different files and folders in the different images that are used in multistage, the working directory, default paths that various tools like UV use, and it's really easy to get lost. To give you an example of the really stupid things that you can run into is that I have here my final version of the Docker file. As you remember, I copied the pyproject.toml file over. If I simply copy over everything, which includes the pyproject.toml file, and then I'll try to run this image. So the image actually builds fine. But when I then try to run it, it can't find uvigor. I have no idea why. If I change this to only use the pyproject.toml file, I'm copying over exactly the same VN folder. There's no change. UV can install dependencies in exactly the same way. But now when I run the Docker image, it works without any issue. For some reason, I'm not able to copy over everything, but if I only copy the pyproject.toml file, it works. I still haven't figured out what is going on. I have no idea why this works. If you have any idea why this is happening, I'd love to hear about it. Post it in the comments below. I literally spent hours trying to figure this out, and in the end, I basically just gave up. Now, I've put a link to git repository in the video description that contains all of the Docker files and all the different versions that I showed you today. Feel free to use these as you wish in your own project. Something that I didn't talk about at all in this video is how to actually use Docker for your local development setup. That's really important because you want your local setup to resemble production as closely as possible to avoid nasty surprises. Check out this video where I go over exactly what you need to do to set that up correctly. Thanks for watching, and see you next time.

---

## 50. 12 Python Built-in Functions I Use Every Day
**Channel:** ArjanCodes | **Views:** 81K | **Date:** 11 months ago | **Duration:** 22:15 | **ID:** ho24rK_AYrQ
**Link:** https://youtube.com/watch?v=ho24rK_AYrQ

### Transcript:
Did you know Python comes with over 60 built-in functions that you can use without importing a single module? Today I'll explore 12 of the most interesting and useful built-in functions with examples, gotchas, and a few nerdy details you may not be aware of. Why 12? I have no idea. It just kind of happened. Let's dive in. The first function I want to talk about is the help function. This is mostly used for the interactive interpreter. And the way that this help function works is that, let's say, from data classes, we're going to import data class. So once you've done that import, you can simply type help data class, and then you're going to get some information about that particular decorator. Or you can also directly launch into the help environment, and then you can simply type the module that you want to know more about. Let's say urllib, for example, and then it's going to give me the help for the urllib. Now, of course, there's always Google and the internet. But if you don't have internet, or you just very quickly want to look up some documentation, the help function is pretty neat. And you can just keep a terminal like this open. And in your editor, you can just switch to this whenever you need some more information about a particular module or function. Part of this, by the way, is also integrated in your IDE. So I'm not that much of an interactive interpreter guy myself. So I don't use it all that often, but it's good to know that it's there. The next built-in function that we need is dir. And I have a Jupyter Notebook here to show you how this works exactly. So, for example, let's say define a couple of variables a and b. So if you print dir in this case, what you're going to see is that there is the entire scope. So we have the built-in Donder dictionary. There's a couple of other global scope things like the name and the package. But as you can see at the end, we also have a and b in there. So dir simply lists the names in the current scope. Now, there are some things to be aware of with dir. Let's say here I have an example where we have a dir with a custom invoice class that sets a client and a total. And there is also a string Donder method implementation. So when I run dir on this, but then when I scroll to the right, you actually see that the instance variables that we created in the initializer are not there. So dir doesn't automatically show you this. And that is because I'm calling dir on a class. And the instance variables are set in the initializer, so it's not part of that. However, if you do dir on instance of class, so here I have that same invoice with a client and a total, then you will see we will get these things. So I just have to scroll right to get to them, but they are going to be right here, client and total. So depending on what kind of thing you're inspecting, you may or may not see the data that you expect. What you can also do in a class is override the dir Donder method like I'm doing here. And then you can actually specify what a call of dir on that particular class should show. So in this case, when I run this, then you see that this is now what we get. So you can involve all sorts of interesting Python magic here to make dir say whatever you want, basically. So be careful with this. You can also call dir with a module. In this case, I'm importing math, and then I print the directory of that particular module. And then you can see all the math functions available right here. You can also call it on a list, then. This is what you're going to get. So you get all the list Donder methods, as well as a couple of other methods that list has, like append, clear, et cetera. You can even call dir on a function, and then you're going to get a bunch of Donder methods that are defined on the function. Remember, in Python, everything is an object, including a function. Or you can call dir on the non-type. Again, that's another object in Python. In short, this shows that dir basically allows you to expect anything in Python. The next function is range, and this is also really helpful. In fact, range is not really a function. It's more of an immutable sequence type. The advantage of the range type, rather than just putting all those values in a list or something, is that the range object is going to take the same amount of memory, regardless of how wide the range is. So that is actually really neat. Here you see a very simple example of using range. So I'm using that in combination with for loop. And when you do that, you see that it prints from 0 until and including 9. So it's exclusive of 10. In other words, exclusive of the upper bound. And you can also see that in this example, where if I go from minus 10 until 10, and that's inclusive of that lower bound. So in fact, here there is a default value of 0 for the starting value of range. And it ends at 9, just like the one before. Now you can also do a lower bound of minus 10, upper bound of 10, and a step of 2. So what that's going to do is going to skip the odd values in this case. So it will still start at minus 10, but then of course the upper bound is going to be 8. Because 8 plus 2 will be 10, and 10 is excluded of the actual range. You can also print the type of the range. And as you can see, that's a class range. And range itself is a subclass of sequence. Now because range is a subclass of sequence, it means you can access all sorts of methods in it. For example, you can use the square bracket notation. You can also fetch the last element, or you can get the length of the range, or the index of a certain value, or a count, or you can create slices, reverse it, compute the minimum and the maximum of the range. And all of that works just like you would expect with a sequence. So this is a really neat feature that few people actually know about. You can also do reverse range. So if you start with 10 and go to 0 and then you do step minus 2, then this is what you get. And if you have a range from 10 to 20 with a negative step, then you're going to get the empty list. Because by applying the step, you will never get to the value of 20. Before I continue, if you're serious about mastering Python and software design, I've created a free guide that walks you through the seven steps I use to design software. Get it at arjan.codes/designguide. It's packed with practical tips and examples. Check it out. The link is in the description. The next one that I want to talk about is all. What all does is that it's going to take some iterable, and then it's going to return true if all the values in that iterable are truthy. Examples of truthy values are a non-empty list, or a positive integer, or a non-empty string. Falsy examples are none or zero. Here I have an empty list, and if I call all on the empty list, it's going to be true, because all the elements, even though it's zero elements, they are truthy. If you have a mixed list like this, containing some integer values, then it's going to return false, because zero is falsy. If I change this to a positive number, then you're going to see it gives me the result true. And same if you have a none value, because none is also falsy. So there you go. Here's another example where I have a class isEven, and that simply has a value. And I use the bool thunder method to determine whether the value is even. And in this case, if I have one, two, and zero, then this is going to return false, because this first element is odd. Actually, the comment here is wrong. If you change this to, let's say, eight, so then we all have even numbers, then this is going to be true. But of course, in Python, you can combine various types of elements. So I could also change this to none, which again is falsy, and then all is going to return false as expected. And this works because all actually uses this donnor method. Now, if that donnor method isn't defined, then it's going to look at the length. So in this case, let's say I have a shopping cart that has items, and this gives me the length of the items in the cart. And then here, as you can see, I have three carts, and the first two, they have items in them, but the last one doesn't. And that means that all is going to give me false, because it checks the length. If you have both length and boolean, like what I have here, then actually bool seems to have a higher priority than length. Because in this case I have a card with a single item b in there, and actually if you look at what these functions return, bool returns true in this case because a is not in the items, and length returns false because this is going to give you the empty list. But if I change this to contain the item a, then in this case bool is going to return false and length is going to return true. So when I run this then we get false. So bool has higher priority than length. Kind of a nerdy detail, still interesting. All also works with strings, so if it's non-empty then it's going to be true, and an empty string is also true because there are no elements, similar to all on an empty list. And finally all works with iterables, and that also goes for generators. So in this case, for example, I can use the range function that I talked about before, and then check this generator of booleans here, and which is going to give us true because all of these conditions here are true, since the range is positive. And here it's going to give us false, because of course the range starts at minus five, so not all the elements are going to be positive. Now related to all is the any function, which checks that at least one element in the interval is truthy. You can see a few examples of that here. So again, the empty list, it doesn't have any truthy elements, so this is going to give us false. Here I have a bunch of mixed values. Since five is truthy, that means that any is going to also give us true. One thing you do need to be careful of with generators is that any is going to stop after it's going to find the first truthy value. So if you basically generate infinite number of falsy values, then basically it's going to run forever, and that's what you see here. So I have some infinite sequence, which just returns positive numbers, and I'm going to continue until I find the first negative number, which of course will never happen. So this is just going to continue on and on and on, so I'm going to interrupt that. And here you see an example where it doesn't continue forever, because in this case I will stop as soon as x reaches the value of 10. The next function is zip, and this is also really helpful. It allows you to take multiple iterables and basically turn them into an iterable tuple of the values. So in this case, for example, if I have names and scores, I can create a zip, and as you can see, we get in this case a dictionary that maps this value to this value. If I turn this in a list, then you can see that zip actually creates these tuples. So this is really helpful if you need to iterate over multiple lists or sets at the same time. And note that it's going to take the shortest of the two. So if I add another value here, for example, like so, so actually and not add a score, then I will print this, then you can see that this value is simply ignored, until of course I add a score here for John, and then it's going to be added. So zip takes the shortest iterable. Now zip is lazy. It's only going to generate those tuples after you actually call it. So if I do this, for example, and then I change the values here, so what will happen is that this will only be called when I call the list initializer on it, because then it's going to extract the value. So what I did here is actually going to end up in the list, so be careful with that. Here's an example of how you could use zip. So in this case, I have these two lists, and I want to iterate over them at the same time, and then this is how you can do that. Next up is filter. This is really nice if you don't want to use a for loop like some kind of Neanderthal, but you actually want to use fancy functional programming. You can use the filter function to filter elements from a sequence depending on some condition defined in the function. You can see an example of that. So I have a list of numbers, and then I call filter on that list of numbers, and I give a very simple function here, lambda function, that determines whether the number is even. So filter is going to check each of the elements in the iterable, and then it's going to only keep the ones that actually adhere to the conditions. So when I run this, we get only the even elements in the list. Now you can use this with lambda function like this, but you can also use a function that you define somewhere else in the scope. So for example, here I have an is positive function, and then I'm using that function to filter out the only positive numbers in this particular list, which is one and two. If you supply none to the filter, so there's no function at all, it's only going to keep the truthy values. So this is what that looks like. This is really great for quick data cleanup, when you just want to remove any null data from your list. If you're dealing with objects, like in this case I have a user, which has a name and is active Boolean value, then you can also use filter by directly accessing the elements in the instance. So for example, here I'm only keeping the users that are actually active. When I run this, we get Alice and Charlie, because Bob is no longer active. The next function is map, and this is also really helpful. I use map quite a lot. What map allows you to do is apply a function on an iterable and then compute those values. So let's say I have a bunch of Celsius values here. Now what I can do is use map to call this particular function here that converts Celsius to Fahrenheit, and then print the list of Fahrenheit values, like so. You can also use this with built-in functions. So for example, here I have strings, and then I can create the uppercase string by calling string.upper on these particular strings, and then this is what we're going to get. Or here's another example where let's say you want to map integer conversion to a list of string values. Now if you just define the result like so, then nothing happens. But as soon as you do something with the results, then the computation is done, and this is printed. So for example here, you can see that the type of result is class map, but then when we turn it into a list, it's actually being computed, and then we get the list of integers 1, 2, and 3. Now I have a few more built-in functions I want to go over. If you're enjoying this video so far, give it a like. That helps me a lot with reaching more people here on YouTube. I really appreciate it. Next up is iter, and this allows you to create an iterable from, for example, a list, a set, or a dictionary. And then what you can do is call next on that to get access to the values one by one, iterate over it. Then what happens, as you call next, when no more elements are left, it's going to raise a stop iteration. So when I run this, then this is what you get, 1, 2, 3, and then end of the iterator. It also works on strings, obviously, so then you're going to get this. Another cool thing that you can do with iterators is repeatedly call a function, like what I'm doing here, until a certain sentinel value is reached. In this case, that's stop. So if I run this, then I can now enter something, i, and that basically gives me this, right? I can just enter a bunch of things. So when I enter stop, then now it's going to stop. So this is a really neat feature that I use from time to time. You can also create a custom iterator by implementing the iterDunder method and the nextDunder method. So in this case, you have a counter, which has a current start value and an end value, and then basically it will increase the counter until it reaches the end. So here I create a counter from one to five, and then I simply use a for loop to go over the values, and I can do that because these Dunder methods were implemented, and you get this as a result. Iterators also work with a generator. So here I have a generator function that yields values of i from a range of zero till three, and then I can simply use that iterator directly and call next on it. You can also use this to read lines from a file, like what I'm doing here, and then actually this file contains a single line, so then it's simply going to print those lines. Next up is slice, and that's also really helpful. So slice allows you to create a subset of an existing iterable and create a new iterable out of that. So let's say we create a slice object from one, five, and two, and you can define a slice by creating a slice object. So in this case, you supply a start value, startIndex, stopIndex, and a step. So in this case, we want to start at index one, we stop at index five, and we take step two. So we're going to get one and three. So when you run this, then this is exactly what you get. And here you can see how that works. So you have a numbers, a list of integers, and then you can pass this slice between the square brackets. So this is the slightly longer syntax, but you can also use a slice directly by using the column notation. So let's say I want to have from one till five with a step of two, and then this is going to give us exactly the same result. If I change the step size to one, then you see we're going to get this as a result. So depending on what you need, you may want to define it like this, or you may want to define it directly as part of your... square brackets. So this is actually pretty interesting. A few people know that you can use slices in this way and then this allows you to actually create them dynamically, which is kind of cool. You can also use slices with strings. So in this case, I'm taking the slice from 7 until 13. That's going to give me Python, but of course you can also type it like so. And then you're going to get exactly the same result. You can even use slice with multi-dimensional arrays, which is also kind of interesting. So here I have a matrix, which is a two-dimensional NumPy array, and then you can use slices to get a row and a column slice. And then this is what you're going to get. And just like before, you can define these ranges in exactly the same way, either by using slice objects or by simply directly defining them in the matrix. And this is going to give you exactly the same results. I use slice quite a bit. I think it's a really helpful tool to quickly get access to a subset of a particular group of data. So the next function that I find very helpful is type. And this is going to give you the type of a particular value in Python. So here's a few examples. So we have 42, which is an int. 3.14 is a class of float. We have a string. We have a list. We have a dictionary. It also works for classes. So basically, if you run type on an instance of a class, then you're going to see that it's a class in the main scope called my class. You can even use type to dynamically create a class. So here I have dynamic class, which is a type dynamic class. And then I'm going to provide it with attributes and even methods. So then if I run this, then this is what we get. And we can call the method that's going to output hello. I don't recommend that you actually do this, but it's really interesting that this is actually possible in Python. Here's another example that's also interesting. So I have a parent class and I have a child class that's a subclass of parent. Then I create an instance of child, and I'm going to check whether the type of this object is a parent. And that's actually false because it's actually a child. So it doesn't check for subclasses. If you call isInstance, then this is going to return true because child is actually a subclass of parent. That's the difference that you see right here. So don't use type to check inheritance relationships. Use isInstance for that. That being said, I don't use these very often in my code because often it means that there is some design issue. There's a few rare cases where it is helpful to use isInstance or type, but typically I only use these for debugging purposes. The final function that I want to show you is vars. And this returns the dictDunder object as a dictionary. So if I have x and y here in my local scope and I print vars, then what you see here is that I get the values in this local scope. And this provides me with a bunch of data. Now I normally don't use vars directly like this. It's not that useful in my opinion. But if you have a class, let's say a point, in this case I have an x and a y value, and I create an instance of that and I print the vars of point, then I'm going to get the instance attributes. And this is actually quite helpful. It's also interesting to see what happens if you dynamically add attributes to class. So here I have my class which has a single attribute x that has value 42. I create an instance of that class and then it prints the vars of that instance. So that's going to print this. That's what you see right here. Then if I add a new attribute like what I do here, then I can also print the variables of the instance and it's going to update that. So that's what you see right here. And then finally, of course, I can also access that by simply accessing the y value on object. Now I would not recommend dynamically setting attributes on an instance like this. I feel like it leads to pretty brittle code if you're not careful. So I would typically avoid that. But at least know that vars always gets the latest list of instance variables. One way that I've used vars in the past is if I have some objects and I need to store it in a JSON file, for example, then I can use vars to simply get all the attributes and the values. And then I have a dictionary that I can store in the JSON file. Now I'd like to hear from you. Which of these functions do you use the most? Did you know about all these functions or did you discover a new one today? Let me know in the comments below. Now mastering these built-in functions means less boilerplates and more expressive Python code. But that's just one aspect of being a great Python developer. In this video, I go over the most important features of Python that you need to know, including one which I see many developers struggle with. Thanks for watching and see you next time.

---
